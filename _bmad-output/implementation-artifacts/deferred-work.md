# Deferred Work

Append-only log of work items deliberately deferred by bmad-dev-auto runs.

- source_spec: `_bmad-output/implementation-artifacts/spec-0-1-colyseus-0-17-upgrade.md`
  status: RESOLVED by Story 0.2 — verified 2026-08-18 at the epic-6 retro triage. `protocolVersionError()` in `server/src/rooms/roomOptions.ts` rejects a mismatched/missing `pv` at matchmake time (amendment 5 moved the gate to the queue's door); client maps 525 to VERSION MISMATCH. The 2026-07-18 triage section already declared this RESOLVED in prose and the entry was never marked — that gap is what this pass exists to close.
  summary: PROTOCOL_VERSION has no runtime enforcement — add a join-time version check (client sends its PROTOCOL_VERSION in join options, server rejects mismatches cleanly) plus a "please refresh" UX path, so stale bundles fail with a clear rejection instead of a cryptic schema-decode error.
  evidence: Verified by both review hunters and the orchestrator — PROTOCOL_VERSION appears in no join options, no WelcomeMsg field, and no server-side gate; the gap is pre-existing (v2 was equally unenforced) but the schema-4 wire break makes stale-bundle failures certain after deploys (Render auto-deploy on push to main). Natural home: Story 0.2 (reconnect auth touches the same join path) or 6.7 (reconnection UX).

- source_spec: `_bmad-output/implementation-artifacts/spec-0-1-colyseus-0-17-upgrade.md`
  status: RESOLVED by Story 0.3 — verified 2026-08-18. `afterStep` skips frame sends to any client whose state is not JOINED; `armJoiningDeadline`/`kickIfStillJoining` kick a stuck JOINING client. Pinned by `server/src/__tests__/joiningGuard.test.ts`. Declared RESOLVED in the 2026-07-18 triage prose, never marked on the entry.
  summary: Guard against JOINING-state clients — skip frame sends to clients that haven't confirmed join (state !== JOINED) in ArenaRoom.afterStep, and add a room-clock deadline that kicks clients stuck in JOINING, so a client that never sends the join confirmation can't grow the transport's unbounded _enqueuedMessages buffer at 20Hz while squatting a playerCap roster slot.
  evidence: Adversarial review (ship pipeline) traced @colyseus/core 0.17.10 — clients.push happens before onJoin, seat-reservation timeout is already consumed at that point, the ping reaper is defeated by auto-pong, and WebSocketClient.enqueueRaw buffers unboundedly for JOINING clients. Pre-existing shape (0.16 had it too), slow growth (~0.5–1 MB/hour/socket), but it is the one live exploit path found. Natural home: Story 0.3 (operability) or a standalone hardening chore.

- source_spec: `_bmad-output/implementation-artifacts/spec-0-1-colyseus-0-17-upgrade.md`
  status: CLOSED — verified 2026-08-18. This entry's own recorded revisit condition ("alongside the PROTOCOL_VERSION join gate") is satisfied: a stale client is refused at matchmake with a clean 525 before the transport sees it. The accepted 0.17 transport-kick behaviour stands unchanged. Declared CLOSED in the 2026-07-18 triage prose, never marked.
  summary: Recorded decision — Colyseus 0.17 force-disconnects (instead of log-and-drop) on unknown message types and undecodable msgpack in production; accepted for now since production clients only send registered types, but the "malformed messages are silently dropped" invariant now holds only inside game/inputs.ts (field-level validation), not at the transport. Revisit alongside the PROTOCOL_VERSION join gate so stale clients get a clean rejection rather than a transport kick.
  evidence: @colyseus/core 0.17 __no_message_handler fallback calls client.leave(WITH_ERROR) outside devMode; verified in installed package source during the ship adversarial review.

## 2026-07-17 — matchSmoke reliability hardening (from Story 0.1)

`server/scripts/matchSmoke.mjs` is timing/physics-flaky on this hardware: A/B testing showed 0/3 clean passes on the 0.16 baseline (`1f81f70`) and the same scattered-timeout taxonomy on 0.17 — torpedo-connect luck (steps 2/4) and the storm's r=90 endgame pocket letting a drone camp past the results timeout (step 5, `server/src/game/match.ts` finish check). The lifecycle itself is proven working (full results broadcast observed on 0.17). Deferred because retuning smoke timing/geometry changes test semantics — out of Story 0.1's upgrade scope. Candidate fixes: deterministic drone seeding for the smoke, longer step budgets, or a zoneOverride whose end radius clears every camping spot. Consider folding into Story 0.3 (operability) or a standalone test-hardening chore.

- source_spec: `_bmad-output/implementation-artifacts/spec-0-2-reconnect-into-your-own-ship.md`
  status: RESOLVED-AS-ACCEPTED 2026-08-18 by Story 6-7 (Eric ruling R8/Q9, epic-6 amendment 49) — grace chaining STAYS UNLIMITED. Eric was offered a count cap and a cumulative-seconds budget and declined both: the exploit is self-punishing (the hull sails straight into the storm while you do it) and any cap would punish the genuinely bad connection this story exists to protect. Story 0.3 metrics would surface it if it is ever seen in the wild.
  summary: Grace chaining is unlimited — a player can cycle drop → 59s ghost → resume-for-a-tick → drop all match, staying functionally absent yet alive for placement; a per-match grace budget (count or cumulative seconds) is a game-design decision for Eric, natural home Epic 6.7 (reconnection UX) alongside the abandon flow.
  evidence: Blind Hunter traced that nothing counts reconnections per session; CONFIG's "bounded liability" comment is per-incident only. Metrics visibility arrives with Story 0.3.

- source_spec: `_bmad-output/implementation-artifacts/spec-0-2-reconnect-into-your-own-ship.md`
  status: PARTIAL 2026-08-18 by Story 6-7 (Eric ruling R9/Q10, epic-6 amendment 48). The ORIGINAL window (socket dies between allowReconnection resolve and the JOIN_ROOM ack) REMAINS OPEN — an acking-aware server hold policy was offered and declined as a Colyseus-internals fight for a narrow window. What WAS closed is the fresh instance of the same fault class that refresh-resume created: persisting the token once at connect would leave a later refresh carrying a dead pre-resume token after any in-page resume rotated it. The token is now re-written on every ack and cleared on any failure. NOTE for whoever takes the original: the SDK invokes `onReconnect` and assigns the rotated token on the NEXT LINE (`@colyseus/sdk/build/Room.mjs:241` then `:243`), so a handler reading `room.reconnectionToken` sees the OLD value — persistence must not hang off that hook.
  summary: Half-resume double-fault — if the socket dies between the server resolving allowReconnection and the client's JOIN_ROOM ack, the rotated reconnection token was never delivered, so the client's retries carry a stale token and fast-fail to DISCONNECTED while the ghost is held for another grace window; consider acking-aware hold policy or token-retry tolerance in Epic 6.7.
  evidence: Edge Case Hunter traced core's token rotation at reconnection resolve vs SDK token update on JOIN_ROOM ack; consequence is a failed resume degrading to pre-0.2 behavior (no seizure, no crash), hence deferred not patched.

- source_spec: `_bmad-output/implementation-artifacts/spec-0-2-reconnect-into-your-own-ship.md`
  status: RESOLVED-IN-PART 2026-08-18 by Story 6-7 (Eric ruling R7/Q8, epic-6 amendment 49) — the ELIMINATED modal and a placement are now synthesized CLIENT-SIDE on resume, from spectator frames plus the player's own roster row, with NO wire change. Deliberately NOT conveyed: the killer, the sink time, and an exact placement (the schema carries 0 until the match finishes), because the faithful version costs a new channel and a PROTOCOL_VERSION bump and Eric chose the bare synthesis. So the "mystery death" is closed; the killer-follow spectate target and an exact placement remain unconveyed by design.
  summary: Sunk-while-away resume skips the client's normal death moment — the missed 'sunk' event means no killer-follow spectate target, no sink feedback, no kill-feed line; a resume-into-spectate should synthesize the death-flow entry. Natural home: Epic 6.7 or Epic 5 sinking-window work.
  evidence: Edge Case Hunter verified spectate entry itself works (spec:true frames drive it) but the event-driven death UX (killerId, telegraph reset, feedback) hangs off a message the dropped client never received.

- source_spec: `_bmad-output/implementation-artifacts/spec-0-3-server-operability-baseline.md`
  status: RESOLVED 2026-07-31 by Story 2-10 (Eric ruling, amendment 53) — `MatchEndSummary.endedBy` ('lastHumanSunk' | 'fieldCleared' | 'lastHumanLeft') classifies every finish sim-side; quit-outs are now distinguishable in `match.end` telemetry before any balance conclusion is drawn (the finer consented-quit vs grace-expiry split was declined). Pinned by 5 fail-proven cause tests.
  summary: The `match.abort {reason:'abandoned'}` path is unreachable through any real room flow — every dispose of an active match drives the leave cascade into `finish()` first (last human's teardown → checkWin → 0 alive humans → finish), so quit-out/abandoned matches are recorded in telemetry as `match.end` with a winnerClass and duration; whether such matches should be distinguished (e.g. an `endedBy` field or reclassification) is a game-design/telemetry decision for Eric before balance conclusions are drawn from match.end data.
  evidence: Blind Hunter traced every dispose path in @colyseus/core 0.17 (disconnect() force-closes clients before dispose; dispose requires zero clients) and the finding was live-proven by metricsSmoke's own choreography — both captains leave and stdout shows match.end winnerClass:"cruiser" rather than an abort. The 'abandoned' branch stays as defensive fallback (it fires if a tick-error abort's dispose finds the match still active).

- source_spec: `_bmad-output/implementation-artifacts/spec-0-4-portal-adapter-seam.md`
  status: VERIFIED STILL OPEN 2026-08-18 — correctly parked at Epic 7, as the 2026-07-18 triage ruled. The five-method seam is unextended and portal adapters (7-3) have not been written. Not actionable before Epic 7; listed here so the next triage does not re-derive it.
  summary: The five-method PortalAdapter seam (the epic's approved shape) omits hooks real portal SDKs demand, so Epic 7 will likely need to extend the interface — no pause/resume around ad breaks (Poki/CrazyGames require muting audio and suspending input while an ad plays), no matchStart/matchEnd pairing on mid-match disconnect (gameplayStart/gameplayStop must pair), unclear gameplay semantics during the death→spectate window, and a loadingProgress bracket that covers only the pre-menu stage load while the actually slow path (post-PLAY connect → first frame) reports nothing; whether and how to extend the seam is a design decision for Eric at Epic 7 (portals are speculative until then).
  evidence: Blind Hunter mapped the seam against documented Poki/CrazyGames SDK lifecycle requirements; acceptance criterion 4 ("only portal/ and the construction site change at Epic 7") holds for adapter implementations but not for interface extensions, so recording the likely extension points now is what keeps Epic 7 honest about scope. Mitigation already in place: safeAdapter guarantees any adapter misbehavior is contained, and all call sites are single choke points that are easy to relocate.

## 2026-07-18 — deferred-work cycle triage (spec-deferred-work-cycle)

Triage of all 9 items above, run per `_bmad-output/implementation-artifacts/spec-deferred-work-cycle.md`. Every item now has an explicit disposition; no existing entry above was edited.

1. **PROTOCOL_VERSION join gate** (spec-0-1, item 1) — RESOLVED by Story 0.2. Client sends `pv` in join options (`client/src/net/connection.ts`); server rejects mismatches in `ArenaRoom.onAuth` via `ServerError(ErrorCode.AUTH_FAILED = 525)` built by `protocolVersionError()` (`server/src/rooms/roomOptions.ts`); client maps code 525 to "VERSION MISMATCH — PLEASE REFRESH THE PAGE" (`connectErrorStatus`). Covered by `client/src/__tests__/connection.test.ts` and `server/src/__tests__/reconnect.test.ts`.
2. **JOINING-state client guard** (spec-0-1, item 2) — RESOLVED by Story 0.3. `afterStep` skips frame sends to any client whose state isn't `JOINED`; `armJoiningDeadline`/`kickIfStillJoining` (using `CONFIG.net.joiningDeadlineSeconds = 10`) kicks clients stuck in JOINING and is re-armed on reconnect. Had zero regression coverage before this cycle; added `server/src/__tests__/joiningGuard.test.ts` this cycle to pin the frame-guard skip and the fire-time kick decision (deadline scheduling and the reconnect re-arm remain verified by inspection, not tests).
3. **Transport force-disconnect decision** (spec-0-1, item 3) — CLOSED. Its recorded revisit condition ("alongside the PROTOCOL_VERSION join gate") is now satisfied — stale clients get a clean 525 rejection at matchmake time, before the transport ever sees them. The accepted behavior (Colyseus 0.17 transport-kicks malformed/unknown messages rather than log-and-drop) stands unchanged.
4. **matchSmoke reliability** (2026-07-17 section) — FIXED THIS CYCLE, scope confined to `server/scripts/matchSmoke.mjs`. The 90u drone-camp pocket became a ~13.5u floor (`endRadiusFraction: 0.015`) that only the scripted human can hold — drone AI never throttles below 0.5 so it can't loiter there and storm-sinks, while A parks inside (a pure 0 floor is an hp race A's 100hp cruiser can't reliably win against the 120hp battleship drone). Torpedo-luck fixed at the delivery mechanics, not just budgets: proactive island avoidance (deterministic `generateMap` from the welcome seed), LOS-gated firing via shared `segCircleHit`, racetrack strafing vs a latched-stationary target, astern unstick. Step-5 assertions relaxed to race-independent lifecycle invariants (winner A placed 1st, every hull placed ≥1, B bounded 2..6). Plus `portOpen` pre-boot guard + SIGKILL teardown fallback, and wider budgets. Proven by 3 consecutive clean full-lifecycle runs (~5.5 min each). No production code touched.
5. **Grace chaining unlimited** (spec-0-2) — REMAINS DEFERRED. A per-match grace budget (count or cumulative seconds) is a game-design decision for Eric; natural home Epic 6.7.
6. **Half-resume double-fault** (spec-0-2) — REMAINS DEFERRED. Whether to fix via an acking-aware hold policy or token-retry tolerance is unsettled; natural home Epic 6.7.
7. **Sunk-while-away death UX** (spec-0-2) — REMAINS DEFERRED. Synthesizing the missed death moment on resume-into-spectate has open UX questions (stale kill-feed entry, killer possibly already sunk); natural home Epic 6.7 or Epic 5.
8. **match.abort 'abandoned' unreachable** (spec-0-3) — REMAINS DEFERRED. Whether quit-out/abandoned matches should be reclassified in telemetry (vs. recorded as ordinary `match.end`) is a decision for Eric before drawing balance conclusions from match.end data.
9. **PortalAdapter seam extensions** (spec-0-4) — REMAINS DEFERRED. Extending the five-method interface for real portal-SDK lifecycle needs is a design decision for Eric at Epic 7; portals remain speculative until then.

- source_spec: `_bmad-output/implementation-artifacts/spec-deferred-work-cycle.md`
  summary: matchSmoke step 2 retains a residual map-geometry torpedo-delivery flake — 1 failure ("no torpedo struck B during the ready room") observed in 10 full runs of the final hardened script; candidate fixes if the rate ever matters are a mid-countdown reposition for B away from adjacent islands, or a seedable-map dev option (previously ruled out as new server plumbing).
  evidence: Post-review verification data: 9/10 runs passed, including two separate 3-consecutive gates (~320s each, identical results traces); the one failure burned the full 120s countdown with zero booms, consistent with spawn/island geometry that starves the LOS-gated firing lanes. Pre-hardening baseline was 0/3 clean, so this is the improved tail, not a regression.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-three-hull-envelopes.md`
  summary: Gun dead ring scales with hull length — shells spawn length/2+2 ahead (64u for the battleship), so a hull hugging a BB's flank inside that ring is un-shootable on every bearing; route to Story 1.4 gun tuning (Eric confirmed defer 2026-07-19).
  evidence: Blind Hunter CONFIRMED arithmetically (hullClearOffset in server/src/game/equipment/ballistics.ts; formula unchanged from prototype, literal board scale tripled the ring from ~22u); broadside dead water ~48u vs the old ~2u.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-three-hull-envelopes.md`
  summary: Hull-aware perception — Eric ruling (2026-07-19, verbatim): "i want to see any part of the ship that is in my sight bubble, and i want radar to pick up any part of the ship that is in radar range"; sight/radar checks in perception.ts are center-based, so up to ~62u of a battleship hull can sit invisibly inside the sight bubble (shells visibly boom on empty water) — needs its own story with a full review gate (perception invariant oracles must become hull-aware too).
  evidence: Blind Hunter traced (center-based dist checks in server/src/game/perception.ts vs 124u hulls whose silhouette IS the hitbox); deferred from 1.3 because the change lands in the anti-cheat core after 1.3's adversarial review had already run.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-universal-standard-gun.md`
  summary: muzzleSpawn (and the pre-existing hullClearOffset path used by torpedoes/mines) is island-blind — a ship pressed against an island firing into it spawns the projectile inside the rock for a wasted-shot splash; benign and pre-existing in class, but worth an island-clearance pass when projectile spawn logic is next touched.
  evidence: Codex cross-model review traced it (ballistics.ts muzzleSpawn + world.ts publish-then-resolve ordering); verified pre-existing parity — hullClearOffset never claimed island clearance either; severity low (reveal originates at the owner's own hull, no info leak).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-universal-standard-gun.md`
  summary: combatSmoke is seed-flaky (~2/24 observed): the map seed is random per room and the fight scenario's naive goto pilot can grind against a fully-blocking island (1.3 rollback collision) until the 130s timeout — pin the map seed via dev room options or teach the smoke pilot basic island avoidance.
  evidence: Orchestrator reproduced 2 failures in 4 back-to-back runs, then 20 consecutive greens with an instrumented copy; when in gun range, damage flowed correctly in every observed run (25/burst, 3 hits to sink, kill credit correct) — failure trace never showed hp loss, consistent with the pilot never closing.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-firing-under-latency-latency-harness.md`
  summary: spawnBallistic's `pending.push(ballisticEvent(shell))` is pre-existing dead code on the wire — signals.ts drops ownerless tick ballistic events and all shell/torp reveals come from perception's ballisticScan — delete it (and its NOTE comment) in a hygiene pass.
  evidence: byte-identical at base 4c18e03 (predates 1.5, verified via git log -L during the 1.5 patch round); signals.ts shell/torp rows require ownerId and the reveal path is ballisticScan with t = reveal time; confirmed independently by both Fable hunters and Codex during the 1.5 review gate.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-torpedo-boat-loadout.md`
  summary: Denial-prediction staleness race (weapons AND the new ability path) — a press/click at the reload boundary can pulse DENIED client-side while the server accepts (reload completes in the same server tick), or inversely; consider RTT-aware readiness or a server denial signal if playtests surface it.
  evidence: Codex + Edge Case Hunter both traced the ability-path case (client/src/main.ts abilityPressDenied reads ~half-RTT-stale you.ammo; server ticks reloads in fireControl before activationControl); the identical mechanism pre-exists for weapon clicks via deniedFire's stale-ammo predicate, so 1.6 inherited, not introduced, it — self-correcting via authoritative frames, feel-level only.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-torpedo-boat-loadout.md`
  summary: latencyHarness A/B lost discriminating power under the 1.6 speed rescale — the strafing mover at 45 u/s is hit 100% even with fireT=0 (B) at 20 and 30 shots, so agreement(A) > agreement(B) can no longer demonstrate D1's benefit; harden the scenario (faster/erratic mover, longer range, or tighter timing) when NFR3 metrics are next consulted.
  evidence: Post-rescale runs — shots=20: A 100%/B 100%; shots=30: A 96.7%/B 100% (A's miss consistent with 2% loss); pre-rescale record was A=95-100% vs B=70-80%. Structural pass (boot, A/B, metrics block, exit 0) still holds; only the advisory comparison saturated.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-torpedo-boat-loadout.md`
  status: RESOLVED by Story 5-6 + interstitial cycle 94 — verified 2026-08-18 against `shared/src/constants.ts`. The fleet envelopes were rescaled (`maxSpeed` 46/38/30 -> 40/35/30, hp 80/100/120 -> 45/60/75; Eric ruling 2026-08-14, epic-6 amendment 24), so the Torpedo Boat at 45 is the fastest hull afloat and the 1.6 inversion is gone. NOTE, small doc drift found in passing: the torpedo-speed comment at `constants.ts:1019` still says "droneSmall at 46 is the fastest afloat" — stale but harmless (the fish at 60 outruns everything either way); route to the 7-5 doc batch.
  summary: Rescale + rebalance the drone envelopes at Epic 5 (Story 5.6, Roving PvE Fleets) — Eric ruled 2026-07-21 that the pinned prototype blocks (small 46 / medium 38 / large 30) get re-tuned against the post-1.6 class envelopes (TB 45 / ML 40 / BS 35); until then droneSmall remains the fastest hull afloat by design acceptance.
  evidence: 1.6 review flagged the inversion (droneSmall 46 > TB 45); Eric's directive after the 1.6 PR: "rescale and rebalance the drones when we get to Epic 5" — note also recorded in epics.md Story 5.6.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-mine-layer-loadout.md`
  summary: server/scripts/weaponsSmoke.mjs mine phases are dead — broken since 1.6 (joins default to torpedoBoat, whose slot 2 is speedBoost) and 1.8's mine→ability flip removed the last click-fire mine path in the game; convert the smoke to join with cls:'mineLayer' and drive actSeq/actSlot presses (slot 1) in a smoke-hygiene pass.
  evidence: Blind Hunter traced dropMines() (inp.slot=2 + fireSeq clicks, no cls in joinClient) at the 1.8 review gate; phases 3-4 cannot produce a mine on any post-1.6 build, and after 1.8 fireControl's weapon wall refuses mine slots entirely.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-mine-layer-loadout.md`
  summary: dropPoint (mines + the new decoy) is island/boundary-blind — an ML with its stern against an island can lay a mine or buoy inside the rock; for the decoy the failure is total (LOS-blocked from every bearing, so it never blips or truesights while the owner burns the charge + 20 s reload with zero feedback) — fold into the future island-clearance pass alongside the 1-4 muzzleSpawn entry.
  evidence: Edge Case Hunter traced decoy.ts→mines.dropPoint at the 1.8 review gate: hull-clear of the OWNER only, no island check anywhere on the drop path; same family as the 1-4 muzzleSpawn ledger entry (island-blind spawn offsets), with a new higher-stakes consumer.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-mine-layer-loadout.md`
  summary: Decoy 30 s persistence past owner death is a cross-reference wire tell — genuine blips never carry a dead ship's id (blipSignal gates target.alive) and deaths are globally visible, so a modified client can flag every dead-owner blip as certainly-fake for up to 30 s; needs an Eric ruling (despawn-on-death vs accept the tell) — spec-ruled behavior (litZone precedent), so not patchable in review.
  evidence: Blind Hunter at the 1.8 review gate; composes two ruled behaviors (persistence + alive-gated genuine blips); visual client unaffected (blips render anonymously), FR10 wire-level only.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-mine-layer-loadout.md`
  summary: Shooting your own minefield at gun range (650 u) gives the owner almost no confirmation — the burst-detonation boom is victimless and visibility-gated (sight 220 u / own zones), and blast dmg is victim-private, so the headline ML interaction reads as "chart markers vanished"; consider an owner-private detonation cue if playtests agree — feel gap, Eric's call.
  evidence: Blind Hunter traced boomSignal visibility vs gun range at the 1.8 review gate; fog-law-consistent, so any fix is a deliberate design addition (e.g. owner-private event or audio), not a bug fix.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-mine-layer-loadout.md`
  status: PARTIAL — Story 1.10 resolved the STALE-PREDICATE variant only (authoritative wire denial `FrameMsg.denied` keyed on actSeq, client denial tone, one-feedback dedup). The TRANSPORT-COALESCING variant (both presses inside one input) REMAINS OPEN. Already stated in the 2026-07-23 resolutions section; marked on the entry 2026-08-18.
  summary: Within-RTT double press on any ability is silently swallowed (denial predicate reads server-acked ammo, so the second press predicts READY, no pulse fires, and the server's no-ammo refusal never travels the wire) — pre-existing boost-era pattern (see the 1-6 denial-staleness entry) now covering three abilities; revisit with RTT-aware readiness or a server denial signal.
  evidence: Blind Hunter traced handleAbilityPress→abilityPressDenied at the 1.8 review gate; same mechanism as the 1-6 deferred entry's staleness race, extended surface, feel-level only (authoritative frames self-correct).

## 2026-07-23 — Story 1.10 resolutions (Firing Arcs for the Class Era)

Story 1.10 (spec-1-10-firing-arcs-for-the-class-era.md) made denial authoritative: a SELF-PRIVATE server denial signal on the wire (FrameMsg.denied, PV10 — reasons 'out-of-arc'/'no-ammo'/'cooling'/'blocked'), a client denial tone, an exactly-one-feedback (slot, seq) dedup, and island/boundary-blocked stern drops. Three entries above resolve on it (entry 1 in part — a transport-coalescing variant remains); no existing entry was edited.

1. **Within-RTT double press — stale-predicate swallow** (spec-1-8, last entry) — RESOLVED by Story 1.10 for the stale-predicate variant only. When both presses ride SEPARATE inputs the server refuses the second and queues a wire denial keyed on its actSeq (`World.activationControl` → `queueDenial` → `FrameMsg.denied`); the client's (slot, seq) dedup finds no predicted key for the stale-READY second press and fires the full pulse + chip flash + denial tone late-but-explicit. Pinned by `server/src/__tests__/denials.test.ts` ("no-ammo: a within-RTT ability double press…") and the goldenFrames `denied-noammo-ability` sub-case. STILL OPEN — a transport-coalescing variant: if two presses land within ONE server tick, the latest-input store evaluates only the newest press, so the older one is neither activated nor denied (no denial is queued for a press the server never sees as distinct) — the silent-swallow class is NOT fully closed.
2. **Denial-prediction staleness race — server-signal halves** (spec-1-6) — RESOLVED (the halves a server signal can fix) by Story 1.10. A click the client predicts READY but the server refuses (reload-boundary race) now travels as a `'cooling'` denial and produces explicit late feedback (denials.test "cooling: a weapon click against an empty (reloading) pool…"). The INVERSE half — client predicts DENIED while the server accepts — is inherent to prediction (no server signal exists for "accepted"; the shot's own success feedback self-corrects) and stays accepted behavior, not deferred work.
3. **mine/decoy dropPoint island/boundary-blindness** (spec-1-8) — RESOLVED by Story 1.10. `equipment/mines.ts dropBlocked()` (shared `pointInCircle` — island circles + the water disk) is checked BEFORE the pool in both the mine and decoy rows: a blocked stern drop returns `'blocked'`, consumes NOTHING (charge + reload kept), and reaches the owner as an explicit denial. Pinned by denials.test blocked cases (island + boundary, mine + decoy) and the goldenFrames `denied-blocked-stern-drop` sub-case. NOTE: the sibling **gun-family muzzleSpawn island-blindness** (spec-1-4) REMAINS OPEN by explicit 1.10 scope ruling — a ship pressed against an island still wastes a gun/cannon shell into the rock (benign splash, no denial).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-firing-arcs-for-the-class-era.md`
  summary: Transport-coalescing press swallow — two input messages (fireSeq/actSeq n and n+1) landing within one server tick leave only the newest in the latest-input store, so the older press is neither activated nor denied (the denial channel structurally cannot see a press the server never evaluates); closing it needs seq-gap detection or a server-side press queue.
  evidence: Both 1.10 review hunters traced fireControl/activationControl over the latest-input store; the client's 1.8 FIFO spaces same-sample presses across ticks, so the residual trigger is network jitter bunching deliveries — real but rare, feel-level.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-firing-arcs-for-the-class-era.md`
  summary: Keyboard ability FIFO cap (SLOT_COUNT) silently drops a further same-window press — it never queues, never reaches the wire, and produces no denial feedback (pre-existing from the 1.8 dual-press queue).
  evidence: Edge Case Hunter traced activateAbility's early return at pendingActs.length >= SLOT_COUNT (client/src/input/keyboard.ts); requires mashing 5+ ability presses inside one 50 ms sample — extreme edge, but formally violates never-silence.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-11-design-tokens-typography.md`
  summary: DESIGN.md frontmatter carries a self-inconsistent card-scrim annotation — hex '#030605' (rgb 3,6,5) vs its inline comment "rendered as rgba(3,7,5,.9)" — needing Eric's one-character doc ruling (hex was treated as authoritative and shipped verbatim).
  evidence: Both review hunters independently flagged it; #030605 decodes to rgb(3,6,5), so the comment's 7 is a typo in one direction or the hex in the other; client tokens pin 0x030605 until the doc rules.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-12-regatta-hoist.md`
  summary: UX-DR6 doc-sync — Eric ruled 2026-07-23 that personal-hue assignment is FIRST-COME-FIRST-SERVED at join (pref-if-free / nearest-free-clockwise-on-tie / seeded-random-free, colors never change mid-match), SUPERSEDING UX-DR6's documented "assigned at match start + fair random draw, losers fall to nearest free hue". The code (game/regatta.ts + ArenaRoom.onJoin) implements the ruling; DESIGN.md (Regatta Hoist section, ~line 152) and epics.md still describe the old match-start/random-draw wording. Reconciling that doc language is Eric's call, not this workflow's (design-doc edits are gated).
  evidence: Ratified in the spec's `<intent-contract>` (Eric rulings 2026-07-23, ruling 1) and Design Notes; the AskUserQuestion answers are the authority. Shipped behavior pinned by server/src/__tests__/regatta.test.ts (FCFS matrix); the DESIGN.md/epics.md text is the only unsynced surface.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-12-regatta-hoist.md`
  summary: UX-DR7 doc-sync — Eric ruled 2026-07-23 that the personal-hue PROPAGATION set extends to ordnance truth-markers (mines, lit zones, decoy buoys), each rendered in the FIRER's hue for ALL observers via new MineView.by / DecoyView.by wire attribution — a deliberate intel grant beyond DESIGN.md's documented propagation list (own hull / nameplate / own-blip ring / radar blips / kill-feed names). DESIGN.md (Regatta Hoist "Propagation" bullet, ~line 161) and the GDD do not mention ordnance-marker recoloring or the added wire attribution; syncing that language + logging the intel-grant tradeoff is Eric's call.
  evidence: Ratified in the spec's `<intent-contract>` (Eric rulings 2026-07-23, ruling 3) and Design Notes ("Counter-intel safety" note confirms the decoy `by` changes only the truesight truth channel, leaving the counterIntel blip indistinguishable). Wire change pinned by signals.test.ts + perception invariant extension; the DESIGN.md/GDD text is the unsynced surface.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-13-class-silhouettes-on-the-water.md`
  summary: EXPERIENCE.md contested-hoist toast — Eric REJECTED it outright 2026-07-23 ("most pointless toast imaginable" — personal color is assigned at join, first-come, and never contended in a way worth a toast). EXPERIENCE.md still carries the contested-hoist toast proposal; that surface is NOT implemented and must NEVER be built absent a new ruling. Removing/annotating the EXPERIENCE.md language is Eric's call (design-doc edits are gated), not this workflow's.
  evidence: Ratified in the spec's `<intent-contract>` (Never list) + Design Notes (Eric rulings 2026-07-23): "contested-hoist toast REJECTED outright". The code never built it; the only open item is the EXPERIENCE.md doc still describing a rejected feature.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-13-class-silhouettes-on-the-water.md`
  summary: DESIGN.md nameplate/kill-feed 14-char callsign entry cap — carried as "[PROPOSAL, matches Kill Feed]" in DESIGN.md (Nameplate + Kill Feed component rows, ~lines 232/235) — is now RATIFIED and IMPLEMENTED (Eric ruling 2026-07-23: tighten 16→14 NOW). Code enforces it (util/text.ts NAME_MAX=14, menu input.maxLength/sanitizeName + stored-name re-slice, feed + plate ellipsize). Dropping the "[PROPOSAL]" tag in DESIGN.md is Eric's call (design-doc edits are gated), not this workflow's.
  evidence: Ratified in the spec's `<intent-contract>` (Eric rulings 2026-07-23, ruling 1) and Design Notes; pinned by menu.test (cap-14 typed + stored-legacy-16 re-slice) and killFeed.test (unchanged 14-cap ellipsis). Only the DESIGN.md [PROPOSAL] annotation is the unsynced surface.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-13-class-silhouettes-on-the-water.md`
  status: RESOLVED by Story 2.3 — verified 2026-08-18. `sanitizeName()` in `server/src/rooms/roomOptions.ts` caps length (`Array.from(trimmed).slice(0, NAME_MAX)`, surrogate-safe) at the join-time source rather than at each display consumer. `ArenaRoom.ts:813` cites this ledger entry by number in its own comment.
  summary: Server never length-caps `options.name` — the join-time callsign is validated/trimmed only for content, not length, so a hostile client can seat an arbitrarily long roster name; every DISPLAY path defends (kill feed + nameplate both mid-ellipsize to 14 code points, surrogate-safe), but a cheap join-time cap (sanitize in ArenaRoom.onJoin / roomOptions) would defend once at the source instead of at each consumer. Client-only story could not touch the server; a hardening pass should add the cap.
  evidence: Story 1.13 was client-only (no shared/server change permitted); the I/O matrix's "Long name (legacy/hostile — server never caps)" row documents the gap. Display safety pinned by nameplates.test (code-point ellipsis incl. surrogate pairs) + killFeed.test; the missing server-side cap is the hardening item.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-13-class-silhouettes-on-the-water.md`
  status: RESOLVED by Story 2.3 — verified 2026-08-18. `sanitizeName()` type-guards first (`if (typeof v !== 'string') return undefined`), so the uncaught seat-time throw on a non-string `options.name` is closed; the same function caps length. `ArenaRoom.ts:813` cites this entry by number.
  summary: Server hardening — `ArenaRoom.onJoin` does `options.name?.trim()` with NEITHER type NOR length validation: a hostile non-string join option (e.g. `name: 123`) throws inside onJoin because a number has no `.trim()` (Colyseus rejects that single join and the server survives, but it is an uncaught seat-time throw), and even a well-typed string is length-uncapped (the companion display-cap entry above). A join-time gate should validate the type (coerce/reject non-strings) AND cap the length once at the source (ArenaRoom.onJoin / roomOptions), rather than leaning on every display consumer to defend.
  evidence: Traced during the Story 1.13 review round: ArenaRoom.onJoin reads `options.name?.trim()` with no `typeof` guard; the optional-chain protects only null/undefined, not a wrong-typed value, so `.trim()` on a number/object throws. Client-only story could not touch the server; folds into the same server-side callsign-sanitize hardening pass as the length-cap entry above.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-13-class-silhouettes-on-the-water.md`
  summary: Workflow hazard — step-01's compile-epic-context regeneration rebuilds `epic-N-context.md` PURELY from planning artifacts, silently ERASING review-patched corrections that were recorded only in that file. Bit this run: the 1.8/1.10 ratified-ruling records were lost and had to be restored from git. Mid-story rulings need to land in the planning docs (Eric's doc-sync) so regeneration reproduces them, OR the context compiler needs to detect and preserve manually-added correction blocks across a rebuild.
  evidence: Observed this run — epic-1-context.md was regenerated and dropped correction blocks that existed only there; recovered via `git`. Root cause: the compiler treats planning artifacts as the sole source of truth and does not diff/merge prior hand-edits, so any correction not mirrored into the planning docs is non-durable.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-14-home-class-select-chrome.md`
  summary: Home mock/DESIGN doc-sync batch — the ratified mock `home-class-picker-1.html` and DESIGN.md still carry gunboat-era + pre-ruling content the shipped chrome supersedes: "DEPLOY AS GUNBOAT" sub-line + first-run Gunboat default + keys 1-4 (superseded 2026-07-19: three classes, keys 1-3, NO default, TB pre-focused), a 12-swatch square Color Hoist (DESIGN spine + AC say 20 round 20px — shipped), filled amber button slabs (DESIGN spine: outline+glow, never a filled slab — shipped), and the "PIP VALUES: PLACEHOLDER" card note (Eric ruled absolute-anchor REAL values 2026-07-24 — shipped without the note). Reconciling mock/DESIGN language is Eric's call (design-doc edits are gated).
  evidence: Ratified in the spec's `<intent-contract>` (Always/Never) + Design Notes (Eric rulings 2026-07-23/24 + orchestrator spine-over-mock rulings); shipped behavior pinned by home.test.ts/classSelect.test.ts (pips TB 4/2/4 BS 3/4/2 ML 3/3/3, 20-swatch hoist, key map 1-3).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-14-home-class-select-chrome.md`
  summary: Mode selector control (Solo · Solo vs AI) deliberately NOT built — Eric ruled 2026-07-24 the sub-line hardcodes "· SOLO" until the mode exists; the actual selector arrives with Epic 6 (6-5 Solo-vs-AI mode + 6-6 mode-select & queue liveness). UX-DR25/EXPERIENCE text describing a home mode pick stays unimplemented until then.
  evidence: AskUserQuestion ruling this run ("No selector yet"); deploySubline pinned to "· SOLO" in home.test.ts; Epic 6 stories own the surface.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-14-home-class-select-chrome.md`
  status: RESOLVED by Story 2.3 — verified 2026-08-18. `client/src/ui/settings.ts` ships the real settings overlay and owns the ESC semantics; the inert gear and its "SETTINGS ARRIVE IN A LATER REFIT" note are gone.
  summary: Settings gear is INERT by ruling — renders top-right on home, click (and bare ESC on home) yields the quiet "SETTINGS ARRIVE IN A LATER REFIT" status-line note; Story 2.3 must wire the real settings overlay into this gear (and take over the ESC-opens-settings semantics).
  evidence: AskUserQuestion ruling this run ("Inert gear now"); inert-note pattern pinned in home.test.ts; 2.3 is the owning story per epics.md.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-14-home-class-select-chrome.md`
  summary: HOW TO PLAY link is INERT — same quiet note pattern ("FIELD MANUAL ARRIVES IN A LATER REFIT") until Story 7.4 builds the static how-to-play page and wires the link (plus ESC/back returning home per DESIGN).
  evidence: Orchestrator ruling this run (documented in spec Design Notes — no dead click, no invented page); 7.4 is the owning story per epics.md.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-14-home-class-select-chrome.md`
  summary: Server-status probe fidelity — probeServer() uses a no-cors fetch, so ANY HTTP response (including a Render proxy 502 while the game process is down, or a captive portal) paints "SERVER: READY", and the single boot-time probe never re-runs (a server that comes up after page load stays "UNREACHABLE" until refresh). An honest health signal needs a server-side decision (lightweight health endpoint or CORS-readable matchmake probe) that the 1.14 spec's "no new server endpoint" ruling forbade; fold into a later operability/hardening pass.
  evidence: All three 1.14 reviewers traced the no-cors opacity; the status line is best-effort by construction this story (connect-attempt outcomes own the line once any attempt starts — that ownership is pinned in home.test.ts). The false-READY/stale-UNREACHABLE windows are real but cosmetic pre-join.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-14-home-class-select-chrome.md`
  status: EXTENDED 2026-08-01 by the Home Page Maintenance Patch (spec-home-page-maintenance.md) — Eric explicitly ruled the chip sub-line ("STD GUN · …") removed too ("that's not needed"), reversing this entry's carve-out by direct instruction; the chip slims to silhouette + role + name + CHANGE CLASS. The cards clause is unchanged and still binding.
  summary: Class-card content ruling (Eric 2026-07-24, post-merge feedback) — the fantasy tagline under the class name ("fast, fragile, the needle-threader" etc.) and the universal GUN loadout row are REJECTED and removed from the class cards ("I never wanted that"); cards show name/key, silhouette, pips, and the two special rows only, labeled by their future keys Q/E (ruled Epic-2 mapping) instead of SPECIAL 1/2. The ratified mock home-class-picker-1.html and DESIGN.md Class Card anatomy still describe both — doc-sync is Eric's call, and no future run may resurrect them from the mock.
  evidence: Direct Eric instruction in the 1-14 follow-up session; enforced by classSelect.test.ts pins (no `fantasy` field on the card view-model; loadout rows = the two specials exactly, keyed Q/E). Chip sub-line ("STD GUN · …") untouched — the ruling named the cards only.

## 2026-07-24 — Doc-sync batch resolutions (Epic 1 retro Action #3, Eric-ruled)

Eric ruled the full batch via the retro's doc-sync proposal (`doc-sync-proposal-2026-07-24.md`: Q1 comment-is-the-typo, Q2 stamp-superseded, Q3 leave-broader-sweep-for-7.5, R1–R7 approved as proposed). Seven entries above resolve; no existing entry was edited.

1. **DESIGN.md card-scrim annotation** (spec-1-11) — RESOLVED: Eric ruled the rgba comment was the typo; comment + refit-card fixed to `rgba(3,6,5,.9)`, hex `#030605` stays authoritative, client tokens untouched.
2. **UX-DR6 doc-sync** (spec-1-12) — RESOLVED: FCFS-at-join wording now in DESIGN.md (Regatta Hoist), EXPERIENCE.md (Component Patterns), and epics.md UX-DR6, each citing the 2026-07-23 ruling.
3. **UX-DR7 doc-sync** (spec-1-12) — RESOLVED: ordnance truth-markers in the firer's hue for all observers (intel grant + counter-intel caveat) documented in DESIGN.md Propagation + Torpedo/mine row and epics.md UX-DR7.
4. **Contested-hoist toast** (spec-1-13) — RESOLVED: EXPERIENCE.md State Patterns entry rewritten as REJECTED/do-not-build; "hoist fallback" removed from the Toast pattern; epics.md UX-DR6 toast clause removed.
5. **14-char cap [PROPOSAL] tags** (spec-1-13) — RESOLVED: both DESIGN.md tags (Nameplate, Kill Feed rows) replaced with "ratified 2026-07-23, Story 1.13". (The two server `options.name` hardening entries above remain OPEN — code work, not doc-sync.)
6. **Home mock/DESIGN doc-sync batch** (spec-1-14) — RESOLVED: DESIGN.md Primary Button sub-line, key chips 1–3, EXPERIENCE.md home/class-select rows, first-run forced choice, and Open Questions 13/15 all synced; mock `home-class-picker-1.html` stamped SUPERSEDED (banner + title) per Eric's Q2 ruling — kept as layout-register audit trail, never content-authoritative.
7. **Class-card content ruling doc-sync** (spec-1-14) — RESOLVED: DESIGN.md Class Card anatomy rewritten (no fantasy line, no GUN row, real-value pips, two specials keyed Q/E, do-not-resurrect note); EXPERIENCE.md card contents synced.

Also filed-and-closed at birth (previously unledgered): **GDD aim-clamp wording** (spec-1-10 flagged it; never ledgered) — three clamp-language sentences (gdd.md Controls/Mouse, Weapon behavior laws, Aiming and Combat) rewritten to the deny-gate per the 2026-07-23 Story 1.10 ruling; the adjacent-line contradiction with the ratified arcs bullet is gone.

Deliberately NOT swept (Eric Q3 ruling): the broader gunboat-era text (DESIGN "Four launch classes" + Gunboat silhouette row + flare lever, "fifth silhouette" drone wording, Journey A/B Gunboat cameos) stays homed at Story 7.5.

## 2026-07-24 — Epic 1 retro Action #1: epic-context recompile hazard CLOSED

**Workflow hazard — step-01 epic-context regeneration erasing review-patched corrections** (spec-1-13, entry above) — RESOLVED via team customization override `_bmad/custom/bmad-dev-auto.toml` (the ledger entry's own "detect and preserve" option, made durable):

- **Durable home:** ratified corrections (Eric rulings, review patches, adjudicated contradictions) must now be recorded in `{implementation_artifacts}/epic-<N>-context-amendments.md` — never only in the regenerable `epic-<N>-context.md`.
- **Salvage before overwrite:** step-01 may not overwrite an existing epic context without first sweeping it for ratified content missing from the amendments file.
- **Re-apply after compile:** every compile re-applies the amendments (amendment wins on conflict) under a `## Ratified Amendments` section; compiled-context verification FAILS if a non-empty amendments file exists and that section is absent.
- **Amendments are never tool-regenerated;** the file is loaded as a persistent fact every run (`persistent_facts` glob), so ratified truth is in context even before any compile.

Verified with `resolve_customization.py` (three-layer merge: protocol appends to activation steps; amendments glob appends to persistent_facts). Satisfies the retro's success criteria: regenerating epic-2-context cannot erase a ratified correction; this closes the 1.13 ledger entry. Epic-1's ratified bullets already round-trip from the doc-sync'd planning docs (Action #3), and the salvage rule protects them besides.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-the-new-input-scheme.md`
  summary: Client same-window click coalescing remains — two mouse clicks inside one 50 ms input sample still ride ONE InputMsg as a fireSeq+2 jump, which the server's per-intent evaluation necessarily treats as one press (the 2.1 server queue fixed transport coalescing across messages, not sampler coalescing within a message); asymmetric with the ability FIFO that spreads same-window presses across inputs; negligible today (gun pool is 1 round / 3 s reload, so the second click would be denied anyway) — revisit if a multi-round click-fire weapon ever ships.
  evidence: Blind Hunter traced sim/inputSampler.ts click folding vs consumeClick's monotonic grammar at the 2.1 review gate; pre-existing behavior, unchanged by this story, no current gameplay consequence.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-the-new-input-scheme.md`
  summary: The keyboard chokepoint's text-entry guard treats ANY focused BUTTON as text entry, so a future focusable in-match DOM element that retains focus would fail-silent kill the whole sim keyboard (helm included) with zero feedback; safe today (refit cards preventDefault mousedown + blur on click; kill feed/toasts/banner are pointer-events:none) — adopt a blur-on-click convention or a fail-loud indicator for any future in-match focusable chrome.
  evidence: Blind Hunter traced textEntryFocused() at the 2.1 review gate; no live repro exists in the current DOM surface set, so this is a hardening convention for Stories 2.2/2.3 (hotbar, settings overlay) which add exactly such chrome.
- source_spec: `_bmad-output/implementation-artifacts/spec-gun-shell-speed-and-return-to-port-fix.md`
  summary: The results screen's RETURN TO PORT button gives zero activation feedback — after a click/Enter/ESC it stays enabled and unlabeled while the chain runs (instant today with the null ad adapter, but up to ~35s ad break + 1s leave race once Epic 7 lands a real portal adapter), so the player sees a frozen results screen with a live-looking button that silently no-ops (latched); needs a disabled state / "RETURNING…" affordance, which is DESIGN.md surface and therefore Eric's call.
  evidence: Blind Hunter at this cycle's review gate traced the latch + safeAdapter 35s cap (client/src/portal/safeAdapter.ts:8) against the unmodified button state in client/src/ui/results.ts; invisible with today's instant adapter, guaranteed visible with any real interstitial.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-the-hotbar.md`
  status: RESOLVED 2026-07-31 by Story 2.9 (Eric ruling, amendment 48) — the hotbar gains an 8th state, ACTIVE: while a boost/decoy window runs, its slot carries a persistent phosphor breathing outline (≥2s cycle, under `pulseCapHz`) plus a remaining-seconds countdown in the quick-info line, dual-coded (outline shape + text, never color alone) and motion-aware, at `denied>activated>active>cooling` precedence (boost keyed off `boostUntil`, decoy off the own-spawn latch).
  summary: The ratified 7-state hotbar grammar has no "active ability window" state — the retired chip row outlined a slot while speedBoost ran, and the new hotbar drops that signal (only the ≤80 ms activated pop and the speed-needle cap remain), so a running boost/decoy window has no persistent hotbar indication; needs an Eric ruling (natural home: Story 2.9 The Build Must Be Felt) before inventing an eighth state.
  evidence: Edge Case Hunter traced the deleted drawOneChip boost-active outline against DESIGN.md UX-DR11's state grammar, which contains no active-window state — adding one is a gated design decision, not an implementation choice.

## 2026-07-26 — Story 2.3 (Settings & Accessibility Options + Legibility Pass)

**Closures:**

1. **Server `options.name` length cap** (spec-1-13, line-127 entry) — CLOSED: `sanitizeName()` in `server/src/rooms/roomOptions.ts` caps at `NAME_MAX = 14` code points (Array.from — surrogate-safe), matching the client entry cap; unit-tested.
2. **Server `options.name` type guard** (spec-1-13, line-130 entry) — CLOSED: same `sanitizeName()` returns `undefined` for any non-string (the old `options.name?.trim()` threw on e.g. `name: 123`); caller falls back to `CAPTAIN-n`.
3. **Inert settings gear / ESC seam** (spec-1-14 entry) — CLOSED: the real settings overlay is wired to the home gear and the uniform ESC topmost-close law (amendments 21/23); the `NOTE_SETTINGS` placeholder is deleted.
4. **Text-entry-guard hardening convention** (spec-2-1 entry) — ADDRESSED for 2.3's chrome: the settings overlay/results modal route through the explicit `isOverlayFocused` chokepoint hook (fail-loud by design: only ESC/Enter act) rather than relying on the BUTTON-focus heuristic. The convention note stays open for future in-match focusable chrome.

**New entries:**

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-settings-accessibility-options.md`
  summary: DESIGN.md typography/color rows are now superseded by amendments 15–17 (hud-micro 9px pin → 14px; label 11 → 17; text-muted usage rules — grey retired for load-bearing text; hotbar data phosphor + white names) and the binding/ESC rows by amendments 21–23 (uniform topmost-close ESC, elimination results modal). Needs the Eric-gated doc-sync batch (Story 7.5 home or a dedicated doc-sync cycle); no design docs were edited in-story per standing rule.
  evidence: Amendments 14–23 in epic-2-context-amendments.md vs DESIGN.md Typography/Colors tables and EXPERIENCE ESC/results primitives.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-settings-accessibility-options.md`
  summary: The results modal's "ships you sank" list is best-effort by construction — `sunk` events are LOS-gated by perception, so a kill scored without line of sight (mine, blind torpedo) contributes to the roster-derived kill TALLY but cannot contribute a NAME to the list. Making the list exhaustive requires a self-private wire addition (e.g. victim name on the killer's own frame), a PV bump — Eric's call whether the gap matters once real playtests surface it.
  evidence: score.ts header documents the derivation; perception.observe() strips unsighted victim ids by ratified anti-cheat rule (per-event visibility).
- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-settings-accessibility-options.md`
  summary: The game-end results modal is ESC-dismissible (per the uniform topmost-close ruling) but has NO reopen path — a player who ESC-closes it idles on the dead ocean with settings→ABANDON as the only exit and the placement table gone for good; whether a reopen affordance (or an inert-ESC exception at game end) is wanted is Eric's call.
  evidence: Edge Case Hunter traced closeResultsAsSpectate with the game-end onSpectate no-op at the 2.3 review gate; behavior follows amendment 23 literally, the gap is the missing way back.

## 2026-07-28 — Story 2.5 (Boon Effect Engine)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-boon-effect-engine.md`
  status: RESOLVED 2026-07-30 by Story 2.8 — `validateBoonDef`/`validateCatalog` landed in shared/src/sim/boons.ts (whitelisted paths, finite positive values, rarity/copies sanity, exclusiveWith symmetry, doctrine-mode unions, non-empty effects; production catalog pinned valid by test); the cap-raise design call ruled by Eric as amendment 41 ("everything arrives loaded" — applyBoon tops up to raised caps); duplicate-equipment fits ruled out by construction (acquisition cards exist only for NOT-carried equipment, so the id-addressed no-op path is unreachable, pinned; Option D swap-refund stays ledgered separately).
  summary: Catalog authoring-time validation belongs to 2.8 — the engine now guards runtime (finite/positive stat folds, sweepRpm ceiling re-applied over the fold, hasOwn lookups everywhere, hp/pool clamps on lowered caps, slot-degeneracy no-ops), but there is no authoring-time `validateBoonDef` (paths on the whitelist, hookIds present in HOOK_REGISTRY, params sane, effects non-empty), and two design calls are deliberately parked: whether a cap-RAISING boon also tops up loaded rounds (the applyGrantEffects +1 precedent is upgrade-specific), and whether duplicate-equipment fits (a two-tube build) are ever wanted — the engine's replace-by-id addressing forbids duplicates today, so allowing them means replacing id-addressing first.
  evidence: Review-gate findings from both Fable hunters + Codex at the 2.5 gate (all runtime guards were absent pre-patch; guards were chosen deliberately minimal to keep 2.8's design latitude intact — see spec Review Triage Log 2026-07-28).

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-xp-tick-kill-bonuses.md`
  status: RESOLVED 2026-07-30 by Story 2.7 (Eric ruling, amendment 37: the level-up toast is SUPPRESSED entirely while dead/spectating — no spectate refit surface exists, and server-side dead-killer banking is unchanged). Implemented as the `frameIsDeadOrSpectating` gate on roomBindings' `pt` toast path (toast AND tone), pinned by roomBindings.test.ts.
  summary: A dead killer's posthumous level-up (kill credit is deliberately not alive-gated) fires the "▲ LEVEL UP — TAB TO REFIT" toast while spectating, where TAB is a no-op and the banked level is unspendable (offerView null when spectating; death is permanent in active) — the reward is real but invisible and unusable; whether the spectate-phase spend surface should exist (or the toast should suppress/reword) is a 2.7 spend-UX design call.
  evidence: Blind Hunter traced the pt event riding the dead killer's spectator frame (goldenFrames `spec:true` row) into roomBindings' toast+tone path while currentOfferView returns null and the chip/cue are hidden; parity with the pre-2.6 PTS readout (also hidden in spectate), so pre-existing shape made newly visible by the reworded toast.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-7-offers-roll-bank-spend.md`
  status: RESOLVED 2026-07-30 by Story 2.8 (Eric ruling, amendment 40: the overlap is RATIFIED outright — the dimmed chrome is inert during the refit combat lockout; no band lift, no card shrink). The overlap pin in upgradeMenu.test.ts was re-taken deliberately at the new 236px card height (rarity/lineage/doctrine lines); inner cards still clear both corner clusters at the 1366×768 and 1280×614 floors.
  summary: The ratified refit-band geometry (924px row at ~58% viewport height) makes the two OUTER cards overlap the 38%-dimmed bottom-left hotbar and bottom-right vitals cluster at the floor viewports (1366×768 / 1280×614 logical) — implemented as ratified and pinned as a CONSCIOUS overlap (inner cards stay clear; cards render above the dimmed chrome), but whether to lift the band, shrink the cards, or ratify the overlap outright is an Eric design call, natural to settle alongside 2.8's catalog/card-content design session.
  evidence: Geometry is arithmetic, not opinion — at 768px height the band tops at ~445px and the corner clusters begin at y≥445 (hotbar) / y≥490 (vitals), leaving ~7px of fully clear band; upgradeMenu.test.ts pins the overlap deliberately so any geometry change is a conscious break, and the implementing agent flagged it as a real design tension during the 2.7 run.

## 2026-07-30 — Story 2.8 (Boon Catalog v1 — THE DECK MODEL)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-8-boon-catalog-v1.md`
  summary: Per-tick hull-snapshot staleness (PRE-EXISTING, surfaced by Codex at the 2.8 gate) — `aliveHulls()` is snapshotted once before stepShells/stepMines, so a ship killed early in a tick still collides with later shells, can still trip mine triggers, and can be homing-acquired as a target for the remainder of that tick (~50ms). Predates 2.8 (same pattern for ordinary shells since the ballistics era); homing/creep are merely new consumers. Fix shape: re-filter or invalidate hulls on death within the tick, with careful attention to determinism and the defined step order.
  evidence: Codex CONFIRMED with file:line traces (world.ts stepShells/stepMines/homing-acquire all reading the pre-step snapshot); orchestrator adjudicated pre-existing (the snapshot idiom and step order are original architecture).
- source_spec: `_bmad-output/implementation-artifacts/spec-2-8-boon-catalog-v1.md`
  status: RESOLVED 2026-07-31 by Story 2.9. The doctrine-card overflow sub-item was already resolved earlier by PR #72 (Container-fit law, amendment 47 — descSize 17->15 + tightened contract copy, before 2.9 started). The remaining five items all close this run: AP pierce hits now render a dedicated identity (derived `#pN` booms -> a contracting crimson ring, distinct from the terminal boom); prop-fouling victims get a real HUD tell (`SLOWED Ns`, dual-coded, rising-edge tone); the hotbar tooltip renders the accrued-boon list as `◆ Name` + a live effect line (no more ABSENCE) with `◆n` quick-info compression; incendiary burn feedback has its own identity (a burn tone + ember-zone visuals + reduced shake, distinct from an ordinary damage event); and EXCLUSIVE rarity color was signed off by Eric AS-IS — storm purple is KEPT, a deliberate DESIGN.md overrule recorded as amendment 49 (not recolored to the recommended white-hot register).
  summary: 2.9 presentation seams deliberately left silent by 2.8's minimal-UI scope — AP shells' 2nd/3rd pierce hits have no dedicated visual (the track survives via derived boom ids but pierce feedback is generic), prop-fouling victims get no HUD tell that they are slowed (the predictor folds it honestly; nothing surfaces it), the hotbar tooltip still renders accrued boons as ABSENCE, the longest doctrine-card contract texts can overflow the 236px card body, incendiary burn feedback rides ordinary 500ms-aggregated dmg events with no burn identity, and the EXCLUSIVE rarity text color sits in the storm purple family (readout register — needs Eric's sign-off in the identity pass).
  evidence: Wave-3 and patch-round agent reports at the 2.8 gate; all are presentation-layer gaps in FR22 territory that Story 2.9 ("The Build Must Be Felt") owns by ratified scope split (amendment 38 — 2-9 owns per-boon presentation/juice).
- source_spec: `_bmad-output/implementation-artifacts/spec-2-8-boon-catalog-v1.md`
  status: RESOLVED 2026-07-31 by amendment 52 (Eric ruling, Story 2.9 pre-implementation questions) — "dazzle stays optics-only" ratifies the 2.8 orchestrator's two-tier-vision reading outright: dazzle halves truesight only, the victim's radar sweep is untouched by design, not oversight. Story 2.9 adds the victim-side DAZZLED tell only; no sim change.
  summary: Dazzle blinds truesight but NOT radar — a dazzled victim's sweep can still paint blips in the band where truesight was taken (the annulus widens inward). Orchestrator ruling from the two-tier vision model (optics blinded, radar unaffected); flagged for Eric because the compensating blip intel has gameplay consequence and only "reduced truesight" was ratified.
  evidence: Blind Hunter traced signals.ts inRadarAnnulus using the dazzle-scaled sightOf; behavior is deliberate, commented in code, and pinned by test — the open question is design intent, not correctness.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-8-boon-catalog-v1.md`
  summary: Smoke screen intent AFFIRMED but DEFERRED past v1 (amendment 38 — "push established systems"), and Option D exclusive swap-and-refund-as-refit-points stays ledgered (best-feeling swap design but a deck-thinning laundering exploit that erases R-commitment drama; revisit only if live play resents permanence). Both carried from the 2026-07-30 brainstorm party review so the ledger and epics doc stop contradicting each other.
  evidence: brainstorming-session-2026-07-30.md party-review rulings 4 and Emergence-Check ruling 5; recorded durably as amendment 38.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-8-boon-catalog-v1.md`
  status: PARTIALLY RESOLVED 2026-07-31 by Story 2-10 (amendments 56/57) — the rare-weight dial is no longer a handwave (raised 0.35→0.7 on batch-sim evidence, Eric-ratified; time-to-first-exclusive distributions delivered per amendment 38) and CONFIG.xp was evidence-confirmed as-is. STILL OPEN: catalog step values / doctrine factors — measured but deliberately NOT retuned (bot-combat evidence is noisy until Epic 6 bot quality; needs real-play or bot-vs-bot evidence).
  summary: Every catalog step value, doctrine factor, spread, range, and the CONFIG.deck rare-weight dial are implementer-drafted handwaves inside ratified pins (guardrail incl. the new salvo aggregate, torpedo-outruns-hulls incl. max shipSpeed stacks, sweep 30 cap, trigger≤blast) — Story 2-10's batch-sim harness owes the evidence pass (time-to-first-exclusive distributions especially, per the brainstorm's escalating-rare-weight ruling).
  evidence: Amendment 38 (dial set with 2-10 batch-sim evidence) + the epic's "tuning is evidence, not vibes" constraint; values enumerated in the spec's catalog table and CONFIG blocks.

## 2026-07-31 — Story 2.9 (The Build Must Be Felt)

Story 2.9 (spec-2-9-the-build-must-be-felt.md) closed FR22 ("a presentation-silent boon is a defect") over the full 42-line catalog: the one wire change (`LitZoneView.mode`, PV 16->17), tiered audible fit cues + victim tells + an audio<->visual twin table, the hotbar/tooltip accrued-boon pass + the ratified 8th ACTIVE state (amendment 48), on-water identity for all 8 doctrines (incl. the frozen self-propelled-mine renderer fix), and a catalog-walking fit-check structural test (`client/src/__tests__/fitCheck.test.ts`) that fails the build on any future presentation-silent line. Client 1120->1137, `npm run lint` 0 errors.

**Closures:** three entries above resolve — the 2.2 active-ability-window entry (RESOLVED by amendment 48's 8th ACTIVE state), the 2.8 "2.9 presentation seams" entry (RESOLVED; its doctrine-card-overflow sub-item had already closed separately via PR #72), and the 2.8 dazzle-radar FYI flag (RESOLVED by amendment 52 — optics-only ratified by Eric as the intended design, not an oversight). See each entry's own `status:` line above; no entry text was otherwise edited.

**New entries:**

- source_spec: `_bmad-output/implementation-artifacts/spec-2-9-the-build-must-be-felt.md`
  summary: Every new draft-copy value this story shipped is an implementer-drafted handwave inside ratified pins, per the standing draft-copy rule: the fit-tone tier envelopes and per-category `fitDetune` transpositions (`client/src/audio/tones.ts`), the SLOWED/DAZZLED/burn tell copy and timing, the per-mode zone looks (ember breathe rate/amplitude, dazzle glare/halo fractions — `CLIENT_CONFIG.litZone`), the mine-creep tell geometry (tick length, wake spacing — `CLIENT_CONFIG.ordnance`), the AP pierce ring's contraction curve, and the cannon own-side weight values (muzzle intensity, dot/glow radii, arc-swell/AP-stretch amounts). None of these are canon; a later identity/audio pass with Eric should ratify or retune them, likely alongside 2-10's batch-sim evidence pass over the mechanical dials.
  evidence: Every value in `client/src/audio/tones.ts` (fitCommon/fitRare/fitExclusive/burn/slowed/dazzled specs, `FIT_CATEGORY_CENTS`), `client/src/config.ts` (`litZone`, `ordnance` blocks), and `client/src/render/projectiles.ts` (`O.cannonCoreR`/`cannonGlowR`/`cannonGlowAlpha`/`arcSwell`/`apStretch`) is a Story 2.9 comment-flagged draft; none cites a ratified spec table.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-9-the-build-must-be-felt.md`
  summary: Three design-doc surfaces need the Eric-gated doc-sync batch (no design docs were edited in-story, per standing rule): DESIGN.md's "don't use purple for anything but the storm, ever" law needs the amendment-49 EXCLUSIVE carve-out recorded as an explicit, named exception; DESIGN.md/EXPERIENCE.md need the `LitZoneView.mode` wire addition (amendment 50 — zones are doctrine-distinct to every observer, a deliberate counterplay-over-concealment ruling) folded into their wire-contract and fog-of-war sections; and the retired `upgrade` two-note tone (superseded wholesale by the fitCommon/fitRare/fitExclusive family) needs its EXPERIENCE.md audio-cue table row updated to the new three-tier family.
  evidence: Amendments 49 and 50 (`epic-2-context-amendments.md`, this story); `client/src/audio/tones.ts`'s FIT family comment names the retired `upgrade` cue verbatim as its template; DESIGN.md's purple law and EXPERIENCE.md's zone/audio sections are unmodified as of this PR (design-doc edits are gated, not this workflow's call).
- source_spec: `_bmad-output/implementation-artifacts/spec-2-9-the-build-must-be-felt.md`
  status: VERIFIED STILL OPEN 2026-08-18, AND ITS PREMISE HAS MOVED — `latchOwnFire` is still the 400ms client-side heuristic in `client/src/main.ts`. The entry sized that window against "the current 1-round/3s-reload cannon"; the cannon's base reload is now 50s (25s at a max-cooldown build) after the 2026-08-04 global-cooldown ruling, so the sizing rationale no longer describes the shipped weapon even though the window is unchanged. Re-derive the window before treating this as safe.
  summary: Own-cannon-fire correlation (which own-fire event gets the heavier cannon look/tone vs the plain gun) is a 400ms client-side latch (`main.latchOwnFire`), not a server-attributed fact — the ballistic wire stays constant-free and cannot say "this shell came from the cannon slot", so the client infers it from "did I just prime-fire the cannon slot within the last 400ms". Honest (it never mislabels an enemy shell) but heuristic: a same-tick gun+cannon double-fire, or a delayed reveal past the window, could mis-attribute the own-side look. Revisit if a multi-round cannon or faster reload ever ships (the window is sized against the current 1-round/3s-reload cannon).
  evidence: `client/src/main.ts` `latchOwnFire()` (~line 1819) and its caller at the fire-control chokepoint; no wire field exists to make this authoritative without a PV bump the story's wire-discipline rule forbade (the ONLY wire change this story permits is `LitZoneView.mode`).

## 2026-07-31 — Story 2-10 (Economy Batch-Sim Harness)

Story 2-10 (spec-2-10-economy-batch-sim-harness.md) shipped the triple-duty harness's first duty (in-process batch sim over the real World+Match, scripted captains + drone fill, seeded/reproducible), the amendment-53 `endedBy` telemetry classification, the evidence campaign (`batch-sim-evidence-2026-07-31.md`), and the amendment-57 ratified tuning pass (CONFIG.xp confirmed; deck.rareWeightPerDryLevel 0.35→0.7). Tests 2280→2305.

**Closures:** two entries above resolve — the spec-0-3 abandonment-classification entry (RESOLVED, amendment 53) and the 2-8 draft-dial entry (PARTIALLY RESOLVED, amendments 56/57 — catalog step values / doctrine factors remain). See each entry's own `status:` line.

**New entries:**

- source_spec: `_bmad-output/implementation-artifacts/spec-2-10-economy-batch-sim-harness.md`
  status: RESOLVED 2026-08-01 by Story 3-1 (Eric ratification, epic-3 amendment 11) — the phased timeline + ratified map bump (2400u / fill 20) make the band reachable by match length alone: pacifist control picks p50 = 12.0 exactly at the 12:00 closure and 21 by the harness cap; lethal lower bound rose ~1–2 → 4. No XP dial was touched. Evidence: batch-sim-evidence-2026-08-01.md. Human confirmation rides Eric's live play (lower-bounds caveat stands).
  summary: The 12–20 picks-per-match draft band is arithmetically unreachable at current match lengths — the shipped storm closes at 3:45 and harness matches end 0:40–1:20 p50 by elimination, while the band requires ~10+ minutes of active play (NFR6's ring-closed-~12:00 shape). Routed to Story 3.1 (epics.md already assigns the harness match-length confirm there); the harness's zone.* sweep support is ready for it. No XP dial change should chase this number before 3.1 lands.
  evidence: batch-sim-evidence-2026-07-31.md campaign matrix (roster baselines, levelMs sweeps showing picks mean 0.72–3.80 across all variants; zone sweep showing insensitivity at current pilot lethality); Eric ratified the attribution at the amendment-55 checkpoint.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-10-economy-batch-sim-harness.md`
  summary: Doctrine-rival cards never leave circulation — once a doctrine is fitted, its rival presents as a free REPLACE forever (ratified ping-pong, amendment 44), so decks floor at the rival cards instead of emptying; "deck exhausted" in the harness is defined as empty-or-terminal-rivals-only. Awareness entry, no rule change proposed; revisit only if live play shows late-game offers degenerating to rival spam.
  evidence: Wave-2 implementation finding (spend-policy loop never terminated under always-prefer-exclusive); documented in server/scripts/batchsim/controls.ts (renamed from pilots.ts, cycle 110) and deckSim.ts headers; visible in the evidence report's depletion stats.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-10-economy-batch-sim-harness.md`
  summary: Harness pilot fidelity bounds what the evidence can claim — GunnerPilot is omniscient (no fog/perception costs) and near-optimal at gunnery, so match lengths are LOWER bounds and kill rates UPPER bounds on human play; economy conclusions drawn from it should be re-checked once Epic 6's utility-AI bots (perception-honest, staggered) exist — the harness's bot-vs-bot third duty is the natural vehicle.
  evidence: Documented v1 tradeoff in pilots.ts (per the 2-10 spec's "omniscient in-process for v1" ruling); dronesSmoke-lineage hunt logic; Epic 6 / AR12 own the honest-bot evolution.
  status: RESOLVED 2026-08-20 (cycle 110, epic-7 amendment 29). The omniscient lethal pilot is DELETED rather than re-checked: `gunner` read `world.ships` to pick and lead targets and is obsolete now that perception-honest bots exist (Story 6-4), and it is structurally unable to evaluate the intel/counter-intel half of the finalized catalog because it never uses a sensor. The lethality baseline is now `--bots N`. `pacifist` SURVIVES as a frozen storm-pacing control and is NOT covered by this entry — it never targets or fires, so it reads only its own ship record, the live ring and the island list, all of which a real client already holds; its omniscience was always inert.

## 2026-08-01 — Home Page Maintenance Patch (spec-home-page-maintenance.md)

The maintenance patch (Eric-invoked, five ruled changes + three AskUserQuestion rulings this run: persist the random hue, broader home tinting, confirm button keeps amber chrome) moved the color picker to the SELECT CLASS modal only, made absent/corrupt `hullcracker.color` roll-and-persist a random Regatta hue that tints home chrome (never amber), slimmed the class chip (loadout sub-line deleted), replaced the modal caption with a left-side `COLOR PREFERENCE:` label, and re-pointed the modal's SET SAIL into CONFIRM SELECTION (save + return home; PLAY is the only deploy path — which is EXPERIENCE.md's original "pick returns to home" contract, restored).

**Closures/updates:** the 1-14 class-card content entry above is EXTENDED (chip sub-line carve-out reversed by direct Eric ruling; see its `status:` line).

**New entries:**

- source_spec: `_bmad-output/implementation-artifacts/spec-home-page-maintenance.md`
  summary: Design-doc supersessions awaiting the Eric-gated doc-sync batch (no design docs edited in-story, standing rule): DESIGN.md:157 ("picks a preference on the home page") and EXPERIENCE.md:35/36 (Color Hoist as a home element, "repeated in layer footer") — the hoist now lives ONLY in the class-select layer; DESIGN.md:242's verbatim caption "PREFERENCE PICK — YOU GET IT UNLESS CLAIMED, THEN NEAREST FREE HUE" — deleted for a bare `COLOR PREFERENCE:` label (the never-imply-claiming law is now carried by the word PREFERENCE alone; FCFS displacement at join is real and now un-explained in UI); DESIGN.md:241's chip sub-line anatomy — sub-line deleted; spec-1-14's amber-unset accent ruling and its "SET SAIL deploys from the bay" I/O row — both re-taken (interface accents now always a real hue; the bay confirm never deploys, matching EXPERIENCE.md:66/124's pick semantics, so those two EXPERIENCE rows need no edit).
  evidence: This run's design-constraint sweep (DESIGN.md:157/241/242/243, EXPERIENCE.md:35/36/66/124 citations); re-taken pins in classSelect.test.ts / home.test.ts / homePersistence.test.ts; Eric's invocation verbatim plus the three recorded AskUserQuestion rulings.
- source_spec: `_bmad-output/implementation-artifacts/spec-home-page-maintenance.md`
  summary: Client-side random default makes the server's no-pref branch (seeded-random free hue in regatta.ts assignHue) production-dead for current clients — every join now carries a colorPref, so the server path survives only as defense for legacy/absent clients. Deliberate (Eric ruled persist-at-client so home/modal/match agree); awareness entry only, no change proposed. Also: with the caption gone, a displaced preference (pref taken → nearest free clockwise) has no UI explanation anywhere pre-join; if players report color confusion, the tooltip/copy question reopens.
  evidence: connection.ts ensureColorPref() (this patch) + connect() colorPref forwarding; server/src/game/regatta.ts assignHue no-pref branch unchanged; DESIGN.md:157 FCFS semantics.
- source_spec: `_bmad-output/implementation-artifacts/spec-home-page-maintenance.md`
  summary: Colorblind-assist toggle leaves already-painted home chrome stale — main.ts swaps the PLAYER_HUES tables live, but home's `repaintAccent` runs only at mount and on hoist picks, so chip/callsign tint keeps the pre-toggle hue while a re-opened bay shows the remapped palette; pre-existing pattern (chip border predates this patch), surface widened by the broader-tinting ruling. Fix shape: settings toggle notifies the home surface (or repaint rides the settings-change event) — needs a small main.ts↔home seam, not a one-liner.
  evidence: Blind Hunter traced main.ts:2293 (live PLAYER_HUES swap) vs home.ts repaintAccent's mount-time-only subscription; divergence reproducible by toggling the assist with the home page up.

## 2026-08-01 — Story 3-1 pre-implementation rulings (Eric, live)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-phased-zone-timeline.md`
  summary: TEAMS — Eric wants the game to support ~20 teams of 1–3 players (with spawn buffers per team and room for PvE ships). Squads are new GDD-level scope: the current GDD is one-ship-per-player FFA with a 20-client room cap, and no epic carries team play. Homed at Epic 6 lobby design, where queueing, rosters, roster-scaled map sizing (6.2), and spawn rules get designed together; the GDD updates then. Eric ruled this explicitly ("Ledger for Epic 6") at the 3-1 pre-implementation round; nothing about teams blocks Epic 3 — the phased timeline and map math are team-agnostic.
  evidence: Eric mid-run message during the 3-1 dev-auto run ("20 teams (of 1-3 players) is the goal, with enough room that all teams can spawn with a buffer of space around them, and room for PvE ships, too"); epic-3-context-amendments.md entry 9. Related: the 3.1 map bump (radius ~2400u, fill ~20 — amendment 7/8) is sized for the closing-rate criterion, NOT for teams; 6.2 owns roster-dynamic sizing.
  UPDATE 2026-08-15 (epic-6 amendment 11): **Story 6.2 is CANCELLED** — Eric ruled the map will not scale with the roster ("We won't be scaling the map size"), so the "6.2 owns roster-dynamic sizing" pointer above is DANGLING and teams are now unhomed for the sizing question. If teams land, sizing reopens in the OPPOSITE direction: ~20 teams of 1-3 is up to 60 hulls, which wants a BIGGER ocean than 2800u, not a smaller one. The `mapRadius(cap)` curve, `capRef`, the `generateMap(seed, playerCap)` parameter and the shipped `WelcomeMsg.playerCap` wire field were all deliberately KEPT rather than deleted for exactly this case — feeding them a real roster is a one-argument change at `ArenaRoom`'s `new World(...)`.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-phased-zone-timeline.md`
  summary: fogSmoke shell-reveal intermittent — one run failed `shell: s1 revealed 3493u from B (outside sight)` and was not reproducible (3 subsequent runs + a detailed pass all green on the re-scanned seed 265). Smoke-side bookkeeping race, not a perception defect (server reveal invariants are property-tested); belongs to fogSmoke's documented flakiness family. Candidate fix if it recurs: snapshot `b.you` at reveal-record time rather than assert time. Home: the smoke-hygiene family (harness/smoke items already ledgered for later epics).
  evidence: 3-1 review-gate patch wave, fogSmoke reruns on the amendment-12 island field; the failing distance (3493u) is a stale/undefined B position read, not a genuine cross-fog reveal — the same run's reveal-position assertions passed at 306–329u on every green pass.
- source_spec: `_bmad-output/implementation-artifacts/spec-join-window-before-countdown.md`
  summary: Leave/rejoin lobby cycling now reopens a fresh 30s gathering window each time (was a 15s countdown re-arm), scaling up the pre-existing ready-room hostage-cycling vector; any cap/shrink-on-restart mitigation is a game-design decision needing an Eric ruling.
  evidence: `Match.notifyRosterChanged` cancels gathering to `waiting` when humans drop below min and deliberately opens a fresh full window on re-min (pinned by the "cancel + fresh window on re-min" test in `server/src/__tests__/match.test.ts`); the mechanic predates this story (countdown cancel/re-arm) but the per-cycle cost grew 15s → 30s+15s. Surfaced by the 2026-08-02 adversarial review pass.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-ring-reveal-storm-rendering.md`
  summary: "[RESOLVED 2026-08-11 cycle 75 — NOT AS DRIFT. Eric ruled at Story 4-8's question gate (amendment 239) that this was a CATEGORY ERROR, not a typo: both documents were right and describe different things. `<25%` is exactly the CRIMSON `damage-marker` band and 25-50% is the AMBER warning band (amendment 41's ratified numbers), and Tier 1 is the THREAT tier — a warning is not a threat. Two things made it load-bearing rather than cosmetic: the amber corollary is only implementable if the amber rail is NOT Tier 1 (otherwise it permanently outranks the final-10s ring, the opposite of the rule's purpose), and below-50% hull is common enough that the 50% reading left Tier 1 active through most of the back half of most fights — pinning the vignette lit and preventing the ring from ever pulsing. `tier1Active` now composes `railCritical` (25%); `railPulsing` (50%) is byte-identical and the rail still breathes exactly as it did. NO DOC EDIT IS OWED: EXPERIENCE.md and epics.md were correct as written.] EXPERIENCE.md's attention-priority tier table words the Tier-1 HP channel as \"HP-rail pulse <25%\" but the shipped rail (and therefore the 3-2 attention seam, per amendment 16's \"the rail's existing threshold\" ruling) breathes below 50% — was believed to be doc drift for the Eric-gated doc-sync batch (Story 7-5 family)."
  evidence: `railPulsing()` in client/src/render/hud.ts gates at the 50% threshold the rail shipped with in 2.4/2.9; amendment 16 deliberately pins the seam to the rail's own predicate so the number has a single source; EXPERIENCE.md:88-96 still says "<25%". Flagged by the 3-2 implementation agent; reviewers confirmed behavior is internally consistent.

## 2026-08-02 — Cycle 34 review gate (tracking mines + aim previews; authored as "cycle 32" pre-merge, renumbered — join-window took 32, story 3-2 took 33)

- source_spec: `_bmad-output/implementation-artifacts/spec-tracking-mines-weapon-telegraphs.md`
  summary: Travel-line previews clip at islands but NOT at sighted enemy hulls that sit on the line short of the burst point — the real shell would intercept early (contactDamage, no burst) while the preview shows an unblocked burst circle at the click. Deliberate for now: contacts render ~100ms-behind interpolated estimates, so hull-clipping the preview would jitter against moving targets and imply a precision the client doesn't have; islands are static truth, hulls are not. If playtests show the lie matters, the fix shape is clipping against the interpolated contact silhouettes with a visual register that reads "estimate", not "truth".
  evidence: Edge Case Hunter finding at client/src/render/aimPreview.ts:166, cross-checked against shared/src/sim/shell.ts earliestHull semantics (non-arcing shells stop on hull contact with no burst); triaged defer at the cycle-34 review gate — an honesty enhancement with a real design question, not a patchable defect.

- source_spec: `_bmad-output/implementation-artifacts/spec-tracking-mines-weapon-telegraphs.md`
  summary: placeRange 90→150 is the first CLIENT-GATE-LOAD-BEARING CONFIG change shipped without any version gate (PROTOCOL_VERSION correctly unbumped — no wire shape changed): a stale cached client compiled with placeRange 90 joining a new server denies 90–150u placements client-side that the server would accept, until the player hard-refreshes. Transient deploy-skew only (Render deploys atomically; precedent exists for creep values), but if gameplay-gate constants keep shifting between deploys, consider a welcome-time CONFIG hash/version advisory so stale clients can prompt a refresh.
  evidence: Blind Hunter finding (client/src/render/weaponArc.ts weaponRangeHit gate vs server equipment/mines.ts activation check, both compiled from CONFIG.mine.placeRange); triaged defer at the cycle-34 review gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-tracking-mines-weapon-telegraphs.md`
  summary: Multi-barrel salvo burst rings — only the first shell of a 2–3 barrel salvo carries the one-shot 400ms latch claim, so barrels 2–3 burst at the CONFIG default radius while barrel 1 rings at the owner's effective radius. Strictly better than pre-cycle (all barrels defaulted) and the honest edge of a heuristic over a self-private click; a proper fix needs salvo identity on the wire, which the anti-cheat design forbids (no range/build-derivable fields on ballistic events). Awareness entry — revisit only if the mixed-size rings read as a bug in playtests.
  evidence: Implementation-agent note at the cycle-34 review-gate fix wave; client/src/net/roomBindings.ts shellClaim one-shot consume semantics + projectiles claims tombstone (MAX_OWN_CLAIMS eviction tests).
- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-br-chrome-bar.md`
  summary: The ratified HUD-composite mock (hud-composite-2.html) still renders the BR chrome bar with grey #7A8496 labels and a white #E2E8F0 T+ number, superseded by the shipped registers (dim-alpha phosphor labels per epic-2 amendment 17; phosphor numbers per the epics AC; ring segment storm-violet per epic-3 amendment 21) — reconciling mock/DESIGN language is Eric's gated doc-sync call.
  evidence: Mock CSS .brchrome (lines 206-217) vs shipped chromeBarSegments registers; the mock predates the 2026-07-26 de-grey ruling.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-br-chrome-bar.md`
  summary: client/src/ui/results.ts scoreSignature joins with a LITERAL 0x01 control byte embedded in the source, making the file read as binary to grep/file (the same hazard the 3-3 review patched in hud.ts by switching to an explicit backslash-u0000 escape) — swap to an explicit escape in a future cycle.
  evidence: The 3-3 patch agent hit the identical pattern in hud.ts (a literal NUL byte made grep fail silently on the whole file) and located this pre-existing sibling while fixing it; pre-existing before 3-3, no behavior impact.

## 2026-08-02 — Story 3-4 evidence campaign (the Endgame Guarantee)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-the-endgame-guarantee.md`
  summary: Island pockets inside the terminal ring — islands inside the 660u endgame ring create no-fire LOS shadows with ZERO storm pressure to dislodge a hull sitting in them (island-LOS blocking is Eric-ratified doctrine 2026-08-02, so this is an observation, not a defect claim). The 3-4 probe measured real matches where a beached hull behind such a rock was unhittable from the approach line (perp offsets 18-43u vs island radii 49-88u) until the attacker flanked. Competent movement resolves it (the seamanship-v2 instrument concluded 50/50), but endgame FEEL with rocks-as-final-cover is an Eric review item once humans play the 12:00+ ring — possible future knobs: island budget/size inside the terminal ring at mapgen, or nothing (Rat Covenant: hiding is legal but priced — though inside the ring the storm exacts no price).
  evidence: batch-sim-evidence-2026-08-02.md (probe geometry of endgame seed-4 idx 8/40 and gunner seed-1 idx 80); generateMap has no terminal-ring-aware island rule (rings roll per-match on a server-private stream AFTER the map exists, so mapgen cannot know the final ring today — any fix is design-level).
- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-the-endgame-guarantee.md`
  summary: Drones never un-beach — server/src/game/drones.ts drive AI has no astern/stuck-detection behavior, so a drone that grounds on an island is pinned for the rest of the match (speed ×islandSpeedMult every contact tick, rudder authority scales with speed). The 3-4 probes caught beached drones sitting immortal-behind-cover at full HP for 600+s until captains flanked. Harmless-ish today (drones are weaponless piñatas and captains can always finish them), but Epic 6's combat-bot AI (6-4) must ship real seamanship — the scripted-pilot un-beach v2 pattern (stuck-detect → rotate-away astern via islandAvoid cross-product sign, negated for signed sternway authority → heading-hold grace; server/scripts/batchsim/controls.ts — RENAMED from pilots.ts in cycle 110; the un-beach machinery survived the pilot retirement intact and is exercised by `pacifist`) is the proven reference implementation.
  evidence: 3-4 probe dumps (drone-12 frozen at one pose hp-flat for 720s; drone-3/drone-9 beached full-HP through the whole endgame); amendment 25 fixed the scripted captains only — production drones.ts deliberately untouched (zero production changes was a 3-4 spec boundary).
  update_2026-08-06: PARTIALLY RELIEVED by the cycle-59 grounding fix (Eric ruling "boundary + directional damp"), NOT closed. The PHYSICS half of this entry is gone: the per-tick `speed ×islandSpeedMult` multiplier that collapsed a grounded hull to 0.083 u/s (and its rudder authority to 0.24 deg/s) is replaced by a DIRECTIONAL, non-compounding speed CAP in `shared/src/sim/collision.ts` (`applyGroundingDamp`) — a grounded hull now holds `islandSpeedMult × rated maxSpeed` (8.75 u/s for a battleship, above its 8 u/s steerage speed), so ANY hull that applies helm turns off a rock in seconds, drones included, through the same input pipeline. A grazing contact now costs no way at all, so a drone brushing a coastline is not slowed at all. What REMAINS open is exactly the AI half this entry names: `drones.ts` still has no stuck-detection and still only orders ahead, so a drone that beaches bow-on with rudder amidships has no reason to apply helm and will sit there — it simply is no longer PHYSICALLY unable to leave. Epic 6-4 still owns real seamanship; the scripted-pilot un-beach v2 pattern is still the reference implementation. Deliberately out of scope for cycle 59 (drone AI was a stated non-goal).
- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-the-endgame-guarantee.md`
  summary: Battleship wins 50% of instrument endgames (25/50 at the 660u ring; 2:1 over mineLayer and torpedoBoat) — a balance-feel signal for the endgame ring, plausibly instrument-inflated (scripted pilots fight stand-and-shoot wars with no kiting/torpedo/mine play, the lighter hulls' designed styles), so no tuning now; review against human playtests at the endgame ring (epic-3 retro or Epic 6-era playtest reads are the natural checkpoints).
  evidence: batch-sim-evidence-2026-08-02.md Run 1 winnerClass tally (battleship=25, mineLayer=13, torpedoBoat=12 over 50 resolved matches); review-gate finding (Blind Hunter), triaged defer 2026-08-02.

## 2026-08-03 — Epic 3 retrospective (Eric live; ~10-player playtest reads + parked contingency)

- source_spec: `_bmad-output/implementation-artifacts/epic-3-retro-2026-08-03.md`
  summary: **RESOLVED 2026-08-14 — BUILT as cycle 82 (0.17.82) under a new explicit Eric ruling. This entry is CLOSED.** Eric verbatim: *"After the 12:00 closure, I want the ring to close in on itself entirely from 15:00 to 16:00, forcing the match to end. Find the center of the final ring, mark it somehow (either a point, or an x or something) at 14:00, close it at 15:00, map is now 100% storm at 16:00 and someone will die."* That is the explicit authorizing ruling this entry was gated on, and the built shape matches the 2026-08-04 sharpening verbatim (close begins 15:00, all storm by 16:00) plus one new clause — the collapse point is MARKED at 14:00. It ships as a FOURTH ring group on the existing four-beat rhythm, collapsing CONCENTRICALLY to the terminal ring's own center (no roll, no seed, no schema field). Amendment 24 is superseded on its post-closure-shrink clause ONLY: `stormDps` is unchanged and no damage ramp was built. Full record: epic-3 amendments 27-30 + `spec-sudden-death-final-collapse.md`. Two things left OPEN for Eric, both named in the cycle report: whether the collapse should carry its own SUDDEN DEATH chrome-bar register (no new copy was invented), and the NFR6 "~15:00" doc text now superseded by a ~17:00 worst-case ceiling (routed to the 7-5 doc batch). Original entry preserved below for the record. — SUDDEN DEATH at ~15:00 — REAFFIRMED AND SHARPENED 2026-08-04 by Eric, still PARKED and still NOT a build directive. Eric verbatim (cycle 44, on being shown the endgame p50 crossing the ~15:00 contract): *"I'm actually heavily considering 'sudden death' at 15 minutes that fully closes the ring in until it is all storm at 16 minutes. someone will win at that point pretty quick. but not today!"* This SHARPENS the shape recorded below from "fully close the ring at ~15:00" to a **one-minute ramp: sudden death begins at 15:00 and the ring closes to ALL STORM by 16:00**. The gating is UNCHANGED — it remains a forcing mechanic that would SUPERSEDE epic-3 amendment 24's geometric no-stalemate bar, and "not today" is an explicit deferral, so no run may build it without a further explicit Eric ruling authorizing the work. Note the trigger has now shifted: the original revisit condition was "live play shows matches failing to conclude," but Eric is considering this while matches DO conclude (50/50 in the cycle-44 endgame pass) — so the motivation is pacing/decisiveness, not stalemate rescue. Original entry follows. PARKED Eric-gated contingency, NOT a build directive. Eric's idea at the epic-3 retro: if live play ever shows matches failing to conclude, fully close the ring at the ~15:00 mark to force a winner. This is a forcing mechanic and would SUPERSEDE epic-3 amendment 24's geometric no-stalemate bar, so no run may build it absent a new explicit Eric ruling; the revisit trigger is live-play evidence of non-concluding matches. Current evidence shows no need: the endgame instrument resolved 50/50 past closure (batch-sim-evidence-2026-08-02.md) and the ~10-player 2026-08-02 playtest ratified pacing ("note this for later in case we need it" — Eric verbatim).
  evidence: Eric ruling at the epic-3 retrospective 2026-08-03 (retro Ruling 2) opened this entry; the explicit supersession it was waiting for ARRIVED 2026-08-14 and is recorded as epic-3 amendments 27-30, so amendment 24 is no longer the law of record on its post-closure-shrink clause (it still is on the damage ramp). Built and evidenced in cycle 82: `batch-sim-evidence-2026-08-14.md`.
- source_spec: `_bmad-output/implementation-artifacts/epic-3-retro-2026-08-03.md`
  status: RESOLVED by Story 6-1 (cycle 88) — verified 2026-08-18. This entry WAS the live-play evidence the queue was built on, and the queue shipped: `StandardQueueRoom` pools captains before any ocean exists, arms a 2:00 deadline at the SECOND captain (amendments 2/3) and seals the cohort at forming (amendment 10). The observed failure mode — friends unable to converge on one room by manual timing — no longer has a mechanism. Amendment 7 additionally corrected this entry's own diagnosis: the friction was the 45s hard deadline, not room topology.
  summary: Lobby-convergence friction, live-play evidence for Story 6-1 (queue-based lobbies) — the 2026-08-02 ~10-player playtest found it "tough getting us all in the same game" even with the cycle-32 gathering window (30s unlocked join window at minHumans); real friends converging on one room is still manual-timing-dependent. The 6-1 spec author should treat this as the primary observed failure mode the queue design must solve (alongside the already-ledgered join-window hostage-cycling vector).
  evidence: Eric's live report at the epic-3 retrospective 2026-08-03 (retro Ruling 1 context + retro Ledger Sweep); spec-join-window-before-countdown.md shipped the interim mechanism this evidence grades.

## 2026-08-03 — TTK & Objective Pip Rebalance (cycle 39)

- source_spec: `_bmad-output/implementation-artifacts/spec-ttk-pip-rebalance.md`
  summary: Batch-sim evidence rerun under the new class HP (TB 125 / ML 150 / BS 175) — the ratified baselines (lethal ~3:00 / pacifist 12:00 match lengths, picks band, endgame-resolution 50/50) were all measured at 70/105/150, so kill rates, match length, and picks-per-match have shifted an unmeasured amount. Eric ruled mid-cycle (2026-08-03) to skip the rerun this cycle (Fable credit budget) and ledger it: run the lethal baseline, pacifist control, and endgame legs report-only in a future cycle and write the evidence doc; no dial changes without ratification.
  evidence: Eric mid-run message 2026-08-03 ("skip the evidence batch sim… put it on the todo list"); prior baselines batch-sim-evidence-2026-08-01.md / batch-sim-evidence-2026-08-02.md.
- source_spec: `_bmad-output/implementation-artifacts/spec-ttk-pip-rebalance.md`
  summary: Ratified UX docs now pin the superseded pip scheme — EXPERIENCE.md (~line 267) records anchors "speed 60 / hp 200 / turn 1.0 → BS 3/4/2" and DESIGN.md (~line 240) the 2026-07-24 relative-maxima ruling, both superseded by the 2026-08-03 objective ladders (BS card now 2/4/2); reconciling is Eric's gated doc-sync call (house rule: no design-doc edits in-cycle).
  evidence: Review-gate finding (Blind Hunter) against _bmad-output/planning-artifacts/ux-designs/ux-Hullcracker.io-2026-07-16/; same pattern as the ledgered chrome-bar mock drift entry.
- source_spec: `_bmad-output/implementation-artifacts/spec-ttk-pip-rebalance.md`
  summary: Unmeasured relative-balance shifts from the hp-ladder move, to fold into the already-ledgered evidence rerun — class hp spread compressed 2.14×→1.4× (TB +79% vs BS +17%, vs the 3-4 instrument's 50% BS endgame win rate), fixed-value boons devalued relative to hulls (shipHull +20/card was +28.6% of a TB, now +16%; damage ladders similarly), and drone-farm efficiency relatively amplified (drone TTK unchanged while player-kill TTK rose ~67%).
  evidence: Review-gate findings (Blind Hunter #5-7, Edge Case Hunter drone probe), arithmetic verified against CONFIG; all three are measurement questions for the deferred report-only batch-sim rerun, not defects.

## 2026-08-03 — Class bay card click selects (cycle 40, Eric ruling)

- source_spec: `_bmad-output/implementation-artifacts/spec-class-bay-card-click-selects.md`
  summary: EXPERIENCE.md:66 documents the class bay's "pick returns to home" contract (card click picks the class AND closes the layer) — superseded by Eric ruling 2026-08-03: a card click now only selects/highlights the card and the bay stays open; the bay closes only via CONFIRM SELECTION/Enter, ESC, or the backdrop dismiss. Only line 66 carries the retired contract — the keyboard rows (highlight + Enter-confirms, ESC-closes; lines 115/124/125) already describe the NEW behavior and need no edit. DESIGN.md/EXPERIENCE.md were not edited in this story per the minimal-design-doc-edits ruling — doc-sync is a separate, Eric-gated pass (batch it with the same-cycle pip-scheme drift entry above).
  evidence: EXPERIENCE.md line 66 ("pick returns to home with chip + Primary Button sub-line updated"); shipped behavior now in `client/src/ui/classSelect.ts` (`select()` highlight path replacing the retired `pick()`/`onPick` exit).

## 2026-08-04 — Story 4-1 The Listening Ring: DEFERRED at the question gate (Eric ruling)

- source_spec: `_bmad-output/implementation-artifacts/bmad-dev-auto-result-4-1-listening-ring-deferred.md`
  status: STANDING RECORD, NOT WORK — verified 2026-08-18. This is the durable record of Eric's 2026-08-04 deferral ruling (Story 4-1, sprint-status `deferred`, number reserved). Revival is Eric-gated and needs its own explicit ruling; the implementation seam is mapped in the result doc so the discovery cost is not re-paid. It should never be counted as an open work item — it is the reason a work item does NOT exist.
  summary: THE HYDROPHONE SENSOR TIER IS DEFERRED, not cut — Eric ruling 2026-08-04 at Story 4-1's pre-implementation question gate. No third perception tier, no listening events on the wire, no compass rose, no audio twins. Eric verbatim: "Lets go ahead and defer the hydrophones, we'll save it as a feature we can add later if I decide we need it. I think there's enough information, and active sonar is going to be added anyway." Revival is Eric-gated and its own explicit ruling; the story number stays reserved (sprint-status `deferred`). If revived, the full implementation seam is already mapped in the result doc (listenScan beside torpedoUpdateScan, appended after the sorted blip subsequence; three hard-coded registry counts to bump; attention.ts Tier1Input clause; PV 19→20) — do not re-pay that discovery cost.
  evidence: Three parallel investigations this run confirmed the substrate is ready (signal registry mature at 15 rows with a compile-time completeness gate; `monoNode` in client/src/audio/context.ts was shipped by Story 2.3 explicitly as plumbing "for when stereo bearing audio lands"; twinMap.ts already enforces audio↔visual twins at the type level) — so this is a DESIGN deferral, never a feasibility block. The deferral argument: (1) engine noise inside radar range is a strictly-worse radar and SATURATES at the endgame, since the terminal storm ring is 660u = radar range by derivation, so every survivor is in every other survivor's hearing range exactly when a pip most needs to mean something; (2) the torpedo case is attention, not time — BS turn rate is 0.4 rad/s (90° in ~3.9s) vs 5.5s of existing truesight warning; (3) sequencing was backwards — torpedo 55dmg/12s reload = 3.2 hits and ~36s to kill a 175hp BS, mine 45dmg = 4 mines, vs a BS volley of 75, so the ring was counterplay for weapons that don't need countering.

- source_spec: `_bmad-output/implementation-artifacts/bmad-dev-auto-result-4-1-listening-ring-deferred.md`
  status: RESOLVED by Story 4-5 (cycle 50, epic-4 amendment 55) — verified 2026-08-18. The foghorn grew its OWN screen-edge bearing chevron rather than deferring alongside 4-1; see `client/src/render/effects.ts:61` and `flashBudget.ts:222` ("the foghorn's bearing chevron one-shot"). The hard intra-epic dependency the 4-1 deferral broke was resolved before create-story 4-5, as this entry required.
  summary: STORY 4-5 (THE FOGHORN) HAS LOST ITS DISPLAY SURFACE — its ratified behavior is "the listening ring lights an arc sweep along the honk's bearing on every hull in earshot", and that ring no longer exists. 4.5 must either grow its own bearing surface (new design work, needs Eric) or defer alongside 4.1. This is the one hard intra-epic dependency the 4-1 deferral breaks; it must be resolved BEFORE create-story 4-5, not discovered during it. Related, softer: 4.7's sound map narrows to 4.3/4.5/4.6 events, and 4.8's attention tier table must not pin a listening-ring Tier-1 channel that doesn't exist (shipped Tier-1 set is the low-HP rail pulse + live denied pulse).
  evidence: epic-4-context.md Cross-Story Dependencies ("Story 4.1's ring is the arc-sweep surface the foghorn (4.5) broadcasts onto") and EXPERIENCE.md:126; epic-4-context-amendments.md amendment 4.

- source_spec: `_bmad-output/implementation-artifacts/bmad-dev-auto-result-4-1-listening-ring-deferred.md`
  summary: WEAPON REBALANCE SIGNAL, not yet a directive — Eric 2026-08-04: "Torps wont be faster (60 to 80 knots is realistic)" and "Im probably gonna raise the cooldowns on most if not all weapons too." Torpedo speed 60 u/s is therefore FIXED as a lever (lethality changes must come from damage, reload, or delivery), and the expected rebalance direction is longer commitment cycles rather than more lethal individual shots. No cycle may implement cooldown changes without an explicit numbers ruling. Natural home: an interstitial balance cycle in the TTK/pip-rebalance mould — which would also be the moment the 4-1 deferral is worth revisiting, since the ring's value is contingent on torps/mines actually being threatening.
  evidence: Eric messages during the 4-1 run, 2026-08-04; current values CONFIG.torpedo (speed 60, damage 55, reloadMs 12000, maxAmmo 1) and CONFIG.mine (damage 45, reloadMs 8000, triggerRadius 32, blastRadius 48) against class hp TB 125 / ML 150 / BS 175 post-cycle-39.

- source_spec: `_bmad-output/implementation-artifacts/bmad-dev-auto-result-4-1-listening-ring-deferred.md`
  summary: ISLAND SHADOWS NOW HAVE NO ANSWER — the 2026-08-02 radar/island LOS ruling affirmed that islands block every sensor at all ranges and explicitly named Epic-6 hydrophones as the designed counter to close-range mutual blindness behind a rock. With hydrophones deferred, that counter is gone and island shadows are a total blind spot until active sonar ships. The LOS ruling itself is UNCHANGED and still stands — this entry exists so nobody "fixes" the blind spot by reopening in-bubble radar paint, which is explicitly rejected doctrine.
  evidence: `bmad-dev-auto-result-radar-los-truesight-shadows.md` (2026-08-02, Eric verbatim "If you're behind an island, I can't see you with any sensor"); epic-4-context-amendments.md amendment 4.

- source_spec: `_bmad-output/implementation-artifacts/bmad-dev-auto-result-4-1-listening-ring-deferred.md`
  status: ROUTED to the Story 7-5 doc-sync batch — verified 2026-08-18 as still-unfixed doc drift, and it has DRIFTED FURTHER: item (c) records PROTOCOL_VERSION as 'currently 18 when it is 19'; it is now 40. Do not fix piecemeal — the epic-6 retro re-scoped 7-5 with four ADDITIONAL Epic 6 items (the retired weapons-safe waiting room, EXPERIENCE.md:67, EXPERIENCE.md:108's scoping, and amendment 41's copy law + amendment 49's DRAFT string). One pass, with Eric, per the minimal-design-doc-edits house rule.
  summary: Doc drift found during 4-1 investigation, for the Eric-gated doc-sync batch (Story 7-5 family) — (a) EXPERIENCE.md:137 and the Story 4.7 AC both say "the 13 existing tones" but the shipped catalog is 22 (`client/src/audio/tones.ts`), the count predating Stories 1.7/1.8/1.10/2.9; (b) the same text calls the denied tone "deferred" when it shipped in Story 1.10; (c) `CLAUDE.md` records PROTOCOL_VERSION as "currently 18" when it is 19. None edited in-cycle per the minimal-design-doc-edits house rule.
  evidence: Investigation subagent read of tones.ts (ToneId union, 22 members) and shared/src/index.ts:126 (PROTOCOL_VERSION = 19) against the ratified docs.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-class-legible-blips.md`
  summary: A Mine Layer and its own decoy buoy SHARE one 3-paint track budget, because the client's per-contact cap keys on blip `id` and a decoy paints under its OWNER's ship id (amendment 11). When both are swept each revolution, the real hull keeps only 1 ghost instead of 2, so the ARPA ghost-spacing speed read is degraded for exactly the class that owns decoys. The spec ratified the shared budget as an accepted consequence and this cycle only corrected the (arithmetically impossible) `MAX_LIVE_BLIPS` sizing comment. Worth Eric's eye at playtest: it may read as desirable decoy fog, or it may want a `paintsPerContact * 2` budget when a decoy is deployed.
  evidence: `client/src/render/radar.ts` `onBlip` keys `capOldestByKey` on `e.id`; `server/src/game/signals.ts` blip `counterIntel` shapes the lie with the owner's ship id. Both review hunters traced the eviction sequence independently and reached the same interleaving.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-class-legible-blips.md`
  summary: MEASURE-ZERO ORDERING TELL in `blipOrder()` — the sort key is `(x, y, t, id)` and ES stable sort preserves insertion order on a complete tie, which is genuine-first (`shipScan` pushes before `decoyBlips`). Before 4.2 a full tie carried zero information because tied payloads were byte-identical; now genuine and decoy payloads differ (live pose vs frozen pose at speed 0), so at bit-identical float coordinates a modified client could learn which of the two is real. Requires exact float equality between a hull's position and its own buoy's, which the stern drop offset makes essentially unreachable — recorded because this codebase treats perception-boundary channels as worth writing down even at measure zero (cf. the cycle-31 forward-secrecy catch).
  evidence: `server/src/game/perception.ts` `blipOrder` + `view()` push order; Blind Hunter finding 2 (PLAUSIBLE, explicitly flagged for the record rather than for action).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-class-legible-blips.md`
  summary: PRE-EXISTING `hueLatch.ts` gap (Story 1.12, untouched by this cycle) — `retryHue` writes `state.rev = rev` BEFORE its null-hue early return, so a marker whose owner has left the roster consumes a hue-revision bump without being repainted, and keeps its pre-toggle hue for the rest of its life after a colorblind-assist toggle. Nil visible effect today (an unresolved marker wears amber, which clears both luminance floors), but the latch's documented contract — "a revision bump repaints every live marker" — is silently false for that population. Newly exercised, not caused, by 4.2's 12s-lived blips.
  evidence: `client/src/render/hueLatch.ts` `retryHue`; flagged independently by both review hunters (Blind finding 4, Edge finding 2) from different directions.

## 2026-08-04 — Global cooldown reduction (cycle 42): ledgered consequences

- source_spec: `_bmad-output/implementation-artifacts/spec-global-cooldown-reduction.md`
  summary: TWO SUBDECKS ARE NOW SINGLE-LINE — deleting the seven per-equipment reload ladders left `speedBoost` holding only `boostMax` (5 cards) and `decoyBuoy` only `decoyDuration` (5 cards), down from 10 each. Eric ruled ACCEPT-AND-LOG at the pre-implementation question gate (AskUserQuestion, 2026-08-04): ship the thinning, design replacement lines later. Nothing breaks mechanically — `drawOffer` picks 4 distinct LINES across the WHOLE deck, not per-category — but a Torpedo Boat's boost subdeck and a Mine Layer's buoy subdeck now contribute a single ladder each, so those categories go dry fast and the offer pool leans harder on the universal categories. Candidate replacements would be new common lines in those categories (e.g. a boost-duration ladder, a buoy-count/`maxAmmo` ladder); both are GAME-DESIGN decisions requiring an Eric ruling, not implementer picks.
  evidence: `shared/src/sim/boons.ts` BOON_CATALOG after cycle 42 (36 lines, was 42); per-category deck sizes pinned in `shared/src/__tests__/deck.test.ts` (speedBoost 10→5, decoyBuoy 10→5, guns 13→8, cannon 17→12, torpedoes 17→12, mines 27→22, ship 10→15, intel 15 unchanged; hull totals TB 59 / BS 66 / ML 69).

- source_spec: `_bmad-output/implementation-artifacts/spec-global-cooldown-reduction.md`
  summary: UNMEASURED BALANCE SHIFT — no batch-sim evidence pass was run for cycle 42 (same posture as cycle 39's TTK/pip rebalance). Three relative shifts are asserted by design, not measured: (1) MULTI-WEAPON BUILDS ARE STRUCTURALLY BUFFED — five `shipCooldown` cards now cut EVERY weapon to 0.5, where the old catalog needed five cards PER weapon to reach only 0.59, so a Battleship running gun+cannon+star shells gets more than what used to cost 15 cards, for 5; (2) SINGLE-WEAPON max cadence IMPROVED for torpedo/mine/star/boost/decoy (0.9^5 = 0.590 vs the new 0.5) — what moved is the opportunity cost, from a per-weapon subdeck into the universal `ship` category every build competes for; (3) the gun's 3s→5s and the cannon's 15s→50s base retunes shift EARLY-GAME time-to-kill hard (a zero-boon Battleship's cannon cadence more than triples) while leaving the max-build endpoint at 2.5s/25s — the early/late spread is now much wider than any previously measured configuration. NOTE the endpoint moved mid-cycle: Eric added a 5th cooldown card (copies 4 -> 5, cap 0.6 -> 0.5) after the first review gate, explicitly so a full cooldown investment reads as a real reward. Natural home: a batch-sim evidence pass in the cycle-39/2.10 mould, or the next live playtest.
  evidence: `CONFIG.gun.reloadMs` 5000 / `CONFIG.cannon.reloadMs` 50000 in `shared/src/constants.ts`; `shipCooldown` (copies 5, `add: -0.1`) in `shared/src/sim/boons.ts`; the `cooldownScale` multiply in `clampStats()` (`shared/src/sim/stats.ts`). Class hp after cycle 39: TB 125 / ML 150 / BS 175.

- source_spec: `_bmad-output/implementation-artifacts/spec-global-cooldown-reduction.md`
  summary: DEV-ENVIRONMENT FRICTION IN GIT WORKTREES — a `.claude/worktrees/*` checkout has NO `node_modules` of its own, and this repo's installs are NESTED per workspace (`client/node_modules/@colyseus/sdk`, `server/node_modules/colyseus`, `@colyseus/*`, `@types/express*`) rather than hoisted to the root. Node resolution therefore walks UP out of the worktree into the main checkout and (a) misses every nested package — 8 server suites and 7 client suites fail to even collect — and (b) resolves `@salvo/shared` to the MAIN checkout's stale `shared/dist`, producing phantom type errors for symbols added in the worktree. Both cycle-41 implementation agents hit this independently and worked around it with gitignored, worktree-local symlinks plus a local `shared` build. Nothing tracked was affected and the main checkout was never modified, but every future worktree-based cycle will re-pay this. Real fixes: run `npm install` from the worktree root as a standard first step, or hoist the nested deps.
  evidence: Both wave-2 subagent reports, 2026-08-04; baseline server suite was 733 tests with 8 dead suites until the symlinks landed, 836 after.

## 2026-08-04 — Global cooldown reduction (cycle 42): review-gate findings deferred for Eric

- source_spec: `_bmad-output/implementation-artifacts/spec-global-cooldown-reduction.md`
  summary: [ACCEPTED 2026-08-04 — Eric reviewed the drift and ruled it fine as-is; the dials stay untuned. Kept as the provenance record for whoever next reads the stale ~0.48/~0.57 figures.] RARE/EXCLUSIVE DRAW RATE DRIFTED UPWARD — deleting the seven per-equipment reload ladders removed 35 COMMON cards while every rare/exclusive line survived, so rare density rose on every deck (TB 14.5% -> 17.2%, ML 11.4% -> 13.2%) and the draw rate rose with it. A review simulation of the exact `pickLine`/`drawLines` semantics puts TB dry-0 P(>=1 rare in an offer) at ~0.59, up from ~0.51 — the new OPENING rate now EXCEEDS the old ratified dry-6 pity CEILING (~0.57, amendment 57, ratified by Eric 2026-07-31 from the 2.10 batch-sim evidence). Because all four acquisition cards are rares, R-slot equipment also arrives earlier in a run. `CONFIG.deck.rareWeightBase`/`rareWeightPerDryLevel` were deliberately LEFT UNTOUCHED this cycle — retuning dials that were ratified from measured evidence is a balance decision needing an Eric ruling plus a fresh batch-sim pass, not an implementer pick. The now-stale rate figures in the `CONFIG.deck` doc comment were annotated in place rather than rewritten. DECIDED: Eric accepted the faster rare cadence as a side effect of the thinner catalog; the dials are NOT retuned.
  evidence: Fable review-gate finding (CONFIRMED, medium) from a 200k-trial simulation of `drawOffer` semantics, 2026-08-04; `shared/src/sim/deck.ts` (`perCardWeight`, weight = copiesInDeck x perCard); `shared/src/constants.ts` CONFIG.deck provenance comment.

- source_spec: `_bmad-output/implementation-artifacts/spec-global-cooldown-reduction.md`
  summary: [RESOLVED 2026-08-04 — Eric ruled RESCALE PROPORTIONALLY; implemented same cycle in `World.applyBoon` -> `rescaleReloadTimers()`, which scales `reloadMsLeft` by `newReloadMs / oldReloadMs` so the progress fraction survives a grant, never touching pool counts and so never granting a free round. Kept for the record.] MID-RELOAD GRANTS DID NOT SHORTEN THE RELOAD ALREADY RUNNING — and cycle 42 made that far more visible. `World.applyBoon` recomputes `ship.stats` and calls `reconcilePools`, which by design adjusts only pool COUNTS (`n`), never the in-flight `reloadMsLeft`. So a player granted `shipCooldown` one second after firing still waits out the OLD, longer timer; the discount applies from the next cycle. This is PRE-EXISTING, DELIBERATE behavior, not a regression: `reconcilePools` documents its scope (amendment 41), `client/src/render/hotbar.ts:1204` names the `reloadMsLeft >= new reloadMs` state explicitly in a comment and draws the cooling ring from slot STATE so nothing inverts, both `reloadFraction` and `coolFraction` clamp to [0,1], and `applySlotEffect` already refuses a degenerate self-replace precisely because "a fresh pool would be a free instant reload". It was therefore NOT changed — renormalizing the timer would hand out partial free reloads, a game-design change Eric has not ruled on. What DID change is the magnitude: with the cannon at a 50s base, an unlucky grant can leave up to ~45s of residual on the old scale (it was ~1.2s worst case on the old 3s gun). DECIDED: Eric chose proportional rescaling (`left * newReload / oldReload`) over the old "finish the cycle you started" rule.
  evidence: Codex cross-model review flagged this HIGH (`server/src/game/world.ts:995`); both Fable reviewers independently traced it and graded it non-defect. Orchestrator adjudicated by reading `world.ts` `applyBoon`/`reconcilePools`, `server/src/game/equipment/ammo.ts`, and `client/src/render/hotbar.ts:1198-1210`. Cross-model disagreement resolved in favour of "intended, but the numbers changed under it".

## 2026-08-04 — Story 4-3 The Gunnery Conversation: ledgered consequences

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-the-gunnery-conversation.md`
  summary: "[RESOLVED 2026-08-11 cycle 75 — Story 4-8 built it, as this entry predicted it would. `client/src/render/flashBudget.ts` is a sliding-window counter keyed per screen region (a `regionCols` x `regionRows` grid, so a region is a stable fraction of the screen at any resolution) or per named element, pruned BY TIMESTAMP so a backgrounded tab cannot burst-degrade on restore. Eric ruled the enforcement shape at the 4-8 question gate (amendment 240): COALESCE co-located same-kind flashes in a frame — amendment 37's one-frame-one-cue grammar extended from audio to visuals — then DEGRADE anything still over budget to a flat, non-flashing mark. It NEVER DELETES, because the biggest stackers are exactly the declared-information channels this entry names: an over-budget muzzle flash still draws at its true position, it just loses its luminance ramp. The verdict is latched at creation per amendment 83. Every world one-shot, the hull hit flash, and the chrome one-shots now claim against it. Kept for the record.] THE AGGREGATE FLASH BUDGET STILL HAS NO MECHANISM." The ratified accessibility floor is "no element or screen region flashes more than 3x/s regardless of how many compliant events stack" — a rule the accessibility review wrote specifically FOR muzzle flashes and Hit Call blooms. 4.3 ships only the PER-SOURCE 300ms same-source tone floor (`client/src/render/gunneryFeed.ts`); there is no GLOBAL per-region cap, and muzzle-carries is the first feature that can actually bind it — up to 19 other hulls can flash inside your 495u `CONFIG.vision.muzzleFlash` halo in the same second, each individually compliant. This is a KNOWN, DELIBERATE gap: Story 4.8 (Attention Priority) owns attention/flash budgeting generally and is the natural home for the aggregate cap.
  evidence: `spec-4-3-the-gunnery-conversation.md` "Accepted consequences to ledger, not fix here" (a); `client/src/render/gunneryFeed.ts` (per-source 300ms floor only, no cross-source/region accumulator); `CONFIG.vision.muzzleFlash = SIGHT * 1.5` = 495u halo, up to 19 enemy hulls in an 18-player match.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-the-gunnery-conversation.md`
  summary: ALL STORY 4.3 EFFECT GEOMETRY/DURATIONS REMAIN IMPLEMENTER DRAFT under the standing draft-copy rule. The shipped `splash`/`spark`/`muzzle` `SPECS` values in `client/src/render/effects.ts` (radii, durations, alpha/scale curves) were never Eric-ratified — the three effects were reused as-is from their prior client-side-guess incarnation, only their visibility/fog rules changed. The five combat-effect token hexes are still marked `[PROPOSAL]` in DESIGN.md. A later identity/audio pass with Eric should ratify or retune both the geometry and the token hexes together.
  evidence: `spec-4-3-the-gunnery-conversation.md` Never-list ("Never retune the shipped effect geometry/durations or the [PROPOSAL] token hexes — those stay implementer draft under the standing draft-copy rule (ledger, do not ratify here)"); `client/src/render/effects.ts` `SPECS` for `splash`/`spark`/`muzzle`; DESIGN.md combat-effect token block still tagged `[PROPOSAL]`.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-the-gunnery-conversation.md`
  status: ROUTED to the Story 7-5 doc-sync batch — verified 2026-08-18 as still-unfixed doc drift. See the consolidated 7-5 scope in `epic-6-retro-2026-08-18.md` (Significant Discovery item 3); the batch is now materially larger than 7-5's own AC describes.
  summary: DOC DRIFT FOR THE ERIC-GATED 7-5 DOC-SYNC BATCH (house rule: no design-doc edits in-cycle). (a) EXPERIENCE.md:181's fall-of-shot line carries NO ownership qualifier, while FR16 and the Story 4.3 AC both say "own"/"my" splashes — amendment 16 resolved this in FR16's favor (splash is self-private, your own misses only), so EXPERIENCE.md:181 is now the drift and must be reconciled. (b) UX-DR37's "13-tone WebAudio set" was already stale before this story (the 4-1 ledger caught it at 22); 4.3 added `hitCall`, so the shipped catalog is now 23 tones.
  evidence: `epic-4-context-amendments.md` amendment 16 ("This RESOLVES the direct doc conflict between FR16 / the Story 4.3 AC ... and EXPERIENCE.md:181 ... FR16's wording wins, EXPERIENCE.md:181 is the drift"); `client/src/audio/tones.ts` `ToneId` union (23 members post-`hitCall`); prior ledger entry 2026-08-04 (Story 4-1) already flagged the 13-vs-22 drift for the same batch.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-the-gunnery-conversation.md`
  summary: THE OWN-CANNON MUZZLE OVERLAP IS ACCEPTED, NOT A BUG. For your own cannon shot, the universal `mz` flash and the own-side `muzzleHeavy` weight both draw at your hull, layered, because amendment 19 forbids putting a weapon weight on `mz` (the flash must stay neutral for every observer) and the wire still cannot say "cannon" to anyone but the shooter. `client/src/net/roomBindings.ts` keeps only the own-side heavy-cannon read as a client-local proximity check; it layers on top of the universal server-stated flash by design rather than replacing it.
  evidence: `epic-4-context-amendments.md` amendment 19 ("Explicitly REJECTED: ... a heavier flash for the cannon, which would put a class tell on the wire that deliberately does not exist today"); commit `821787f` ("handleShell keeps only the own-side heavy-cannon weight, which layers on the universal flash by design").

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-the-gunnery-conversation.md`
  summary: FIRING INTO FOG IS NOW A RATIFIED PROBE, not a defect to fix. Because `hc`/`sp` tell the shooter hit-vs-miss beyond sight, a player can deliberately range shots into fogged water to test for a hull — the generalized form of the ratified decoy disambiguation oracle. Amendment 17 accepted this knowingly (it is the entire point of overriding the `boom` row's anti-leak rule for the owner-hit case), and it is not free: it costs a reload plus a self-revealing muzzle flash if the prober is within 495u of any observer. Ledger this as an intended emergent behavior to watch in playtest, NOT as something to suppress.
  evidence: `epic-4-context-amendments.md` amendment 17 ("Leaking 'something of yours connected out there' is now the intended feature, not an accident"); `server/src/__tests__/gunnery.test.ts` (fog-range hit/miss coverage); `server/src/__tests__/decoy.test.ts` (extended "no Hit Call" case now asserts `sp` reaches the shooter while `hc` does not, making the oracle observable on the wire).

## 2026-08-04 — interstitial cycle 44 (the per-weapon balance pass)

- source_spec: `_bmad-output/implementation-artifacts/spec-weapon-balance-and-radar-vector-length.md`
  summary: **RESOLVED 2026-08-04 by Eric — option (a), ACCEPT. No action.** Eric verbatim, shown the numbers below: *"match time is fine."* The retune STANDS as shipped: no gun-damage claw-back, no storm-timeline change, no contract renegotiation needed. The ~15:00 figure is hereby understood as a soft pacing reference, not a bar the tuning must be held to. Eric's stated interest is in going the OTHER direction if anything — a sudden-death forcing mechanic at 15:00 (see the SUDDEN DEATH entry, reaffirmed and sharpened the same day, still parked and explicitly "not today"). Original finding preserved below for the record. ERIC DECISION PENDING [SUPERSEDED — decided] — the Story 3.4 endgame median now EXCEEDS the ~15:00 contract. The no-stalemate guarantee itself HOLDS (50/50 resolved, 0 unresolved, 100% past full closure, no forcing mechanic needed — amendment 24's geometric bar is met), but the resolved p50 moved 830.0s (13:50) -> 925.7s (15:26), with p95 18:24 and max 18:33. This is a PACING question, not a correctness one, and the cycle deliberately did NOT decide it. Three options recorded: (a) accept and redefine the contract at ~15:30; (b) claw back some gun damage (e.g. 15 -> 18) and re-measure — the gun is the dominant lever, since the endgame pilot fires slot 0 only so its p50 is close to a pure function of gun damage; (c) leave the tuning and shorten the storm timeline so closure arrives earlier. Natural moment to settle: alongside the next balance ruling, or at the Epic 4 retrospective.
  evidence: `batch-sim-evidence-2026-08-04.md` (endgame pilot, 50 matches, seed 7, post-retune) against `batch-sim-evidence-2026-08-02.md` ("resolved p50 830.0s (13:50, ~15:00 contract at the median)"). Re-running this pilot was mandatory because the gun nerf directly weakens the instrument the pillar is measured with: 25 -> 15 cuts the endgame pilot's post-closure kill rate ~40%.

- source_spec: `_bmad-output/implementation-artifacts/spec-weapon-balance-and-radar-vector-length.md`
  summary: THE SAME-OWNER MINE CASCADE IS PRE-EXISTING IN KIND BUT WIDENED IN DEGREE by the 2-deep rack, and is NOT a one-hit-kill law violation. The law and `damageGuardrail.test` are per-HIT, and a cascade resolves as separate `hitShip` applications per mine — so two co-located 55hp mines apply 55 + 55, not a single 110 hit. What changed: stacking two mines at effectively the same point previously required waiting out an 8s reload, whereas the 2-deep rack makes a co-located double-trap instantly layable, and its 110 total now one-cascades a full-hp 100hp medium drone (the old instant capability was a single 45). Capability itself is old (maxLive 5 always allowed 5x45 = 225 with patience). Flagged so that if the SPIRIT of the 80hp floor is ever extended to cascades, this is the known loophole and its entry point.
  evidence: Adversarial review (Fable systems hunter, CONFIRMED) traced `world.ts` `detonateMine`/`chainMines` (same-owner cascade within blastRadius) against `consume` semantics permitting two drops ~50ms apart; `damageGuardrail.test.ts` asserts per-hit ceilings only.

- source_spec: `_bmad-output/implementation-artifacts/spec-weapon-balance-and-radar-vector-length.md`
  summary: THE BATCH-SIM HARNESS IS BLIND TO THREE OF THE FOUR WEAPONS RETUNED THIS CYCLE. Its `gunner` pilot fires slot 0 only, so the torpedo, mine, and cannon changes are structurally invisible to every before/after column — the measured +30% match length is close to a pure read of the GUN nerf alone. Concretely this made the Mine Layer's win share appear to FALL (31 -> 26 of 200) in the same cycle that buffed the mine hardest, purely because the harness never lays one. A multi-slot pilot (or per-slot pilots) would make future weapon rebalances measurable rather than partially observable. Natural home: whichever cycle next touches `server/scripts/batchsim/pilots.ts`, or a standalone harness chore.
  evidence: `server/scripts/batchsim/pilots.ts` (gunner fires slot 0); `batch-sim-evidence-2026-08-04.md` "Caveats of record" and the ML win-share row, which is explicitly annotated as a harness blind spot rather than a balance finding.
  status: DISSOLVED 2026-08-20 (cycle 110, epic-7 amendment 29) — the premise is gone rather than the gap closed. `gunner` no longer exists, so there is no slot-0 pilot to make multi-slot. Weapon coverage now comes from `--bots N`, whose bots fire every weapon they carry through `EQUIPMENT_TACTICS` (measured in the cycle-110 campaign: broadside, torpedo, mine, star shell, captive torpedo and buoy all launched, and the card ledger reports NEVER OFFERED (none) / OFFERED BUT NEVER FITTED (none)). Anyone re-reading this entry wanting a multi-slot SCRIPTED pilot should note the surviving script, `pacifist`, is a deliberate no-fire control and must stay that way.

- source_spec: `_bmad-output/implementation-artifacts/spec-weapon-balance-and-radar-vector-length.md`
  summary: **matchSmoke step 4 STILL FAILS after this cycle's fixes, and the balance pass AGGRAVATED a pre-existing flake rather than causing it.** This UPDATES the 2026-07-17 "matchSmoke reliability hardening" entry above, which already named "torpedo-connect luck (steps 2/4)" as a failure mode and recorded **0/3 clean passes on the 0.16 baseline on this hardware**. The retune makes that step structurally harder: step 4 must land 2 torpedo hits inside the storm-free window, and the 12s -> 30s reload cuts the number of attempts in that window from roughly 7 to 2-4, so torpedo-connect luck now decides the step far more often. Two real fixes were made and KEPT because both are correct independent of the flake — `ZONE_OVERRIDE.beatMs` 30000 -> 45000 (the storm-free window must scale with the weapon cycle it accommodates; at 30s beats the storm demonstrably sank B before A could, failing `B sunk by undefined, expected A`), and the step-5 endgame budget is now DERIVED from `FULL_CLOSURE_MS` instead of a hardcoded 480000 (a bare beatMs bump had silently stranded it, producing `timeout: results broadcast`). Neither made the step reliably green, and the cycle deliberately STOPPED bumping numbers rather than tuning a documented-flaky harness by trial and error. NOT a regression introduced by the retune: all 2733 unit tests pass, and three of four smokes pass post-retune including TWO that exercise torpedo kills end-to-end (`weaponsSmoke` sinks a 150hp mineLayer with 3 fish; `dronesSmoke` sinks a drone) plus `combatSmoke` (gun kill, 125 -> 5 over 9-10 shells). The real fix remains what the 2026-07-17 entry proposed and no cycle has yet done: deterministic drone seeding, or a step-4 choreography that does not depend on torpedo-connect luck. Now materially more urgent — at a 30s reload this smoke is close to unusable as a gate.
  evidence: Three post-retune runs of `server/scripts/matchSmoke.mjs` on a scratch server (port 2599): beatMs 30000 -> `B sunk by undefined, expected A`; beatMs 60000 -> `timeout: results broadcast` (closure moved 360s -> 720s past the flat 480s budget); beatMs 45000 + derived budget -> `B sunk by undefined, expected A` again. The 2026-07-17 ledger entry above documents the same step failing 0/3 at baseline, before any balance change.
## 2026-08-04 — The Public Register (interstitial cycle 45): ledgered consequences

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  status: ROUTED to the Story 7-5 doc-sync batch — verified 2026-08-18 as still-unfixed doc drift. See the consolidated 7-5 scope in `epic-6-retro-2026-08-18.md` (Significant Discovery item 3).
  summary: DOC DRIFT FOR THE ERIC-GATED 7-5 DOC-SYNC BATCH (house rule: no design-doc edits in-cycle). (a) UX-DR17's kill-feed spec says "max 5 lines, 6 s TTL" (`epics.md:160`, plus the matching DESIGN.md kill-feed block); Eric raised it to 6 lines / 8s TTL for the higher event rate a global feed carries (amendment 33) and the code now ships 6/8000. (b) The Story 1.12 restyle AC at `epics.md:534` restates the same 5-lines/6s numbers and is drift by the same ruling. (c) Epic-3 amendment 19's "AFLOAT counts ALL hulls, humans AND drones" is SUPERSEDED by amendment 32 (captains only, local player still counted) wherever design docs restate it. All three are text-only reconciliations — the ratified behavior is in `epic-4-context-amendments.md` amendments 24-25.
  evidence: `epic-4-context-amendments.md` amendments 32, 33, 34; `client/src/ui/killFeed.ts` (`LINE_TTL_MS = 8000`, `MAX_LINES = 6`); `client/src/score.ts` (`isAfloatHull(meta, droneHue)`); Eric AskUserQuestion answers 2026-08-04 ("Raise to 6 lines / 8s TTL", "Humans only — AFLOAT tracks the win condition").

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  status: RESOLVED by Story 5-1 (epic-5 amendment 4) and formalized by Story 6-3 (epic-6 amendment 13) — verified 2026-08-18. `Match.checkWin()` no longer reads `isDrone` at all: `server/src/game/match.ts:315` is `isParticipant(s) && isAfloat(s.lifecycle)`, and `isParticipant` is `role !== 'fleet'`. A lone captain no longer has to clear the wildlife.
  summary: DRONES STILL GATE THE WIN — DEFERRED TO STORY 6-3, NOT AN OVERSIGHT. Eric ruled drones are not combatants (amendment 23) and the kill feed, the XP economy, and now `n AFLOAT` all treat them that way — but `Match.checkWin()` is BYTE-IDENTICAL to HEAD this cycle and a lone captain must still clear every drone to win. The naive fix (drop the `aliveDroneCount() > 0` guard) is WRONG: that guard exists because `humans.length <= 1` is already true at activation for a solo match, so removing it makes a `minHumans:1` match finish the instant it goes active — breaking `server/src/__tests__/drones.test.ts` (the "a solo human still gets a battle royale" suite) and `server/scripts/dronesSmoke.mjs`. Story 6-3 (The Participants-Only Win Check) owns this and must solve solo-mode termination as part of it. KNOWN INTERIM INCONSISTENCY, Eric-accepted with the framing in front of him: a 4-human / 16-drone room reads `4 AFLOAT` while all 16 drones still gate the ending, so AFLOAT currently means "rivals left" and becomes literally "hulls left to clear" only when 6-3 lands.
  evidence: `epic-4-context-amendments.md` amendments 31-32; `server/src/game/match.ts:395-409` (`checkWin` and its doc comment naming the insta-finish hazard); `server/src/__tests__/drones.test.ts:190` and `server/scripts/dronesSmoke.mjs:34` (both `minHumans: 1`); `shared/src/constants.ts` `CONFIG.match.minHumans = 2` (production is unaffected either way).
  resolution: RESOLVED, AND EVERY FACTUAL CLAIM ABOVE IS NOW FALSE — read this line before acting on that summary. (1) Drones stopped gating the win on 2026-08-11 in Story **5-1**, not 6-3: Eric's epic-5 amendment 4 dropped the `aliveDroneCount() > 0` guard and `checkWin` has counted captains only ever since. (2) The prediction that this "must solve solo-mode termination as part of it" was REVERSED by that same ruling — the removal happened first and the termination question moved to **Story 6-5** (see the 2026-08-11 entry below and the `LEFT OPEN` block at `match.ts`'s `checkWin`). 6-3 was explicitly told to leave it alone. (3) The named blockers are gone: the "a solo human still gets a battle royale" suite no longer exists and `server/scripts/dronesSmoke.mjs` was DELETED with the match-start drone fill (epic-5 amendment 41). (4) The `n AFLOAT` inconsistency it describes is closed — AFLOAT and the win check have agreed since 5-1. Story 6-3 (cycle 89) then generalized the predicate itself: `isDrone` split into `isHuman` / `isParticipant` / `isFleetHull` in `server/src/game/participants.ts` (epic-6 amendment 13), so the win check now reads "participants only" by name rather than by proxy.

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  status: RESOLVED by Story 6-3 (epic-6 amendment 13) — verified 2026-08-18. The `isDrone` flag was split into `isParticipant`/`isHuman`/`isFleetHull` in `server/src/game/participants.ts`, and the client's second predicate is pinned by test so the two cannot drift silently. Story 6-4 then proved the seam: adding `'bot'` to `ShipRole` left all three predicate bodies byte-identical (amendment 26).
  summary: A NON-COMBATANT PREDICATE NOW EXISTS IN TWO PLACES, BY NECESSITY, AND THEY MUST BE KEPT IN STEP. The server decides combatant status from `ShipRecord.isDrone` (`signals.ts`, the `sunk` row's public clause); the client decides it from the roster hue sentinel `REGATTA_NO_HUE` (`score.ts` `isAfloatHull`/`isLiveRival`), because `PlayerMeta` carries no `isDrone` field and adding one would widen the schema. They agree today because `ArenaRoom` assigns the 255 sentinel to exactly the ships it creates with `isDrone=true`. When Story 6-4 (Combat Bot AI) introduces bots that ARE combatants, both sites change and a bot must receive a real wheel hue — otherwise the server would publish its sinking while the client refused to count it afloat.
  evidence: `server/src/game/signals.ts` (`visible()` public clause `!wreck.isDrone`); `client/src/score.ts` (`isAfloatHull(meta, droneHue)`, `isLiveRival`); `server/src/rooms/ArenaRoom.ts:430-433` (drones get the `REGATTA_NO_HUE` sentinel); `server/src/__tests__/regatta.test.ts` ("fillToCapacity drones keep the 255 sentinel — never a wheel hue").

## 2026-08-04 — The Public Register (cycle 45): review-gate defers

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  summary: IDENTITY–LOCATION FUSION FOR THE MINE LAYER, within doctrine but nowhere written down. Amendment 17's `hc` (Hit Call) deliberately carries impact `x,y` with NO victim id; the credited `sunk` now names WHO died, same tick. A Mine Layer 1500u away receives `hc{x,y}` + `sunk{id:'b', by:me}` together and learns captain B died at that fogged coordinate — a per-tick COMPOSITION that neither event discloses alone. The spec's "no clause leaks anything the roster does not already broadcast" is true per-event and false per-tick. This is inside amendments 17/30 (shooter-only disclosure is the ratified feature, and Eric explicitly wanted mine kills reported), so it is NOT a defect — but the composition should be an explicit design fact before any future signal widens either half.
  evidence: Fable Blind Hunter review-gate finding, 2026-08-04; `server/src/game/signals.ts` (`hc` row carries x,y and no victim id; `sunk` row's credited clause carries the victim id and no position); `epic-4-context-amendments.md` amendments 17, 18, 30.

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  summary: AN UNSEEN KILL'S FEED LINE CAN CONTRADICT A STILL-RENDERING GHOST CONTACT. `client/src/net/roomBindings.ts` gates `contactViews.markSunk` on `seen`, so for a kill you did not witness the contact view is left to fade on its own timer. If a hull leaves your bubble and dies 2s later just outside it, you read "B SUNK BY A" in the feed while B's not-yet-faded contact still renders as a live hull. Before PV 23 the line and the ghost could never coexist. Purely cosmetic — clearing the ghost would NOT leak (the feed already told you B is dead) and leaving it is defensible as honest history — but no ruling covers which reading wins and no test pins either. Needs an owner call, ideally alongside Story 4-8 (Attention Priority).
  evidence: Fable Blind Hunter review-gate finding, 2026-08-04; `client/src/net/roomBindings.ts` `handleSunk` (`markSunk` behind `if (e.seen)`); `client/src/render/contacts.ts` (prune-on-fade).

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  summary: AFLOAT AND KILLS NOW USE DIFFERENT DENOMINATORS — the other half of the old consistency argument is orphaned and needs an Eric ruling. Amendment 32 made `n AFLOAT` captains-only, but `n KILLS` beside it is still the server roster tally, which counts drone kills. Epic-3 amendment 19 used "consistent with `n KILLS` beside it, which already counts drones" as a LOAD-BEARING argument for counting drones in AFLOAT; amendment 32 adjudicated only the AFLOAT half. A solo captain's bar can now read `1 AFLOAT · 5 KILLS` having never engaged a captain. Either KILLS becomes captains-only too (consistent, but it would also change the results modal and the XP-visible tally), or the split is ratified as intentional. The comment at `client/src/ui/chromeBar.ts` was corrected in-cycle to describe the split honestly; the BEHAVIOR was deliberately not changed.
  evidence: Fable Blind Hunter review-gate finding, 2026-08-04; `epic-4-context-amendments.md` amendments 19 (superseded half) and 32; `client/src/ui/chromeBar.ts` kills doc comment; `server/src/rooms/ArenaRoom.ts` `syncRoster` (kills mirrors the server tally, drones included).

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  status: RESOLVED 2026-08-18 by Story 6-7 (Eric rulings R3/R4/R5, epic-6 amendment 47) — THE DEPARTURE SCUTTLE. A quit or a grace expiry now routes through `world.sinkShip(id)` with no killer instead of a silent `removeShip`, so a departure produces a real `sunk`, a kill-feed line, a plume and a placement, and credits nobody. Eric ruled it "reads as an ordinary sinking", so the "LEFT THE BATTLE" option was declined and NO new copy ships. A consented ABANDON MATCH and a 60s timeout take one identical path. Also fixed in the same change: the roster row used to be deleted a tick before the event was framed, which would have printed UNKNOWN VESSEL instead of the captain's name.
  summary: A MID-MATCH QUITTER STILL VANISHES SILENTLY — one whole class of exits stays off the register. `Match.onPlayerLeave` records a placement and calls `removeShip` WITHOUT emitting a `sunk`, so a captain who disconnects or quits produces no feed line at all while AFLOAT silently drops by one. The starting intent for this cycle was that the feed stop omitting the match; it is now complete for sinkings and still blind to departures. Neither the spec's Never list, its edge matrix, nor amendments 29-34 record a decision either way — genuinely undecided, not deliberately excluded. Needs an owner ruling (a "LEFT THE BATTLE" register line, or an explicit ratification that quits are invisible). Related to Story 6-7 (Reconnection UX).
  evidence: Fable Edge Case Hunter review-gate finding, 2026-08-04; `server/src/game/match.ts` `onPlayerLeave` (placement + `removeShip`, no `pending.push`); `server/src/game/world.ts` `sinkShip` is the only `sunk` emitter.

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  status: SPLIT 2026-08-18 by Story 6-7 (epic-6 amendment 49). The OWN-SINKING half is RESOLVED (Eric ruling R7 — the ELIMINATED modal is now synthesized client-side on resume; see the spec-0-2 entry above). The CATCH-UP half is RESOLVED-AS-ACCEPTED (Eric ruling R6/Q7): a resuming player simply misses those feed lines. The roster-delta mechanism this entry proposes was evaluated and REJECTED on the project's own recorded reasoning — the `sunk` row's ratification rationale states verbatim (`server/src/game/signals.ts:1346-1349`) that kills "aggregate in the counters as (+1, +1) and cannot be paired back from deltas", so a delta digest could name who sank but never WHO SANK THEM, in what order, or when; and for the REFRESH case there is no "before" snapshot to diff at all. A faithful catch-up needs the server to retain a sunk log (`by` survives nowhere today) and costs a new channel + a PV bump; it remains available and unbuilt.
  summary: THE "EVERY CAPTAIN'S SINKING REACHES EVERY CLIENT" GUARANTEE IS VOID ACROSS A RECONNECT, AND AN OWN-SINKING DURING THE GRACE WINDOW NEVER REPLAYS ITS ELIMINATION MODAL. Frames are per-tick sends with no catch-up: a client inside the Story 0.2 reconnect grace misses every `sunk` in that window and nothing replays them on resume (only `MSG.results` is cached and re-sent). Worse and PRE-EXISTING: if your OWN hull sinks while you are disconnected, the own-`sunk` that latches the elimination modal and the placement is simply never delivered, so a resumed player spectates with no ELIMINATED modal and no placement. The global feed makes the first gap newly visible (the register now claims completeness) but neither is caused by this cycle. Story 6-7 (Reconnection UX) is the natural home; a roster-delta-derived catch-up is the obvious mechanism since `kills`/`deaths`/`alive` survive the gap in the schema.
  evidence: Fable Edge Case Hunter review-gate finding, 2026-08-04; `server/src/rooms/ArenaRoom.ts` (per-tick `client.send(MSG.frame, ...)`, no replay buffer; only `MSG.results` cached for resume); `client/src/score.ts` `scoreAfterReconnect`; `client/src/main.ts` `handleSunkObserved`/`canOpenElimination` (own-id gated, event-driven only).

- source_spec: `_bmad-output/implementation-artifacts/spec-global-kill-feed.md`
  summary: THE `UNKNOWN VESSEL` FALLBACK COPY IS IMPLEMENTER DRAFT, NOT ERIC-RATIFIED. Fixing the review finding that a departed player's raw Colyseus session id would print in EVERY client's kill feed required a fallback label, and `UNKNOWN VESSEL` was chosen in-cycle to match the register's naval voice (it sits beside "LOST WITH ALL HANDS"). The DEFECT fix is not in question — printing session ids to the whole room is unambiguously worse — but the display string is user-visible copy that no design doc rules on, and it belongs in the next copy/identity pass with Eric alongside the other draft-copy items.
  evidence: Fable Edge Case Hunter review-gate finding, 2026-08-04; `client/src/ui/killFeed.ts` (`UNKNOWN_VESSEL`); `client/src/net/roomBindings.ts` `feedNameRef`; precedent at `client/src/main.ts` (`rosterNameOrNull`, whose comment already flags the session-id fallback as wrong for the score card).

## 2026-08-04 — DAMAGE CONTROL heal strip (cycle 46): ledgered consequences

- source_spec: `_bmad-output/implementation-artifacts/spec-damage-control-heal-strip.md`
  summary: HEAL OBSERVABILITY IS AN OPEN ERIC-GATED QUESTION. Ruled self-private for now (amendment 63), explicitly "ship the narrow version, ledger the question" — matching the house instinct from epic-4 amendments 1/15-20 of taking the smallest new information channel. The open question: should repairing be visible to observers inside truesight, restoring as INFORMATION the vulnerability window that battle-royale heals get from a visible channel? Revisit after live play, once damage races have actually been felt.
  evidence: The spec's Boundaries ("Never make the heal observable to anyone but its owner (ledgered as an Eric-gated revisit, not built here)"); the genre comparison (Apex/PUBG/Fortnite/Tarkov all gate healing behind an interruptible channel; this heal has none).

- source_spec: `_bmad-output/implementation-artifacts/spec-damage-control-heal-strip.md`
  status: PARTIAL — the STRIP GEOMETRY half is RESOLVED 2026-08-05 by cycle 47 (Eric rulings, amendments 65-68: the band lifted 0.58 → 0.534 to buy the room, and the rail is now 40px tall with a 22px family key chip, 16px type and 8px vertical padding; both DESIGN.md type deviations below are retired rather than ratified — the rail no longer runs at the HUD micro tier and no longer needs the "proportional below" chip carve-out). The HEAL TONE half stays OPEN and unratified, still wanting the later identity/audio pass with Eric.
  summary: THE HEAL TONE AND STRIP GEOMETRY ARE UNRATIFIED IMPLEMENTER DRAFT under the standing draft-copy rule, exactly like the Story 4.3 effect geometry. The tone (`client/src/audio/tones.ts` `heal` row): `{ freqStart: 620, freqMid: 466, freqEnd: 466, duration: 0.13, volume: 0.34, type: 'sine' }` — the fit family's two-note pattern INVERTED. The strip geometry (`CLIENT_CONFIG.refit` in `client/src/config.ts`): `stripHeight` 16px, `stripGap` 2px (seam under the card row), `stripKeyChip` 14px, `stripFontSize` 10px, `stripPad` 8px, `stripColGap` 10px. Two deliberate DESIGN.md type deviations the implementer flagged and justified against the 22px vertical budget remaining below the card row at the 1280x614 logical floor (the row ends at y=592 there): the rail runs at the 10px HUD micro tier rather than the cards' 11px label tier, and its key chip is 14px rather than the 22px family size (leaning on DESIGN.md's own "proportional below" precedent from the ability chamfer). These want ratification or retune in a later identity/audio pass with Eric.
  evidence: `client/src/audio/tones.ts` `heal` tone row; `client/src/config.ts` `CLIENT_CONFIG.refit` strip block (comment cites amendment 47, the container-fit law, as the geometry's forcing constraint).

- source_spec: `_bmad-output/implementation-artifacts/spec-damage-control-heal-strip.md`
  summary: `tickRepairs` IS NOT GATED ON `damageEnabled`, so a hull heals normally in the weapons-safe ready room. Flagged by the implementing agent as a judgment call rather than an unruled invention: the rulings specify only an alive gate (amendment 62 — dead or full-HP is rejected; nothing says heal should also be inert pre-match). Practically inert today (nothing can damage you in the ready room, so there is nothing to heal), but record it as a known, deliberate un-gated path in case a future phase makes ready-room damage possible.
  evidence: `server/src/game/world.ts` `tickRepairs()` — guards only `if (!ship.alive || ship.repairHp <= 0) continue;`, no `this.damageEnabled` check (contrast the zone-damage and dazzle/incendiary paths at `world.ts` lines ~1409 and ~2015, both explicitly gated on `damageEnabled`).

## 2026-08-05 — the DAMAGE CONTROL rail made choosable (cycle 47): ledgered consequences

- source_spec: `_bmad-output/implementation-artifacts/spec-heal-strip-legibility.md`
  summary: THE BAND ANCHOR NOW RUNS ON FIVE PIXELS OF SLACK AT THE 1280×614 FLOOR, and this is the constraint any future refit-band change collides with first. `bandTopFrac` 0.534 is wedged between the own-hull keep-out (`band.y > h/2` → `row.y > 325`) and the container-fit law (`row.y + cardHeight + stripGap + stripHeight ≤ 614` → `row.y ≤ 330`). Growing the card, the rail, or `pipsAbove` by more than a few px, or adding a second rail, has NO room left and forces a real design call (shrink a card, drop the keep-out, or make the floor viewport a special case). Not a defect — the margins are pinned with literal values in `upgradeMenu.test.ts` so the collision fails loudly in the suite — but the next person to touch this geometry should read this first.
  evidence: `client/src/config.ts` `CLIENT_CONFIG.refit.bandTopFrac` comment (the two inequalities); `client/src/__tests__/upgradeMenu.test.ts` "keeps both floor margins non-negative at the lifted anchor" (pins row.y 328 / band.y 310 / rail bottom 612 against the 614 edge).

- source_spec: `_bmad-output/implementation-artifacts/spec-heal-strip-legibility.md`
  summary: THE LIFTED BAND OVERLAPS THE DIMMED CORNER CLUSTERS MORE THAN AMENDMENT 40 MEASURED. That amendment ratified the overlap outright ("the dimmed chrome is inert during the refit combat lockout; cards render above it"), and the lift does not change its logic — but it does change its extent: at 1366×768 the card row now starts at y=410 instead of y=445, so more of the 38%-dimmed hotbar and vitals cluster sits under the outer cards. The property amendment 40 actually protects is unchanged and still pinned: the two INNER cards stay clear of both clusters. Recorded so the increase is a known consequence of an Eric-ruled lift rather than a silent drift discovered later.
  evidence: `client/src/__tests__/upgradeMenu.test.ts` "overlaps only the two DIMMED corner clusters, never the inner cards (1366x768)" — green at the new anchor, re-taken deliberately.

- source_spec: `_bmad-output/implementation-artifacts/spec-heal-strip-legibility.md`
  summary: THE REFIT BAND IS ANCHORED IN PHYSICAL PIXELS BUT SCALED IN CSS — a pre-existing mismatch between the geometry model and the runtime, surfaced by the cycle-47 review. `UpgradeMenu.place()` computes `refitBandLayout(window.innerWidth, window.innerHeight)` in PHYSICAL px and writes `band.y` to the panel's `top`, but `PANEL_CSS` scales the panel's contents by `--hc-ui-scale` about `transform-origin: top center`. So at the 125% tier the band's real footprint is 1.25x its laid-out height hanging off an UNSCALED anchor — which is NOT what the geometry suite's "1280x614 logical floor" models (that models a layout computed in logical units, i.e. `refitBandLayout(w/1.25, h/1.25)`). The two have simply never agreed. Consequence today is bounded and now pinned (a new scale-tier test walks every committed tier at four viewports), and cycle 47 kept the shipped geometry legal by trimming `stripGap` to 6 — but the MODEL is still wrong, which means every future refit-band change has to rediscover this by hand. The real fix is to make `place()` scale-aware (lay out in logical units and scale the anchor with the contents); it was NOT taken here because it moves the band for every user at 90% and 125% alike, which is a visible change wanting its own cycle and probably Eric's eye.
  evidence: `client/src/ui/upgradeMenu.ts` `place()` (physical `window.innerHeight` -> `panel.style.top`) vs `PANEL_CSS` (`transform: translateX(-50%) scale(var(--hc-ui-scale, 1))`, `transform-origin: top center`); `client/src/__tests__/upgradeMenu.test.ts` "keeps the scaled band inside the viewport at every UI-scale tier" (the new pin, which reported "1600x768 @125%: band 1.5px past the bottom" at an 8px seam); `CLIENT_CONFIG.settings.scaleGateWidthPx` 1600 is a WIDTH-only gate, which is why a 768px-tall viewport can select the 125% tier at all.

- source_spec: `_bmad-output/implementation-artifacts/spec-heal-strip-legibility.md`
  summary: DOC DRIFT FOR THE ERIC-GATED 7-5 DOC-SYNC BATCH (house rule: no design-doc edits in-cycle). DESIGN.md's Components table has no DAMAGE CONTROL rail row at all — the surface shipped in cycle 46 and was resized in cycle 47 without ever entering the design spine — and its key-chip family sentence still reads "1–3 chips on cards" when the shipped set is 1–4 on cards plus 5 on the rail. The Refit Card row's placement clause ("[PROPOSAL] the 4-card row anchors in the below-center band") is still directionally true but now describes a band that also carries a 40px rail below the row.
  evidence: `DESIGN.md:221` (key-chip family sentence), `DESIGN.md:229` (Refit Card row and its placement clause); no `damage control` match anywhere in DESIGN.md or EXPERIENCE.md.

## 2026-08-05 — every shell that connects deals damage (cycle 48): ledgered consequences

- source_spec: `_bmad-output/implementation-artifacts/spec-salvo-per-shell-damage.md`
  summary: A gun burst over a hull that an EARLIER shell of the same multi-barrel click just sank emits `sp` (fall of shot — the MISS mark) instead of `hc`, so the shooter sees miss markers painted on the exact point of their own kill.
  evidence: `resolveBurst` (server/src/game/world.ts) increments `resolved` only AFTER the `!victim.alive` guard, and `resolved === 0` routes to `emitSplash`. Verbatim tick output from a click that kills on shell 1: `burst s1 | dmg b 15 hp:0 | sunk b by:a | hc a`, then `burst s2 | sp a` and `burst s3 | sp a`. Both review hunters found this independently. The site is PRE-EXISTING (the deleted salvo gate also sat after the alive check), but cycle 47 made clicks deal up to 3x damage, so they now kill mid-fan far more often and the wrong mark is common rather than rare. NOT fixed in-cycle because the fix means changing what "resolved" means in Story 4.3's ratified `hc`/`sp` grammar (amendments 16-18) — counting GEOMETRIC victims instead of live ones — which is an Eric design call, not a patch. Note the same file's existing comment explicitly forbids counting geometric victims for the ZERO-DAMAGE star shell (it would mint an unsanctioned detection channel); a fix must preserve that exclusion. No anti-cheat exposure either way: `sp` and `hc` are both self-private to the shooter.

- source_spec: `_bmad-output/implementation-artifacts/spec-salvo-per-shell-damage.md`
  summary: A same-tick shell intercepted by a hull that an earlier shell of the same click already sank still emits `boom` + `hc` and is consumed by the wreck, denying its burst to live hulls nearby.
  evidence: In `resolveShell` (server/src/game/world.ts) `emitHitCall` fires before the `!victim.alive` check, and the per-tick `hulls` snapshot is built once and never refreshed when a ship sinks mid-tick, so `earliestHull` still collides with wrecks for the remainder of that tick. Pre-existing; same aggravating factor as the entry above (mid-click kills are now common). Fixing it means either refreshing the tick's hull list on sink or moving the alive lookup above the Hit Call — both touch shared collision ordering, so it wants its own focused cycle.

- source_spec: `_bmad-output/implementation-artifacts/spec-salvo-per-shell-damage.md`
  summary: `CLIENT_CONFIG.litZone.burnShakeScale` is inert for every DoT flush the server can actually emit, so the ratified "a burn shakes at a fraction of the amplitude" rule is not delivered.
  evidence: `shake.trigger` takes damage in hp and maps it through `shakeMagnitude`, which floors at `PX_SUBTLE = 4px` for anything at or under ~15hp (client/src/render/shake.ts). A single incendiary flush is ~2.75hp, so 2.75 and 2.75 x 0.35 both produce the identical 4px floor — the scale only changes the output above ~43hp. Pre-existing and unchanged by cycle 47 (it was equally inert per-event), but the Story 2.9 burn-identity comment asserts the behavior in prose, so either the constant or the comment is wrong. The burn TONE does its job; only the amplitude half of the rule is missing.

## 2026-08-05 — the foghorn (cycle 50): ledgered consequences

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-the-foghorn.md`
  status: ROUTED to the Story 7-5 doc-sync batch — verified 2026-08-18 as still-unfixed doc drift. See the consolidated 7-5 scope in `epic-6-retro-2026-08-18.md` (Significant Discovery item 3).
  summary: DOC DRIFT FOR THE ERIC-GATED 7-5 DOC-SYNC BATCH (house rule: no design-doc edits in-cycle). Four items, all superseded by amendments 51-61. UX-DR31 still lists the foghorn emote as "specced but unbound (open question)" — the key is F and UX open question #20 is closed. The Story 4.5 acceptance criteria still say the honk "lights an arc sweep along its bearing on every listening ring in earshot" and bound its payload by "the listening tier's bearing contract" — there is no listening ring, and the surface is a screen-edge chevron. FR13 still names foghorns as something hull microphones detect, which is moot while amendment 1 stands. And `CLAUDE.md` still records `PROTOCOL_VERSION` as "currently 23" when it is now 26 — the same line has now gone unfixed across three cycles (amendments 39, 50, 59).
  evidence: `epics.md:180` (UX-DR31), `epics.md:861-864` (Story 4.5 ACs), `epics.md:49` (FR13), `CLAUDE.md` Key Decisions block.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-the-foghorn.md`
  summary: THE HORN SAMPLE PATH SHIPS FULLY IMPLEMENTED BUT UNUSED — it is exercised by tests only, because no licensed or CC0 foghorn recording exists in this repo and none may be sourced unattended. Eric chose to build the seam now (amendment 57) precisely so a real recording is later a file plus one catalog line with no code, wire, or protocol change. **The asset is Eric's to supply or approve** — until he does, every player hears the synthesized horn. Adding a SECOND horn of any kind is Eric-gated content (amendment 52), so a future cycle must not quietly populate the catalog to "prove" the seam.
  evidence: `client/src/audio/horns.ts` (`HornVoice = {kind:'synth'} | {kind:'sample', url}`, `HORNS` with the single `standard` synth entry, `HornBufferCache`); `client/src/__tests__/horns.test.ts` (the sample path's cache/dedupe/sticky-failure coverage, all against an injected fetchDecode).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-the-foghorn.md`
  summary: THE CLIENT'S HONK COOLDOWN ARMS AT SEND WHILE THE SERVER'S ARMS AT EMIT, so a press landing exactly on the boundary can be accepted locally and then silently dropped by the server. The server's window opens strictly later (one-way latency plus tick quantization), so the client is the more permissive of the two at the seam — the opposite of what a naive reading assumes. The cost is bounded and benign BECAUSE of amendment 58: nothing about a honk is client-predicted, so a dropped press leaves no local state to reconcile and no visual to retract; it simply does not sound. No fudge margin was added because that would be an invented gameplay number. If Eric ever wants the boundary press to be impossible rather than merely harmless, the fix is a ruled-on client-side margin.
  evidence: `client/src/main.ts` `handleFoghornPress` (arms `g.nextHonkAt` at press time, comment records the true relationship); `server/src/game/world.ts` `consumeHonk` (arms `ship.nextHonkAt = this.now + CONFIG.foghorn.cooldownMs` at emit time).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-the-foghorn.md`
  summary: THE SCREEN-EDGE CHEVRON IS NOT YET PLACED IN THE ATTENTION TIER TABLE — Story 4-8 owns that call. It is a new animated screen-space channel that today arbitrates with nothing: it is not a Tier-1 threat channel, and it does not hold or freeze while the low-HP rail pulse or the live denied pulse is active. In a crowded room up to 8 chevrons can be alive at once (`maxMarks`), each fading over 1.2s, which is exactly the kind of channel the epic's "information noise must never bury the hunt" guardrail exists to arbitrate. Note also that the plume-style constraint does NOT apply here: unlike wounded smoke, a chevron has a per-mark tier, so 4-8 CAN reason about it as a weighted channel rather than an unkeyed particle budget.
  evidence: `client/src/render/foghorn.ts` (the mark list and `maxMarks`); `client/src/render/attention.ts` (`tier1Active` — the chevron is absent from it); `CLIENT_CONFIG.foghorn.chevron`.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-the-foghorn.md`
  summary: THE TWIN TABLE PROVES A CUE HAS A TWIN, NOT THAT THE TWIN FIRES — and Story 4-7's twin walk should audit every call site accordingly. This cycle found the foghorn playing the shipped `denied` tone at a site where none of the `denied` row's documented visuals (aim-arc pulse, reticle flash, hotbar chip) could fire, because the horn has no surface of its own; the type-level exhaustiveness check passed the whole time. Eric resolved it by dropping the cue (amendment 60) rather than inventing a visual, so no new grammar exists — but the class of defect is site-level, not table-level, and only a walk of every `audio.play()` call site can find the rest.
  evidence: `client/src/audio/twinMap.ts` (`TONE_TWINS` is exhaustive over `AudioCueId` at the type level); the four `denied` call sites in `client/src/main.ts` (weapon click, ability press, ability-capped, server echo) each pair the tone with a visual, and the foghorn's did not.

## 2026-08-05 — radar realism cycle (cycle 51): three questions Eric deliberately left open

- source_spec: `_bmad-output/implementation-artifacts/spec-radar-realism.md`
  status: RESOLVED by Story 4-6 (cycle 73, epic-4 amendment 216) — verified 2026-08-18. Eric DELETED the Bounty Bloom outright rather than rebuilding its channels: no radar paint, bloom, ring, bearing, range or area disclosure of the leader, ever. `client/src/ui/bounty.ts:18` records the deletion. The dependency this entry raised cannot fail because the dependent feature no longer exists.
  summary: BOUNTY BLOOM DEPENDS ON TWO CHANNELS THAT NO LONGER EXIST IN `return` MODE, and the bounty story must resolve it. DESIGN.md:237 specs an unbuilt radar-layer feature (GDD E6 #47) as "an expanding ring in the leader's personal color around their class blip at true position" — but `HC_RADAR_GRAMMAR=return` deletes both personal hue on radar (amendment 65) and the class blip (amendment 66). Eric declined to rule, verbatim: *"I don't know honestly because I haven't addressed the 'bounty' story at all yet. I'm not sure I want the kill leader's position to be known globally, for example."* Note the deeper question he raised is not a rendering question at all — whether a global position broadcast should exist is a fog-of-war design call that also touches the perception invariant's declared-exception list. Options surfaced and NOT chosen: a hueless expanding ring on an anonymous return (keeps "someone is leading, over there", loses "who"), a sanctioned identity exception on radar, or dropping the feature. Natural home: **Story 4-6 (The Bounty)**, which is still `backlog` in sprint-status.yaml and now carries this as recorded scope.
  evidence: `DESIGN.md:237` (Bounty Bloom component row, "[PROPOSAL]" ~2s decay); amendments 65-66 and 71 in `epic-4-context-amendments.md`; the feature is unbuilt — no `bounty` match anywhere in client/src or server/src.

- source_spec: `_bmad-output/implementation-artifacts/spec-radar-realism.md`
  summary: THE COLORBLIND ASSIST'S BLIP CLAUSE IS PARTLY INERT IN `return` MODE and needs a real answer later. DESIGN.md:163 defines the assist's radar behavior as "boosts blip outlines and raises the minimum decayed blip opacity" — but a `return`-mode blob has no outline to boost, and the identity problem the assist solved on blips (telling boat A from boat B by hue) does not exist once every return is monochrome. Eric: *"Colorblind mode should address this, we should make a note to circle back to this question in the future."* Interim behavior shipped this cycle: the raised decayed-opacity floor (`assistMinAlpha`) is KEPT and works unchanged, the outline clause is simply inert, and the family-palette work for hulls/nameplates/kill-feed is untouched. The forward-looking risk is amendment 65's provenance channel: once a SECOND sensor exists and hue distinguishes radar from sonar, green-vs-amber sits near the red-green axis and the assist will need to carry provenance through a non-hue channel (persistence and edge character are the candidates already named).
  evidence: `DESIGN.md:163` (assist definition, and its explicit rejection of Variant P as a CVD answer); `CLIENT_CONFIG.blip.assistMinAlpha` 0.35 (client/src/config.ts); amendment 71 in `epic-4-context-amendments.md`.

- source_spec: `_bmad-output/implementation-artifacts/spec-radar-realism.md`
  summary: AMBER IS RESERVED BUT UNASSIGNED, PENDING ERIC'S SOUND-REPRESENTATION DECISION. Amendment 64's three-channel split gives hue the job of sensor provenance, which only pays off when a second sensor exists. Eric on what that sensor is: *"I don't know yet, I'm thinking about how to represent sound information but I'm not sold on the 'listening ring' concept entirely."* So this cycle spends no hue on a sensor that does not exist. Two facts worth carrying forward: DESIGN.md already gives the Listening Ring phosphor GREEN (lines 107, 236), which would collide with radar's green if the ring ships as specced alongside `return` mode; and DESIGN.md:161 already reserves amber (~25-52°) as a never-a-combatant hue, so assigning it to a sensor is consistent with the doc rather than a deviation. Also note this cycle FREES amber's only on-water use — the hue-latch boot color for unresolved contacts retires with the hue system in `return` mode. Natural home: whichever story lands acoustic sensing (hydrophones were deferred 2026-08-04 in favor of active sonar).
  evidence: `DESIGN.md:107` (listening-ring token, `{colors.phosphor}`), `DESIGN.md:236` (Listening Ring component row, "pure intensity grammar"), `DESIGN.md:161` (reserved hue bands); `client/src/render/hueLatch.ts` (the amber boot for unresolved hues); amendments 65 and 71.

### 2026-08-10 — RESOLVED by Eric ruling (Story 4-6, amendments 216-221)

The first cycle-51 entry above — the Bounty Bloom depending on two channels `return` mode had already
deleted — is now CLOSED. Eric ruled on Story 4-6's pre-implementation gate: no location disclosure of the
kill leader ships at all, in any form, at any range. This is a fourth option beyond the three surfaced in
2026-08-05 (a hueless anonymous ring, a sanctioned identity exception, dropping the feature) — the Bounty
Bloom is deleted outright and the bounty survives only as a held throne (`ArenaState.bountyId`) presented
through the BR Chrome Bar, the kill feed, and a self-only toast/tone. No sanctioned identity exception on
radar was needed. Full record: `epic-4-context-amendments.md` amendments 216-221; `spec-4-6-the-bounty.md`.
The other two cycle-51 entries above — the colorblind assist's partly-inert blip clause, and amber
reserved-but-unassigned pending the sound-representation decision — remain OPEN; this ruling does not
touch either.

## 2026-08-05 — radar realism cycle (cycle 51): review-gate findings ledgered rather than fixed

- source_spec: `_bmad-output/implementation-artifacts/spec-radar-realism.md`
  summary: THE `silhouette` + `pseudonym` FLAG COMBO RENDERS EVERY BLIP PERMANENTLY AMBER, and nobody ruled on whether that is acceptable. The two cycle-51 flags are deliberately independent (amendment 63) so a happy medium can be built, which means all FOUR combinations are reachable — but in silhouette mode the client resolves a blip's color by looking its id up in the roster, and a pseudonymous id matches no roster entry, so `resolveHue` falls back to its amber "unresolved" boot color and `retryHue` re-probes every frame forever without resolving. Cost is trivial (one map lookup per blip per frame) and it is arguably the HONEST presentation for that combo — the client genuinely does not know who that is — but it was never designed, and anyone flipping that pair in production should expect an all-amber scope rather than a bug report. The other three combos are unaffected. Not fixed because the answer is a design call, not a defect: either it is fine as-is, or silhouette+pseudonym should render in neutral phosphor instead of the error-ish amber, or the combo should be rejected at flag-resolution time.
  evidence: Found by the Fable adversarial reviewer at the cycle-51 gate and reported as an observation rather than a defect. `client/src/render/hueLatch.ts` (`resolveHue` amber boot + `retryHue`); `client/src/render/radar.ts` (silhouette path calls `resolveHue(e.id, this.hueFor)`); the pseudonym ids (`trk-` + 8 hex) are generated in `server/src/game/world.ts` and deliberately collide with no roster id, pinned by test.

- source_spec: `_bmad-output/implementation-artifacts/spec-radar-realism.md`
  summary: COAST RETURNS CAN MASS-PAINT A LARGE ARC IN ONE FRAME IF THE DERIVED BEAM ANGLE EVER STEPS BACKWARD. `sweepCrossed` guards only the exact zero-width advance, so any backward step of the beam between two rendered frames makes the crossed span compute as nearly a full turn (`wrapPositive(to - from)` ≈ TAU), birthing almost every in-range island's near arc at once at full brightness. Two live paths: a `sweepPeriodMs` change transient between anchor samples exceeding one frame's ~0.024 rad advance, and a multi-second tab pause painting a large arbitrary arc on refocus. Purely cosmetic, self-heals within one phosphor decay (~12s), and ship blips are unaffected (they are server-pushed events, not beam-derived). NOT fixed because the real fix is to clamp or reject backward beam steps in the sweep derivation, which touches the shipped `silhouette` path too — and this cycle's whole discipline was that the default grammar stays byte-identical. Natural home: whichever cycle next touches sweep derivation, or Story 4-8 (the readability gate), which already owns "information noise must never bury the hunt."
  evidence: Fable adversarial reviewer at the cycle-51 gate, ranked PLAUSIBLE and traced (not reproduced). `client/src/render/returnMarks.ts` (`sweepCrossed`, the span computation in `islandReturns`); `client/src/net/clock.ts`'s monotonic guard rules OUT the server clock as a backward source, so the period-change transient is the live path.


## 2026-08-07 — the eighths ladder (Story 4-9, cycle 60): review-gate findings ledgered rather than fixed

- source_spec: `_bmad-output/implementation-artifacts/spec-4-9-the-eighths-ladder.md`
  summary: THE FOGHORN'S VOLUME BAND CAN NOW READ QUIETER THAN TRUESIGHT ON A DEDICATED INTEL-TRUESIGHT BUILD, contradicting Eric's original foghorn anchor ("I always hear anyone's within truesight range at full volume"). Bands are anchored on `stats.radarRange` (amendment 122), and nothing binds `sightRange <= radarRange / 2`, so a full `intelTruesight` stack (1.12^5 -> 581.5u sight) with no `intelRadar` (660u radar) puts bands 5-8 INSIDE the listener's own truesight bubble: a hull at 500u you can plainly see resolves to band 7 = 0.625 gain. NOT FIXED, and the obvious fix is wrong — clamping to full volume inside `sightOf` would reintroduce a dazzle term into the band resolver, which is exactly what amendment 122 removed to make "dazzle cannot deafen" structural rather than clamped (a dazzled listener at 500u would then drop back to 0.625, i.e. dazzle would deafen). So this is a genuine design fork for Eric: accept that hearing rides intel range and the truesight anchor holds only at base stats, or re-anchor the ladder. Needs a ruling, not a patch.
  evidence: Edge-case hunter at the step-04 gate, traced and reproduced against the boon math. `server/src/game/signals.ts` (`hornBandFor` reads only `me.stats.radarRange`); `shared/src/sim/boons.ts` (`intelTruesight` mult 1.12, 5 copies; `intelRadar` mult 1.15); the outcome is already pinned by a server test labelled "taken knowingly" — by the implementer, not by Eric, which is why it is ledgered here.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-9-the-eighths-ladder.md`
  summary: THE FOGHORN'S RANGE DISCLOSURE ROUGHLY DOUBLED IN RESOLUTION AND NO RULING SAYS SO. Pre-4.9 a fogged listener learned the honker's range as one of THREE annuli (0-330 / 330-495 / 495-660); post-4.9, after the band-4 floor, it is FIVE (0-330 / 330-412.5 / 412.5-495 / 495-577.5 / 577.5-660) — single-honk range resolution improves from a 165u bucket to an 82.5u one, alongside the exact bearing the row already carries. This follows directly and unavoidably from Eric's own choice of eight volume regions over quarters, and amendment 51's letter still holds (bearing and volume only, no position, no correlation handle) — but Eric picked the option without being shown this consequence, and the code comment justifying the resolution ("never finer than the gain curve consumes") is circular, since this story chose the gain curve. Recorded so the widening is an explicit decision rather than an emergent one. If Eric wants it back, the lever is fewer distinct gains, not a wire change.
  evidence: Both the orchestrator's seeded suspicion and the blind adversarial hunter at the step-04 gate, quantified and confirmed. `server/src/game/signals.ts` (`hornBandFor`), `shared/src/types.ts` (`FoghornEvent.v`), amendments 51, 122 and 124.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-9-the-eighths-ladder.md`
  summary: THE OWN-VS-ENEMY CULL RING NOW HANGS OFF A 400ms BEST-EFFORT OWNERSHIP LATCH. The server never puts an owner id on a ballistic reveal (deliberately — it would be range-derivable intel), so the client infers "this is mine" from a click-time latch with a 400ms window. Story 4.9 made that inference load-bearing for render range: a missed latch gives your own torpedo the ENEMY 287.5u cull ring instead of 370u, so a high-RTT player's own fish vanishes ~82.5u earlier than a low-latency player's with no in-game explanation. Reachable without latency too — `handleShell` consumes a standing torpedo latch for any shell revealed near your hull, so firing the gun and then a torpedo inside 400ms can eat it. NOT FIXED because the error is deliberately one-sided: treating uncertain tracks as enemy costs an own-fish sprite early, whereas the inverse would give every enemy torpedo a long uncorrected ghost, which is the whole reason the detect cull exists. Hardening it needs a real ownership signal, and the only honest one would be a wire field the anti-cheat posture forbids.
  evidence: Both hunters at the step-04 gate, independently, with a concrete repro sequence. `client/src/render/projectiles.ts` (`trackCullRadiusSq`, `OwnFire`), `client/src/sim/ownFire.ts` (`OWN_FIRE_WINDOW_MS = 400`), `client/src/net/roomBindings.ts` (`handleShell` / `handleTorp` latch consumption).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-9-the-eighths-ladder.md`
  summary: A STRAIGHT-RUNNING ENEMY TORPEDO CAN BECOME PERMANENTLY INVISIBLE, and the shorter detect ring makes it easier to reach. Ballistic reveal is exactly-once (`seenBallistics`), the cull is client-side, and only HOMING torpedoes emit a `torpU` that can resurrect a culled track. So a straight fish revealed at the detect boundary, then culled when it leaves the client's ring, is never drawn again even if the observer's own movement re-encloses it — and it can hit unseen. This existed before Story 4.9 at the wider ring; shrinking the ring to 287.5u means an observer's own speed opens and closes that gap far more often. NOT FIXED because the honest fix is a server-side re-reveal rule for ballistics that have gone out and come back, which is a perception-invariant change needing its own story and its own oracle.
  evidence: Blind adversarial hunter at the step-04 gate, traced. `server/src/game/perception.ts` (exactly-once `seenBallistics`; the `torpU` scan skips `shell.homing === undefined`), `client/src/render/projectiles.ts` (client-side cull, `spawnFromUpdate` resurrect path is homing-only).

### 2026-08-07 — RESOLVED by Eric ruling (amendment 126), same day they were filed

The first two cycle-60 entries above — the foghorn band reading quieter than truesight on a dedicated
intel-truesight build, and the honk's range resolution roughly doubling — were put to Eric in plain
terms and **both are ACCEPTED AS SHIPPED**: *"Fine on both. That's fine."* They are CLOSED. Do not
"fix" either without a fresh ruling; amendment 126 records why the obvious fix to the first one is
worse than the flaw (it would let dazzle deafen again) and what the real lever on the second is
(fewer distinct gains, not a wire change). The other two cycle-60 entries — the soft ownership latch
and the permanently-invisible straight torpedo — remain OPEN.

### 2026-08-07 — cycle 61 (Story 4-10, the physical return model)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-10-the-physical-return-model.md`
  status: RESOLVED by interstitial cycle 72 (epic-4 amendment 215) — verified 2026-08-18. Eric removed the storm return from the scope entirely by PREFERENCE ("I don't want the storm ring highlighted by radar anymore"), deleting `stormSample`, `STORM_EDGE` and `model.stormBandU` end to end. The one radar return with no LOS test no longer exists, so the standing "islands block every sensor" ruling now has no exception. The storm still renders on the water unchanged.
  summary: THE STORM WALL PAINTS THROUGH ISLANDS — it is the one radar return with no LOS test, and this is deliberate, not an oversight. Ship echoes, coast returns, surf and (as of the cycle-61 review gate) sea clutter all obey the standing 2026-08-02 "islands block every sensor at all ranges" ruling; the storm band does not, so a stretch of the closing wall standing behind a headland still paints. NOT FIXED HERE because Story 4-11 owns occlusion wholesale and will do it against the cycle-59 height raster with a real shadow length — adding a second bounding-circle LOS test now would be work 4-11 immediately deletes, and it would cost per-cell segment tests along the whole in-range arc of a ring that can be thousands of units around.
  evidence: `client/src/render/radarSources.ts` `walkRing` emits band cells with no `islandBlocksSegment` call and no occluder shortlist, where `buildIslandCoverage` (`radarHeatmap.ts`) and `stampClutter` both carry one. Raised independently by both the blind adversarial hunter and the edge-case hunter at the step-04 gate; the deferral was a spec-level decision, not a miss.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-10-the-physical-return-model.md`
  summary: THE PER-FRAME COST GATE HAS NEVER BEEN TAKEN ON THE REFERENCE DEVICE, only in a headless harness. Cycle 61 measured 1.70 ms at 0.5x zoom (buffer 0.88 ms + weather ~0.8 ms) against a 2.0 ms budget, and the harness's buffer-only figure (0.88 ms) now agrees with the shipped reference-device number (0.95 ms) within noise — which is why this cycle passed its gate honestly rather than by the cross-environment extrapolation an earlier draft attempted. But amendment 99's standing rule says "on the reference device", and no run on that device exists for any radar cycle since the figure was first recorded. Every arc story re-affirms this rule, and 4-11 adds SERVER cost at 20Hz on top, so the gap compounds.
  evidence: `client/src/config.ts` `cellU` cost-table comment (harness-measured); amendment 99; the Story 4-10 spec's Block-If names 2.0 ms at min zoom without naming the machine. Raised by the blind adversarial hunter at the step-04 gate.

### 2026-08-08 — cycle 68 (Story 4-11, height-aware radar shadows)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-11-height-aware-radar-shadows.md`
  summary: INVISIBLE OCCLUDERS — raster land with no island polygon. `extractIslands` drops blobs under `minIslandArea` (1200u²) from `islands[]`, but they stay LAND in the mask that `buildHeightRaster` quantizes, so they seal to q >= 1. Since cycle 61 those specks already painted as radar terrain returns while drawing as nothing on the chart; as of THIS story they also cast shadow and DELETE BLIPS, cast from water that renders as empty and that ships sail straight through. Not fixed here because the fix belongs in map generation (zero a dropped speck's cells in the mask before the raster is built, or scrub raster cells with no owning Island), which is outside this story and changes what every seed's radar looks like — a generation change deserves its own cycle even though it is not a wire change (the raster never travels; both sides rebuild it).
  evidence: `shared/src/sim/map.ts` `extractIslands` skips sub-`minIslandArea` blobs while `snapshotLand` snapshots the mask verbatim at `map.ts:606`; `blipGate` now keys occlusion to the raster while sight/detect/collision/render all key to polygons. Raised by the edge-case hunter at the step-04 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-11-height-aware-radar-shadows.md`
  summary: THE GATE IS A POINT PREDICATE, THE PAINT IS A FOOTPRINT — different granularities. `blipGate` evaluates visibility at the hull's CENTRE only, while the client prices the coverage mask cell by cell. So a hull whose centre sits just past the residual reach but whose bow cells do not is dropped whole, and one whose centre is visible discloses its whole mask including cells that are individually shadowed. Bounded and not exploitable (the mask says only "metal here" and carries no position beyond the footprint the server chose to disclose), but the two predicates are not the same predicate and the docs do not say so. Not fixed here: making them agree means either gating per cell (a real per-tick cost on the anti-cheat path) or attenuating the disclosed mask per cell (a wire change).
  evidence: `server/src/game/signals.ts` `blipGate` takes a single `Vec2`; `client/src/render/radar.ts` `marchEcho` marches the whole footprint slab. Raised independently by Codex and, in a different form, by the blind adversarial hunter at the step-04 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-11-height-aware-radar-shadows.md`
  summary: THE PERCEPTION ORACLE FOLDS A SUPERSET ON CORNER GRAZES — a latent seeded-fuzz flake, not a leak. `cellAOracle` folds any cell whose slab interval satisfies `exit >= entry`, so a ray passing exactly through a cell CORNER folds both corner-adjacent cells, where production's DDA (`nx <= ny` steps x only) folds exactly one. The oracle is therefore strictly stricter, so a disagreement fails LOUD in the safe direction — but it is measure-zero geometry that a future seed can land on, and it would present as a mysterious invariant failure rather than as the corner case it is.
  evidence: `server/src/__tests__/perception.test.ts` `cellAOracle` slab test vs `shared/src/sim/radarShadow.ts` `walkCells`. All current fixtures are axis-aligned and off-boundary. Raised by the blind adversarial hunter at the step-04 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-11-height-aware-radar-shadows.md`
  summary: THE CLIENT'S `aground` KILL SWITCH HAS NO SERVER TWIN. When `sampleHeight(own) > 0` the client drops all pending wire blips and stops marching entirely, while the server keeps disclosing normally — so a prediction divergence or a raster quantization that puts own centre one land cell inland silently costs the player every legitimate disclosure until it clears. Pre-existing (the switch predates this story) and arguably intended for a beached hull, but this story makes the raster load-bearing for disclosure, which raises the stakes of the two sides disagreeing about what "aground" means.
  evidence: `client/src/render/radar.ts` `aground()` gates `resolvePending` and `marchBeam`; no equivalent predicate exists in `server/src/game/signals.ts`. Raised by Codex at the step-04 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-11-height-aware-radar-shadows.md`
  status: RESOLVED by interstitial cycle 98 — verified 2026-08-18, and the fix went the OPPOSITE way to this entry's suggestion. The dim mask must NEVER be destroyed while bound as a Sprite mask (that was the client-freeze defect two playtesters hit); `radar.ts:1263` now calls `bakeDimMaskTexture(want, this.dim.texture)`, redrawing the live canvas in place so ONE source serves the Radar's life. Steady-state memory is unchanged. `textures.ts:196` explains why swapping-and-destroying is forbidden — do not 'fix' this back.
  summary: `bakeDimMaskTexture` mints a 1024² canvas texture per `Radar` construction and never destroys it. Matches the shipped `bakeSweepTexture` precedent exactly, so it is only a leak if `Radar` is rebuilt per rejoin rather than per session — worth one look, and if it is, both textures want the same fix.
  evidence: `client/src/render/textures.ts` `bakeDimMaskTexture`; no `destroy()` on either baked texture. Raised by the blind adversarial hunter at the step-04 gate.

### 2026-08-08 — cycle 69 (Story 4-12, radar wakes)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-12-radar-wakes.md`
  summary: THE DECOY LAYS NO WAKE, AND UNDER THIS STORY THAT IS A NEW TELL — deferred BY ERIC, not missed. A decoy is frozen at its drop pose at speed 0, so it never advances a wake head and therefore paints no track, while every moving hull now does. "A wakeless paint is a stopped ship or a decoy" is inference a defender can now make, which dents amendment 11's wire-indistinguishability law. Put to Eric at the pre-implementation gate with the fix costed (stamp the decoy with the dropping ship's ribbon at release, which reads exactly like a ship that came to a stop and reuses data already kept); he answered *"Decoy will get major changes soon so lets not worry about it for now."* So NO decoy special-casing exists anywhere in the cycle, in either direction, and the next agent must not read the gap as an oversight and "fix" it into a mechanism the rework will delete. Reopen this WITH the decoy rework, not before.
  evidence: amendment 201. `shared/src/sim/wake.ts` `appendWakeSample` requires travel before a second sample exists and `eachWakeSegment` needs two — so a stationary source produces zero segments by construction, with no decoy branch in `server/src/game/signals.ts` or `client/src/render/wake.ts`.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-12-radar-wakes.md`
  summary: THE RECENCY CHANNEL READS AS TEXTURE, NOT AS LENGTH — a measured consequence of amendment 203's corridor that no amendment predicted, and a candidate Eric tuning request. The story asks a wake to carry "course and recency"; recency was designed to show as the tail dropping under `bands[0].at` first, so the visible track SHORTENS as it ages. What it actually does is fade in LIT FRACTION (solid head, progressively dotted tail — 1.00 to 0.78 across the four buckets at 300u) plus a real reach difference (fresh water still reads at 577.5u where the oldest bucket reads nothing). Cause: the feasible corridor between `bands[0].at` 0.12 and `minPeak`'s worst draw 0.13571 is only 13% wide and the draw window spends half of it, leaving the range term ~3% of intensity to work with across the whole scope. NOT FIXED HERE because every lever is ratified: widening the corridor means raising the wake past `minPeak`'s worst draw (breaking a bound three tests defend and one Eric has twice declined to move), and a brightness ramp along the track is exactly amendment 160. Whether the dotted-tail reading is BETTER than a shortening one is a look question only Eric can answer.
  evidence: amendment 203's arithmetic; the solved values and the measured 1.00 → 0.78 bucket ladder in the wave-2b calibration; `client/src/render/radarSources.ts` `wakeAgeScale`.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-12-radar-wakes.md`
  summary: CHOP IS MODEST AND IT IS A BIG-HULL PHENOMENON. At the derived Kelvin `1/sin θ` multiple the halo is ~3-4 cells for a battleship and ~1 for a torpedo boat, because the envelope width is derived from the ribbon's own thickness and a torpedo boat's beam is 9u against a 9u lattice. So the camouflage Eric asked for lands mainly around battleships and mine layers, and a torpedo running past a torpedo boat is barely disguised at all. Not changed here because the coefficient is NOT the lever — it carries clutter's three ratified bounds — and the honest lever (`CHOP_HEAD_MULTIPLE`) is a look knob that wants Eric's eye on the live build rather than a guess. Revisit after he sees it.
  evidence: `client/src/render/radarSources.ts` `KELVIN_SIN` / `CHOP_HEAD_MULTIPLE` and the note left in that source; amendment 206.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-12-radar-wakes.md`
  summary: THE ADVERSARIAL SERVER COST IS 0.9% OF THE TICK — under the bar, but the closest this arc has run, and it needed two cost devices to get there. A 20-ship / 20-observer room with every hull carrying a full 12s track measured ~835 µs/tick on the first implementation; a per-ribbon bounds memo and a conservative swept prefilter (both of which provably decide nothing — the exact clauses still gate) brought it to 425-470 µs. For comparison the cycle-68 shadow gate's adversarial figure was 278 µs. Ordinary spread-out rooms sit far below this, and the broadphase culls whole ribbons, but the headroom is now thin enough that the NEXT feature adding per-observer per-entity work at 20Hz should budget against this number rather than against the old baseline.
  evidence: the wave-2a bench harness (temporary, not committed) mirroring the 4.11 methodology; `server/src/game/signals.ts` wake row broadphase and memo.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-12-radar-wakes.md`
  summary: `WAKE_STAMP_MIN_MS` IS A REBUILD FLOOR ON A CACHE, AND ITS SAFETY ARGUMENT IS TIMING-BASED. Twenty in-bubble hulls bump the client wake stamp's segment-set version several times a second each, so the cache rebuilt every frame and cost 2.19 ms at 0.5× zoom. The floor (`radarCellU / fastest hull speed` = 200 ms) fixes it, and the argument is that the beam crosses any given bearing once per 4 s revolution, so a staler stamp only ever leaves the ribbon head less than one lattice cell behind itself when the beam arrives. That is sound at the shipped `sweepRpm` 15 and `radarCellU` 9 — but it is a coupling between a CACHE and the SWEEP RATE that is not pinned anywhere, so a future sweep-rate boon or a lattice retune could invalidate it silently. Worth a pin.
  evidence: `client/src/render/wake.ts` `WAKE_STAMP_MIN_MS`; `CONFIG.vision.sweepRpm` / `sweepRpmMax` (30, so a maxed sweep boon halves the revolution period).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-12-radar-wakes.md`
  summary: THE POINT-PREDICATE / FOOTPRINT MISMATCH NOW APPLIES TO A SECOND CHANNEL. Cycle 68 ledgered that `blipGate` evaluates visibility at a hull's CENTRE while the client prices the whole coverage mask cell by cell, so a disclosed mask can carry cells that are individually outside the annulus, outside the swept wedge, or behind cover. The `wk` row inherits that shape exactly: it gates on a segment MIDPOINT and then emits the segment's full square-capped mask, which extends `widthU/2` past each endpoint — roughly 20u of cells on the wrong side of a boundary at the widest hull. Raised as CONFIRMED by the cross-model Codex pass and, independently, as an inherited-and-acknowledged imprecision by the in-family reviewer; both readings are right, which is why it is ledgered rather than patched. NOT FIXED HERE because the two available fixes are the same two cycle 68 rejected — gate per cell (real per-tick cost on the anti-cheat path, now multiplied by segment count rather than hull count) or attenuate the disclosed mask per cell (a wire change) — and doing it for wake alone would leave the hull mask inconsistent with it. Whoever closes this should close BOTH channels in one cycle.
  evidence: `server/src/game/signals.ts` `wakeGate` takes a segment midpoint, `wakeMask` emits the full rect; `shared/src/sim/radarRaster.ts` `paintSegmentCoverage` square caps extend `widthU/2` beyond each endpoint. The cycle-68 hull-mask twin is the entry above dated 2026-08-08.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-12-radar-wakes.md`
  summary: `paintSegmentCoverage` IS THE COST FLOOR ON BOTH SIDES, AND A SINGLE CLIENT REBUILD IS A HITCH RISK. A worst-case in-bubble stamp rebuild (20 hulls plus 4 torpedoes, all in one sight bubble at full ahead, ~1,000 tiny segments) measured ~10.8 ms, up from ~2.1 ms before the review-gate patches added per-paint glint to the synthesized path. The 150ms rate floor amortizes it to about +0.15 ms/frame and the frame budget holds comfortably (worst 1.74 ms against a 2.5 ms bar), but the SPIKE lands on roughly one frame in 10-25 and is a stutter risk on low-end hardware. The cost is inside the shared pipeline itself — spine allocation, glint scan, bridge-to-fixed-point, crop — so it is a `shared/` optimisation and the implementing agent correctly declined to make it with `shared/` frozen. The same pipeline runs per segment on the SERVER's `wk` path with only a one-slot memo, so the next server tick-cost measurement should attribute against it too. Two probes were already tried and rejected on evidence: a per-revolution mask memo recovered ~9% (unexplained state for no gain, not kept) and an island shortlist for the new LOS clause recovered little but WAS kept because it stops that clause scaling with map island count.
  evidence: measured at the cycle-69 review gate on a generated map, 240 timed frames at 60fps, both zoom extremes; `shared/src/sim/radarRaster.ts` `paintSegmentCoverage`; `client/src/render/wake.ts` `WAKE_STAMP_MIN_MS`.

- source_spec: cycle 71 (wake length + tail fade, amendments 213-214)
  summary: A DRONE ENVELOPE OVERSHOOTS THE 3/8 WAKE RUNG BY 2%, AND THAT IS RECORDED RATHER THAN REPAIRED. Amendment 213 anchors the wake clock so the fastest PLAYABLE hull's full-ahead track is exactly `CONFIG.vision.detect` (247.5u at 45 u/s). One drone envelope — the retired destroyer's 46 u/s, preserved byte-for-byte by the shipClasses identity test — lays 253u instead. Eric ruled the cut from the cockpit of a torpedo boat, so the captain-facing number is the one that landed on the rung; the drone overshoot is bounded by its own assertion (`< detect × 1.05`) so it cannot grow silently. Whoever retunes drone kinematics should decide deliberately whether the anchor should become the fastest hull of ANY kind, which would move every captain's wake to keep a drone's on the rung — probably the wrong trade, but it should be a decision rather than a drift.
  evidence: `shared/src/__tests__/zone.test.ts` — the SHIP_CLASS_IDS pin and the HULL_IDS overshoot bound sit adjacent so the distinction is visible at the point of change.

- source_spec: cycle 71 (wake length + tail fade, amendments 213-214)
  summary: RECENCY NOW HAS TWO CHANNELS SPENDING THE SAME COIN, AND ONLY ONE OF THEM IS DERIVED. `wakeAgeFloor` (intensity) is solved out of the wake corridor and lands at ~0.968 — effectively inert close in, which is why amendment 214 added `wakeTailKeep` (cell count) beside it. The two now compose, but they are calibrated by different means: the intensity floor falls out of an inversion of the reach fit, while the keep floor is a chosen 0.35 bounded from below by chop's measured density. If a future cycle widens the wake corridor — a different coefficient, a different grain scale, a re-fit reference range — `wakeAgeFloor` will move on its own and the two channels' relative contribution will shift with no test noticing, because each is only bounded independently. Worth a pin asserting their combined near-range and far-range behaviour if the corridor is ever reopened.
  evidence: `client/src/config.ts` `HEAT_WAKE_AGE_FLOOR` (derived) vs `wakeTailKeep` (chosen, chop-bounded); `client/src/__tests__/radarHeatmap.test.ts` the tail-fray suite.

- source_spec: cycle 71 (wake length + tail fade, amendments 213-214)
  summary: THE ON-WATER FOAM SHORTENED WITH THE RADAR RIBBON AND WAS NOT SEPARATELY JUDGED. "One wake, one length" (amendment 209/210, Eric's own correction) means the shared `wakeLifeMs` drives BOTH renders, so cutting the radar track from 540u to 247.5u also cut the on-water foam by the same factor. That is structurally correct and was the explicit intent of promoting `life` to shared CONFIG — but the ORIGINAL complaint that produced the 12s figure was about the on-water wake being *"fucking tiny and hard to see"* at 1.1s, and 5.5s is now 5x that rather than 11x. The two renders cannot be tuned apart without reopening a ruling Eric made emphatically, so if the foam reads short again the answer is a new length for BOTH, not a fork.
  evidence: `shared/src/constants.ts` `wakeLifeMs`; `client/src/__tests__/wake.test.ts` asserts the client has no `life` knob to fork it with.

- source_spec: cycle 72 (the storm comes off the scope, amendment 215)
  summary: RE-ADDING ANY STORM RETURN MUST RE-DERIVE THE AREA QUESTION, NOT ASSUME THE WALL. Amendment 128 settled a real problem — the volume return falls off as 1/d^2, shallow enough to stay legible clear across the map, so an AREA return would own roughly half the scope late-match and bury every contact inside it against the epic's "information noise must never bury the hunt" guardrail. The fixed-thickness WALL was the compromise that answered it, and Eric was shown the full outside-the-ring region and "defer the storm entirely" at the time and rejected both. **He has now rejected the compromise too**, which means the wall no longer has standing as the default a future storm return would fall back to. Whoever revisits this should treat all three options as open and re-derive against the current scope contents (the scope gained wake and chop in cycle 70, so it is busier than it was when 128 was argued).
  evidence: `client/src/render/radarSources.ts` header carries the full removal note and what 128 settled; `VOLUME` in `client/src/render/radarFalloff.ts` survives unconsumed for exactly this eventuality.

- source_spec: cycle 72 (the storm comes off the scope, amendment 215)
  summary: `VOLUME` IS AN UNCONSUMED BRANCH OF THE RETURN MODEL AND ITS CURVE IS NOW UNEXERCISED BY ANY MATERIAL. The 1/d^2 geometry survives the storm's deletion on the argument that the point/surface/volume table is amendment 106's ratified model rather than a tuning knob. Its pure curve tests still run, so the exponent cannot silently change — but nothing rasterizes it any more, which means a future volume material inherits a branch that has not been exercised end to end through the march, the grain envelope and the band quantizer since cycle 71. The first material to use it should re-prove the three coefficient bounds at the shipped envelope rather than assuming they transfer, per amendment 135's standing rule that a bound proved at nominal is not proved.
  evidence: `client/src/render/radarFalloff.ts` `VOLUME` and its comment; `client/src/__tests__/radarFalloff.test.ts` covers the curve but no rasterized volume case remains.

## 2026-08-10 — Story 4-6 (The Bounty, cycle 73): three review edges recorded, not fixed

- source_spec: `_bmad-output/implementation-artifacts/spec-4-6-the-bounty.md`
  summary: WHETHER SINKING THE HOLDER PAYS THE BOUNTY CAN DEPEND ON ROOM JOIN ORDER WHEN TWO CAPTAINS DIE IN THE SAME TICK, AND NO RULING CHOSE THAT DISCRIMINATOR. `sinkShip` recomputes the throne per sink, and a burst that kills several ships resolves them in `this.ships` insertion order. If challenger C is TIED with holder A and one burst kills both A and captain B, then resolving B first lifts C to a strict overtake and transfers the throne off A *while A is still alive in the same tick* — A's own sink then reads `victimHeldBounty === false`, so no `bty` flag, no `— BOUNTY CLAIMED` line, and no `CONFIG.bounty.killLevels` bonus for whoever killed A. Resolving A first pays it. The spec's "evaluate once per sink, in sink order" blesses sequential resolution, but intra-tick sink order here is ships-map insertion order, i.e. who joined the room first. Rare (needs a tie plus a simultaneous second captain kill) and no invariant breaks — but it is an unruled discriminator on a payout, and the honest fixes (snapshot the holder for the whole tick, or resolve a tick's sinks in a defined order) are both design calls rather than patches.
  evidence: `server/src/game/world.ts` `sinkShip` (pre-sink `victimHeldBounty` read, then `recomputeBounty()` at the tail); `server/src/game/bounty.ts` `nextBountyHolder`; iteration order is `this.ships` (a Map) insertion order.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-6-the-bounty.md`
  status: PARTIAL — verified 2026-08-18. The OVERFLOW PRESSURE dropped: the `BOUNTY: <NAME>` label grammar was retired by the 2026-08-10 rework (epic-4 amendments 222-227) in favour of a bare `· ☠︎ <NAME>` skull mark, cutting the appended segment from ~25 characters to ~16 (`chromeBar.ts:263-264`, `:315`). The CLAMP ITSELF IS UNCHANGED: `chromeBarLayout` at `chromeBar.ts:343` is still `Math.max(0, screenW / 2 - width / 2)` — left edge only, no right-edge guard. Narrower row, same missing guard.
  summary: THE CHROME BAR IS ONLY GUARDED AGAINST THE LEFT SCREEN EDGE, AND THE BOUNTY REGISTER IS THE SEGMENT THAT NOW FALLS OFF THE RIGHT. `chromeBarLayout` clamps `x = max(0, screenW/2 - width/2)`, which protects the left edge only; the bounty register appends up to ~25 characters (` · BOUNTY: ` plus a 14-char ellipsized name) to a row that was previously fixed-width. On a narrow viewport the tail can clip past the right edge or run under the DOM kill feed (top-right, `z-index:900`), and the clipped segment is precisely the new optional one. The centering code is pre-existing; only the reachable widths are new, and nothing in the tests pins a max-width behaviour. Natural home is Story 4-8, which owns the readability gate and already has to arbitrate this row.
  evidence: `client/src/ui/chromeBar.ts` `chromeBarLayout` (the single-sided clamp) and `CHROME_BAR_SEGMENTS = 13`; `client/src/ui/killFeed.ts` stack position/z-index.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-6-the-bounty.md`
  summary: A KILLER WHO DISCONNECTS WHILE THE FATAL SHELL IS IN FLIGHT STILL PRINTS "BOUNTY CLAIMED" THOUGH THE PRICE WAS COLLECTED BY NOBODY. `sinkShip` emits `by` verbatim, then `creditKill` looks the killer up with `this.ships.get(by)` and silently returns when they are gone — no kill, no `captainKills`, no bonus. The client's `bountyPaid(e)` helper can only see `by`, so the feed renders `… SUNK BY UNKNOWN VESSEL — BOUNTY CLAIMED`. The self-sink half of this family WAS fixed this cycle (`by !== id`); this half is not fixable from wire data alone — it needs the server to qualify `by`, or to carry "was the price collected" as its own bit. Cosmetic, and the odd `SUNK BY UNKNOWN VESSEL` phrasing is pre-existing for every kill by a departed player, not new here.
  resolution: RESOLVED SAME DAY BY AMENDMENT 222, not by a fix — the defect was a property of the CLAIMED/LIFTED grammar, and that grammar is deleted. The feed no longer asserts anything about whether the price was collected: a leader sunk by a departed killer now reads `☠︎ VICTIM SUNK BY UNKNOWN VESSEL`, which is true. `bountyPaid` and `bountyKillSuffix` no longer exist. The residual `SUNK BY UNKNOWN VESSEL` phrasing remains, unchanged and pre-existing for every kill by a departed player of any rank — it is not a bounty concern. Note the SIBLING entry above (intra-tick sink ordering) is NOT resolved by the rework: `bty === 'v'` still drives the XP bonus, so the ordering question survives the grammar change intact.
  evidence: `server/src/game/world.ts` `creditKill` (`if (!killer) return;` after the event's `by` is already fixed); `client/src/net/roomBindings.ts` `bountyPaid`.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: THE LISTENING PASS WITH ERIC IS THE REMAINING HALF OF STORY 4-7 AND WAS DELIBERATELY NOT CLOSED — all six new timbres (`gunReport`, `impact`, `splash`, `sunkWitness`, `hpHurt`, `hpCritical`) ship stamped UNRATIFIED IMPLEMENTER DRAFT, alongside the `heal` tone and the fit-tier envelopes + per-category detunes that have been awaiting ratification since Story 2.9.
  evidence: Sound design IS game design — UX open question #8 says so in terms, and the house rule forbids inventing feel without Eric. The contrast arguments are written cue-by-cue in `client/src/audio/tones.ts` (why each cannot be confused with its register neighbours), so a listening session has an argued starting point rather than a blank sheet. Amendment 238. This is 20 minutes of Eric listening and saying "that one's wrong", not an agent task.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: THE `mz` OWN-FIRE SUPPRESSION BALL IS 124u — 37% OF TRUESIGHT — so an enemy gun firing at normal engagement range produces a flash and no report; and the suppression defers to a fire cue (`fireGun`/`fireCannon`) that does not always play.
  evidence: `nearOwnShip` uses `MAX_HULL_LEN` (the largest hull in the game, 124u) while an own muzzle spawns at most ~64u from hull centre, so the ball is roughly double what it needs to be. Two hulls trading fire at 110u each suppress the other's report. Separately, `handleShell`'s fire cue is gated on a live 400ms click latch, so under high RTT (or a second gun-family reveal inside the window, or a consumed star-shell latch) no fire tone plays AND `mz` is still suppressed — your own gun silent on both channels, strictly worse than pre-4.7. Two predicates guard one invariant and should be one. Not fixed because tightening the radius trades a real silence for a possible double-sound, which is the worse failure; amendment 235 records the burst half that WAS fixed.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: PAN IS NORMALISED BY LATERAL WORLD DISTANCE, NOT BY ON-SCREEN POSITION — so at high user zoom a flash at the screen edge and one two-thirds out pan identically, and while spectating everything in the outer ~44% of the screen pans at exactly the cap.
  evidence: `worldCue` divides by `reachU` (base radar, 660u), but the camera fits radar range to the SHORT axis and then applies user zoom 0.5-1.5 and a spectate zoom factor 0.5-1, a scale the pan never applies; camera look-ahead lead adds a further small offset between screen centre and the listening hull. The comment claiming "a world dx IS a screen dx" was corrected in-cycle to describe lateral world offset. NOT self-evidently a defect: zoom-independent panning means the sound field does not swing when the player zooms, which may be the better behaviour — it needs Eric's ear, not a unilateral fix.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: SPECTATE IS UNFOGGED, so a spectated late match degenerates into uniform distant chatter — every `mz`/`boom`/`burst` on a 4800u-wide ocean reaches the client, and the overwhelming majority land past the 660u falloff scale where gain pins at the floor and pan clamps at the cap.
  evidence: Spectator frames carry every gunnery event by design (`server/src/game/signals.ts` returns true for spectators), and three cue families at up to 3.3 Hz each of acoustically identical cues is the worst case. Not a leak — a spectator's perceivable set legitimately IS everything — purely a mix question nobody has ruled on. Natural home is Story 4-8, which owns the aggregate budget and cross-channel arbitration.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: THE GAIN FLOOR AND THE SPLASH VOLUME COMPOUND, so the FR16 bracket-and-walk cue is the first thing to become inaudible — `worldFloorGain` 0.25 x `splash.volume` 0.24 gives an effective 0.06 for a distant own miss, against `impact` at roughly twice that.
  evidence: The two knobs were authored independently (one in `CLIENT_CONFIG.audio`, one in the tone spec, which is itself asserted to be the quietest in the catalog) and nothing reconciles them, so hit/miss discrimination degrades with range in precisely the direction that damages bracket-and-walk. Both are feel numbers and belong in the same listening pass as the timbres — deliberately not retuned unattended.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: WITHIN ONE FRAME AND ONE CUE FAMILY, THE FIRST EVENT WINS RATHER THAN THE LOUDEST — a near, loud detonation can be refused by a far, near-inaudible one that happened to be earlier in the event array.
  evidence: `worldTone` calls `ToneFloor.request(t)` in event order, so ordering inside a frame decides which of two same-family events sounds. The fix (buffer per-family candidates for the frame, play the highest-gain one) is real but more than a patch, and the audible consequence is small because both cues carry the same information class. Recorded so a future audio pass does not rediscover it.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: THE HP-BAND STINGS ARE BOUNDED BY THE REUSED 300ms FLOOR BUT HAVE NO HYSTERESIS MARGIN, so DAMAGE CONTROL regen oscillating across a band under sustained fire can still re-fire the sting seconds apart.
  evidence: `repairHp` pays into hp every server tick while incoming fire subtracts, and `hpBandEdge` re-arms by construction (the property its tests celebrate is the same one that makes this unbounded). The 300ms floor bounds the pathological rapid case; a proper margin is a feel number that is Eric's to set, so the code comments in `hpSting.ts` and `main.ts` say so explicitly to stop a future agent inventing one. Amendment 236.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  status: REFRAMED, NOT RESOLVED — verified 2026-08-18. `state.net.you` is still never cleared on death (`roomBindings.ts:696` only ever assigns it). What changed is its STATUS: Story 5-3 deliberately BUILT ON this behaviour — every field of the results modal derives from `net.you` precisely because it survives death (epic-5 amendment 30) — so what this entry filed as a latent staleness bug is now a LOAD-BEARING INVARIANT. Anyone 'fixing' it by nulling `you` on death would break the results modal. Re-file as a documented invariant rather than a defect.
  summary: PRE-EXISTING — `state.net.you` IS NEVER CLEARED ON DEATH, so every consumer reading it during spectate sees the stale wreck pose, not just the audio ones this story fixed at their call sites.
  evidence: `handleFrame` assigns `net.you` only when `f.you` is present and never nulls it. Story 4-7 fixed the two audio call sites behind a `hasLiveOwnHull` helper with a deliberately tight blast radius, leaving `nearOwnShip`'s other caller untouched: an enemy torpedo surfacing near where you sank still plays the own-fire `fireTorp` whoosh while spectating, dressed in your own doctrine. Shipped behaviour that predates this story; fixing it properly means deciding what `net.you` MEANS after death, which touches the predictor, HUD and camera.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  status: ROUTED to the Story 7-5 doc-sync batch — verified 2026-08-18 as still-unfixed doc drift. See the consolidated 7-5 scope in `epic-6-retro-2026-08-18.md` (Significant Discovery item 3).
  summary: DOC DRIFT RECORDED, NOT FIXED (Eric-gated 7-5 batch) — three statements about audio are now further from the truth than when amendment 6 first ledgered them.
  evidence: EXPERIENCE.md:137 and the Story 4.7 AC in epics.md:188 still say "the 13 existing tones" (the catalog is now 31 tones plus the foghorn = 32 audible cues); the same text still calls the denied tone "deferred" (it shipped in Story 1.10); and UX-DR36 still names the LISTENING RING as the visual backstop for mono-audio users, which amendment 1 deferred out of existence — the backstop is now each cue's own on-screen mark. Also stale: the AC's first named new event is the listening-ring grammar, which left scope on 2026-08-04. House rule is no design-doc edits in-cycle.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: WHAT STORY 4-8 INHERITS FROM THIS CYCLE — eight new audible cues, of which several have ANIMATED visual twins, all of them now participants in the aggregate photosensitivity/attention budget that still has no mechanism.
  evidence: Amendment 224 states the law: every animated HUD channel answers to 4-8's tier arbitration and draws from the "no element or screen region exceeds 3 flashes/s in aggregate" floor. The twins for `gunReport`, `impact`, `splash` and `sunkWitness` are the muzzle flash, spark/pierce marks, splash marks and sink plume — all animated effects, and now all of them can be triggered in bulk during a firefight. The `hpHurt`/`hpCritical` twin is the low-HP rail pulse, which is a shipped TIER-1 channel. 4-8 should take its aggregate measurement AFTER this cycle, not against the pre-4.7 channel set.

- source_spec: `spec-4-7-the-real-time-sound-map.md`
  summary: THE TWIN WALK WAS PERFORMED (all 32 call sites that can sound a cue) AND ITS TWO LIVE DEFECTS WERE FIXED IN-CYCLE; four further findings are recorded here as documented-and-deliberate rather than fixed.
  evidence: The walk is the audit the ledger asked Story 4-7 to run, on the amendment-60 principle that a type-level table proves a cue HAS a twin, never that the twin can FIRE. Fixed in-cycle: the predicted ability-denial path sounding while the hotbar is hidden after death (the same defect shape as amendment 60, and the server-denial path already carried the guard it was missing), and the `denied` tone being unbounded while its pulse is capped at one trigger per 300ms. NOT fixed, all pre-existing and none of them silent-with-no-mark: (F3) `handleShell`/`handleTorp` gate their own-fire cues on bare `nearOwnShip`, so while spectating an enemy shell or fish revealing within 124u of where you sank plays YOUR own fire cue — misattribution, not silence, and it is the `net.you` entry above; (F4) a witnessed sinking whose contact snapshot has aged out groans with no crimson ring, which is the ratified protection direction anyway and keeps its kill-feed line; (F5) a foghorn row carrying neither position nor bearing sounds with no chevron, reachable only on a malformed payload and documented at the site as deliberate (a chevron at a defaulted bearing points confidently at the wrong horizon); (F6) `slowed`/`dazzled` fire alive-agnostically at the call site while their tells render only when alive — latent coupling that cannot diverge today because the server zeroes both marks on sinking, worth knowing if that ever changes.

## 2026-08-11 — Story 4-8 Attention Priority & the Readability Gate (cycle 75): ledgered consequences

- source_spec: `spec-4-8-attention-priority.md`
  status: VERIFIED STILL OPEN 2026-08-18 — `degradeAlphaFactor: 0.35` is unchanged at `client/src/config.ts:1785` and remains an unratified implementer draft. Carried since Epic 4; now THREE epics old and named in the epic-6 retro's standing Eric-ruling batch.
  summary: `CLIENT_CONFIG.flashBudget.degradeAlphaFactor` (0.35) IS AN IMPLEMENTER DRAFT AWAITING ERIC'S EYE. The MECHANISM is ratified (amendment 240 — coalesce, then degrade to a flat non-flashing mark) but this particular number is an orchestrator handwave: it decides how bright an over-budget flash draws as a flat mark, i.e. exactly how much of a muzzle flash survives a 20-hull scrum. It is stamped as draft in the config comment, following the convention Story 4-7 used for its unratified timbres (amendment 238) and Story 4-3 used for its effect geometry. A listening/looking pass with Eric should ratify or retune it against the staged worst-case capture rather than in the abstract.
  evidence: `client/src/config.ts` `flashBudget.degradeAlphaFactor`; the same standing draft-copy rule that governs the `SPECS` geometry and the `[PROPOSAL]` token hexes in the 4-3 entries above.

- source_spec: `spec-4-8-attention-priority.md`
  summary: THE REGION BUDGET IS SHARED ACROSS CHANNELS, AND THAT IS THE RULING WORKING AS WRITTEN — but its consequence deserves Eric's eye on the water. One burst hitting five hulls produces five sparks AND five hull flashes in one screen region: three animate, seven degrade, and the first arrivals win. That is precisely what "no element or screen region flashes more than 3x/s regardless of how many compliant events stack" demands — a per-channel budget would be the per-source floor we already had — but it means a busy region degrades quickly and which specific flashes keep their ramp is arrival-ordered rather than importance-ordered. If playtest shows an important mark losing its ramp to an unimportant one that arrived first, the fix shape is a priority within the region (threat marks claim ahead of juice), NOT a larger budget.
  evidence: `client/src/render/flashBudget.ts` `claim` is keyed on region alone; `client/src/render/effects.ts` `isBudgetedFlash` covers every one-shot except `wake`/`torpwake`; surfaced by the Wave 2a implementer as a deliberate consequence of amendment 240 rather than a defect.

- source_spec: `spec-4-8-attention-priority.md`
  summary: `torpwake` IS EXCLUDED FROM THE FLASH BUDGET, by implementer decision, documented at the site. It is the creeping mine's trail dot — a `wake` sibling laid on a cadence at 0.4 alpha, non-additive — and budgeting it would let a mine crawling through a region spend that region's three onsets on its own track, degrading the muzzle flashes and Hit Calls that land there next. Reversing it is a one-word change in `isBudgetedFlash`. Recorded because it is a judgement call about what counts as "a flash" that no amendment rules on.
  evidence: `client/src/render/effects.ts` `isBudgetedFlash`; `wake` is excluded on the same reasoning and was never a flash channel.

- source_spec: `spec-4-8-attention-priority.md`
  summary: DOC DRIFT FOR THE ERIC-GATED 7-5 DOC-SYNC BATCH, found while reading the attention contract end to end (amendment 243, none edited in-cycle). (a) The Listening Ring still reads as a ratified Tier-1 channel in EXPERIENCE.md:92, epics.md:187/906 and DESIGN.md:236 despite amendment 1's deferral. (b) `epics.md:162` (UX-DR19) still carries the Bounty Bloom unretired, though DESIGN.md:237 and EXPERIENCE.md:71/173 struck it under amendment 216 — and epics.md:880's "BOUNTY: <NAME>" feed line contradicts DESIGN.md:234's note that that label is retired. (c) The squint-test staging at epics.md:908 says "bounty active" where the copy ruling made it "kill leader". (d) No doc states what a Tier-2 channel does under another Tier-2, nor whether Tier 3 freezes under Tier 2 alone; cycle 75 implemented the ratified asymmetry LITERALLY (Tier 3 freezes under any higher tier including Tier 2 alone; a Tier-2 channel does not hold under another Tier 2). (e) Amendments 65/71's unqualified "amber is RESERVED and UNASSIGNED" language is scoped to radar sensor provenance but reads as though it vacates amber's live HUD chrome role at DESIGN.md:139; cycle 75 read the HUD role as untouched.
  evidence: The full quoted set with line numbers is in `bmad-dev-auto-result-4-8-attention-priority-questions.md` and amendment 243.

- source_spec: `spec-4-8-attention-priority.md`
  summary: TWO OF THE THREE NAMED TIER-3 CHANNELS HAVE NOTHING TO FREEZE, verified rather than assumed. EXPERIENCE.md's Tier-3 row lists "bank-chip breathing, toasts, XP-rail wrap"; only the bank chip actually animates. `ui/upgradeToast.ts` is a one-way DOM `transition: opacity` fade on its TTL — there is no keyframe to hold, and "freezing" it would mean pinning a toast on screen. The XP rail's fill alpha is assigned once in the constructor and never animates at all. No animation was invented so that it could be frozen. If a future cycle gives either an animated form, it inherits a Tier-3 obligation automatically.
  evidence: `client/src/ui/upgradeToast.ts` (CSS transition, `LINE_TTL_MS`); `client/src/render/xpRail.ts` (`railFill.alpha` set in the constructor); Wave 2b implementer report, cycle 75.

- source_spec: `spec-4-8-attention-priority.md`
  summary: THE DENIED HOTBAR CHIP IS RE-DERIVED EACH FRAME FROM THE CURRENT `primedSlot` RATHER THAN LATCHED TO THE SLOT THAT OWNED THE ONSET — pre-existing Story 1.10 behaviour, deliberately mirrored by cycle 75's new degraded flag rather than diverged from. `hotbarDenied` returns `flash || (isWeapon && slot === status.primedSlot && g.deniedFlash)`; if the player re-primes within the 80ms pulse, the denied mark (and now its degraded form) follows the NEW primed slot instead of staying on the one that was denied. Found by the cross-model Codex reviewer at the 4-8 gate and adjudicated DEFER: the line is byte-identical at `5a6951f~1` (verified by diff), so fixing it means changing shipped denied-feedback behaviour, which 4-8's Never-list forbids ("no retuning of any shipped channel"). `hotbarDeniedDegraded` mirrors the same expression on purpose, so the flag can never describe a different pulse than the flash beside it — latching one without the other would be strictly worse. If a future cycle latches the denial to its onset slot, both must move together.
  evidence: `client/src/main.ts` `hotbarDenied` / `hotbarDeniedDegraded`; `git show 5a6951f~1:client/src/main.ts` confirms the predicate pre-dates Story 4-8.

- source_spec: `spec-4-8-attention-priority.md`
  summary: AMENDMENT 240'S OWN JUSTIFICATION DOES NOT HOLD FOR ITS BIGGEST STACKERS, and this is the sharpest open question the cycle leaves. The ruling argues a degraded flash is safe because it renders at "their already-ratified `motion: 'off'` keyframe — a state the game already ships and every player can already select." That is TRUE for the chrome channels: the foghorn chevron's degraded form is exactly `chevronPop` at intensity 0, and the hotbar slot's is provably `slotSkin(state, 0)`. It is FALSE for `muzzle`, `spark`, `splash`, `burst`, `pierce` and the hull hit flash — those are deliberately NOT motion-gated (Story 4.3 promoted `muzzle`/`spark` out of `isJuiceEffect` as sensor readings), so their motion:'off' form is the FULL ramp, and flat-`degradeAlphaFactor` is a NOVEL, fainter presentation no player could previously select. Nothing is hidden and nothing is lost — presence, position and the discriminating geometry all survive, and both Fable reviewers cleared the information law — but whether a 4th-arriving muzzle flash at ~0.315 alpha for 120ms is CATCHABLE under exactly the load it matters is unproven by argument and must be settled by eye. The readability-gate capture should include a close-up of a degraded `muzzle` and `spark` so Eric rules on `degradeAlphaFactor` with the evidence in front of him rather than in the abstract.
  evidence: Fable information-lens reviewer F2 at the cycle-75 review gate, traced against `client/src/render/effects.ts` `oneShotAlpha`/`isJuiceEffect` and `client/src/render/foghorn.ts`/`hotbar.ts` degraded forms; amendment 240's text; the `degradeAlphaFactor` draft stamp entry above.

- source_spec: `spec-4-8-attention-priority.md`
  summary: THE ON-WATER DENIED ARC IS BUDGET-*COUNTED*, NOT BUDGET-*BOUND*, and its own per-source floor sits marginally above the aggregate one. `DeniedPulse`'s `PULSE_RATE_MS` is 300ms, which permits ~3.33 onsets/s against the ratified 3/s ceiling — pre-existing since Story 1.10 and unchanged here. The arc claims budget (so it counts toward its element's window) but has no degraded form of its own, because `render/firing.ts` already draws the denial as a flat recolor with no luminance ramp to spend; the verdict is therefore surfaced on the primed slot's hotbar chip instead. Recorded so the channel ledger is not read as claiming the arc can be degraded. Closing the 3.33-vs-3.0 gap would mean retuning a shipped floor, which this story's Never-list forbids.
  evidence: `client/src/render/deniedFire.ts` `PULSE_RATE_MS`; `client/src/render/firing.ts` (flat denial recolor, never motion-gated); Fable information-lens reviewer F4 at the cycle-75 review gate.

- source_spec: `spec-4-8-attention-priority.md`
  summary: THE READABILITY GATE PROVES EVERYTHING EXCEPT ITS OWN HEADLINE CLAIM, and that is recorded rather than papered over. The AC asks a squint test to confirm *"threat channels read first."* That claim is TEMPORAL — the tier system asserts Tier 1 keeps ANIMATING while Tier 2 holds at its lit keyframe and Tier 3 freezes at its dim one — and a still image cannot show motion-versus-hold. What the captures do establish, under a stack asking one screen region for ~93x the ratified flash budget: every channel is present, legible and un-deleted; no bearing is dropped; no mark becomes confusable with another. What they cannot establish is the ordering of attention over time. In the frame the crimson low-HP rail is a ~6px sliver at the right edge while the storm vignette floods the whole screen and the near radar returns are the loudest thing by area. Demonstrating rather than arguing this needs a VIDEO capture or a per-channel alpha trace over time; the arbitration itself is pinned by unit tests either way. Home: a later pass, naturally bundled with Epic 7's real-hardware reference-device work.
  evidence: `readability-gate-2026-08-11.md` Part 5 and the three PNGs in `readability-gate/`; the tier holds are unit-pinned in `client/src/__tests__/attention.test.ts` and the per-consumer suites.

- source_spec: `spec-4-8-attention-priority.md`
  summary: AT 1.5x ZOOM THE RADAR RETURN CELLS OVERLAP THE TOP-CENTER BR CHROME BAR — `T+02:57` renders on top of a blue/green return blob in the staged capture. A real legibility collision between world paint and HUD chrome. **Not introduced by Story 4-8**: it is the `return` grammar's world-anchored cell heatmap (cycles 62-72) meeting the Story 3.3 chrome bar, and it is visible in any busy scope at close zoom. Recorded because the readability gate is exactly where such a collision should be caught, and because it is the sort of thing that reads as a 4-8 regression to anyone who sees it first in these captures. Fix shapes if Eric wants one: a backing scrim behind the bar's row, or excluding the bar's band from the return layer's draw. Needs a design ruling, not a patch.
  evidence: `readability-gate/worstcase-zoom-1.5x-close.png`; `client/src/render/radarHeatmap.ts` (world-anchored cells) vs `client/src/render/hud.ts`'s chrome-bar row; observed at the cycle-75 readability gate.

## 2026-08-11 — Epic 4 retrospective (unattended; significant-discovery alert + 3 new standing agreements)

- source_spec: `_bmad-output/implementation-artifacts/epic-4-retro-2026-08-11.md`
  summary: SIGNIFICANT DISCOVERY — STORY 5.4 (FOG BANKS) WAS SPECIFIED AGAINST A RADAR THAT NO LONGER EXISTS, and must be re-derived before it is specified. Its AC reads *"inside one, my truesight radius shrinks and I vanish from others' truesight — while radar may still paint me"*, written when radar was a binary LOS-gated blip. The shipped radar is a physical return model (reflectivity x geometry falloff, height-aware terrain shadows via a shared accumulator, server-rasterized hulls, wakes with their own clutter corridor, a display-time near-range opacity mask). Three unanswered consequences: (1) the return model has NO fog term and amendment 106's coefficient table is an explicit handwave a new material must extend — and amendment 108 already ruled that *"fog and rain are their own epic-scale feature"*, which is a direct warning about this story's size; (2) *"radar may still paint me"* is now a statement about a CURVE, not a boolean — attenuate, leave untouched, or add clutter are three different features with three different anti-cheat surfaces, and the `wk` row (which paints WATER, not hulls) must answer separately; (3) cycle 72 is the governing precedent for AREA returns — a full outside-the-ring storm return was rejected because a 1/d^2 area material *"would own half the scope late-match and bury every contact in it"*, and a fog bank is an area material. Treat 5.4's radar interaction as a design-with-Eric gate at spec time. THIS DOES NOT BLOCK EPIC 5 — 5.1, 5.2, 5.3 and 5.6 are unaffected, so the mitigation is ORDERING, not delay.
  evidence: `epics.md:1043-1056` (5.4 AC); amendments 106, 108, 127-137 (the return model); the cycle-72 spec (area-return rejection); epic-4 retro "Significant Discovery" section.

- source_spec: `_bmad-output/implementation-artifacts/epic-4-retro-2026-08-11.md`
  summary: TWO SMALLER EPIC-5 WATCH ITEMS FROM THE SAME REVIEW. (a) Story 5.1's STEP_ORDER refactor is BIGGER than when it was written — `world.step()` gained wake rasterization, radar-shadow-gated blip production and the per-segment wake gate during Epic 4, so "pure refactor, tests green" is a much larger claim against 4080 tests than against the 2614 it was written for; it deserves review attention despite being the least glamorous story in the epic. (b) Story 5.2's sinking window meets amendment 205's WAKE-OUTLIVES-ITS-SHIP rule: a hull decelerating over ~5s still lays wake and that wake persists past `sunk`. Probably good, but it is a new disclosure surface for a ship that is still a valid target and still win-eligible, so it needs an explicit line in the 5.2 spec rather than being discovered at its review gate. (c) Story 5.5's whirlpools carry and ROTATE hulls; wake is server-owned world state derived from pose, so a curved wake should fall out for free — but TORPEDO wake under a current has never been exercised.
  evidence: `epics.md:996-1010` (5.1), `:1011-1026` (5.2), `:1058-1073` (5.5); amendment 205; epic-4 retro "Next Epic Preview".

- source_spec: `_bmad-output/implementation-artifacts/epic-4-retro-2026-08-11.md`
  status: BEING ACTED ON 2026-08-18 — this is the meta-entry, and it is the direct ancestor of epic-6 retro action item #6. Its diagnosis was right and has worsened: 178 entries then, 250 now, and no triage since 2026-07-18 (THREE epics). The epic-6 retro ran the mechanical half of the triage this entry asked for — cross-referencing every entry whose named home has shipped against the actual code — and found the root cause it did not name: resolution is recorded in the RESOLVING story's spec and amendments, and almost never written back onto the entry, so resolved items are re-counted as open forever. The file also carries THREE different annotation styles (`status:` key, inline `[RESOLVED …]`, and separate narrative sections), which is why every previous count disagreed.
  summary: THE LEDGER ITSELF NEEDS A TRIAGE PASS — 178 entries, 8 explicitly resolved, and no triage has run since the 2026-07-18 deferred-work cycle. Story 4-7 alone deferred 11 findings at a single gate. Individually every defer is justified and evidenced; collectively the file has stopped being a queue and become an archive, which means a genuinely urgent entry is now indistinguishable from a two-epic-old one. The 2026-07-18 cycle is the reference implementation: partition every open entry into resolved-by-later-work / still-open / needs-Eric, and record the disposition without editing the original entries. Natural home: an interstitial chore cycle before Epic 5 closes.
  evidence: `grep -c source_spec:` = 178 vs `grep -c "status: RESOLVED"` = 8; `spec-deferred-work-cycle.md` (the 2026-07-18 reference pass); epic-4 retro Challenge 8.

- source_spec: `_bmad-output/implementation-artifacts/epic-4-retro-2026-08-11.md`
  summary: THREE NEW STANDING TEAM AGREEMENTS, each earned by a specific epic-4 failure and each cheap to check at a review gate. (1) A BOUND IS A CLAIM, NOT A RULING — a numeric constraint stated alongside an Eric ruling carries the SPEC AUTHOR's authority, not Eric's, and is independently reviewable (amendment 133; Story 4-10's clutter bound was faithfully implemented and lit ZERO pixels). (2) ASSERT BOUNDS AGAINST THE SHIPPED PATH, NEVER A CLEAN FIXTURE — every 4-10 bound assertion ran the noise-OFF `CLEAN` fixture, which is why two coefficient defects survived a fully green 1,868-test suite; recorded at the time as *"the cycles 54/55/57 shape again"*, i.e. it recurred at least three times before it was named (amendment 135). (3) EVERY PERCEPTION ORACLE IS CHECKED FOR SHRINK-DETECTION, NOT ONLY GROWTH — the independently-reimplemented `mz`/`sm` oracle asserted `dist <= SIGHT * 1.5` and kept passing VACUOUSLY once the halo moved to `SIGHT * 1.25`; an upper-bound-only assertion cannot detect a channel getting smaller (amendment 125), and the 4-4 sibling is a fuzz suite that ran its new `sm` oracle ZERO times until a hull was seeded wounded.
  evidence: amendments 125, 133, 135; epic-4 retro "Team agreements" and Key Insight 5.

- source_spec: `_bmad-output/implementation-artifacts/epic-4-retro-2026-08-11.md`
  summary: PREFER "SHIP IT BEHIND A FLAG AND LOOK" OVER "CHOOSE FROM FOUR WRITTEN DESCRIPTIONS" FOR ANY QUESTION ABOUT A VISUAL — three author-reversals this epic, all on visual rulings made before seeing the thing. Amendment 180 (grey NO-DATA) was chosen by Eric from four options at a question gate *before seeing any of them on the water* and was REVERSED BY ITS OWN AUTHOR on sight; amendment 110 (no torpedo wakes) was likewise reversed by its owner; amendment 102's *"closer to a low island = longer shadow"* was found BACKWARDS under its own formula. All three were caught, and none was cheap. Question gates that ask about RULES have a materially lower error rate than ones that ask about VISUALS. Directly relevant to Epic 5, which carries THREE named design-with-Eric visual gates (5.3 reveal-zoom motion exemption, 5.4 fog rendering, 5.5 whirlpool feel treatment).
  evidence: amendments 102/186, 110/199, 180/194; epic-4 retro Challenge 5.

- source_spec: `_bmad-output/implementation-artifacts/epic-4-retro-2026-08-11.md`
  summary: A LEDGER ENTRY ASSERTING A DOC IS WRONG GETS RE-DERIVED BEFORE IT IS ACTIONED. The 25%-vs-50% HP threshold sat in this file as documentation drift from Story 3-2 and was carried an entire epic — and turned out to be a CATEGORY ERROR, not a typo: both documents were right and describing different things (EXPERIENCE.md's `<25%` is the CRIMSON threat band; the shipped rail's 50% is the AMBER warning band). Had it been "fixed" as drift at any point, the amber corollary would have become unimplementable (a 25-50% rail would permanently outrank the final-10s ring) and Tier 1 would have sat jammed on through the back half of every fight — an arbitration whose whole purpose is keeping the climax readable, stuck in its climax state. Closed as a misdiagnosis by amendment 239, not fixed.
  evidence: amendment 239; the former `deferred-work.md:323` drift entry; epic-4 retro Challenge 6.

- source_spec: `_bmad-output/implementation-artifacts/epic-4-retro-2026-08-11.md`
  status: SUPERSEDED 2026-08-18 by the epic-6 retro — the human-playtest checkpoint is now carried a FOURTH epic and has grown again. The current consolidated list lives in epic-6 retro action item #1 (which adds epic-6's min-2 queue path, the 2:00/0:10 rhythm, the frozen start line, the departure scuttle and the two-human sinking-window duel). Track it there; this entry is the origin record. The underlying cause is now diagnosed rather than merely restated: every solo session Eric runs finds real defects (four epic-6 cycles exist because of them), but no solo session can exercise anything that needs two humans.
  summary: THE HUMAN-PLAYTEST CHECKPOINT IS NOW CARRIED A SECOND EPIC, AND IT GREW. Epic 3 filed BB 50% instrument endgame share + terminal-ring island pockets with "full-roster matches that reach 12:00 closure" as the named checkpoint; no multiplayer session happened during Epic 4, so both stay open unchanged. Epic 4 adds three claims of its own that are specifically about what a HUMAN EYE picks out under load and therefore carry single-observer evidence: wake legibility under clutter (amendment 203's *"camouflage is by structure, not strength"* — measured as lit-fraction contrast, never tested against a second pair of eyes); whether a 4th-arriving muzzle flash at ~0.315 alpha is CATCHABLE (`degradeAlphaFactor`); and the readability gate's own headline claim that *"threat channels read first"*, which is TEMPORAL and which a still image structurally cannot prove. The next full-roster session should carry this combined list rather than re-deriving it.
  evidence: epic-3 retro ledger sweep (unchanged entries); amendments 203, 240; `readability-gate-2026-08-11.md` Part 5; epic-4 retro "Acceptance mode" section.

## 2026-08-11 — Story 5-1 (cycle 76; lifecycle state machine + STEP_ORDER registry)

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-lifecycle-state-machine-step-order-registry.md`
  status: RESOLVED by Story 6-5 (epic-6 amendment 29) — verified 2026-08-18, and the debt was MIS-FRAMED rather than unpaid. `isParticipant` is `role !== 'fleet'`, so AI captains count in `afloatCaptains()`; a 1 human + 19 bot roster never latches early and last-participant-afloat just works (a bot may legitimately win, and the results modal names it). The genuinely undefined case — one human among PvE fleet hulls ONLY — is untouched, still pinned by `drones.test.ts:911` and still refused by `queue.ts`.
  summary: STORY 6-5 (SOLO VS AI) NOW OWES A TERMINATION RULE, created by Eric's 2026-08-11 drone-gate ruling (epic-5 amendment 4). With the win check counting captains only, drones can never win AND a human can no longer lose to them by attrition — so a solo-human-versus-drones match has no defined end. Production is unaffected today (`CONFIG.match.minHumans` is 2, so a live match cannot start solo), which is exactly why the ruling was safe to take in 5-1; the hazard lands the moment 6-5 introduces a real solo mode. Story 6-3 retains the rest of its scope (formal multi-mode participants-only coverage). Note this REVERSES the direction of epic-4 amendment 31, which deferred the gate removal TO 6-3 on solo-termination grounds: the removal happened first and the termination question moved to 6-5.
  evidence: `match.ts` `checkWin` (the LEFT OPEN block records it at the site); epic-5 amendment 4; epic-4 amendment 31 (the superseded defer); `CONFIG.match.minHumans = 2`.
  resolution: RESOLVED 2026-08-17 (cycle 97, Story 6-5; epic-6 amendment 29) — and the debt turned out to be already discharged rather than needing payment. This entry conflates "solo MATCH" with "solo HUMAN". Story 6-5's roster is 1 human + 19 AI captains, and `isParticipant` is `role !== 'fleet'`, so bots count in `afloatCaptains()`; `latchOutcome()` returns early while `captains.length > 1`, the match runs normally, and `latchedWinner` MAY BE A BOT (the results modal names it). The sinking hold generalises for free via `holdsForSinkingCaptain`'s `isParticipant` test. Amendment 13's participant seam — built ahead of its consumer over the orchestrator's objection — is what paid this off. STILL OPEN and deliberately untouched: 1 human + ONLY PvE fleet hulls has no defined end, is pinned as current correct behaviour by `drones.test.ts:911`, and is still refused by `queue.ts`. 6-5 did not open that case.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-lifecycle-state-machine-step-order-registry.md`
  summary: `endedBy: 'lastHumanLeft'` IS NOW EFFECTIVELY UNREACHABLE THROUGH THE ROOM, and nobody decided that — it fell out of the drone-gate removal. Any event leaving one afloat captain now finishes immediately, so a DEPARTURE can only be terminal if it lands between a sink and the next win check, which `ArenaRoom` makes impossible (it calls `world.step()` and `match.update()` synchronously). The telemetry enum value, its classification branch and its unit test all still exist and are green — the unit test drives sink-then-leave with no tick between — but no real socket session can now produce it, and `metricsSmoke`'s real finish moved from `lastHumanLeft` to `fieldCleared`. Either the enum value should be retired or the classification revisited; it is currently a telemetry category that can only be reached synthetically.
  evidence: `match.ts` `classifyEnd`; `matchTelemetry.test.ts` (`lastHumanLeft` case, now synthetic); `metricsSmoke.mjs` observed `"endedBy":"fieldCleared"`.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-lifecycle-state-machine-step-order-registry.md`
  summary: BATCH-SIM `--captains 1` RUNS ARE NOW DEGENERATE — the lone captain wins on the activation tick, so the sample carries no combat at all and any economy/balance evidence gathered at that setting is worthless rather than merely thin. Recorded in `runner.ts` beside the existing minHumans caveat, and the two `lastHumanSunk` batchSim tests moved to `captains: 2`. Anyone reaching for the batch-sim harness for balance evidence must use >= 2 captains; a future guard that refuses `--captains 1` outright would be cheaper than re-discovering this from a flat dataset.
  evidence: `server/scripts/batchsim/runner.ts` (comment at the captains option); `batchSim.test.ts` `lastHumanSunk` cases; epic-5 amendment 4.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-lifecycle-state-machine-step-order-registry.md`
  summary: HALF THE `matchSmoke` FLAKINESS LEDGER ENTRY IS RETIRED FOR FREE. The 2026-07-17 entry names two flake modes; one of them — *"the storm's r=90 endgame pocket letting a drone camp past the results timeout (step 5)"* — no longer exists, because B's sink IS the finish under the captain-only win check, so step 5's storm mop-up was deleted end to end. Runtime dropped from a ~12-minute worst case to ~4 minutes and the smoke passed first attempt with no flake. Torpedo-connect luck in steps 2/4 REMAINS open and unchanged.
  evidence: `deferred-work.md` 2026-07-17 matchSmoke entry; `matchSmoke.mjs` (step-5 removal); observed `MATCH SMOKE OK (first attempt, no flake)`.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-lifecycle-state-machine-step-order-registry.md`
  summary: RESOLVED SAME CYCLE by Eric's amendment-9 ruling (captain-relative placements make the provisional and final numbers agree by construction, with no client change) — retained for the record. A PRE-EXISTING PLACEMENT QUIRK WAS BRIEFLY EASIER TO HIT, THOUGH IT WAS NOT INTRODUCED HERE. `client/src/score.ts` derives an eliminated captain's PROVISIONAL placement counting human rivals only, so it snaps from e.g. "#2" to a drone-inclusive "#N" when the real results land. Identical on main — but survivors now hold real placements in a dense 1..N range including drones, so the gap between the provisional and final number is wider and more visible in a drone-heavy match. Not a defect in this cycle's work; flagged because Story 5-3 (Omniscient Reveal & Results) is the story that owns this screen and should decide whether the provisional number should count drones.
  evidence: `client/src/score.ts` provisional placement derivation; `match.ts` `computePlacements` survivors tier; identified during the Story 5-1 review gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-the-sinking-window.md`
  summary: NEEDS AN ERIC RULING — WOUNDED SMOKE STOPS AT SINK-ENTRY, WHICH IS A SINKING TELL BY OMISSION. `tickSmoke` stays `isAfloat`-gated and was deliberately NOT one of amendment 15's three re-opened seams, so a critically-wounded hull emits its last `sm` puff at sink-entry and then sails on clean for five seconds while still under way and still shooting. An observer watching a hull smoke and then abruptly go clean gets a clean read on "that hull is in its window". Widening `tickSmoke` would make a FOURTH seam and would change an enemy-facing channel, which is a design call and not the implementer's. Note the information is in any case already free by composition (amendment 19), so this is about whether a sinking hull SHOULD pour smoke — dramatically it probably should — not about plugging a leak.
  evidence: `server/src/game/world.ts` `tickSmoke` (`isAfloat` gate, unchanged); epic-5 amendments 15 and 19; raised by the Story 5-2 edge-case review gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-the-sinking-window.md`
  summary: TWO ORCHESTRATOR RULINGS FROM THIS CYCLE ARE AWAITING ERIC'S CONFIRM OR VETO, both recorded with that status in the amendments file. (a) Amendment 17: the match is NOT held open for a sinking hull, so `checkWin` fires at sink-entry and the last captain to die in a match gets a TRUNCATED window — the only hull that ever does. The question gate signalled the opposite lean before the architecture was built; overruling it costs a `finishing` sub-state in the match machine that Story 5-3 would then work around. (b) Amendment 18's ledgered half: the death GROAN still sounds at sink-entry at the entry position while its crimson plume now draws at founder, so cue and plume are ~5s and up to 110u apart. Defensible (the groan marks the killing blow, the plume marks the sinking) but never seen on the water; if it reads badly the CUE is the thing to move, not the plume.
  evidence: epic-5 amendments 17 and 18; `server/src/game/match.ts` `checkWin`; `client/src/net/roomBindings.ts` `handleSunk` cue-vs-`queueWreck` split.
  resolution: HALF-RESOLVED 2026-08-12 (cycle 78). Item (a), amendment 17, was VETOED by Eric on a 1v1 playtest and is reversed by amendment 20 — the match is now held open for a sinking captain. Item (b), the groan-vs-plume separation, REMAINS OPEN and is now easier to judge, since a 1v1 loser's full window is finally visible.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-the-sinking-window.md`
  summary: THE `matchSmoke` HEADLESS SMOKE IS FLAKY ON CURRENT BALANCE, PRE-EXISTING AND UNRELATED TO THE SINKING WINDOW — it took four runs to pass during cycle 78. Two distinct failures: step 2 (`no torpedo struck B during the ready room`) twice, and step 5 (`A damageDealt=70 < 125`) once. Both are choreography failures in phases the sinking window cannot touch, and the sim path was proven correct by a direct World repro (ready-room torpedo produces a `boom` with `hit:'b'` and zero hp lost, exactly as `damageEnabled` requires). Instrumentation showed A pinned orbiting at ~165u with island-blocked LOS eating the fight clock, after which the storm chipped B to 55 hp so A's single 70-damage fish both killed B and failed the >=125 assertion. The smoke's own comments about "ready-room drone traffic" are also stale — drones fill only at activation. Worth its own fix cycle; until then, treat a single matchSmoke failure as inconclusive rather than as a regression signal.
  evidence: four consecutive cycle-78 runs with three distinct failure modes; `server/scripts/matchSmoke.mjs` steps 2 and 5; direct World repro of the ready-room torpedo path.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-the-sinking-window.md`
  summary: WANTS ERIC'S EYE ON THE WATER — FOUR SIDE EFFECTS OF LIFTING HULLS ABOVE THE CHART LAYER (amendment 22). He ordered the ordering, not each of these: (1) the storm-side fill and the in-zone wash NO LONGER TINT HULLS, so a ship inside the storm loses that read — the most likely of the four to be unwanted; (2) wounded-smoke plumes now draw UNDER hulls; (3) lit-zone glow and own mine/decoy chart marks now draw under hulls; (4) the charted island and contour linework now draws under hulls. All four read as "the hull is more visible", which is the ask, and none can be individually reverted without re-splitting the ship layer. Also unverified by human eyes: the re-anchored radar mask across the whole sight bubble, and whether the hull-alpha feather at the sight boundary still feels like the fog composite it replaced.
  evidence: `client/src/render/stage.ts` CHART_LAYER_ORDER (ship between blip and aim); epic-5 amendment 22; flagged by the implementing agent, not caught by any test.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-the-sinking-window.md`
  summary: THIS CYCLE SHIPPED WITH NO LIVE OR VISUAL VERIFICATION — everything is code plus 4189 unit tests. Three things specifically want one human look before they are trusted: the `GOING DOWN WITH THE SHIP!` banner's timing and legibility, the hotbar/firing arc surviving the window while the hull visibly slows, and the camera holding on the own hull instead of cutting to the killer. The audio seam in particular had NO test coverage at all before this cycle, which is exactly why a double-sounding shot on every shot of the beat survived implementation and was caught only at the review gate.
  evidence: Story 5-2 client wave report ("No visual sanity check"); review finding P1 (`hasLiveOwnHull`), which the full suite was green against.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-the-sinking-window.md`
  summary: TWO SMALL TEST-HYGIENE ITEMS, NEITHER PRODUCTION-REACHABLE. (a) Ready-room respawn is now effectively `max(respawnDelay, sinkingWindowMs)` = 5000ms rather than 3000ms, because `processRespawns` gates on `isSunk` and a hull cannot be sunk until founder — so `respawnDelay` is unobservable and the `SUNK — RESPAWNING IN Ns` placard has no frames left to draw in. Unreachable in a real match (`damageEnabled` is active-only and `respawnEnabled` is ready-room-only, mutually exclusive), so it bites tests and standalone Worlds only; the honest fix is to arm the client ETA off `founderDeadline` or put `respawnAt` on the wire. (b) About a dozen existing server tests were retrofitted to cross the window with a single `w.step(5000)` call, which runs ONE tick at `dtMs = 5000` — so motion, reloads, mines, storm, smoke and wake each integrate a five-second step and the post-founder states those tests pin are no longer reached by the path production takes. Assertions hold; the fidelity does not.
  evidence: `server/src/game/world.ts` `processRespawns` (`isSunk` gate) and its own "production-unreachable" comment; `match.ts:418-419`; the `w.step(CONFIG.ship.sinkingWindowMs)` retrofits in `denials`/`foghorn`/`smoke`/`spectator`/`xp`/`decoy`/`equipment`/`cannon`/`starShells`/`combat`/`world`/`perception`/`upgrades` tests.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-omniscient-reveal-results.md`
  summary: NEEDS AN ERIC DECISION HE DEFERRED IN THE SAME BREATH — THE BOONS + LAST-OFFER BLOCKS. Asked whether the results modal should review the build he died with, Eric said *"I don't know if I care about what boons I have selected at this point, i'll need to think on that."* Both blocks are ratified in UX-DR27 and drawn in mockup F3, and both cost ZERO wire (`net.you` is never cleared on death, so `you.boons`/`you.offer` are still in hand), so they were BUILT rather than withheld — per the standing "ship it behind a flag and look" agreement. Each is one self-contained `HTMLElement | null` render function with one append line, so cutting either is a two-line deletion and nothing else reads them. This is an open OWNER decision, not a defect: look at the modal and keep or cut.
  evidence: epic-5 amendment 28; `client/src/ui/results.ts` `makeBoons`/`makeOffer` and the cut-ability comment above them.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-omniscient-reveal-results.md`
  summary: THE REVEAL FRAMING POPS WHEN YOU TAKE MANUAL CAMERA CONTROL, AND THIS IS ACCEPTED RATHER THAN SOLVED. The reveal frames the whole map at ~0.26x, which is BELOW the ratified spectate clamp floor of 0.5x — the clamp was deliberately left untouched (epic-2 amendment 8 governs the manual wheel, and lowering it would hand every spectator a permanent whole-map view, a different feature). So the reveal factor is exempt while it is live, and the first manual wheel or pan releases it back into [0.5, 1.0] — a visible jump. Eric anticipated this when he ruled it (*"we might revisit the clamp later"*), so it is ledgered rather than engineered around. Revisiting the clamp is the fix if it reads badly.
  evidence: epic-5 amendment 25; `client/src/render/camera.ts` `setZoomFactor`/`pan` (which clear the reveal target themselves so the wiring cannot forget it); `SPECTATE_ZOOM_MIN` unchanged at 0.5.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-omniscient-reveal-results.md`
  summary: THIS CYCLE SHIPPED WITH NO LIVE OR VISUAL VERIFICATION — the second epic-5 cycle in a row to do so, and this one is ENTIRELY presentation, so the gap bites harder here than it did for 5-2. Everything is code plus 4305 unit tests. Specifically wanting one human look: whether the whole-map framing actually reads as a reveal or just as "very far away" (hull scale drops to roughly 0.26x, and the mockup's own legend flags that all-hull nameplates may read as CLUTTER at that zoom, with *"killer + wreck only"* recorded as its fallback); whether the 0.62 dim leaves the reveal legible behind the modal without hurting the modal's own contrast; whether the zoom SNAP at `motion: off` reads as broken rather than as instant; and whether the modal still fits its container now that it carries a MATCH LOG plus two build blocks. The map chart also draws island coastlines and contour bands across the FULL disc at once for the first time ever — no frame-budget measurement was taken at that framing.
  evidence: no browser session this cycle; mockup `death-reveal-results-1.html` F2 legend #7 (the nameplate-clutter fallback); `client/src/__tests__/containerFit.test.ts` covers height but not the new blocks' worst case.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-3-omniscient-reveal-results.md`
  summary: THE STAGE'S TOP-LEVEL LAYER ORDER IS NOT ASSERTABLE, WHICH LEAVES THE REVEAL'S "HIDE, NEVER FADE" RULE UNPINNED. `WORLD_LAYER_ORDER`/`CHART_LAYER_ORDER`/`HUD_LAYER_ORDER` are exported declared arrays with a build-failing completeness check, but the ROOT mount order (`worldRoot, plateRoot, fogSprite, chartRoot, hudRoot`) is an inline `addChild` inside the unexported `createStage()`, which needs a live Pixi Application jsdom cannot provide. That order is exactly what makes a fog FADE wrong — `plateRoot` sits below the fog while hulls sit above it — so the rule currently lives in comments and in `Fog.setVisible`'s behaviour, not in an assertion. Promoting the root order to a declared array would close it for the same cost the three sub-orders already pay.
  evidence: `client/src/render/stage.ts` (header: "a thin Pixi adapter (not unit tested)"); epic-5 amendments 22 and 24; the test agent declined to make the production change rather than widen scope unasked.

- source_spec: `_bmad-output/implementation-artifacts/spec-lazy-offer-draw.md`
  summary: A DEGENERATE OFFER-LESS LEVEL IS SERVER-SPENDABLE BUT CLIENT-UNREACHABLE, AND THE STATE IS TERMINAL. Since the lazy-draw bugfix a level ALWAYS banks, so `bankedLevels > 0` with `offer: null` is legitimate when a draw comes up empty. `spendHeal` accepts that level over the wire (it needs only a banked level), but the client can never send it — `offerView` refuses to open the band on an empty offer, and the digit-5 heal requires the band visible — so the level is permanently unspendable. Both review models flagged the visible half independently at the cycle-80 gate and the FALSE CUE was fixed then (`cueLine` now takes `refitable`, so the rail no longer tells you to press a TAB that opens nothing, while the chip still reports the bank you earned); what remains ledgered is the unreachable heal and the chip counting a level that can never be spent. NOT FIXED because reaching it needs the deck emptied to zero drawable lines — about 59 fits against roughly 25 levels in a full match — and because it is a FROZEN state once entered: an empty deck can never refill, since both refill paths (doctrine return, acquisition subdeck) require a fit, which requires an offer, which requires a non-empty draw. Deliberately NOT dismissed as impossible: epic-2 amendment 67 dismissed deck-run-dry as "near-hypothetical" and amendment 72 records that it was in fact the shipped bug's terminal state. If a future change can shrink a deck faster (a bigger `CONFIG.offer.size`, a card that burns cards, a much longer match), re-derive this before assuming the margin still holds.
  evidence: epic-2 amendments 69-72; `server/src/game/world.ts` `materializeOffer`/`spendHeal`; `client/src/ui/upgradeMenu.ts:329` (`offerView` empty-offer refusal) and `client/src/render/xpRail.ts` `cueLine`; the Fable reviewer's 24,000-op fuzz over 60 seeds found no other coherence gap, and its trace proving the empty-deck state cannot refill.

- source_spec: `_bmad-output/implementation-artifacts/spec-death-plume-at-sink-entry.md`
  summary: IS THE KILL FLASH STILL EARNED NOW THAT A DETONATION SHARES ITS TICK? — an Eric call, deliberately not made unattended. Amendment 21's 300ms hull kill flash was ruled specifically because the five seconds after a kill had NO enemy-side feedback; amendment 32 puts the crimson plume on that same tick, on the same hull, so the flash's founding argument is weaker and the two now claim 2 of the ratified 3 `WorldFlashGate` onsets per screen region per second (NFR13 / EXPERIENCE.md:138), leaving a region one onset from degrading in a ring-closure scrum. Left EXACTLY as shipped rather than retuned or removed: it is a ratified channel and the spec's own Block-If routed it here.
  evidence: epic-5 amendments 21 and 32; `client/src/net/roomBindings.ts` `openWreckWindow` (flash and plume now adjacent statements); `client/src/config.ts:830` `sinkFlashMs: 300`; `CLIENT_CONFIG.flashBudget.maxPerSecond: 3`.

- source_spec: `_bmad-output/implementation-artifacts/spec-death-plume-at-sink-entry.md`
  summary: THIS CYCLE'S TWO ADVERSARIAL REVIEW SUBAGENTS NEVER RETURNED, so the review was performed in-session by the orchestrator instead of by two independent passes. Both were launched, both stalled on their first read for ~2 hours under heavy machine contention (three other background jobs), and both were stopped. The change is small, fully unit-pinned and `npm run check` green, but it has NOT had the independent-eyes pass the workflow normally guarantees — and, like cycles 79 and 80 before it, it has had no human or browser look either. A follow-up review is recommended in the spec frontmatter for exactly this reason.
  evidence: the cycle-81 run log; `followup_review_recommended: true` in the spec frontmatter; the Review Triage Log entry naming the in-session pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-ambient-scene-rework.md`
  summary: SHOULD THE HOME SCENE SHOW COMBAT? — an Eric call, deliberately not made unattended. The rebuilt menu backdrop is now a real slice of the game (real terrain, real radar, real hulls under way, real wake and fog), and the one thing conspicuously absent from it is the fighting: no muzzle flashes, bursts, hull-hit flashes, splashes, wounded smoke, torpedoes, denied pulses or kill feed. That is the spec's binding "Never" list, and its reason is not taste — introducing a FLASHING channel to the page every player stares at while typing a name is a photosensitivity-relevant DESIGN decision (the standing `ambient.ts` law, EXPERIENCE.md:138's accessibility floor, and the ratified 3-onsets-per-region-per-second `WorldFlashGate` budget, which the menu currently does not even instantiate). The scene is wired so that adding it is cheap — `Effects` is already constructed and its one-shot/burst layers are simply never reached — so this is a ruling, not a rebuild. If the answer is yes, it needs a companion answer on whether the flash budget and the motion setting govern the menu the way they govern the match.
  evidence: `client/src/render/ambient.ts` (the single-layer `new Effects(stage.layers.wake)` wiring and its comment); the spec's `Never` list; `CLIENT_CONFIG.flashBudget.maxPerSecond: 3`.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-ambient-scene-rework.md`
  summary: DOC-SYNC — DESIGN.md's `islandFill` / `islandStroke` "provisional carry-over — Open Question" (DESIGN.md:41 and the colors table at :149) IS NOW RESOLVED BY DELETION, and the doc still says otherwise. Cycle 59 took the in-match chart off the provisional yellow onto the ratified hypsometric `terrain` ramp; cycle 82 took its last consumer — the Story-1.14 ambient's ellipse island stand-ins — off it too, and the two tokens were removed end to end (config.ts, and the `tokens.test.ts` identity pins now assert their ABSENCE). Nothing in the codebase can render that yellow any more. The design docs are not edited in-story (standing rule), so the row belongs in the Story 7.5 doc-hygiene batch: strike the Open Question and record that terrain colour is the hypsometric ramp, full stop.
  evidence: `client/src/config.ts` (the tombstone comment where the two tokens were); `client/src/__tests__/tokens.test.ts` (a) guard scan + the absence pins; DESIGN.md:41 / :149.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-ambient-scene-rework.md`
  summary: THE HELM'S HARD COAST CLAUSE STEERS OFF THE NEAREST COAST ONLY, so a channel narrower than `2 x avoidHardU` (180u) has no safe heading in it — inside the hard radius the escape normal of the CLOSEST island becomes the only steering term, and on a strait that normal points at the opposite shore. Not fixed because it is undemonstrated on the shipped ocean and the fix (summing every escape normal inside the hard radius) would re-open a helm that was tuned against a real 150s clearance test; the measured minimum BOW clearance over a 10-minute run of the shipped seed was 44.0u (mine layer) and 51.8u (torpedo boat), i.e. comfortable. Re-derive this before changing `mapSeed`, the rival bands, or `avoidHardU`: at 2-3% land coverage a strait is rare, not impossible, and the failure is visible (a hull oscillating against a coast on the menu).
  evidence: `client/src/render/ambientScene.ts` `avoidCoast` / `desiredHeading` (the hard clause returns the closest coast's normal alone); the cycle-82 edge-case review; `CLIENT_CONFIG.home.ambient.avoidHardU: 90`.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-ambient-scene-rework.md`
  status: RESOLVED 2026-08-14 by cycle 86 (spec-home-scene-variety.md) — and it was a REAL DEFECT, not a theoretical one. Eric asked for a different scene each load, which turned every seed-dependent property into a claim about oceans nobody has looked at, so it was measured: sweeping 40 random oceans put a rival past base radar range on 5 of them (worst 763u against 660u). The cause was not the band arithmetic this entry worried about but ANCHOR SEA ROOM — 180u, while the formation orbits to 528u, so the whole formation was placed onto coastlines and the coast-escape clause walked hulls outward. `anchorClearU` is now DERIVED from the formation's own radius, the outer band is pulled in, and an escape-cone leash biases the escape heading homeward past 72% of radar range. 60 oceans: zero off-scope, zero grounding, worst range 550u. The simulated assertion this entry asked for now exists and is proven in BOTH directions (it fails on the old config, naming the exact seed). Still open from the last sentence: `pickWater`'s roomiest-miss fallback can in principle return an aground point, and nothing clamps a hull to the map disc — both measured clean across 60 seeds.
  summary: THE SCENE'S OFF-SCOPE MARGIN IS PINNED AS CONFIG ARITHMETIC, NOT AS A SIMULATED PROPERTY. `ambient.test.ts` adds `max(rivalBands.maxU)` + `observerRoamU` (636.9u) and compares it to base radar range (660u) — 23u of headroom — but nothing asserts a bound on SIMULATED range, and the helm can legitimately carry a hull well outside its own band (coast avoidance outweighs the band spring roughly 4:1, and the hard clause suspends the band term outright). A 10-minute run of the shipped seed measured a true maximum of 572.6u and zero off-scope frames, so it holds today; if it ever stops holding, a rival silently stops painting and simply is not in the picture, with no cue. The honest pin is a simulated max-range assertion over a long run, which is slow enough that it belongs in a deliberate test-quality pass rather than bolted on here. Also unpinned: `pickWater` returns the ROOMIEST candidate when all 48 seeded samples miss, which on a pathological seed could be a point still aground, and nothing clamps a hull to the map disc.
  evidence: `client/src/__tests__/ambient.test.ts` (the band-vs-radar arithmetic assertion); `client/src/render/ambientScene.ts` `pickWater` / `desiredHeading`; the cycle-82 adversarial review's instrumented 10-minute run.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-ambient-scene-rework.md`
  summary: THE MENU SCENE ALLOCATES PER FRAME, against its own stated render-loop rule. `ambient.ts` documents `this.wakes` as "reused, never reallocated (render-loop rule)", but `ambientContacts` builds a fresh array plus one `Contact` object per sighted hull every frame and `ambientPaints` builds an array every frame; `drawScrim` also constructs a new `FillGradient` on every layout without destroying the previous one's texture (pre-existing from Story 1.14, now less exposed since the resize path moved onto the renderer's own event). Minor GC churn on a page that can idle indefinitely, and no measured cost — ledgered because the rule is either the rule or it is not.
  evidence: `client/src/render/ambient.ts` (`wakes` comment and `drawScrim`); `client/src/render/ambientScene.ts` (`ambientContacts` / `ambientPaints`); the cycle-82 adversarial review.
## 2026-08-14 — Sudden death, the final collapse (cycle 82)

- source_spec: `_bmad-output/implementation-artifacts/spec-sudden-death-final-collapse.md`
  summary: **ERIC DECISION PENDING — does the final collapse deserve its own player-facing register?** The cycle shipped NO new copy: the chrome bar's `RING CLOSES IN` -> `RING CLOSING` -> `RING CLOSED` grammar (epic-3 amendments 20/26) is generic over group count and reads correctly through the collapse unchanged, so `RING CLOSED` now first appears at 16:00 instead of 12:00. But the mechanic is called SUDDEN DEATH in Eric's own words and in the ledger, and the player is never told that the thing closing on them at 15:00 is different in kind from the three closes before it — the only cue is the X on the water. Inventing a register was explicitly out of scope (a copy ruling is Eric's), so this is a question, not a defect. Natural moment: alongside the next HUD pass, or a live playtest of a match that actually reaches 12:00.
  evidence: epic-3 amendment 30 (the mark is the telegraph grammar's third form, NOT a new register); the shipped `ringReadout` in `client/src/ui/chromeBar.ts` needs no change to be correct, which is why nothing was forced.

- source_spec: `_bmad-output/implementation-artifacts/spec-sudden-death-final-collapse.md`
  summary: NFR6 AND THE MATCH-TIME COVENANT NOW STATE A BOUND THE GAME NO LONGER HOLDS — `epics.md` (FR24, NFR6), the GDD, EXPERIENCE.md and the brief all say "~12:00 closure" and "matches complete inside ~15:00". Full closure is now 16:00 and the measured worst case is 16:43 (max over 12 pacifist matches), with a hard ceiling under ~17:30 for a captain hoarding banked levels. The direction is favourable — before this cycle the worst case was UNBOUNDED, since amendment 24 documented non-concluding matches as structural — but the numbers in the docs are wrong as written. NOT reworded in this cycle per the standing minimal-design-doc-edit rule (a change signal authorizes only what it rules on); routed to the 7-5 doc-sync batch, which already owns several rows of this kind.
  evidence: `batch-sim-evidence-2026-08-14.md` (pacifist control 12/12 resolved, p50 16:27, max 16:43); epic-3 amendments 27-30; `_bmad-output/planning-artifacts/epics.md:66` (FR24) and `:95` (NFR6).

- source_spec: `_bmad-output/implementation-artifacts/spec-sudden-death-final-collapse.md`
  summary: PICKS PER CAPTAIN RISES IN MATCHES THAT ACTUALLY REACH THE COLLAPSE — pacifist p50 16 (mean 14.7) against the 12-20 design band, because a full timeline is now 4 minutes longer and the passive XP tick pays out across all of them. Still INSIDE the band and only visible in matches that reach 12:00 at all (the lethal baseline is untouched), so no XP dial was moved and none should be moved on this evidence alone. Recorded so that a future economy read does not mistake the shift for a deck or tick regression: it is a pacing consequence of a longer worst case, which was the point of the cycle.
  evidence: `batch-sim-evidence-2026-08-14.md` picks-per-captain line; epic-3 amendment 2 (no XP dial may chase the picks band) is respected — nothing was retuned.

- source_spec: `_bmad-output/implementation-artifacts/spec-sudden-death-final-collapse.md`
  summary: **ERIC DECISION PENDING — sudden death guarantees a DEATH but not a WINNER, and it decides the match by HP rather than by play.** From 16:00 every afloat hull takes an identical 0.2 hp/tick, so two hulls that enter the collapse with equal HP sink on the SAME TICK, which Story 5.2 amendment 14 defines as `winnerId ''` / DRAW. Two untouched same-class captains are exactly equal by construction, and that is precisely the stand-off the feature exists to break — uniform synchronized damage makes same-tick mutual destruction structurally MORE likely here than on any combat path. Related and inherent: once the ring is at r=0 the outcome is fully determined by remaining HP and banked heals, with no positioning or shooting left to do, so a battleship that avoided every fight beats a torpedo boat that won three. Both follow directly from Eric's "geometry, not the damage curve" constraint and neither is a defect, but no tiebreak was built and none should be invented — a tiebreak (last-to-take-damage, most kills, highest HP fraction) is a design ruling. Also unresolved in the same family: whether a heal should be spendable DURING the collapse at all, since `bankedLevels` is uncapped and hoarded heals are what set the true worst case (see the amendment-30 correction).
  evidence: `server/src/game/match.ts` `mutualDestructionWinner()` returns undefined when non-drones share a sink stamp; `applyStorm` applies `CONFIG.zone.stormDps * dt` uniformly with no per-hull variation; epic-3 amendment 27 (the damage ramp was explicitly NOT authorized). Surfaced by the cycle-82 Blind Hunter review; no multi-hull collapse-to-conclusion case exists in the server tests.

- source_spec: `_bmad-output/implementation-artifacts/spec-sudden-death-final-collapse.md`
  summary: THE SERVER-SIDE COLLAPSE FIXTURE CANNOT TELL "the terminal ring's own centre" FROM "the map origin" — `server/src/__tests__/zone.test.ts`'s `collapsing()` timeline uses `offsetCap: 0`, which makes every ring concentric with (0,0), so the suite's central geometric claim (the collapse is concentric with the OFFSET terminal ring) is untestable in the one file that runs it through the authoritative `World`. The property IS covered twice elsewhere — `shared/src/__tests__/zone.test.ts` at `offsetCap 1.0` and `zoneSmoke.mjs` at 0.3, which measures drift of exactly 0 over a real socket — so this is a coverage nicety, not a hole. Worth closing next time that fixture is touched: the test positions hulls by distance from the origin, so a nonzero cap means re-expressing those positions relative to the ring centre.
  evidence: cycle-82 Blind Hunter review; the fixture's own comment ("offsetCap 0 makes the rings concentric with the origin, so distance 0 IS the point the ring closes onto") documents the blind spot as a convenience.

- source_spec: `_bmad-output/implementation-artifacts/spec-sudden-death-final-collapse.md`
  summary: `startZone` NOW DERIVES ONE MORE ZONE SEED THAN `rollZoneRings` CONSUMES — the seed count comes from `zoneGroups()`, which includes the collapse group, but the collapse ring is appended concentric and draws from no stream, so the last seed is computed and discarded. Ring geometry is genuinely byte-identical (pinned by test) because `zoneRingSeed(i)` depends only on `i` and the world seed, so this is waste rather than a bug — one extra `Math.random()` per room in `ArenaRoom`, and a dangling seed that reads oddly next to amendment 29's "consumes no seed". Left alone deliberately: tightening it means threading a "rolled rings" count through three call sites (world, ArenaRoom, the batch-sim runner) for no behavioural gain.
  evidence: `shared/src/sim/zone.ts` `rollZoneRings` (`rolled = radii.length - 1`); `server/src/game/world.ts` `startZone`; `server/src/rooms/ArenaRoom.ts` per-room nonce array. Surfaced by the cycle-82 Blind Hunter review and independently by the implementing agent.

- source_spec: `_bmad-output/implementation-artifacts/spec-sudden-death-final-collapse.md`
  summary: THE COLLAPSE MARK SITS AT THE BOTTOM OF THE CHART STACK, UNDER EVERY TACTICAL MARK — `zone` is second from the bottom of `CHART_LAYER_ORDER`, so the X draws beneath smoke, mines, decoys, blips, ships, aim and the sweep. It is consistent with the ring edges (same layer, same grammar) and the storm fill plus the chrome-bar countdown carry the same warning, so this is not a defect. But the X is the only thing on the water that says WHERE the map is about to become storm, it is a ~24px cross at ~0.5 alpha, and at 14:00 the scope is at its busiest. Worth an eye at the next design pass — the question is whether the collapse point deserves promotion above the blip layer, which no other zone element has ever needed.
  evidence: `client/src/render/stage.ts` `CHART_LAYER_ORDER`; `CLIENT_CONFIG.zone.markArmPx` 12 / `telegraphAlpha` 0.5. Surfaced by the cycle-82 Blind Hunter review.

## 2026-08-14 — Story 5-6 (cycle 83; Roving PvE Fleets + the bigger ocean)

- source_spec: `_bmad-output/implementation-artifacts/spec-5-6-roving-pve-fleets.md`
  summary: AR18's COMMITTED BATCH-SIM TUNING METHOD LOSES ITS IMPLEMENTATION, a named and accepted cost of amendment 41 (Eric ruling, taken against the orchestrator's recommendation: *"drones the spawn at the start of the game with players should be removed from the game entirely in their present form"*). Deleting the match-start drone fill takes `ArenaRoom.fillToCapacity`, `dronesSmoke.mjs`, `drones.test.ts`'s fill suite, and the batch-sim harness's `--drones N` flag with it. AR12 named that same drone-lobby batch-sim harness as TRIPLE-DUTY infrastructure (economy tuning, pre-launch load test, bot-vs-bot AI evaluation), and AR18 commits to *"batch-simulate XP tick and kill-bonus outcomes with drone lobbies before human playtests"* — a commitment that now has no implementation to fulfil. Keeping the fill behind `HC_DEV_OPTIONS` was offered and declined; the cost is accepted rather than mitigated. PARTIALLY DISCHARGED THE SAME DAY by amendment 44: `MatchEndSummary.pveKillsByClass` now carries per-size PvE kill counts out of REAL matches, so the economy-tuning leg has a better evidence source than the synthetic lobby ever was. What stays open is the other two duties — pre-launch LOAD TEST and bot-vs-bot AI EVALUATION — which need populated lobbies rather than telemetry. Epic 6's combat bots are where those are rebuilt: bot ships driven through the honest (non-omniscient) perception path, not a revival of the deleted drone fill.
  evidence: epic-5 amendments 41 and 44; `_bmad-output/implementation-artifacts/bmad-dev-auto-result-5-6-pve-fleets-questions.md` Q14; `_bmad-output/planning-artifacts/epics.md` AR12/AR18; `deferred-work.md` 2026-08-01 entry (`AR12 own the honest-bot evolution`, the same seam this entry now hands Epic 6 the other half of).

- source_spec: `_bmad-output/implementation-artifacts/spec-5-6-roving-pve-fleets.md`
  summary: `CONFIG.match.fillTo` IS NOW UNUSED BY PRODUCTION CODE and was deliberately NOT deleted. With the match-start drone fill gone (amendment 41) nothing fills to it, and map sizing was re-pointed off it onto `CONFIG.map.playerCap` in the same cycle — so the constant survives only in the batch-sim tunable whitelist and in test `new World(...)` arguments. Left in place rather than removed because deleting a CONFIG key is wire-contract-adjacent (CONFIG rides the welcome snapshot) and because the cycle was already large; the project's own standing style ("no dead knob survives", cycles 69/72) says it should go. A future cycle should delete it end to end, retiring rather than adapting the tests that reference it.
  evidence: `shared/src/constants.ts` `CONFIG.match.fillTo`; `server/scripts/batchsim/overrides.ts` tunable whitelist; `client/src/ui/phase.ts:44` (a stale comment still referencing the fill).

## 2026-08-14 — Cycle 87 (Home Tagline — Nautical Puns)

- source_spec: `_bmad-output/implementation-artifacts/spec-home-tagline-nautical-puns.md`
  summary: DOC-SYNC — the UX mockup `_bmad-output/planning-artifacts/ux-designs/ux-Hullcracker.io-2026-07-16/mockups/home-class-picker-1.html` still shows "Last hull floating wins" as the wordmark tagline at lines 308 and 406; that is now stale mock-era content, since the shipped tagline draws from the 20-entry pun pool (epic-5 amendment 46). Per standing project convention, design/mockup docs are not edited in-story, so this is ledgered rather than fixed. Neither DESIGN.md nor EXPERIENCE.md pins the tagline text — only the mockup does — so no design-doc edit is owed here, just the stale mockup line.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-tagline-nautical-puns.md`
  summary: THE WIN CONDITION IS NOW STATED NOWHERE A NEW PLAYER CAN READ IT, AND THE RULING THAT RETIRED IT RESTED ON A FALSE PREMISE. Amendment 46's ruling 3 removed `LAST HULL FLOATING WINS` from the home wordmark, and the option was put to Eric with the justification that "HOW TO PLAY carries the rule now". IT DOES NOT: the home page's HOW TO PLAY control paints the stub `FIELD MANUAL ARRIVES IN A LATER REFIT` (`client/src/ui/home.ts:52`, `NOTE_HOWTO`), pinned as a stub by `home.test.ts` — there is no field manual behind it. So a first-time player now reads a pun, clicks HOW TO PLAY, gets a placeholder, and enters their first match never having been told the objective; the only surviving statement of it, `LAST HULL FLOATING — YOU WON` (`client/src/ui/results.ts:159`), is shown exclusively to the player who already won. NOT A DEFECT IN THIS CYCLE — the implementation faithfully enacts the ruling, and both alternatives (keep the rule and add the pun below it; let the rule stay in the rotation) were explicitly offered and declined. But the premise the ruling was taken on is false, so the ruling is worth RE-PUTTING to Eric with the correction: the choice is between accepting that new players learn the objective by playing, restoring a rule line, or letting the win condition ride into the field manual whenever that ships. Surfaced by the cycle-85 Fable review gate and confirmed against the code by the orchestrator.
  evidence: `client/src/ui/home.ts:52` (`NOTE_HOWTO = 'FIELD MANUAL ARRIVES IN A LATER REFIT'`); `client/src/ui/home.ts:338` (the HOW TO PLAY control); `client/src/__tests__/home.test.ts` field-manual stub pin; `client/src/ui/results.ts:159`; epic-5 amendment 46 ruling 3.

## 2026-08-14 — Epic 5 retrospective (unattended; scope ruling + significant-discovery alert + 5 new standing agreements)

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  status: STANDING RULING RECORD, NOT WORK — verified 2026-08-18. Stories 5.4/5.5 remain `deferred` in sprint-status with numbers reserved, and the PHASE ruling it records (*"i have enough systems"* — a new mechanic now needs a fresh ruling) held throughout Epic 6: no new mechanic shipped without one. Like the hydrophone entry, this should not be counted as open work — it is the record of why work does not exist.
  summary: STORIES 5.4 (FOG BANKS) AND 5.5 (WHIRLPOOLS) ARE DEFERRED BY ERIC RULING, numbers reserved, not cut — and the same ruling declares the SYSTEMS LAYER COMPLETE (*"i have enough systems"*), which is a PHASE ruling: a new mechanic from here needs a fresh ruling and "it was in the epic plan" is no longer sufficient grounds. What each story carries into any revival: 5.4 still owes the MANDATORY radar re-derivation against the shipped physical return model (no fog term exists in it; epic-4 amendment 108 rules fog/rain an epic-scale feature; cycle 72 is the governing precedent AGAINST area returns, and a fog bank IS an area material), and 5.5 still owes UX open question #23 (the on-water feel treatment, never designed) plus an untested interaction — torpedo wake under a current has never been exercised. Note Story 6.2's AC naming fog banks and whirlpool placements among the things both sides must rebuild deterministically from the seed is now VACUOUS rather than satisfied, and must not be read as discharged.
  evidence: epic-5 amendment 47; the epic-4 retrospective's significant-discovery alert; epic-4 amendment 108; cycle 72's storm-wall removal; `epics.md` Story 6.2 acceptance criteria.

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  status: PARTIAL — verified 2026-08-18 at the epic-6 retro. The BOT-VS-BOT leg was rebuilt by Story 6-4 (`batchSim.mjs --bots N`, six-bar quality table, `bot-evidence-2026-08-16.md`). The LOAD-TEST leg IS STILL UNBUILT: `server/scripts/` holds `batchSim.mjs` and thirteen smokes and NO `loadTest.mjs`, while Story 7-6's release-gate AC names that file by name. RAISED TO A SIGNIFICANT-DISCOVERY ALERT in `epic-6-retro-2026-08-18.md` — third epic this capability has been assumed present.
  summary: SIGNIFICANT DISCOVERY — STORY 6-4 (COMBAT-BOT AI) HAS AN ACCEPTANCE CRITERION WITH NO IMPLEMENTATION BEHIND IT. Its AC reads *"bot quality is measured, not felt: the batch-sim harness's third duty — bot-vs-bot evaluation"*, and amendment 41 deleted the match-start drone fill AND `batchsim/runner.ts`'s fill and its `--drones N` flag. Three consequences for planning: (a) 6-4 must first rebuild a synthetic-lobby harness, inside a story already carrying utility scoring, a staggered observe cadence and a lint-enforced import boundary; (b) AR12's other two duties went with it — the economy leg is covered better than before by amendment 44's `pveKillsByClass` from live matches, but THE LOAD-TEST LEG IS UNCOVERED AND IT IS A BETA GATE (Story 7-6's release gate names `loadTest.mjs` by name); (c) the new fleet AI is NOT reusable as combat bots — `game/fleetAI` shares no boundary with 6-4's perception-gated bots, which may read only `perception.observe()`, and the ledger should not let that assumption form.
  evidence: epic-5 amendment 41 (which names the cost and homes it at Epic 6); `epics.md` Story 6.4 + Story 7.6 acceptance criteria; AR12/AR18; the 2026-08-14 Story 5-6 ledger entry that filed the original AR18 defer.

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  status: RESOLVED by Story 6-5 (epic-6 amendment 29) — verified 2026-08-18. Solo vs AI shipped as a queue-free `client.create('arena', { solo: true })` door, so beta launch has a real match at population zero. The termination rule was discharged rather than paid: bots are participants, so the standard win check already terminates a 1+19 match. See the companion entry above for the residual human-among-fleets case.
  summary: STORY 6-5 (SOLO VS AI) IS NOW BETA-CRITICAL AND STILL OWES ITS TERMINATION RULE — the two facts compound. `CONFIG.match.minHumans = 2` means one player cannot start a match at all, and at beta launch population is zero, so Solo vs AI is the ratified answer to the beta's central structural problem. But amendment 4 left a human-versus-drones match with NO DEFINED END (drones can never win; a human can no longer lose to them by attrition), and `minHumans = 2` is the only thing hiding it today. The story that fixes the beta's biggest gap is therefore also the story carrying an undefined termination rule created two epics earlier. Give it the rule at spec time; do not let it arrive as a review finding.
  evidence: epic-5 amendment 4; the 2026-08-11 Story 5-1 ledger entry; `CONFIG.match.minHumans`; `epics.md` Story 6.5.
  resolution: RESOLVED 2026-08-17 (cycle 97, Story 6-5; epic-6 amendment 29). Both halves closed. The beta gap: a solo player now reaches a full battle royale in one click via `client.create('arena', {solo:true})` — no queue, since a solo queue has nothing to pool (Eric ruled the queue design out: "There's no need even for a queue for a game that makes me play against 19 bots!"). The termination rule: NOT invented, because it was never missing for this roster — see the companion resolution on the 5-1 entry above. `CONFIG.match.minHumans` is UNCHANGED at 2; what moved is `sanitizeExpectedCaptains`'s floor (`CONFIG.match.minHumans` -> 1), without which a solo room was literally inexpressible and would board, freeze and wait forever with no error.

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  summary: EPIC 4'S ACTION FOLLOW-THROUGH WAS THE WORST IN PROJECT HISTORY — 1 of 9 held, 6 not addressed, 2 correctly parked — AND THE CAUSE IS STRUCTURAL RATHER THAN CARELESS. Every unmet item was an Eric-decision or an evidence campaign (a looking pass, a ruling, a triage pass, a playtest), and this epic ran unattended-fast on story work instead. Items that need one person's eye do not get discharged by a build cycle. The scheduling consequence, recorded as the lesson: an epic that runs unattended should not carry owner-decision actions as if a cycle will absorb them — they need their own explicit interstitial slot or a batched sitting. Epic 5's action 8 batches nine such calls precisely to test this.
  evidence: the epic-4 retrospective's action table; this retro's follow-through table.

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  summary: THREE OF FIVE ORCHESTRATOR RULINGS WERE OVERTURNED THIS EPIC AND ALL THREE WERE ABOUT PRESENTATION — amendment 8 (survivor placement tiers) superseded same-day by amendment 9; amendment 17 (do not hold the match open) VETOED by Eric on first contact with the water (amendment 20); amendment 18's plume clause reversed by amendment 32. Each was defensible in the abstract and each was caught within a day. NEW STANDING AGREEMENT: an orchestrator ruling about PRESENTATION is provisional until Eric has seen it — ship it, flag it as an orchestrator ruling (which the amendments already do well), and expect the correction rather than defending the reasoning. SECOND NEW AGREEMENT, from amendment 17's specific failure: weigh *"how often does this bite?"* against how the game is actually PLAYED AND TESTED TODAY, not against the designed-for population — 17's arithmetic was right about a 20-player lobby and wrong about 100% of duels, which is 100% of how one person tests with two tabs.
  evidence: epic-5 amendments 8, 9, 17, 18, 20, 32.

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  summary: THREE FURTHER STANDING AGREEMENTS ADDED. (a) A REWARD'S BALANCE NUMBER IS THE EXCHANGE RATE, not its damage or its time-to-clear — amendment 45 found the PvE farm was XP-NEGATIVE (two small hulls cost ~48 hp to clear and paid ½ level, while 50 hp of repair costs a full banked level), so a captain who farmed correctly and won came out behind one who ignored the fleet entirely; no amount of skill fixed it because the loss was in the exchange rate. Check any new reward against what its victims pay to undo the damage they took earning it. (b) WHEN A PRESENTATION RULING EMPTIES A COUNTER, CHECK WHETHER TELEMETRY WAS READING IT — amendment 38 stopped incrementing `kills` on drone victims and silently emptied `MatchEndSummary.killsByClass`; amendment 44 restored it. This split has now been re-derived three times (amendments 9, 38, 44). (c) PRE-REFUTE THE PLAUSIBLE FALSE FINDING IN THE LEDGER — amendment 35 records that the cross-model gate flagged the global witness sweep as a CONFIRMED defect, inferring from the existence of `fleetId` that fleets should be separate aggro networks; that rule was never made, the gate is line of sight and nothing else, and the amendment says *"do not 'fix' this"* in as many words. A ledger entry that pre-refutes a plausible false finding is cheaper than the third review cycle that rediscovers it.
  evidence: epic-5 amendments 35, 38, 44, 45; epic-5 amendment 9's telemetry clause.

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  status: SUPERSEDED 2026-08-18 by the epic-6 retro — same checkpoint, now four epics old. Consolidated in epic-6 retro action item #1. Retained as the epic-5-era snapshot of the list.
  summary: THE HUMAN-PLAYTEST CHECKPOINT IS NOW THREE EPICS OLD AND HAS GROWN AN ENTIRE VISUAL LAYER. Carried from epic 3: BB 50% instrument endgame share, terminal-ring island pockets (both still needing full-roster closure-reaching matches). Carried from epic 4: wake legibility under clutter, `degradeAlphaFactor` catchability, *"threat channels read first"*. NEW FROM EPIC 5, because three cycles shipped with no visual verification at all: whether the whole-map reveal framing reads as A REVEAL or just as *very far away* (hull scale drops to ~0.26x and the mockup's own legend flags all-hull nameplates may read as CLUTTER at that zoom, with "killer + wreck only" recorded as its fallback); whether the 0.62 dim leaves the reveal legible without hurting modal contrast; whether the `motion: off` zoom SNAP reads as broken rather than instant; whether the sinking settle + kill flash + killing-blow plume compose correctly on the water; whether the aggro bracket reads at the edge of the bubble; fleet spacing at the ruled ~400u spread; whether a 2800u ocean feels emptier between fights; AND the two-human sinking-window duel the feature was actually designed for, which has never been exercised by two people who both wanted the kill. A single full-roster session discharges all of it.
  evidence: this retro's challenges 2 and 3; the Story 5-3 and 5-6 ledger entries; epic-3 and epic-4 retrospective checkpoints.

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  status: VERIFIED STILL OPEN 2026-08-18, and it recurred — Epic 6 renumbered again (cycle 95's mine rulings merged on top of cycle 94's PvE rebalance and renumbered around it; the epic ran 15 cycles across parallel worktrees). Nobody has proposed a fix and no cycle has attempted one. Genuinely unhomed: it is a process/tooling item with no owning story.
  summary: REBASE RENUMBERING IS NOW A MEASURABLE TAX AND NOBODY HAS PROPOSED A FIX. One commit this epic reads *"Story 5-6: renumber ~180 amendment citations around cycle 81's amendment 32."* Cycles 83/84 collided, then 85/86/87 collided again; the tagline cycle was authored as cycle 85 / amendment 45 and renumbered TWICE. Parallel worktree cycles landing against an append-only, numerically-cited amendment ledger will keep producing this. Recorded rather than solved — it may not be worth a fix — but it is the third epic in which it appears, and the cost is now large enough to be worth someone's deliberate judgement rather than continued absorption.
  evidence: commit `8960bd5` ("renumber ~180 amendment citations"); `gds-workflow-status.yaml` next_expected entries for cycles 84 and 87, each recording its own renumbering.

- source_spec: `_bmad-output/implementation-artifacts/epic-5-retro-2026-08-14.md`
  status: CARRIED FORWARD 2026-08-18 into the epic-6 retro, both halves confirmed still open. (a) The win-condition gap is unchanged — HOW TO PLAY is still a home row with no page behind it, and the gap WIDENED this epic because Solo vs AI now ships as the launch-day first match, so a new player's first experience is a mode with no explanation attached. (b) The NFR1 whole-frame verdict is now carrying THREE epics of growth and is gated on hardware nobody has yet. Both are named in the epic-6 retro's Epic 7 preparation table.
  summary: TWO EPIC-7 ITEMS BECAME URGENT RATHER THAN FINAL-POLISH. (a) Story 7-4 (How-to-Play) now also owns the win-condition gap amendment 46(c) opened — the objective is stated NOWHERE a new player can read it, since HOW TO PLAY paints the `FIELD MANUAL ARRIVES IN A LATER REFIT` stub and the results banner is winner-only. In the epic where beta readiness was declared the priority, that is a first-run defect rather than a documentation nicety. (b) Story 7-1's NFR1 whole-frame verdict now carries TWO full epics of unmeasured render growth — epic-4's radar instrument (return heatmap, shadows, wakes, chop, flash budget) plus epic-5's 2800u ocean, up to 63 fleet hulls, and a whole-disc reveal chart that draws every island coastline and contour band at once for the first time ever, with no frame measurement taken at that framing. This is the largest unquantified risk in the project and the one item where finding the answer late is genuinely expensive; a cheap early read is worth taking before the reference device arrives.
  evidence: epic-5 amendment 46 (correction of record); `client/src/ui/home.ts` `NOTE_HOWTO`; the Story 5-3 ledger entry (no frame-budget measurement at reveal framing); epic-4 retrospective action 9.

## 2026-08-15 — Story 6-3 (cycle 89; the participants-only win check)

- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-the-participants-only-win-check.md`
  status: VERIFIED STILL OPEN 2026-08-18 — re-checked against `server/src/game/match.ts`. The boarding grace only arms the countdown when it expires with `>= minHumans` present, and the collapse-and-requeue path still fires only from the `countdown` phase, so a queue-formed 2-captain room whose second captain consumes a seat but NEVER boards remains stuck in `waiting`. Unchanged by Stories 6-4 through 6-7.
  summary: A SEALED COHORT THAT NEVER BOARDS HAS NO ESCAPE, AND STORY 6-3 BUILT THE FIX WITHOUT WIRING IT TO THIS CASE. The collapse-and-requeue path (`hooks.requeue()` then `hooks.disconnect()`) fires only from the `countdown` phase. A queue-formed 2-captain room whose second captain consumes a seat but NEVER BOARDS sits in `waiting` forever: `boardingReady()`'s grace expires, but `humanCount() 1 < minHumans 2` never arms the countdown, and no collapse path exists from `waiting`. The boarded survivor can still ESC → ABANDON, so it is not a hard lock, but they get no signal and no automatic re-queue — exactly the experience amendment 18 was written to eliminate, reached by a different door. NOT INTRODUCED BY THIS CYCLE (it predates the requeue signal), and not reachable through `gathering` in production since `CONFIG.match.joinWindow` is 0. The new `requeue()` hook is the obvious fix and the work is small; it was left alone because widening the trigger from one phase to two is a lifecycle change that deserves its own review rather than riding a review-gate patch.
  evidence: `server/src/game/match.ts` `notifyRosterChanged` (the collapse branch is inside the `countdown` arm) and `boardingReady()`; `CONFIG.match.joinWindow = 0`; epic-6 amendments 8, 10, 18; found by the Story 6-3 Fable review gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-the-participants-only-win-check.md`
  summary: THE SPECTATE BANNER HAS NO HUE CHANNEL, SO THE DRAW IS DISTINGUISHED THERE BY COPY ALONE. Amendment 14 ruled that a draw must not look like a loss; the results modal now renders it in the `info` token while victory keeps phosphor and defeat amber. The SPECTATE banner cannot make the same distinction: `render/hud.ts`'s `SPECTATE_STYLE` hardcodes `fill: AMBER` for every spectate line, so `VICTORY — AWAITING RESULTS` has always been amber too. The draw is legible there (`DRAW — SPECTATING`, new this cycle) but only as text. Pre-existing and NOT a regression — flagged because the hue channel now exists on one outcome surface and not the other, which is the kind of asymmetry that reads as a bug later. Making `spectateBanner` take a per-outcome tint is a small `hud.ts` change; it was not taken because amendment 14 asked for a draw CASE on that surface and got one, and widening a ratified visual grammar is Eric's call.
  evidence: `client/src/render/hud.ts` `SPECTATE_STYLE`; `client/src/ui/phase.ts` `spectateBannerText`; `client/src/ui/results.ts` `BANNER_HUES`; epic-6 amendment 14.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-the-participants-only-win-check.md`
  summary: `isParticipant` AND `isFleetHull` ARE PERMANENT COMPLEMENTS, SO THE SEAM'S REAL DIVERGENCE POINT IS `isHuman` — AND ONLY TWO SITES READ IT. Amendment 13 split `isDrone` into three predicates ahead of Story 6.4's AI captains. Worth recording what the split actually buys, because it is narrower than it looks: `isParticipant` is defined as `role !== 'fleet'`, i.e. exactly `!isFleetHull` for every present and future role, so those two can never disagree and every site keyed on either gives identical answers even after `'bot'` lands. The only predicate that can diverge from the other two is `isHuman` (`role === 'captain'`), and it has exactly TWO readers: `World.queueDenial` (denials need a socket at the other end) and `Match.humanCount` (FR34 — the countdown must count people, never bots). Story 6.4 should therefore audit those two sites and any NEW site it adds, rather than re-auditing all sixty. The fleet/participant pair remains worth having for READABILITY — a site now says which question it is asking — but it carries no behavioural risk.
  evidence: `server/src/game/participants.ts`; `server/src/game/world.ts` `queueDenial`; `server/src/game/match.ts` `humanCount`; epic-6 amendment 13; verified at the Story 6-3 Fable review gate.

## 2026-08-16 — Cycle 91 review gate (client frame-loop containment)

- source_spec: `_bmad-output/implementation-artifacts/spec-client-frame-loop-containment.md`
  summary: A FAILED `onOwnStats` NOW DESYNCS THE CLIENT SILENTLY AND FOREVER, AND CHOOSING BETWEEN THAT AND A LOUD RETRY IS AN ERIC CALL. Cycle 91 reordered `applyFrame` so `net.you` advances BEFORE `deps.onOwnStats` fires, which stops a throw there re-firing every frame and skipping reconcile/contacts/events on each. The trade, surfaced by two independent reviewers: because the mirror has advanced, `ownStatsChanged` is false on every subsequent frame, so **the stats recompute is never retried**. The predictor, HUD, radar, camera and fog keep running on the PREVIOUS build while the server runs on the new one — client prediction fights reconciliation for the rest of the match, with exactly one console line. This is STRICTLY BETTER than the shipped behaviour (which desynced *and* broke every subsequent frame), and cycle 91 removed the main way to reach it by gating `applyOwnStats` on the class table. But "silent permanent desync" versus "loud retry" is a real choice: a bounded retry (say 3 attempts) would self-heal a TRANSIENT throw at the cost of re-introducing repeated throws for a persistent one. Not taken because picking is a UX/design judgement, not a patch. Related: the throw still propagates out of `handleFrame` into an unguarded Colyseus message callback — the edge-case hunter verified the browser's event dispatch contains it and one message is decoded per event, so it does not cascade, but nothing in the client catches it either.
  evidence: `client/src/net/roomBindings.ts` (the reordered predicate/mirror/fan-out block); `client/src/main.ts` `applyOwnStats`; cycle 91 blind-hunter finding 3 and edge-case-hunter finding 1.

- source_spec: `_bmad-output/implementation-artifacts/spec-client-frame-loop-containment.md`
  summary: A PERSISTENTLY THROWING FRAME PATH IS NOW SURVIVABLE BUT INVISIBLE TO THE PLAYER, AND NOTHING ESCALATES IT. Cycle 91's containment reports each distinct `(phase, message)` signature exactly once, capped at 8 per phase. For the dominant case the file itself names — a throw that repeats EVERY frame — the player now gets a live picture, no repeating console evidence, and NO user-visible signal at all, where before they got a hard freeze that at least read as "something broke". If the throw sits upstream of input sampling inside `simTick`, the helm is still dead. Nothing escalates "this has now thrown for 100 consecutive frames" into a visible fault state (a banner, a reconnect prompt, anything). Worth a ruling because the honest options differ in kind: leave it quiet, add a client-side fault banner, or ship a real error-reporting sink. RELATED AND UNSHIPPED: the client has NO `window.onerror` / `onunhandledrejection` handler and no production error tracking of any kind, so a fatal client exception is currently silent to the player AND invisible to us — which is why the original boon-cards report arrived as a player complaint rather than a stack trace. The spec listed a global handler as Ask First and it was not taken.
  evidence: `client/src/app/loop.ts` (`MAX_REPORTED`, the per-phase cap); cycle 91 blind-hunter finding 2; `_bmad-output/implementation-artifacts/investigations/boon-cards-control-loss-investigation.md` Missing Evidence table.

## 2026-08-16 — The Intel Range merge (deferred from the frame-loop containment cycle) — **RESOLVED SAME DAY**

> **RESOLVED 2026-08-16 by cycle 92 (0.17.92), epic-6 amendment 22, PV 37→38.** Built exactly as ruled below: one `intelRange` line (common, ×4), `sightRange` derived as radarRange/2 and removed from `BOON_STAT_PATHS`, the 5/8 muzzle/smoke rung observer-scaled through one `muzzleFlashReach(me)` resolver anchored on radar range. Ladder ordering verified HOLDS at all five stack levels; zero boons byte-identical to base.
>
> **RECORDED BECAUSE IT SHOULD NOT HAPPEN AGAIN:** the deferral itself was a mistake. Eric had already answered the scope question with "both, as one unit of work"; the assistant then re-asked at a spec-LENGTH gate and marked "split" as *Recommended*, talking him out of his own decision on an internal process metric. The skill's own text calls that 1,600-token limit "a proposal, not a gate". A token count is not a reason to leave a broken system in the game — players were still spending picks on cards that did nothing, which was the whole complaint. **Do not let a spec-size heuristic override an explicit scope decision.**

Deferred at the gds-quick-dev step-02 scope gate: the combined spec measured ~3,300 tokens against a 1,600 ceiling. **This was FULLY SPECIFIED and design-complete — deferred on size, not on uncertainty.** Every ruling below is Eric's, given 2026-08-16.

- source_spec: `_bmad-output/implementation-artifacts/spec-client-frame-loop-containment.md`
  summary: THE INTEL RANGE MERGE IS RULED, SCOPED AND UNBUILT — FOUR ERIC RULINGS OF RECORD. `intelTruesight` and `intelRadar` collapse into ONE `intelRange` line, and the whole eighths ladder becomes a fixed fraction of it. **(1) THE LADDER SCALES** — *"It scales. Intel range means your detection range on all levels gets further."* This supersedes the flat-halo clause at `server/src/game/signals.ts:1546-1550` and epic-4 amendments 15/42/119. It removes the odd one out rather than creating an exception: 3/8 detect (`sightOf × detectFactor`), 4/8 sight (`sightOf`) and 8/8 radar (`me.stats.radarRange`) are ALREADY observer-scaled; 5/8 muzzle/smoke was the only flat consumed rung. **(2) `intelSweep` STAYS A SEPARATE LINE** — a rate, not a range; `intel` ends with two lines. **(3) THE MERGED LINE IS ×4 COPIES** — *"Make it 4 copies, its powerful."* At ×1.15/card that tops out at radar 1154.3 / detect 432.9 / sight 577.2 / muzzle-smoke 721.5 / farRadar 1010.1. **(4) THE LADDER NAMES BLEND BOTH RETIRED LADDERS** — `['IMPROVED OPTICS', 'HIGH-GAIN ANTENNA', 'DIRECTOR TOWER', 'CAVITY MAGNETRON']` — and the card copy discloses the sight rider through the NOTE line, not a second stat row (`Radar range: 660 → 759.` + `Sight, gun, cannon and star shells reach with it.`), matching the pattern already ratified for gun/cannon/star reach. THE STRUCTURAL PRIZE: with every rung a fixed fraction of one number, `detect < sight < muzzleFlash < farRadar < radar` holds at EVERY stack level by arithmetic rather than by invariant — which is why the merge is the real fix for the two-stack overrun, not merely a buff. Also note the 4-copy cap is a PERFORMANCE ruling as much as a balance one: client radar render cost is purely quadratic in `radarRange` across the whole reachable range (the `minRayRad` clamp does not engage below 2000u), so ×4 caps the worst frame at ×3.06 rather than ×4.05.
  evidence: `_bmad-output/implementation-artifacts/investigations/boon-cards-control-loss-investigation.md` (findings 2/3, the ruling sections, and the implementation surface); `shared/src/sim/boons.ts:338-345`; `server/src/game/signals.ts:1530`,`:1567`,`:1655` (`hornBandFor`, the fail-closed precedent); `shared/src/constants.ts:345`,`:351` (the detect rung/factor lockstep pin to mirror).

- source_spec: `_bmad-output/implementation-artifacts/spec-client-frame-loop-containment.md`
  summary: THE MERGE'S IMPLEMENTATION DECISIONS ARE ALSO SETTLED — CARRY THESE OR RE-DERIVE THEM EXPENSIVELY. **Anchor on `me.stats.radarRange × 0.625`, NEVER on `sightOf`** — both yield the identical number once `sightRange = radarRange / 2`, but the radarRange anchor makes dazzle-independence STRUCTURAL (`sightOf` is the sole dazzle entry point in perception), preserving *"a flash is a light source, dazzle does not shrink it"* by construction rather than by care; this is the same argument amendment 122 made when the foghorn moved onto intel range. **ONE exported resolver** called by both the `mz` and `sm` gates — `signals.ts`'s own stated rule is that a second hand-rolled copy of a sight tier is the desync class the file exists to prevent. **`sightRange` becomes DERIVED and is re-pinned at BOTH sibling sites** — `clampStats` (`shared/src/sim/stats.ts`, immediately after the three `rangeU` re-pins) and `applyBoonStats` (`shared/src/sim/boons.ts`, after the same block) — and `'sightRange'` leaves `BOON_STAT_PATHS`, which makes it structurally underivable elsewhere. Keep the `baseStats()` seed. **Two client sites deliberately do NOT scale:** `radarDim.ts` (a legibility ramp whose own header argues its outer radius is a statement about the SIGHT BUBBLE, not intel range) and `HEAT_WAKE_REF` at `client/src/config.ts:489` (a module-load curve fit, not a perception gate — the server gates wakes separately; per-observer would mean re-fitting per stats change). **THE HIGHEST-RISK ITEMS**, in order: the two hand-written literals in the independent perception oracle (`server/src/__tests__/perception.test.ts:2711` and `:2730`, both `SIGHT * 1.25`) which do NOT inherit the production fix and must each be re-derived BY HAND, since the file's rule forbids importing the helper; and `server/src/__tests__/__snapshots__/goldenFrames.test.ts.snap` which must be REGENERATED, not hand-edited, because both the boon ids and the RNG draw results move (pool 59→53 cards changes every weighted draw). `PROTOCOL_VERSION` 37 → 38 (`shared/src/index.ts:377`). The amendment belongs in `epic-6-context-amendments.md` as **Amendment 13** in that file's `## Amendment N — <thesis>` format — NOT epic-4, which is closed — naming the epic-4 amendments it supersedes. Scale: ~12 production files, ~25 test files. Also closes the `deferred-work.md:635` foghorn/truesight entry, whose whole premise was that *nothing binds `sightRange <= radarRange / 2`* — the derivation makes that bound structural.
  evidence: `shared/src/index.ts:377`; `server/src/__tests__/perception.test.ts:2711`,`:2730`; `server/src/__tests__/__snapshots__/goldenFrames.test.ts.snap`; `shared/src/__tests__/deck.test.ts:72`,`:81`,`:92`,`:101` (intel 15→9, TB deck 59→53); `shared/src/__tests__/boons.test.ts:130-132` (36→35 lines); `client/src/__tests__/zone.test.ts:121-124` (`1.15**5` → `**4`); `client/src/render/radarDim.ts:16-28`; `client/src/config.ts:489`; `_bmad-output/implementation-artifacts/epic-6-context-amendments.md:252`.

## 2026-08-16 — Story 6-4 review gate (combat-bot AI): dead knobs deleted, behaviours ledgered

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-combat-bot-ai.md`
  summary: BRACKET-AND-WALK FIRE IS UNBUILT AND ITS FEEDBACK CHANNEL WAS DELETED — REBUILDING THE BEHAVIOUR MEANS REBUILDING THE CHANNEL WITH IT. Wave 2's `foldView` returned a `GunneryFeedback` whose `splash` field carried the bot's own `sp` fall-of-shot impact point, documented as "what makes bracket-and-walk fire possible in fog" — and no tactics code ever read the return; `ingest()` discarded it. The review gate deleted the whole return (foldView is now void; the `sp` event is deliberately ignored in `foldEvent`) under the project's "no dead knob survives" rule (cycle 69 grey-NO-DATA and cycle 72 storm-wall precedents), rather than inventing an unmeasured aim-correction loop at the gate. A future bracket-and-walk behaviour is a REAL competence feature — a shooter walking fire onto a fogged plot is exactly what `sp` exists for (epic-4 FR16) — but it changes measured combat balance, so it needs its own cycle: re-add the splash capture AND its consumer in the same change, and re-run the bot-vs-bot campaign to measure the hit-rate shift.
  evidence: `server/src/game/ai/utility.ts` `foldEvent`/`foldView` (the deletion and its comment); `server/src/__tests__/botPolicy.test.ts` "a splash (sp) is deliberately IGNORED"; the Story 6-4 review-gate FIX 3 ruling.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-combat-bot-ai.md`
  summary: `BotProfile.aggression` WAS DELETED, NOT WIRED — RE-ADDING IT MEANS BUILDING ITS CONSUMER IN THE SAME CHANGE. Six tuned values (0.5–0.85) shipped with a doc comment claiming wave-3 tactics scale closing throttle and shot-taking marginality with it; neither consumer was ever built, and all three reviewers flagged the dead dial. The review-gate ruling allowed wiring it into exactly one honest consumer, but every candidate (cruise-throttle scaling, shot-marginality gating) required inventing a new tuning formula late in the cycle against the "no balance dial moves" constraint — so the DELETE branch of the ruling was taken. The six values are preserved here for a future cycle that wants the dial back: raider 0.6, duelist 0.85, bulwark 0.75, siege 0.55, forager 0.5, trapper 0.6. Any revival must land the field and its consumer together (the `BotProfile` interface doc now says so) and re-measure profile win-rate spread.
  evidence: `server/src/game/ai/profiles.ts` (the interface doc's "every field here HAS a consumer" clause); the Story 6-4 review-gate FIX 3 ruling; reviewer findings (all three passes).

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-solo-vs-ai-mode.md`
  status: VERIFIED STILL OPEN AND UNRULED 2026-08-18 — raised to a SIGNIFICANT-DISCOVERY ALERT in `epic-6-retro-2026-08-18.md` and filed as an epic-6 retro action item owned by Eric, with Story 7-6's hardening sweep as its home. Safe against room hijack (verified twice, independently, against @colyseus/core 0.17.44); the open question is COST, and no mitigation is built.
  summary: SERVER HARDENING — THE QUEUE-FREE SOLO DOOR LETS AN UNAUTHENTICATED HTTP REQUEST MINT A FULL SIMULATING ROOM, AND NOTHING RATE-LIMITS IT. **UNRULED — needs Eric.** Story 6-5 opened `ArenaRoom.static onAuth` to any request carrying `solo === true`. That is SAFE against room hijack (verified twice at the review gate, independently by a Fable pass and by Codex, against `@colyseus/core` 0.17.44: every arena locks inside `onCreate` BEFORE its listing is persisted to the driver, so `joinOrCreate` can never discover one and always falls through to creating the asker their own room; `joinById` throws on `locked` before `onAuth` runs). What it does open is COST: a bare `POST /matchmake/create/arena` now runs `generateMap` plus a 20-hull / 19-bot-mind 20Hz simulation for the ~15s seat-reservation window, with NO websocket ever opened and NO matchmake rate limit anywhere (the transport `maxMessagesPerSecond` guard only covers connected clients). Bounded — the room auto-disposes at seat expiry, so there is no permanent leak — but a loop at ~10 requests/sec sustains ~150 concurrent full arenas on the single Render process. Before this story no unauthenticated path could mint a simulating room at all, because the queue required two live sockets. Codex separately found that a solo player dropping with a reconnectable code holds the whole 20-ship sim for the 60s `reconnectGraceSeconds` with zero humans connected, which stacks with the above; ADJUDICATED at the gate as NOT a defect and NOT to be removed — in solo that hold preserves the player's OWN match, which is Story 0.2's whole promise. Candidate mitigations, none built: a per-IP create throttle, or a global concurrent-solo-room ceiling. This is inherent to any queue-free solo door and was accepted knowingly rather than discovered late.
  evidence: `server/src/rooms/ArenaRoom.ts` `static onAuth` (the solo branch) + `finishCreate`; `server/src/rooms/roomOptions.ts` `sanitizeSolo`; `@colyseus/core` 0.17.44 `MatchMaker.mjs` (`handleCreateRoom` persists after `onCreate`; `findOneRoomAvailable` queries `{locked:false, private:false}`) and `Room.mjs` (`DEFAULT_SEAT_RESERVATION_TIME = 15`); epic-6 amendment 29's "THE RESIDUAL" section; both review-gate reports (cycle 97).

- source_spec: `_bmad-output/implementation-artifacts/epic-6-context-amendments.md` (amendment 32, cycle 99)
  summary: BOTS ARE TOLD LESS ABOUT THE STORM THAN THE HUMAN HUD IS — `BotWorldPort` exposes ONLY `zoneLiveRing`: no phase, no closing rate, no `closesInMs`, no revealed next ring. So a bot cannot tell a stationary ring from one closing at 16.1 u/s and cannot anticipate either. **This is the single largest remaining source of bot storm damage:** after cycle 99's ring-as-constraint fix, 9 of the 12 residual ring crossings happen on a CLOSING beat — the ring overtaking the hull rather than the bot wandering out. Widening the port to the `zoneLiveState` the schema ALREADY mirrors to every client would be a PARITY RESTORATION rather than a cheat (ArenaState carries `zoneState`, `zoneStartT`, `zoneCur*` and `zoneNext*` from the reveal beat, plus the chrome-bar countdown — it is precisely what a client is sent). It is also the prerequisite for a closing-RATE-derived deadband, which is the physically correct quantity that cycle 99 could not build and substituted the hull's turn radius for. NOT built in cycle 99: the fix was scoped to the defect Eric reported, and this is an information-model change.
  evidence: `server/src/game/ai/types.ts` `BotWorldPort`; `server/src/rooms/schema/ArenaState.ts` (`zoneNext*` etc.); cycle 99 residual measurement (12 crossings, 9 on a closing beat); epic-6 amendment 32b.

- source_spec: `_bmad-output/implementation-artifacts/epic-6-context-amendments.md` (amendment 32, cycle 99)
  summary: THE PRICED STORM EXCURSION — a bot deliberately accepting storm damage to escape a lethal threat. **Offered to Eric at the cycle-99 gate and DEFERRED, not rejected.** His wording was *"they need to be more aware of it"*, not *"they must never leave"*, so the door is open. Cycle 99 shipped the hard version: the ring constrains every chosen heading, so a cornered bot flees TANGENTIALLY along the rim and never willingly crosses. The soft version would permit a crossing when `stormDps × predicted_seconds_outside` is small against current hp and the threat is lethal — the prediction is computable (`distance_outside / inward_speed`) and it turns an oversight into a real decision, at the cost of one more tunable. Accepted consequence of the hard version, ledgered: a cornered bot will now turn and fight where a human might have run through the storm. Would compose well with the `BotWorldPort` parity entry above, since pricing an excursion properly wants the closing rate.
  evidence: epic-6 amendment 32b ("REJECTED: the prototype's unconditional override" is a DIFFERENT rejection — that one is about refusing to fight in the rim band, not about priced excursions); the cycle-99 investigation's trade-off section.

- source_spec: `_bmad-output/implementation-artifacts/epic-6-context-amendments.md` (amendment 32, cycle 99)
  summary: THREE BOUNDED CONSERVATISMS IN THE BOT FIRE GATE, all ledgered rather than fixed, all found at the cycle-99 review gate. (1) The terrain gate tests from the HULL CENTRE, not the muzzle, because `BotSelf` carries no hull class by design and `muzzleSpawn` is unreachable from `ai/`. Codex called this over-blocking; the adversarial pass REFUTED it (the extra span lies inside the bot's own silhouette, and collision push-out keeps hull vertices out of polygons, so a false denial requires the hull to overlap the very coast it is pressed against) and the measurements agree — gun fire per sim-second ROSE 1.5% after the gate landed. Adjudicated as known conservatism. (2) An ACOUSTIC HOMING torpedo steers in flight and could curve around a headland the straight-line gate clips, so that one doctrine is over-blocked in niche geometry. (3) A bot can down-score an opponent reachable only by ARCING cannon (who can plunge on the bot while the bot's flat reply is blocked) in favour of a clear-line drone; bots have no damage-source response anyway, so nothing regressed. Also: a bot does not throttle DOWN at the sudden-death collapse to shrink its turn circle, so it orbits the collapse point at ~90-165u and eats storm slightly earlier than a clever human would.
  evidence: cycle-99 Fable review (ATTACK 3/4 and the final-minute trace); Codex cross-model review finding 2; `server/src/game/ai/tactics.ts` `shotReaches`.
## 2026-08-17 — Cycle 98 (dim-mask render freeze): two threads found, neither in scope

- source_spec: `_bmad-output/implementation-artifacts/spec-radar-dim-mask-render-freeze.md`
  summary: THE CLIENT FRAME-LOOP CONTAINMENT DOES NOT COVER PIXI'S OWN RENDERER LISTENER — the exact gap this cycle's crash escaped through. Cycle 91 wrapped OUR ticker callbacks (`client/src/app/loop.ts` guards `simTick` and `render`, reporting through `onError(err, phase)`), on the reasoning that "a throw costs one frame instead of the session". But Pixi registers its own renderer as a SEPARATE ticker listener at `UPDATE_PRIORITY.LOW`, and a throw inside THAT listener — which is where this cycle's `BindGroup.getResource` crash lived — never passes through our guard at all. Pixi 8's ticker clears `_requestId` before `update()` and only re-requests after it returns, so the session-ending failure mode cycle 91 was built to prevent is still fully reachable from any renderer-internal throw (a destroyed texture, a bad filter, a lost GL context). Fixing the specific destroy (this cycle) does not close the class. Candidate: register our own LOW-priority guard listener around `app.render()`, or drive rendering manually (`autoStart: false` + explicit `renderer.render()` inside the guarded callback) so every frame path funnels through one try/catch and one `onError`. NOT built here — it changes the client's frame-driving architecture and deserves its own cycle with its own evidence.
  evidence: `client/src/app/loop.ts` (the guarded callbacks and `onError` hook); the reported stack trace's tail (`VM.emit` -> `Yt.render` -> `rh.render`, i.e. Pixi's ticker emitting into its own render listener, with no frame of ours between); `_bmad-output/implementation-artifacts/investigations/radar-dim-mask-render-freeze-investigation.md` Follow-up 2026-08-17 #2, Hypothesis 4.

- source_spec: `_bmad-output/implementation-artifacts/spec-radar-dim-mask-render-freeze.md`
  status: NOT REPRODUCED 2026-08-18 — `npm run check` ran green end to end at this retro (exit 0; 746 shared + 1505 server + 2855 client = 5106 tests, 183 files) after rebuilding `shared/`. That is one clean run against a FLAKE, so this entry is NOT closed: it stays open as a hardware-timing flake with no reproduction this pass. NOTE for the next reader: the two TS errors seen before the rebuild were stale `shared/dist`, not a real break.
  summary: TWO TIMING-SENSITIVE TESTS FAIL ON THIS HARDWARE AND `npm run check` IS NOT RELIABLY GREEN — neither caused by this change, and the client one is GENUINELY MARGINAL rather than merely starved. **(a) `client/src/__tests__/radarHeatmap.test.ts`** times out at 5000ms, a DIFFERENT test each run. It fails even when the client suite runs ALONE on an otherwise idle machine (1 file failed / 89 passed), so the trigger is vitest's own intra-suite thread parallelism, not cross-session contention; run the FILE alone it passes 68/68 with a 22ms slowest test. **(b) `shared/src/__tests__/map.test.ts` "a production map generates within budget"** asserts `elapsed < 500` and measured 625ms inside a full `npm run check`, passing at 402ms when run alone — and that test's own comment says it exists to catch an order-of-magnitude generator regression "without failing the build because three workspaces happened to run their suites at" once, i.e. its author anticipated exactly this and the threshold is still too tight. Candidate fixes: a per-file `testTimeout` override or `poolOptions.threads.maxThreads` cap for (a); a wider budget or a warm-up/median-of-N for (b). Deliberately NOT tuned here — changing test semantics or CI cost is not a call to make inside an unrelated client bugfix. NOT CAUSED BY THIS CHANGE, established two independent ways: the failure reproduces with this cycle's client changes fully reverted to the baseline commit, and `radarHeatmap.test.ts` constructs no `Radar` and never imports the dim-mask path at all (confirmed separately at the acceptance audit). The shared one is in a workspace this client-only cycle does not touch.
  evidence: Four `npm run check` / `npm test -w client` runs (2026-08-17) failing on `radarHeatmap.test.ts` with disjoint test names each run, including one with the client suite running alone on an idle machine (1 failed / 89 passed files); baseline reproduction after `git checkout 7a733b4 -- client/src/render/{textures,radar}.ts` and removing `dimMaskLifetime.test.ts`; isolated file run 68/68 in 12.04s wall, 7.26s test time, 22ms slowest test; `map.test.ts:554` measured 625.2ms against its `toBeLessThan(500)` inside a full check and 402ms run alone. Two `npm run check` runs were additionally killed by SIGTERM under concurrent load earlier in the session.

## 2026-08-17 — Story 6-6 review gate (mode select & queue liveness)

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-mode-select-queue-liveness.md`
  summary: THE TOP-LEFT LIVENESS BLOCK OVERLAPS THE WORDMARK BELOW ~768px OF VIEWPORT HEIGHT, AND IT CANNOT BE FIXED WITH PADDING. **UNRULED — needs Eric, and it is a placement he chose.** Verified by screenshot on a real dev build: clean at 1440x900 and 1366x768, COLLIDES at 1280x720 (the register's second line runs through `HULLCRACKER.IO`). Cause: the home overlay is `justify-content: safe center` with `overflow-y:auto`, so as the viewport shortens the centered column rides up until the wordmark reaches the fixed `top:22px` block. The obvious fix — top padding on the overlay — is NOT AVAILABLE: the block is deliberately absolutely positioned so it costs the port column zero height (amendment 47's container-fit law), and that column is already ~722px against the guarded 768px floor with only ~46px of headroom, so the ~62px of clearance needed would break the law at the SUPPORTED floor to fix a case below it. So the block can only overlap or move. 768px is the project's guarded floor and 720p is below it (where the column already scrolls) — but a real 720p laptop/Chromebook has a ~640px viewport after browser chrome, and Epic 7 names Chromebooks as a target, so real users will land here. Candidate, not taken because it changes Eric's stated placement: move the register to the BOTTOM-left (`top:22px` -> `bottom:22px`), a one-line change that still costs the column zero height and cannot collide, since the bottom of the port is empty at every size measured.
  evidence: screenshots at 1440x900 / 1366x768 / 1280x720 against a real Vite+server dev build on scratch ports; `client/src/ui/home.ts` `makeLiveness()` (`position:absolute;top:22px;left:26px`) and `OVERLAY_CSS` + `applySafeCenterScroll` (`client/src/ui/fit.ts`); `client/src/__tests__/containerFit.test.ts` (the 768 guard, structural only — jsdom has no layout, so it cannot catch this class of defect).

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-mode-select-queue-liveness.md`
  summary: `hullcracker.mode` IS WRITTEN ON EVERY DEPLOY AND HAS NO REACHABLE READER — THE MODE-PERSISTENCE HALF OF THE STORY IS INERT AS SHIPPED. **UNRULED — needs Eric.** Found independently by BOTH review passes. `loadSavedMode()` has exactly one consumer: `enterPort`'s `lastDeploy ?? {...}` fallback, on the auto-requeue path. That path requires an `rq` (cohort collapse) from a live match, which requires a deploy in the same page load, which already sets the in-memory `lastDeploy` — so the fallback cannot fire, as the surrounding comment in `main.ts` itself concedes. The spec's I/O matrix row ("last deploy was `soloVsAi`, page reloaded -> restores it") therefore describes behaviour that cannot occur today. NOT deleted, and deliberately so: Eric ruled Q8(a) ("persist last-used mode only") and UX-DR25 requires the pick to persist, so removing it would need a fresh ruling; it is also the exact substrate DUO/TRIO will consume the moment a third mode exists, and it is harmless (a fail-open localStorage write). What is missing is a REACHABLE consumer, and every candidate is new unruled behaviour (e.g. restoring the mode across a full page reload, or auto-requeueing after a refresh-while-queued) — inventing one at the gate would be inventing a game mechanic, which the project forbids.
  evidence: `client/src/ui/home.ts` `saveMode`/`loadSavedMode`; `client/src/main.ts` `enterPort` (the `lastDeploy ?? {...}` fallback and its own comment: "The fallback can only be reached if a collapse somehow preceded any deploy, which the lifecycle does not allow"); both cycle-98 review-gate reports.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-mode-select-queue-liveness.md`
  summary: `/liveness` HAS NO NEGATIVE CACHE AND NO CLIENT BACKOFF, SO A DRIVER OUTAGE IS POLLED AT FULL RATE BY EVERY VISITOR. The 2s cache deliberately does not cache FAILURES (so a blip cannot pin a stale answer), and the cycle-98 gate added in-flight coalescing so concurrent pollers share one query — but with the driver down there is no successful result to cache, so each 10s tick from each home screen still reaches a failing driver, and `startLivenessPoll` retries on the same fixed interval regardless of consecutive failures. Bounded and non-fatal (every failure path already renders as absence, and the home screen stays fully usable), and strictly better than before the coalescing fix. Candidates, none built: a short negative cache, or exponential client backoff after N consecutive failures. Related but distinct from the multi-queue-room and wall-clock-vs-room-clock items below, which are the same subsystem.
  evidence: `server/src/liveness.ts` (the "failures are never cached" comment and its test); `client/src/net/liveness.ts` `startLivenessPoll` (fixed interval, no failure counter); cycle-98 Edge Case Hunter finding P7.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-mode-select-queue-liveness.md`
  summary: TWO NARROW LIVENESS EDGES ACCEPTED AT THE GATE, BOTH FLAGGED BY BOTH REVIEWERS. (1) MULTIPLE QUEUE ROOMS: `foldLiveness` takes the FIRST `{name:'queue'}` listing for the countdown block (their clients all still count toward `playersOnline`). `StandardQueueRoom` is never locked and sets no `maxClients`, so exactly one exists in practice — but a cold-start race, especially across processes, could mint two, and then the SOLO button reports a pool the player may not join, and could flip between cohorts poll to poll. The "exactly one in practice" argument is a single-process one sitting in a module whose whole premise is multi-process. (2) WALL CLOCK vs ROOM CLOCK: `resolveDeadlineAt` stamps `Date.now() + startsInMs` once per cohort, but the match actually forms off `this.clock.currentTime` (a Colyseus `ClockTimer`). Over a 2:00 hold, event-loop stalls or a server NTP step let the two diverge, so the published deadline can disagree with the real form instant by seconds. Both are cosmetic and self-correcting within one 10s poll; neither is worth the complexity at this stage.
  evidence: `server/src/liveness.ts` `foldLiveness` (the "There is exactly one in practice" comment); `server/src/rooms/StandardQueueRoom.ts` `resolveDeadlineAt` and its clock comment; cycle-98 Edge Case Hunter findings P4/P5 and Blind Hunter's matching PLAUSIBLE items.

## 2026-08-18 — STORY 6-7 Reconnection UX (cycle 102): opened threads

- source_spec: `_bmad-output/implementation-artifacts/spec-6-7-reconnection-ux.md`
  summary: LIVE SPECTATE IS A STORY THAT DOES NOT EXIST YET, AND ITS CENTRAL QUESTION IS ERIC'S. **UNRULED — needs Eric.** The match-URL investigation priced watching someone else's live match as capability D and found it blocked in FOUR independent places: every arena is locked at birth and `joinById` refuses locked rooms BEFORE `onAuth`; `_reserveSeat` never consults `locked`, so a spectator would eat a CAPTAIN's seat against `maxClients = playerCap`; the fog fails closed, so a shipless observer sees an empty ocean and making it work needs a THIRD observer class in `frames.ts` (a new disclosure category against an invariant with exactly six declared exceptions); and Eric closed multi-tab play on 2026-08-17. Only the first two are engineering. The ruling needed is a four-option table — spectate only after the match ends (already how `spectates()` behaves), spectate live fogged to one chosen captain's view (zero cheat exposure, reuses `observe()` with another observer id, probably the most watchable), spectate live unfogged (a second browser becomes a wallhack — unacceptable without a ruling), or unfogged on a broadcast delay (needs a frame ring buffer that does not exist). Eric confirmed the SPLIT at the 6-7 gate (R12) and declined to rule on the fog yet. Recorded here because the investigation's own negative search confirmed that NOTHING in `epics.md`, `deferred-work.md` or any epic-context amendment tracked match URLs, shareable links, spectate links or replay — this is a genuinely new thread, not a parked one.
  evidence: `_bmad-output/implementation-artifacts/investigations/match-url-spectate-reconnect-investigation.md` (Findings 2, 7, 8; Deduction 2's option table); `server/src/game/frames.ts` `spectates()`; `client/src/app/sessionLock.ts`.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-7-reconnection-ux.md`
  summary: CAN A QUITTER WIN? **UNRULED — needs Eric.** If the last two captains both depart, the FIRST departure latches the SECOND as the winner, so a match can be "won" by someone who abandoned it. Surfaced by the Codex cross-model pass at the 6-7 review gate, which read it as a scuttle regression; the orchestrator verified it is NOT — `checkWin`/`latchOutcome` are untouched by cycle 102, and `afloatCaptains()` sees an identical set whether a departed hull is `sinking` (post-scuttle) or absent (pre-scuttle), so this is shipped behaviour reached by a new route. It is adjacent to a ruling Eric DID make: amendment 47 chose the scuttle over the literal AC precisely so an absent captain could not win — this is the residual case where every remaining captain is absent. Options: leave it (a winner is always named), treat an all-departed finish as a draw with no winner, or rank the departed by sink order so the FIRST quitter places last. Ledgered rather than decided, because changing win semantics was not in this cycle's scope.
  evidence: `server/src/game/match.ts` `latchOutcome`/`outcomeLatched` (untouched by cycle 102); Codex cross-model review of `00f2be5..3aa28d3`, finding 1, re-classified as pre-existing by the orchestrator.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-7-reconnection-ux.md`
  summary: THE FAILED-RESUME COPY IS IMPLEMENTER DRAFT, NOT ERIC-RATIFIED. `COULD NOT REJOIN YOUR MATCH — BACK IN PORT` (home status line, tone `info`) was authored in-cycle because the story's own AC demands that a failed reconnect route home "with a plain explanation, never a dead screen". It follows the shipped two-part `<STATE> — <REMEDY>` grammar and sits beside `VERSION MISMATCH — PLEASE REFRESH THE PAGE` and `QUEUE CLOSED — PLEASE TRY AGAIN`. But amendment 41 governs — *"a ruling to put information somewhere is not a licence to author the copy"* — and Eric explicitly declined to write new banner copy at the 6-7 gate (R10 left `RECONNECTING…` bare). Same standing as the `UNKNOWN VESSEL` fallback: wants ratification or replacement in the next copy/identity pass. The BEHAVIOUR is not in question, only the words.
  evidence: `client/src/net/connection.ts` `resumeFailedStatus()`; epic-6 amendment 41 (the copy law) and amendment 49 (which flags this string by name).


## 2026-08-18 — LEDGER TRIAGE, MECHANICAL HALF (epic-6 retro action item #6)

Run at Eric's direction during the Epic 6 retrospective, discharging the **first half** of the
triage this file has been asking for since `epic-4-retro`'s entry above (178 entries then, 250 now,
none triaged since 2026-07-18 — three epics). **No existing entry text was edited**; 38 entries
gained a `status:` line, which is the annotation this file has always been missing.

### What the count actually was

"250 entries" is a count of `- source_spec:` markers, **not a count of open work**. Before this pass:

| | |
|---|---|
| Total entries | 250 |
| Carrying any adjudication | **22** (17 `status:` key, 4 inline `[RESOLVED …]`, 1 inline `UPDATE`) |
| Unannotated | 228 |
| …of those, naming a home story that HAS SHIPPED | **76** |
| …naming no home at all | 149 |

### The root cause, which no previous entry named

**Resolution is recorded in the RESOLVING story's spec and amendments, and almost never written back
onto the ledger entry.** The two oldest entries in this file — the PROTOCOL_VERSION join gate and the
JOINING-state guard — were declared RESOLVED *in this same file* by the 2026-07-18 triage, in prose,
forty lines below themselves. Both shipped, both are tested, and both still read as open to any count.
The same happened one cycle apart at the other end of the file: the top-left liveness/wordmark
collision was resolved by amendment 43 in cycle 101 and never marked.

Compounding it, the file carries **three different annotation styles** (a `status:` key, an inline
`[RESOLVED …]` prefix inside `summary:`, and a separate dated narrative section). That is why every
previous count of this file disagreed with every other one.

### This pass

Every entry whose named home has shipped was cross-referenced against the actual code. **38 annotated:**

- **15 RESOLVED with cited evidence** — the PV join gate (0.2), the JOINING guard (0.3), the transport
  force-disconnect decision (closed by its own recorded revisit condition), the drone-envelope rescale
  (`maxSpeed` 46→40, cycle 94), callsign length-cap AND type-guard (both by `sanitizeName`, whose call
  site cites this ledger by entry number), the settings gear (2.3), the foghorn's lost display surface
  (grew its own chevron, amendment 55), drones gating the win (`checkWin` now reads `isParticipant`),
  the two-place non-combatant predicate (6-3 seam, pinned), the bounty bloom's dependencies (the bloom
  was deleted outright), the storm wall painting through islands (the storm return was deleted), the
  dim-mask texture leak (cycle 98 rewrote the lifetime — and the fix went the OPPOSITE way to the
  suggestion here, so read the note before touching it), 6-5's termination rule (×2 — mis-framed, not
  unpaid), and the lobby-convergence evidence (the queue shipped).
- **3 PARTIAL** — the within-RTT double press (1.10 fixed the stale-predicate variant only), the AR18
  harness (bot leg built, **`loadTest.mjs` still absent** — raised to a Significant Discovery), and the
  chrome bar's right edge (the register shrank when `BOUNTY:` was retired; the clamp is unchanged).
- **7 VERIFIED STILL OPEN** against current code, so the next pass need not re-derive them —
  `degradeAlphaFactor` (still 0.35, three epics old), the never-boarding sealed cohort (the boarding
  grace only arms at `>= minHumans`), the PortalAdapter seam, `latchOwnFire` (**its premise moved**:
  the window was sized against a 3s cannon reload that is now 50s), the solo-create cost vector
  (unruled, Eric), the rebase-renumbering tax, and the `radarHeatmap` flake (not reproduced this run —
  `npm run check` was green end to end at 5106 tests — but one clean run does not close a flake).
- **1 REFRAMED** — `state.net.you` is still never cleared on death, but Story 5-3 deliberately BUILT ON
  that (the results modal derives every field from it *because* it survives death). What was filed as a
  latent bug is now a load-bearing invariant; "fixing" it would break the results modal.
- **2 STANDING RECORDS, NOT WORK** — the hydrophone deferral and the 5.4/5.5 deferral + systems-layer
  ruling. These record why work does *not* exist and should never be counted as open.
- **Superseded/routed** — the human-playtest checkpoint (now four epics old, consolidated in epic-6
  retro action #1) and five doc-drift entries routed to the Story 7-5 batch, which the epic-6 retro
  re-scoped with four further Epic 6 items.

**Result: entries with a shipped home and no adjudication fell from 76 to 38.**

### What is deliberately NOT done here

The **second half** — routing the **149 entries that name no home at all** — is the part with the real
value, and it is not mechanical: it needs a home assigned per entry, and at least 7 of them need a
ruling from Eric rather than an owner. The 38 still-unannotated shipped-home entries are the ones
needing genuine investigation (the gun dead ring, the `hueLatch` revision gap, the `sweepCrossed`
backward step, the permanently-invisible straight torpedo) rather than a cross-reference.

### The mechanism fix this pass argues for

Annotating 38 entries does not stop the file regrowing. **Whatever resolves an entry should mark the
entry** — not merely record it in its own spec. Until that is true, this pass will need running again,
and the next reader will again mistake a marker count for an open-work count.

## 2026-08-18 — Story 7-1 (cycle 103, performance & load pass)

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-performance-and-load-pass.md`
  status: OPEN — needs Eric (beta scope)
  summary: INTEGRATED-ONLY HARDWARE DOES NOT HOLD 60 FPS, and NFR1 as amended does not cover it. Forced onto the reference MacBook's Intel UHD 630, the fully populated NFR1 scenario runs at p50 66.8-83.6 ms (~12-15 FPS) and the omniscient reveal breaches both render (14.3 ms vs 10) and headroom (2.1 ms vs 3.6). On the discrete Radeon Pro 5300M the same build passes every leg with 10.9-13.8 ms of headroom. This is NOT a regression and NOT a failure of amendment 3's `powerPreference` fix — it is what a player gets when their machine has no discrete GPU to ask for, and a large share of laptops are exactly that. NFR1 pins the bar to the reference MacBook and explicitly retired the low-end device, so this sits OUTSIDE the ratified bar and is recorded rather than treated as a breach. The question Eric owns: does integrated-only hardware get a supported path for beta (a resolution cap, a reduced-effects mode, or an honest "recommended hardware" line), or is it out of scope? The lever is NOT pooling/batching — see the fill-rate finding below.
  evidence: `_bmad-output/implementation-artifacts/perf-gate/nfr1-frame-budget-integrated-stress.json`; epic-7 amendments 3 and 4.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-performance-and-load-pass.md`
  status: OPEN — reframes the standing perf assumption
  summary: THE CLIENT IS FILL-RATE BOUND, NOT ENTITY BOUND, AND THREE RETROSPECTIVES ASKED THE WRONG QUESTION. Measured three ways on the reference device: (a) at dpr 2 the NFR1 population (68 hulls, 704 scene nodes) and the readability population (20 hulls, 582 nodes) cost the SAME — 67.2 ms vs 66.7 ms; (b) the same scene at dpr 1 (1.44 M px) costs 16.7 ms against 67 ms at dpr 2 (5.76 M px), with a half-viewport dpr-2 point (2.88 M px) at 49.3 ms; (c) the SHIPPED build's home screen — no match, no harness, just the ambient scene — shows the identical 50.8 ms at dpr 2. CPU callback work was ~7 ms inside a 67 ms frame throughout, i.e. the CPU was idle waiting on the GPU. Consequence for whoever picks up the integrated-hardware item: pooling, batching and decay caps cannot move this number; overdraw (full-screen translucent composites — fog, storm plane, vignette, masked layers) and effective resolution are the only levers. Also worth knowing: a cost-only benchmark structurally cannot observe any of it, which is the standing argument for keeping the headful capture basis rather than reverting to targeted benchmarks.
  evidence: `perf-gate/README.md` ("The reframing" section); epic-7 amendment 5.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-performance-and-load-pass.md`
  status: OPEN — Story 7.8 (named in its AC)
  summary: THE `P` NETCODE TOGGLE SHIPS IN PRODUCTION, CONFIRMED PRESENT IN CODE. `client/src/input/keyboard.ts:360` binds it unconditionally (no `import.meta.env.DEV` guard), wired at `client/src/main.ts:2040`, implemented at `main.ts:555` — it flips own-ship render between predict and interp, calls `predictor.forceSnap()`, logs to console and shows a `NETCODE: PREDICT/INTERP` banner. NFR17 says nothing debug ships and Story 7.8's AC names this toggle explicitly, so it is 7.8's scope and was deliberately NOT fixed here (Story 7.1's spec forbids it by name to keep the perf pass scoped). Recorded now because it was verified in code during this cycle rather than assumed. Note it is the only such toggle: `?stage=worstcase`, `?direct=1` and `?multi=1` are all correctly `import.meta.env.DEV`-gated, and `?homeseed=` is ungated but harmless.
  evidence: verified by the cycle-103 instrumentation inventory; `client/src/input/keyboard.ts:360`, `client/src/main.ts:555,2040`.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-performance-and-load-pass.md`
  status: OPEN — Stories 7.2 / 7.4 must re-measure
  summary: NFR2's HEADROOM IS QUANTIFIED AND THE THIRD-PARTY BUDGET IS NOW A NUMBER, NOT A HOPE. The shipped build reaches interactive home in 2 310 ms on a throttled residential profile (24 Mbps / 30 ms RTT) against the ~10 s budget — FCP 948 ms, bundle 301.8 kB gzipped in one chunk. That leaves ~7.7 s for the ad, analytics and consent scripts that NFR2 as amended now counts against the same budget and that do not exist yet. `client/scripts/loadCapture.mjs` is the instrument and takes an `HC_DIST` snapshot override so a before/after pair can be taken honestly; both 7.2 and 7.4 should re-run it when their scripts land rather than re-deriving a method. Also recorded: the one-chunk 736 kB bundle draws a Vite chunk-size warning on every build and remains unsplit — not a breach at this budget, but it is the first thing to spend if that headroom ever tightens.
  evidence: `perf-gate/nfr2-cold-load.json`; epic-7 amendment 7.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-performance-and-load-pass.md`
  status: OPEN — needs per-subsystem timing before it can be fixed
  summary: THE OMNISCIENT REVEAL BREACHES NFR1'S RENDER LEG — 11.8 ms against 10 ms, measured on the reference device's discrete GPU at dpr 2 with the corrected instrument. The FRAME still holds (total 11.8 ms inside 16.6, headroom 4.8 ms, ZERO dropped frames, cadence a locked 16.7 ms), so there is no player-visible stutter; what is breached is the ratified ALLOCATION of the frame. Two facts bound it. (a) It is the FRAMING, not the roster: re-run at the same framing with the 20-hull readability population instead of 68 it still costs 11.3 ms against 11.8, so whole-disc drawing is the cost and it is nearly independent of how many ships are afloat. (b) It is CPU-side SCENE-GRAPH work — 9.1 ms of the 11.8, against 2.9 ms of draw — and it is NOT a per-frame terrain redraw, because `MapChart.update` re-strokes only on a meaningful zoom change and the reveal zoom is constant across the window. NO ATTRIBUTION IS CLAIMED beyond that: naming the responsible system needs per-subsystem timing, which exists nowhere in the client, and the tempting guess (wakes, one per hull) is the one the 20-hull control already argues against. Whoever picks this up should build the per-system timing first; that instrument is also what the integrated-hardware question needs.
  evidence: `perf-gate/nfr1-frame-budget.json` (omniscient-reveal row) and `perf-gate/nfr1-frame-budget-readability-subset.json` (the 20-hull control); epic-7 amendments 4 and 11.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-performance-and-load-pass.md`
  status: OPEN — mechanism gap, Story 7.8 territory
  summary: NFR17's DEAD-STRIP CHECK IS REAL BUT WIRED INTO NOTHING. `client/scripts/readabilityCapture.mjs --verify-bundle` greps the built assets for `HC_STAGED_WORSTCASE_4_8`, `__hcStage`, `worstcase` and `worstCase`, and it passes — but it is a manual invocation documented in a README, absent from `npm run check`, from `npm run build`, and from `render.yaml`'s buildCommand. Four separate comments in the codebase claim the dead-strip is "CHECKED rather than asserted"; it is checked only when a human remembers to check it. Cheap fix: add it to `npm run check` or to the build script. Related and already ledgered separately: the ungated production `P` toggle means the shipped bundle violates NFR17 TODAY, so this is not a hypothetical.
  evidence: verified during cycle 103 — the script exists and passes; `grep -n verify-bundle package.json render.yaml` finds nothing.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-analytics-consent-and-privacy.md`
  status: OPEN — Story 7.3 inherits the component
  summary: `renderPage`'s document-level CAPTURE-phase ESC listener is never removed on the privacy page, because `mountPrivacyPage()` discards the returned handle and so `destroy()` is unreachable. Harmless for a standalone document that is only left by navigation — but `ui/page.ts` was built in this story explicitly so Story 7.3's How-to-Play can reuse it, and an undisposed capture-phase handler that navigates to `/` is a live hazard the moment the chrome is used as an in-app overlay rather than a whole page. Related and same file: `renderPage` has no guard against being called twice without `destroy()`, which would stack two `inset:0` roots with duplicate ids and fire `onBack` twice per ESC.
  evidence: `client/src/privacy/main.ts` discards the `MountedPage`; `client/src/ui/page.ts`'s ESC binding is added at capture phase on `document` and unbound only by `destroy()`. Review gate, Story 7.2.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-analytics-consent-and-privacy.md`
  status: OPEN — needs an Eric ruling, legally shaped
  summary: NO GLOBAL PRIVACY CONTROL / DO-NOT-TRACK SIGNAL IS READ. The privacy policy originally claimed those settings were "respected"; that claim was FALSE and has been deleted rather than implemented. It is worth a deliberate decision rather than silence: `navigator.globalPrivacyControl` is a legally binding opt-out in several US states, and Hullcracker.io serves them. (PREMISE CORRECTED cycle 108: this entry originally reasoned from R11's data-controller wording, which epic-7 amendment 19 supersedes — the policy no longer names a controller. The GPC question is unaffected: the obligation follows from serving those states, not from who the controller is.) Reading it as a pre-emptive `denied` is a handful of lines in `analytics/consent.ts` and would compose cleanly with Consent Mode BASIC (nothing loads anyway until an explicit grant), but it changes who gets measured and so is not an implementer's call.
  evidence: `grep -rn "doNotTrack|globalPrivacyControl|DNT" client/src server/src` returns nothing. Review gate, Story 7.2.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-analytics-consent-and-privacy.md`
  status: OPEN — presentation, cheap
  summary: The privacy policy renders its contact address and both Google policy URLs as PLAIN TEXT, because `makePageParagraph` uses `textContent`. For a document whose only stated remedy is "write to us", an unclickable address is real friction — and the same limitation means the page cannot link onward to Google's own policy, which it cites twice. Adding a small link helper to `ui/page.ts` would serve Story 7.3's How-to-Play page too.
  evidence: `client/src/ui/page.ts` (`makePageParagraph` sets `textContent`); `client/src/privacy/policyCopy.ts` cites `https://policies.google.com/privacy` and `POLICY_CONTACT` as bare strings. Review gate, Story 7.2.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-analytics-consent-and-privacy.md`
  status: OPEN — sub-floor viewports only
  summary: The consent card has no `max-height` and no scroll surface, and its width floor is `calc(100vw - 48px)` with no minimum. Below the ratified 1366x768 floor — a short landscape phone, a large OS font scale, or a viewport narrower than 48px — the card can outgrow the viewport with its head unreachable, or collapse toward zero width leaving consent unanswerable and the state stuck `undecided` forever. Not a breach of any ratified target, which is why it is ledgered rather than fixed: UX-DR39 sets the floor at 1366x768 and the card is correct there.
  evidence: `client/src/ui/consentBar.ts` `BAR_CSS`. Review gate, Story 7.2.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-analytics-consent-and-privacy.md`
  status: OPEN — inherent to any tag loader, worth knowing
  summary: `ga.ts` sets `ready = true` immediately after appending the script element, which is synchronous and cannot know the remote script actually loaded. With `googletagmanager.com` blocked by an extension or a firewall, `ready` stays true forever, `window.gtag` remains the local queue shim, and events accumulate on `window.dataLayer` with nothing draining them. Bounded in practice (five events per match cycle, and every cycle ends in a page reload), and unfixable without an `onload`/`onerror` handshake that would add its own failure modes — but it means the seam's "no unflushable queue" property does not extend below the shim.
  evidence: `client/src/analytics/ga.ts` `startGa`/`injectTag`. Review gate, Story 7.2.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-mode-subline-removal.md`
  status: OPEN — needs an Eric ruling (wire/shape, not copy)
  summary: `LivenessPayload.queue` NOW HAS NO CLIENT CONSUMER AT ALL. `deadlineAt` already had none after epic-6 amendment 41 cut the countdown; `pooled` and `cap` lose theirs in cycle 105, which deleted the SOLO button's `N/20 QUEUED` sub-line (amendment 50). The block is still parsed and localized by `client/src/net/liveness.ts`, still declared in `shared/src/types.ts`, and still emitted by the server's `/liveness` handler — deliberately left in place, because removing a wire field is a shape decision with no ruling behind it and this cycle's ruling was about on-screen copy. Note the modal's own count comes from `QueueStatusMsg`, not from `/liveness`, so nothing on screen depends on this block today. Whoever takes it should decide between deleting it end to end (a PROTOCOL_VERSION bump, since `LivenessPayload` is wire contract) and keeping it as a deliberate reserve for a future home surface.
  evidence: `grep -rn "\.queue" client/src/net/liveness.ts client/src/ui/home.ts` after cycle 105 — the only remaining reads are the parse/localize at the boundary; `home.ts` reads none of it.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-mode-subline-removal.md`
  status: OPEN — needs an Eric ruling (copy; amendment 41 bars inventing one unasked)
  summary: `epics.md:1197`'s D6 STEER LOSES ITS COPY. D6 asks the port to steer a player toward Solo vs AI when Standard is empty; the constant `STARTS INSTANTLY` sub-line on the SOLO VS AI button was that steer's only words, and Eric deleted it in cycle 105 (*"it doesn't need to be there"*). What remains of the steer is STRUCTURAL: the SOLO VS AI door itself, sitting on its own row directly under the mode row, plus the bottom-left `PLAYERS ONLINE` / `LIVE GAMES` register with its ratified honest zero (amendment 39) — a player looking at `PLAYERS ONLINE: 0` has the fact, just not the sentence. Whether D6 wants a replacement surface at all is Eric's call; amendment 41's process rule forbids authoring one unasked, which is why nothing was written in its place.
  evidence: `_bmad-output/planning-artifacts/.../epics.md:1197` (D6); epic-6 amendments 37 (the steer's origin), 41 (the process rule) and 50 (the deletion).

- source_spec: `_bmad-output/implementation-artifacts/spec-home-mode-subline-removal.md`
  status: OPEN — review-gate finding (cycle 105), needs the same ruling as the `queue` block above
  summary: `parseLiveness` STILL FAILS THE WHOLE PAYLOAD CLOSED ON A FIELD NOTHING READS. `client/src/net/liveness.ts`'s `isQueue` guard nulls the entire `LivenessPayload` when the `queue` block is malformed, and since cycle 105 deleted the SOLO sub-line nothing renders that block at all — so an older, foreign or reshaped server would blank the surviving `PLAYERS ONLINE` / `LIVE GAMES` register for a reason that no longer exists. Cycle 105 CREATED this coupling by removing the last reader, and deliberately did not resolve it: dropping `isQueue` from the gate contradicts that module's ratified all-or-nothing contract (*"a payload is either wholly trustworthy or it is null... there is no partial render"*), and deleting the block end to end is a wire decision. Both need Eric. Resolve together with the `LivenessPayload.queue` entry above — they are one question.
  evidence: `client/src/net/liveness.ts` `parseLiveness` / `isQueue`; raised independently by BOTH review agents at the cycle-105 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-mode-subline-removal.md`
  status: OPEN — latent, pre-existing; the cycle-105 defect class survives on ONE surface
  summary: `ui/queueModal.ts`'s `paintSlot()` is the byte-equivalent of the `paintModeSubline()` cycle 105 deleted — it writes an inline `visibility: 'visible'`, the exact inherited-property mechanism that let the home escape its settings yield. It is NOT exploitable today, and for a structural reason: the queue modal is a `document.body` SIBLING at z 1150, never a descendant of the home root, so no ancestor is trying to hide it. But it is also protected by two INCIDENTAL, UNASSERTED facts — ESC precedence hands the key to CANCEL rather than opening settings, and the modal's own full-bleed backdrop covers the gear. Nothing pins either. Anything that changes the modal's parentage, its backdrop, or its ESC handling re-opens the defect. Cycle 105's structural pin walks the home SUBTREE and therefore cannot see this. A warning comment was added at the function; a pin was not, because the modal has no yield hook to assert against.
  evidence: `client/src/ui/queueModal.ts` `paintSlot`; `client/src/ui/home.ts` `homeYieldStyle` / `setYielded`; raised independently by BOTH review agents at the cycle-105 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-mode-subline-removal.md`
  status: OPEN — latent, pre-existing
  summary: `showHome()` MOUNTS UNYIELDED REGARDLESS OF WHETHER SETTINGS IS ALREADY OPEN. The home takes its yield only via a later `setYielded()` call, so a mount that happens while the settings overlay is visible renders and hit-tests the whole port over the open panel — the same user-visible outcome as the cycle-105 defect, by a different route. Unreachable today only through call ordering (`endSession()` closes the settings overlay before the home is rebuilt on the non-reload path), which is a property of the caller rather than of the home. A `showHome(..., { yielded })` parameter, or a `setYielded(settingsOverlay.visible)` immediately after the handle is assigned in `main.ts`, would make it structural.
  evidence: `client/src/ui/home.ts` `showHome` mount path; `client/src/main.ts` `endSession()`. Raised by the edge-case review agent at the cycle-105 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-home-mode-subline-removal.md`
  status: OPEN — quality-gate gap, project-wide, pre-existing
  summary: ESLINT DOES NOT SEE ANY TEST FILE. `eslint.config.js` ignores `**/__tests__/**`, so `npm run check`'s lint leg covers zero test code across all three workspaces — the complexity-10 rule, unused-symbol detection and every other rule give no assurance there. This matters more than it looks: test files are where a lot of this project's reasoning lives, and cycles routinely land more changed lines in a test file than in the source it pins (cycle 105 itself was ~2/3 test code). "Lint clean" in any cycle report is therefore narrower than it sounds. Un-ignoring them is likely to surface a backlog, so it wants its own pass rather than a drive-by.
  evidence: `eslint.config.js:77` `ignores: [... '**/__tests__/**' ...]`; `npx eslint client/src/__tests__/home.test.ts` returns "File ignored because of a matching ignore pattern". Raised by the adversarial review agent at the cycle-105 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-adsense-h5-games-ads.md`
  status: OPEN — needs a live, configured Google CMP to design against; not resolvable from code alone
  summary: THE SETTINGS "ANALYTICS" ROW CANNOT TELL AN EEA VISITOR THE TRUTH. With no local override stored it renders ON, which is correct under the granted GLOBAL default but FALSE for anyone matched by the EEA/UK/CH region default, where all four signals are denied until Google's CMP is answered. The client has no way to know which case it is in — there is deliberately no geo lookup (amendment 14 R6), and nothing reads the CMP's actual consent state back. Fixing it properly means reading Google's own state (`__tcfapi` / `googlefc`) and rendering a third "managed by Google's dialog" state, which is a new vendor integration rather than a tweak. Cycle 107 knowingly shipped the ON default because amendment 16 ruled the row must report what is actually being measured, and ON is right for the large majority.
  evidence: `client/src/ui/settings.ts` renders `c.granted() !== false`; `client/src/main.ts` maps `undecided → null`; `client/src/analytics/consent.ts` `consentRegionDefaults()` denies all four for 32 regions. Found independently by both review agents at the cycle-107 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-adsense-h5-games-ads.md`
  status: OPEN — safe direction (measurement loss only), wants verification against a live CMP
  summary: NOTHING RE-ASSERTS THE LOCAL ANALYTICS OVERRIDE AFTER GOOGLE'S CMP SETTLES, and the injected consent block carries no `wait_for_update`. Our defaults and the stored override are sent synchronously at boot; `adsbygoogle.js` loads async and a RETURNING consented EEA visitor's stored TCF choice is applied afterwards, so whichever update lands last wins at the signal level. Funnel events are still suppressed by the local `denied` check, so the practical leak is bounded — but the signal state can disagree with the player's setting, and hits fired before the CMP restores go out as cookieless pings. Google's Advanced-mode guidance recommends `wait_for_update` for exactly this; it was NOT added because its timing effect cannot be verified without a configured CMP, which does not exist until the AdSense dashboard flags are set.
  evidence: `client/src/analytics/index.ts` `boot()`; `client/src/ads/adsHead.ts` `consentDefaultsScript()`. Raised by both review agents at the cycle-107 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-adsense-h5-games-ads.md`
  status: OPEN — cannot be verified until Google's CMP actually renders on the site
  summary: GOOGLE'S CONSENT DIALOG SITS OUTSIDE THE RATIFIED Z-LADDER. The `consent.zIndex: 1250` rung was retired with the self-built card, but the constraint that produced it has not gone away: every rung above home (queue modal 1150, class bay 1200) is `position:fixed;inset:0` and hit-tests every pixel even when it paints nothing, so a consent control below them is visible but unclickable. The surface subject to that is now Google's CMP, which picks its own z-index with no coordination and is not dismissible by the game's ESC handling. Verify the rendered z-index against the ladder once the CMP is live, or suppress the game's `inset:0` layers while it is open.
  evidence: `client/src/config.ts` z-ladder note; the retired `consent.zIndex` rung and its rationale in cycle-104's amendment 14 R14. Raised by the edge-case review agent at the cycle-107 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-adsense-h5-games-ads.md`
  status: OPEN — narrow, pre-existing shape made newly consequential by Advanced mode
  summary: A STORAGE THROW NOW FAILS OPEN INSTEAD OF CLOSED. `loadConsent()` returns `'undecided'` on any `localStorage` failure, and under Consent Mode BASIC that meant "measure nobody". Under ADVANCED it means "follow the granted global default", so a player who explicitly set ANALYTICS OFF and later hits a quota-full, blocked or private-mode `getItem` silently reverts to measured with no signal. The fix is to distinguish an `'unavailable'` read from a genuine `'undecided'` and treat the former as denied; it was not taken in cycle 107 because it changes the meaning of a value read on every boot and deserves its own pass rather than a drive-by at a review gate.
  evidence: `client/src/analytics/consent.ts` `loadConsent()` catch; `client/src/analytics/index.ts` `dispatch()`. Raised by the edge-case review agent at the cycle-107 gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — the feature is SHIPPED and ENTIRELY UNMEASURED; only play can close it
  summary: THE RADAR BUOY IS UNEXERCISED BY EVERY INSTRUMENT THE PROJECT HAS. Zero buoys were deployed in 2 600 bot-matches: `server/src/game/ai/tactics.ts` says so in its own comment — the buoy is now a CLICK-PLACED weapon on the mine's rear sector (R2.7), not an `actSeq` ability, and *"its tactics belong with the buoy itself and are a later agent's"* — while the scripted captain pilots fire slot 0 only. So the relay (R2.8), the jamming density (`jamFakes = 10`, a `[DRAFT]`), the autonomous gun (R2.21), the 20s life on a 30s reload and the destructible 50 hp hull have never been generated, delivered or acted on. It also DEPRESSES the Mine Layer's measured numbers by whatever fraction of its ~82 picks per 1 000 bot-matches went into a weapon it never used. Two ways to close it: give the bots buoy tactics (which also unblocks the next balance campaign), or take it from Eric's own play.
  evidence: `batch-sim-evidence-7-5-2026-08-19.md` Q3 / "What this pass could NOT measure" item 1; `server/src/game/ai/tactics.ts`. Story 7-5 evidence pass, 2026-08-19.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — bot-tuning finding, structural; biases every per-class balance number
  summary: BOTS NEVER FIT AN ACQUISITION CARD — 0 fits out of 2 495 offers — AND IT IS A POLICY GAP, NOT A COINCIDENCE. An acquisition card carries its TARGET equipment's category (`EQUIPMENT_CATEGORY[equipmentId]`), a bot profile's weight table only names categories that profile's hull ALREADY carries, and an unnamed category scores `UNLISTED_SCORE = 0.5` — below every real weight. A bot can therefore only take an acquisition when the WHOLE HAND is unlisted, which the universal lines make almost impossible. Consequences: **~12 % of every offer hand a bot sees is dead to it**, and **no bot ever fields a third weapon**, so every per-class number in a bot campaign is measuring the AI's gap as much as the catalog. The captain campaign is the control that proves the CARDS are fine — acquisitions convert at 0.50–0.60 fits per offer, the highest conversion of any tier. Fix before the next balance campaign is trusted to per-class resolution.
  evidence: `batch-sim-evidence-7-5-2026-08-19.md` Q3 "Never picked"; `CONFIG.bots.boonWeights`, `UNLISTED_SCORE`. Story 7-5 evidence pass, 2026-08-19.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — consequence of R2.7, documented in-code, needs an Eric ruling to change
  summary: A RADAR BUOY IS AN ORDINARY ORDNANCE SUBJECT, SO IT ATTRACTS AND ABSORBS FRIENDLY FIRE. Two live consequences, both flowing from the single decision that made "destructible by anything that damages a ship" true by construction: (1) an ACOUSTIC HOMING torpedo locks onto a buoy INCLUDING ONE ITS OWN OWNER PLACED, because the owner exclusion keys on the SHIP id and a buoy is not a ship; (2) your own shells can INTERCEPT your own buoy, taking the interceptor's contact damage and stopping the shot. Neither is a defect against any ruling — a buoy is a real object in the water — but both are player-facing surprises with no cue, and the homing case in particular can waste a fish on your own equipment. Whether either wants an owner exclusion is a design call.
  evidence: `shared/src/sim/shell.ts:99-105` (the homing note, written when the behaviour changed); `server/src/game/world.ts` `hitBuoy` call sites (burst, interception, blast). Story 7-5 wave 2, R2.7.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — presentation gap; the disclosure is correct, the render under-serves it
  summary: A RELAYED RETURN RENDERS ALONG THE OWNER'S RAY. The server gates a buoy's returns correctly — island LOS and the height-aware shadow march run FROM THE BUOY (R2.8) — but the CLIENT's scope march runs from the local hull, so a contact the buoy paints from around an island can render at speck intensity for a viewer whose own line to that water is blocked. That is the exact case the relay exists for, and it is the case that reads weakest. Nothing leaks and nothing is wrong on the wire; the fix is a render-side question (march relayed cells from the relaying buoy, or exempt them from the owner-anchored attenuation) and it interacts with the near-range dim mask (epic-4 amendment 181) and with amendment 83's "never re-evaluate a frozen paint", so it wants its own pass rather than a drive-by.
  evidence: `client/src/render/radar.ts` beam march origin; `client/src/__tests__/blipProvenance.test.ts` ("a relay from ACROSS the map paints exactly where its cells say"). Story 7-5 wave 2 review gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — needs an Eric ruling (wire shape, would bump PROTOCOL_VERSION)
  summary: `BuoyView` CARRIES NO HP CHANNEL, SO A KILLED BUOY IS INDISTINGUISHABLE FROM AN EXPIRED ONE. The buoy has 50 HP and is destructible by anything that damages a ship, but the wire shape carries only `{id, x, y, until, own, by}` — no damage state, no death cause. The owner sees their buoy vanish and cannot tell whether it timed out or was shot, which is exactly the information that would tell them someone is nearby and hunting their sensors. The type's own doc calls this out deliberately: adding a damage-state channel is a wire decision, not an implementation detail. Decide between leaving it silent (a buoy is cheap and disposable) and adding a channel (a kill is intel worth having).
  evidence: `shared/src/types.ts:1128-1141` (`BuoyView` and its preceding NOTE). Story 7-5 wave 2.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — a potential tell that partially defeats the feature
  summary: JAMMING FAKES ARE NOT WATER-FILTERED, SO A FALSE RETURN CAN LAND ON AN ISLAND. `scatterJamFakes` scatters points across the buoy's 330u circle with no land test, and nothing else in the return grammar can paint a ship-shaped return on dry land. A player who knows the map — and both sides rebuild it deterministically from the seed — can therefore discard some fraction of the fakes by inspection, which is the one thing R2.11's "wire-indistinguishable from real blips" was written to prevent. Not a leak (a fake still discloses nothing real, so the perception carve-out is unaffected) and not necessarily worth fixing at 10 fakes, but it is a real erosion of the denial the card is sold on. The fix is a rejection sample against the same island geometry every other consumer uses.
  evidence: `server/src/game/world.ts` `scatterJamFakes` call site; `shared/src/constants.ts:1389` `jamFakes: 10`. Story 7-5 wave 2 review gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — design question, not a defect; measured and safe either way
  summary: `barrelSpacingU` 12u IS SMALLER THAN THE 15u BURST RADIUS, SO BARREL IS A DAMAGE MULTIPLIER RATHER THAN A PATTERN. The three burst circles always overlap, so any hull covering the clicked point is inside all three and a maxed 3-barrel click is a single 45 hp hit that forgives ~10–15u of aim error. Measured, not inferred: `maxGunOnlyTick` is exactly 45.0 on EVERY hull type across three campaigns, and 168 full-health 45 hp small drones were killed by one such click in the `endgame` campaign. **The class-hull guardrail is untouched** — 0 one-tick-from-full class kills in 940 — and the deletions moved the ceiling the safe way (max click fell from 72 % of a Torpedo Boat to 36 %). The question is Eric's: if BARREL is meant to read as a visible spread (his words: *"fires +1 bullet at once… in parallel lines"*) the spacing must exceed the 30u burst DIAMETER; if it is meant to be a damage upgrade that forgives aim error, 12u is already correct.
  evidence: `batch-sim-evidence-7-5-2026-08-19.md` Q4 + Q5 (`balanceProbe.ts` BARREL PARALLEL TRACKS block). Story 7-5 evidence pass, 2026-08-19.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — ACCEPTED as shipped by Eric (R2.19); revisit only on a specific trigger
  summary: THE CAPTIVE MINE'S TORPEDO HAS NO MAX RANGE. It inherits base `CONFIG.torpedo` behaviour, which runs until impact, so a missed fish crosses the map until it hits something or reaches the rim. Eric ruled it accepted rather than an oversight — *"sounds fine to me, until I start adding torpedo max ranges or shit like that"* — so no cap was added. Ledgered as THE FIRST THING TO REVISIT if a torpedo max-range mechanic is ever introduced; whoever adds one must decide the captive fish's budget in the same change rather than letting it inherit silently.
  evidence: `plan-7-5-wave-2.md` R2.19; `server/src/game/equipment/mines.ts` captive launch (the fish is marked by `targetX === null && burstRadius > 0`). Eric ruling 2026-08-19.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — orchestrator calls flagged to Eric, both shipped as defaults and both one line to flip
  summary: TWO GUN-BUOY BEHAVIOURS WERE DECIDED BY THE ORCHESTRATOR, NOT BY ERIC. (1) **A buoy's gun hit aggros NOBODY** (R2.21a) — decided by the established precedent in `drones.ts` that a mine hit does not acquire its layer *"because a mine's layer may be dead or across the map"*, which is exactly a buoy's case. The alternative, the drone acquiring the buoy's OWNER, was rejected as "an auto-turret picks fights its owner then inherits" and is unreachable under the existing model anyway (the attacker is not a `ShipRecord`). (2) **A gun buoy does NOT shoot a rival buoy** — Eric's *"anything it sees that isn't the owner"* reads as yes, since buoys are destructible 50 hp entities, but it was defaulted to NO because buoy-vs-buoy turret duels resolve with no player input at all and the buoy's stated job is watching water, not area denial. Both are open to reversal.
  evidence: `plan-7-5-wave-2.md` R2.21a and its "Open, flagged to Eric, defaulted for now" block; `server/src/game/world.ts` buoy gun target selection. Story 7-5 wave 2.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — Eric's call on a balance direction; measured, with the confound named
  summary: THE BATTLESHIP LOST ~26 % OF ITS OUTPUT AND ITS TOP SEAT, AND NO REBALANCE WAS TAKEN. Wins 52 % → 34.6 % over 130 bot-matches; kills −26 %, damage −26 %, levels −21 %. It is concentrated in the broadside-led `siege` profile (kills −44 %), whose thesis is standoff at intel range and whose weight table still buys `intelRange` at 2.4 while its main weapon is now capped at the 5/8 rung (412.5u base vs the gun's 660u). **THE PROFILE WAS NEVER RETUNED FOR THE NEW WEAPON, so −44 % is an UPPER bound on the weapon's own loss** and the harness cannot separate "the weapon is weaker" from "the AI is mis-tuned for it". The field also got FLATTER (35/21/45 vs a BEFORE where one class took 52 % and the Mine Layer was a 12 % also-ran — the exact cycle-96 complaint), which may be the intended trade. Two things to do before this is read as a balance verdict: retune `siege` for a 5/8-rung weapon, and give the bots buoy tactics + an acquisition weight (the two entries above).
  evidence: `batch-sim-evidence-7-5-2026-08-19.md` Q2 and Findings #2/#5. Story 7-5 evidence pass, 2026-08-19.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-upgrade-cards-v2.md`
  status: OPEN — a ratified tunable that no longer does what it was tuned to do
  summary: `deck.rareWeightPerDryLevel = 0.7` WAS TUNED AGAINST A 53–58 CARD DECK AND THE DECK IS NOW 41. Deck lifetime fell 39 % — an economy played to exhaustion runs exactly 44 draws AFTER against 72.2 (55–105) BEFORE, with zero variance, because 44 is arithmetic rather than luck. The escalating soft pity now INVERTS past dry=3: rare-landing rate runs 46 / 51 / 48 / 35 / 21 / 9 / 3 % as dry levels climb, where BEFORE it ROSE 57 % → 68 %. A 41-card deck simply runs out of rare copies. Real matches rarely reach that far (gunner picks p50 3 / mean 4.5; bots mean 2.4), so this is a tail property of the deck-only MODEL rather than a live problem today — but the constant is ratified (cycle 39) and its premise is gone, so it should be re-derived rather than left to be rediscovered.
  evidence: `batch-sim-evidence-7-5-2026-08-19.md` Finding #7 and Q1's deck-only rows; `CONFIG.deck.rareWeightPerDryLevel`. Story 7-5 evidence pass, 2026-08-19.
