# Epic 8 Context — Ratified Amendments (durable)

This file is the DURABLE home for every ratified correction to Epic 8's context: Eric rulings, review patches, adjudicated contradictions. `epic-8-context.md` is regenerable and anything living only there is erased on recompile; on any conflict between an entry here and planning-artifact-derived content, the entry here WINS. Append-only / deliberate-edit only — no tooling step may regenerate, prune or rewrite it.

Format: numbered, dated, source-attributed. "Supersedes" names the exact planning-artifact clause it overrides.

---

## Story 8.0 — Colyseus 0.18 Upgrade (cycle 134, 2026-09-14)

1. **`@colyseus/core` 0.18.13 and `@colyseus/schema` 5.0.32, not the AC's 0.18.12 / 5.0.27.** Eric ruling 2026-09-14 (AskUserQuestion, "Latest patches"). The story's pinned target set was registry-verified on 2026-09-09; core 0.18.13 shipped 2026-09-12 as a security fix in exactly the area the staging gate lives (*a room's `onAuth()` and plugin `onAuth` hooks now run when a client joins with a valid `@colyseus/auth` token*), and schema had moved to 5.0.32. Eric took the latest patches under his standing *"keep everything CURRENT"* ruling. **Supersedes** the version literals in epics.md Story 8.0 AC, AR19, and game-architecture.md D10 for those two packages ONLY; every other package installs at the version the AC lists (`colyseus` 0.18.5, `@colyseus/tools` 0.18.3, `@colyseus/sdk` 0.18.2, `@colyseus/auth` 0.18.2, `@colyseus/database` 0.18.3, `@colyseus/admin` 0.18.5, `@colyseus/monitor` 0.18.3, `@colyseus/playground` 0.18.4). The actually-resolved set is recorded in `spec-8-0-colyseus-0-18-upgrade.md`.

2. **The three unmounted Epic 9 modules (`@colyseus/auth`, `@colyseus/database`, `@colyseus/admin`) live in `server/package.json` `dependencies`, not `devDependencies`.** Eric ruling 2026-09-14. They are production modules in Epic 9; installing them now proves the whole 0.18 set (including the `drizzle-orm` 1.0.0-rc.2 pre-release inside `@colyseus/database`) resolves together before any account code exists. Accepted cost: a few unused packages in the production image until Story 9.1 mounts them. Nothing is mounted, routed, or configured by Story 8.0 — no env var, no `render.yaml` change.

3. **`postgres` and `drizzle-kit` are DEFERRED to Story 9.1 — Story 8.0 does not install them.** Eric ruling 2026-09-14. **Supersedes** the *"`postgres` 3.4.9 and `drizzle-kit` 0.31.10 present"* clause of the Story 8.0 AC and the matching line in AR19 / D10. Reason: nothing in 8.0 runs a migration or opens a database socket, so "present" bought nothing; and the AC's `drizzle-kit` 0.31.10 is the 0.44-era kit while `@colyseus/database` 0.18.3 hard-pins `drizzle-orm` 1.0.0-rc.2, whose matching kit is the `1.0.0-rc` line — installing a mismatched kit now would have handed 9.1 a trap. Story 9.1 installs both at the versions verified against the ORM the module actually ships with; the mismatch is ledgered in `deferred-work.md`.

4. **Measured facts that correct the story text (no ruling needed — recorded so no implementer re-derives them).** (a) The AC's *"~26 Colyseus-importing test files"* is 11 test files (10 server, 1 client) plus 15 headless smokes in `server/scripts/`; none boots a real server, and there is no `@colyseus/testing` dependency. (b) `setSimulationInterval` still exists in 0.18 as a deprecated forwarder to `setTimestep`; the AC's conversion is nonetheless made for real at its one call site. (c) `setMetadata` moved from shallow-merge to full REPLACE; both rooms already write whole listing-meta objects, so the only change is comment honesty plus a pin. (d) `Client#id` has no use in this repo. (e) `@colyseus/tools` `config()`/`listen()`, `matchMaker.*`, `IRoomCache`, `ISeatReservation`, `static onAuth(token, options, context)`, `allowReconnection`, `ClientState`/`CloseCode`/`ErrorCode`, `createRouter` and the reconnection-token format are byte-identical 0.17.44 → 0.18. (f) `colyseus.__globalEndpoints` is REMOVED in 0.18 — `liveness.test.ts` read it. (g) The 0.18 JOIN_ROOM handshake is a wire break a 0.17 client fails BEFORE `protocolVersionError` runs, so server and client must deploy together; the PV bump (49 → 50) is still made because the schema encoder changed.

## Story 8.1 — The Card Model and Catalog Engine (cycle 135, 2026-09-15)

5. **Interim state between 8.1 and the content stories is "stay playable".** Eric ruling 2026-09-15 (AskUserQuestion, recommended option). Story 8.1 ships an interim deck of every catalog line whose mechanism already exists (the five ladders and the deck-gun family with catalog-v3 numbers; `heavyTorpedo` / `navalMines` / `broadside` / `starShells` slot fills; the add-ons whose doctrine verbs exist). Lines for weapons and consumables that later stories build are authored in full shape (copy 1 + empty tiers) but carry `stub: true` and are EXCLUDED from `buildDeck()` and therefore from every offer until their story flips the flag. Rejected: "engine only, degraded staging" and "author every v3 tier now". **Supersedes** nothing; fills a gap the epics leave open (the AC allows stubbing but says nothing about the draw pool).

6. **Legacy equipment ids: `torpedo` → `heavyTorpedo`, `mine` → `navalMines`.** Eric ruling 2026-09-15. The shipped single torpedo module becomes the HEAVY line and the shipped mine module becomes NAVAL MINES; Story 8.13 adds `lightTorpedo`, `supercavTorpedo` and `captiveMines` as new modules. Rejected: `torpedo` → `lightTorpedo`; keeping legacy ids until 8.13.

7. **The 29 `LINE_IDS` are locked** (Eric ruling 2026-09-15): `armor, speed, turning, radarSweep, reload, deckGun, deckGunTurret, deckGunBarrel, lightTorpedo, heavyTorpedo, supercavTorpedo, navalMines, captiveMines, missile, machineGun, flak, monitor, broadside, starShells, hullRepair, shieldBlock, smokeScreen, chaff, decoyBuoy, acousticHoming, foulingMines, heatSeeking, dazzleShells, phosphorShells`. The 18 equipment + consumable ids match AR56's server registry list byte-for-byte; display names live only in client copy (AR29). Changing an id is a wire change (PV bump).

8. **Interim refit card (until 8.6/8.7): kind word in neutral colour.** Eric ruling 2026-09-15. Rarity and category die in 8.1; the CURRENT refit card's meta row shows the v3 kind word (`WEAPON · UPGRADE · ADD-ON · CONSUMABLE`) plus the copy count, no tier-ramp colour, and the hotbar's per-slot `◆n` category count is removed. Rejected: applying DESIGN.md's tier ramp now; a blank meta row. Story 8.7 owns the real card face.

## Interstitial cycle 136 — Version scheme change (2026-09-15)

9. **The `0.17.X` version freeze is LIFTED: one-time move to `0.18.1`, then the patch number keeps counting landed build cycles across epics.** Eric ruling 2026-09-15 (AskUserQuestion, "0.18.1 once, then keep counting cycles"). Cycle 136 = 0.18.1; the next landed cycle is 0.18.2, and so on, with NO epic tie (rejected: minor-per-epic; real semver). `VERSION` + root `package.json` stay the single source, injected into the client by Vite. **Supersedes** the 2026-08-01 ruling "versioning is frozen at 0.17.X until all epics complete" (CLAUDE.md, project-context.md — CLAUDE.md is hook-frozen and now carries a stale line; it is NOT edited).

## Story 8.2 — Legal Decks, Default Decks, and the Door (cycle 137, 2026-09-15)

10. **DEFAULT DECK COUNTS.** Eric ruling 2026-09-15 (AskUserQuestion; delivered as a spreadsheet screenshot, transcribed here exactly). Every hull carries the same 30 universal cards: `armor 3, speed 3, turning 3, radarSweep 3, reload 3, hullRepair 3, shieldBlock 3, smokeScreen 2, chaff 2, deckGun 2, deckGunTurret 1, deckGunBarrel 2`. On top of those: **Torpedo Boat** `lightTorpedo 3, heavyTorpedo 3, machineGun 3, acousticHoming 1`; **Mine Layer** `navalMines 3, captiveMines 3, flak 3, foulingMines 1`; **Battleship** `missile 3, monitor 3, starShells 3, dazzleShells 1`. Each deck sums to 40 with exactly three equipment lines; every count is at or under its catalog cap. Five lines are UNHOMED (in no default deck): `supercavTorpedo, broadside, decoyBuoy, heatSeeking, phosphorShells`. Line composition follows FR56 except that the Battleship no longer carries HEAT SEEKING. **Supersedes** FR56's counts and catalog-v3 §1's starter counts. `DEFAULT_OWNED` (a fresh account's unlocks) is the union of the three decks' line ids — 24 of the 29 lines.

11. **INTERIM: the default decks ARE the frozen deck now; stub lines stay in the list but are never dealt.** Eric ruling 2026-09-15 (AskUserQuestion). From Story 8.2 on, a hull's default deck is the 40-card deck it sails; a stub line (amendment 5 — mechanism not built yet) stays in the frozen 40 so the legality check and the door see the real deck, but `buildDeckState` never deals it into `DeckState`, so it can never reach an offer. That leaves 23 drawable cards per hull (40 − 16 stub cards − the one carried copy) until Stories 8.7–8.15 flip the stub flags, at which point the same decks fill out with NO further code change. Rejected: keeping Story 8.1's interim "every buildable line at cap" deck. **Supersedes** nothing; fills the gap between 8.1's interim deck and the content stories.

12. **DRONES STAY GUN-ONLY — `FLEET_FIT` is STRUCK from the epic.** Eric ruling 2026-09-15 (AskUserQuestion). Eric: *"I don't know what that FLEET_FIT thing is, that should not have been added to the epic. It was never even discussed with me."* Epic-5 amendment 34 stands (*"each has a gun to defend itself"*): a PvE fleet hull carries its deck gun and nothing else, gets no deck, no cards, no `DeckState` (the frozen `EMPTY_DECK` and an empty `deckList`, pinned). Nothing named `FLEET_FIT` is built in 8.2 or any later story. Correction of record against epics.md Story 8.2 AC, Story 8.5 AC, FR45, AR38 and AR52 wherever they mention a `FLEET_FIT` — those clauses are void.

## Story 8.3 — The Draw (cycle 138, 2026-09-15)

13. **THE DRAW MODEL IS CONFIRMED AS "RESHUFFLE AFTER EVERY DRAW".** Eric ruling 2026-09-15 (AskUserQuestion). Eric's words: *"fairly from what is left in the deck"* means the deck reshuffles after every draw — it does NOT need to exhaust before unchosen cards re-enter the pool. The shipped model already does this: a draw READS the pool at full weight and only a FIT removes one card, so a passed-on card is back at full weight next level with no exhaustion step in between. Story 8.3 pins this reading and adds the missing pieces (the at-cap guard, the once-per-ship exhaustion callback, the `/metrics` counter); no new draw engine is built. **Supersedes** nothing; confirms `epic-8-context.md`'s "Draw economy" paragraph as already correct and closes the ambiguity a fresh reader could take from "fairly from what is left."

14. **AN EXHAUSTED DECK PRESENTS NO OPTIONS, BUT THE LEVEL STILL BANKS.** Eric ruling 2026-09-15 (AskUserQuestion). Eric's words: *"When you have expended all your cards, you are no longer presented with options."* An empty draw banks the level (the chip counts it), materializes no offer, queues no `pt` event, and shows no TAB cue. It stays spendable only via the menu heal — client-unreachable today (`deferred-work.md:982`) — until Story 8.8 makes heal a card. The "still banks" option was taken; rejected: "does not bank" and "open the band with heal only". **Supersedes** nothing; resolves the open re-derivation question left at `deferred-work.md:1995`.

15. **CORRECTION OF RECORD, DEFERRED TO STORY 8.8.** Eric ruling 2026-09-15 (AskUserQuestion). Eric's words: the free per-level heal "has been gone a while, it was replaced with a 1% missing HP per second while out of combat (over 30s since the last time you took damage)." Measured fact: `development` at `b18fba6` still carries `CONFIG.damageControl.levelMissingPct: 0.1` and `levelRegenMs: 5000` — the per-level, 10%-of-missing-hp-over-5-seconds heal — and no out-of-combat regen mechanism exists anywhere in shipped code or in any planning doc. Story 8.3 does NOT touch the level heal; Story 8.8's AC already carries a "check which version is live" clause, and that check must be resolved WITH Eric before either version is built. **Supersedes** nothing; ledgers the discrepancy for Story 8.8 (see `deferred-work.md`'s 2026-09-15 Story 8.3 section).

## Story 8.4 — The Damage Gate and the Ordnance Collector (cycle 139, 2026-09-15)

16. **SHELLS AND BURSTS DETONATE ANY ARMED MINE; CAPTIVE MINES STAY IMMUNE.** Eric ruling 2026-09-15 (AskUserQuestion). "Shooting ANY mine detonates it" (FR57, AR44) is read as: a shell or a burst sets off any mine on the water — the shooter's own or anyone else's — provided the mine has finished its arm delay; a mine still arming is not set off by a shell, and a CAPTIVE mine (the torpedo-launching kind, R2.18 / epic-7 ruling 2026-08-19) is never set off by a shell or a burst. Rejected: "any mine, armed or not, captive included"; "any armed mine, captive included". **Supersedes** the shipped owner-only gate in `detonateMinesInBurst` (own mines were the ONLY mines a burst could detonate); keeps the R2.18 captive carve-out.

17. **A MINE IS A POINT TARGET FOR A SHELL.** Eric ruling 2026-09-15 (AskUserQuestion, recommended option). A shell detonates a mine when its path passes within the SHELL's own radius (`CONFIG.<gun>.shellRadius`) of the mine's centre; no new mine-size number exists. A burst detonates a mine when the burst radius covers the mine's centre (unchanged rule). Rejected: the mine's trigger radius; a new CONFIG hit radius.

18. **A MINE BLAST CHAINS INTO ANY MINE IN RANGE, WHOEVER LAID IT.** Eric ruling 2026-09-15 (AskUserQuestion, recommended option). One detonation sets off every ARMED, non-captive mine within its blast radius regardless of owner, resolving the same tick through the existing visited set. Rejected: same-owner chains only (the shipped rule). Captive mines keep R2.18 in full: they neither receive nor propagate a chain (consistent with amendment 16). **Supersedes** the same-owner condition in `chainMines`.

19. **A BURST OVER A HULL THAT SANK THIS TICK MARKS `hc`, NOT `sp`.** Eric ruling 2026-09-15 (AskUserQuestion, recommended option) resolving `deferred-work.md` `:590`. When a multi-barrel click's first shell sinks a hull and a later shell of the same tick bursts over the wreck, the burst counts the wreck as a GEOMETRIC victim for the hit-call mark only: `hc` is emitted, no damage is dealt, nothing else changes. The zero-damage star shell keeps its exclusion (geometric victims never count for it — no unsanctioned detection channel). Rejected: keep `sp` and pin it as accepted. Refines the Story 4.3 `hc`/`sp` grammar (epic-4 amendments 16–18) for this one case.

20. **A MINE IS NEVER A COLLISION SUBJECT FOR A SHELL IN FLIGHT — ONLY THE BURST AT THE CLICKED POINT CAN SET ONE OFF.** Eric ruling 2026-09-16, verbatim: *"If I did not DIRECTLY click on the mine, then UNDER NO CIRCUMSTANCES WHATSOEVER SHOULD IT BLOCK A SHOT, REGISTER A HIT OR MISS, OR GIVE ANY INDICATION WHATSOEVER TO THE SHOOTER THAT ANYTHING MIGHT BE THERE. AT ALL."* A shell passing over a mine on its way to the aim point flies on untouched: the mine does not detonate, the shell is not consumed, no boom, no `hc`, no `sp`, nothing. A mine detonates by gunfire ONLY when the burst at the point the shooter clicked covers the mine's centre (the click IS the shooter's own information; the resulting mine boom stays sight-gated). Amendment 16's "shell or burst" therefore reads "burst"; amendment 18 (chains) and the captive/arming carve-outs stand. **Supersedes** amendment 17 (the in-flight point-target rule) in full and the Story 8.4 review ruling that a mine-consumed shell emits no mark (there is no mine-consumed shell any more).

## Story 8.5 — Nine Slots (cycle 140, 2026-09-16)

21. **INTERIM SPAWN SEED: A HULL SPAWNS WITH TODAY'S CARD-BACKED CLASS WEAPONS APPLIED AS CARDS.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). `loadoutFor()` is identical for every hull (gun + boost + seven empty), but until Story 8.10 hands out the level-zero card the spawn applies, through the ordinary card path, the card lines today's fit carries: Torpedo Boat `heavyTorpedo` (→ Q), Battleship `broadside` (→ Q) and `starShells` (→ E), Mine Layer `navalMines` (→ Q). The deck loses those copies exactly as today's carried seed does (`spawnDeck`/`carriedLines` read the seed from a per-hull list instead of the fitted loadout). Rejected: "truly empty until the first draw" (an interim gameplay hole); "one weapon per hull". Story 8.10 removes the seed. **Supersedes** nothing; fills the gap between the 8.5 AC's identical loadout and UX-DR54's "nothing from the deck is aboard yet", which 8.10 owns.

22. **THE RADAR BUOY GOES DARK FROM 8.5 UNTIL 8.15 DELETES IT.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). No card fits the legacy `radarBuoy` id and the loadout no longer fits it per hull, so no ship can reach it in play; its module, config row and tests stay (tests fit it directly). The Mine Layer loses the buoy now. Rejected: an interim Mine-Layer-only fit; deleting the module in 8.5.

23. **EVERY HULL GETS THE BOOST IN SLOT 1 NOW, AT THE SHIPPED `speedBoost` NUMBERS.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). Slot 1 holds the existing `speedBoost` module (+10 u/s, 6 s, 18 s reload, one charge) on every captain hull — the Battleship and the Mine Layer gain a boost they never had. Story 8.9 renames it to the v3 `boost` id and re-tunes it. Rejected: Torpedo Boat only until 8.9.

24. **PvE FLEET DRONES ARE ENVIRONMENT: THE DECK GUN AND NOTHING ELSE, BY THE LEAST-WORK PATH.** Eric ruling 2026-09-16, first verbatim: *"PVE FLEET DRONES DO NOT GET EXTRA SHIT. THEY ARE NOT PLAYER SHIPS. THEY DO NOT ACQURE LEVELS. THEY DO NOT GET WEAPONS BEYOND THE DECK GUN."* — then clarified in the same session: *"They don't need slots for PVE ships unless it is more work to create them without it. That's really what I was getting at. As I said, they are NOT combatants. They are part of the environment. They work the way that they do and that's it. They don't need special consideration."* Reading of record: drones get no design attention; the spawn's EXISTING fleet branch (the one that already hands a fleet hull the empty deck, amendment 12) also hands it a gun-only slot array (slot 0 the gun, the other eight empty) — one flag on the loadout builder, no new drone tests, no drone pins beyond the ones that exist. Extends epic-5 amendment 34 and epic-8 amendment 12. Rejected: an identical captain array with an unused boost (would need a "never presses Shift" pin — more consideration, not less).

25. **THE INTERIM HOTBAR KEEPS TODAY'S PITCH AND MAY CLIP ON SMALL VIEWPORTS.** Eric ruling 2026-09-16, verbatim: *"Ignore it entirely. The next story fixes the HUD."* Nine rows at the shipped 62 px slot / 14 px gap make a 670 px column that overruns the 614 px floor viewport and reaches the top edge at 768 px; no geometry work is done in 8.5 — Story 8.6's bar replaces it. Rejected: a tighter single column; two columns.

26. **A KEY ON AN EMPTY SLOT IS DENIED ON THE CLIENT ONLY — NO WIRE DENIAL REASON.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). The slot flashes its denied pulse and the denied tone plays; nothing is sent (a fair client cannot reach the server's `'empty-slot'` case, which stays server-internal as today). Under the held start line the lock wins and the key stays feedback-free. Rejected: a new `'empty'` `DenialReason` on the wire.

27. **DIGITS `1`–`4` WITH THE REFIT WINDOW CLOSED STAY INERT UNTIL STORY 8.7.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). The belt cannot be stocked before 8.7, so the digits keep their refit-only meaning in 8.5; 8.7 wires belt firing and the post-close grace. Rejected: wiring the digits to slots 5–8 now for an always-empty denial.

28. **HOW-TO-PLAY IS NOT TOUCHED IN 8.5.** Eric ruling 2026-09-16 (AskUserQuestion). Its EQUIPMENT lines (Q/E "class weapons and gear", R "pick up at sea", no Shift) stay as written, knowingly stale, until Story 8.20's re-cut. Rejected: a minimal update of the lines that become false.

29. **THE INTERIM SHIFT KEY CHIP SHOWS THE `⇧` ARROW (U+21E7).** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). The 22 px mono chip holds one glyph; the mock names the chip "Shift" without fixing one. Rejected: a wider SHIFT word chip; the letter S. Story 8.6 restyles the bar.

30. **A CLICK ON AN EMPTY BELT ROW IS SILENT UNTIL STORY 8.7.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). Key and click on one belt row behave the same way — nothing — until 8.7 wires the belt; the client-side empty denial (amendment 26) applies to the WEAPON row (Q/E/R) only. Rejected: belt clicks denying while the digits stay inert.

## Story 8.6 — The HUD Bar (cycle 141, 2026-09-16)

31. **THE BAR IS BUILT AT THE RATIFIED MOCK'S EXACT PIXEL VALUES — THE JULY 1.6× MICRO-TYPE LIFT DOES NOT APPLY TO IT.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). `mockups/hud-composite-3.html` is the register of record for every size on the bar: 54 px slots, 44 px belt squares, 16 px-high 9 px mono key chips, 11 px `LV n`, 15 px HDG / 9 px KTS, 18 px HULL and wipe numerals, 9 px tier numerals and W/S/A/D letters. The UI-scale setting (90/100/125 %) is the remedy if it reads small. **Supersedes** epic-2 amendment 15's lift (9→14, 11→17, slot 54→62) for the hud-bar's surfaces ONLY; every other HUD/DOM surface keeps the lifted register. Rejected: applying the lift to the bar (≈1000 px wide, no longer the mock); mock type on 62 px slots.

32. **BOTH GLOBES ARE 104 PX.** Eric ruling 2026-09-16 (AskUserQuestion). The helm globe takes the mock's 104 px (its labels already seat at the 9 px floor there), and the HP globe GROWS from the spec's 96 px to match, so the bar is symmetric. **Supersedes** UX-DR46's 96 px HP globe and UX-DR47's `[ASSUMPTION: 132 px]` helm globe (and DESIGN.md's matching `hp-globe.size` / `helm-globe.size` entries). Consequence for Story 8.7: the refit-card row hangs 8 px above the bar's top, so its seat is derived from a 104 px globe, not the 96 px in UX-DR53's worked example. Rejected: 104 helm / 96 HP (asymmetric); 132 px helm.

33. **THE SHIFT KEY CHIP SHOWS THE WORD `Shift`.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). The bar's key chips are the mock's 16 px-high 9 px mono boxes that widen to fit their text (min-width 16, 3 px side padding); the boost slot's chip reads `Shift`, Q/E/R and 1–4 read their letter or digit, the deck gun's chip is a ghost (transparent border and text, keeps the row's alignment). **Supersedes** amendment 29 (the interim `⇧`) and the 22 px single-glyph chip on the bar. Rejected: keeping the `⇧` arrow.

34. **THE ACTIVE STATE SURVIVES ON THE BAR.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). While the boost runs (6 s after Shift), the Shift slot keeps epic-2 amendment 48's ACTIVE grammar — the breathing phosphor outline (≥ 2 s cycle, under `pulseCapHz`, motion-aware) — with the seconds-left count rendered as the cooldown wipe's centred 18 px/600 tabular numeral and NO dark overlay; when the window ends the wipe takes over for the reload. Precedence stays `denied > activated > active > cooling`. UX-DR41's state list was silent, not negative; the ruling of record stands. Rejected: dropping ACTIVE (the 6 s window would have no readout beyond the speed needle).

35. **THE HP GLOBE CARRIES THE PENDING-HEAL BAND.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). Hull points still trickling in (`OwnShip.repairHp`, the per-level auto-heal's pool) paint as a dimmer band ABOVE the live waterline, rising to the incoming total, at the rail's `railPendingAlpha` (0.32) — the same logic today's HP rail uses. The mock shows only the ≥ 50 % solid state; nothing is lost and Story 8.8's HULL REPAIR card reuses the band. Rejected: live hull only.

36. **THE REFIT BAND LIFTS TO BAR-RELATIVE PLACEMENT IN 8.6, WITH TODAY'S CARD SIZES.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). Measured in-cycle: with the bar seated (top 614 px at 1366×768), the shipped `bandTopFrac` 0.534 anchor put the card row (y 410–646) and the DAMAGE CONTROL strip under it (y 652–692) ON TOP of the bar's slot squares, worse at the 1280×614 floor. Story 8.7's placement rule (UX-DR53: row bottom = hud-bar top − 8 px) is applied now: the whole band — card row plus the DAMAGE CONTROL strip, which stays until Story 8.8 — is anchored so its LOWEST edge sits 8 px above the bar's top; the own-hull keep-out is WAIVED (as Eric already ruled for v3); the band is laid out in the same logical units as the bar and scaled with it, which retires the physical-anchor / CSS-scale mismatch of `deferred-work.md` (the cycle-47 entry). Story 8.7 then only re-sizes the cards (216×226) and seats REDRAW. **Supersedes** `CLIENT_CONFIG.refit.bandTopFrac` and the keep-out arithmetic in its comment. Rejected: accepting the overlap as an interim like amendment 25.

37. **THE REFIT TOOLTIP FITS ABOVE THE CARD WHEN IT CAN, OTHERWISE IT OPENS DOWNWARD OVER THE CARD ROW — NEVER OVER THE BAR, NEVER CLIPPED.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). Measured after amendment 36's lift: the water above the band is 130 px at the 1280×614 floor (and at the 125 % tier on a 768-tall screen) while the tallest catalog tooltip is 261 px, so 45 of 114 panels would clip at the top. Interim until Story 8.7's 236 px slot-tooltip re-cut (UX-DR48): when the panel fits above the band it opens there as today; when it does not, it opens from the band's top edge downward, covering part of the card row. Rejected: accepting the clip on short screens (information lost); pulling the 8.7 re-cut forward.

38. **`IN STORM` AND THE VICTIM TELLS STACK UNDER THE TOP-CENTRE CHROME BAR.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). The two satellite readouts (the storm warning and the `SLOWED` / `DAZZLED` tells) used to sit above the retired bottom-right vitals cluster; Story 8.6's first cut seated them centred above the bar, where the review gate confirmed a slot tooltip covers them on every hover and the open refit band (amendment 36) hides them behind the DAMAGE CONTROL strip. They now stack directly beneath the chrome bar (`AFLOAT · KILLS · T+ · RING · ☠︎ NAME`), top-centre, where no tooltip, card or bar reaches. The storm vignette remains the primary storm signal. Rejected: lifting them above the band only while Tab is open (a hover still covers them); the old bottom-right anchor (UX-DR40: nothing on the right edge).

## Interstitial fix — damage is whole numbers; the banner clears the chrome bar (2026-09-17, no version change by Eric's instruction)

39. **DAMAGE IS ALWAYS A WHOLE NUMBER — ERIC'S INTEGER SCALE IS THE SPEC, NOT A ROUNDING OF IT.** Eric 2026-09-17, on staging with two deck-gun cards: *"I should NEVER deal .5 damage … I gave you the exact scale in planning, it did not include decimals."* Correction of record against catalog-v3 R14, whose text turned his 15 → 16 → 17 → 18 → 20 into "+1.25 per tier: 15 → 16.25 → 17.5 → 18.75 → 20 (Eric wrote it rounded)" — the planning pass reinterpreted him and Story 8.1 built it. Fix: `damage` and `contactDamage` join `EQUIPMENT_INT_FIELDS` and are floored once after the fold like pools and barrels, which lands the deck gun exactly on his scale with no other line moved (every other damage value in the game is already whole). Standing rule for 8.12–8.14: author damage ladders in whole numbers; a `.25`/`.5` step is never a design choice.

40. **THE `GOING DOWN WITH THE SHIP!` BANNER SITS UNDER THE CHROME BAR, NOT OVER IT.** Eric 2026-09-17. The DOM banner had been pinned at `top: 16px` since Story 2.3, over the chrome bar's row at y 24 — a pre-existing overlap, not something 8.6 moved, but a real defect. It now seats at 50 logical px (scaled with the UI-scale var) beneath the chrome row, and the IN STORM / tells column (amendment 38) starts below the banner's box (`vitals.stormAbove` 24 → 54) so a sinking captain in the storm reads chrome bar → banner → IN STORM → tells, stacked. Quiet fix at Eric's instruction: no version bump, no changelog entry.

## Story 8.7 — Consumable Slots and the Refit Card v3 (cycle 142, 2026-09-17)

41. **THE BELT HOLDS NOTHING IN PLAY AFTER 8.7 — EVERY CONSUMABLE STUB STAYS SET UNTIL ITS OWN STORY.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). None of the five consumables has a game effect inside Story 8.7's scope (HULL REPAIR is 8.8, SHIELD BLOCK / CHAFF / DECOY BUOY are 8.15, SMOKE SCREEN is 8.16), so 8.7 builds the whole mechanism — the shared `canStock`/`stock` fold, the stack with no reload, use-removes-a-copy, the two activation channels split by `isWeapon`, the two-meaning digits with the post-close grace, the greyed `SLOTS FULL` card, the five-row card face — proven by tests on a non-stub TEST catalog and a test consumable row, while the production consumable registry ships EMPTY and all five `stub` flags stay. No consumable is dealt, the belt stays empty on staging, and `PROTOCOL_VERSION` stays 52. Story 8.8 flips `hullRepair` first. Rejected: pulling HULL REPAIR forward into 8.7 (would take 8.8's core and force the open amendment-15 heal question now). **Supersedes** nothing; fills the gap between the 8.7 AC (mechanism) and the content stories.

42. **THE SLOT TOOLTIP AND THE REFIT HOVER PANEL KEEP THEIR SHIPPED WIDTHS — UX-DR48'S 236 PX IS NOT BUILT.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). No tooltip mock exists anywhere; the shipped slot tooltip is 320 px and the refit hover panel 300 px, both grown for the July 18 px type lift, and 236 px would break the accrued-boons fit budget. Story 8.7 re-cuts CONTENT only: a weapon slot's tooltip lists its line + tier, a belt slot's its consumable, shape (`KEY FIRES` / `KEY PRIMES · CLICK FIRES`) and stock; the tooltip core moves out of `hotbar.ts` into its own module (the ledgered split). Amendment 37's fit-above-else-downward placement STANDS as the rule. Rejected: shrinking both panels to 236 px at the bar's mock register. **Supersedes** the `236 px` width in UX-DR48 and DESIGN.md's `slot-tooltip.width`.

43. **TEXT MUST BE READABLE — THE REFIT CARD'S 9 PX REGISTERS COUNTER-SCALE AT THE 90 % TIER; NOTHING ON ANY SURFACE RENDERS UNDER THE 9 PX FLOOR.** Eric ruling 2026-09-17, verbatim: *"Text *MUST* be readable."* Context: the ratified card face (amendment 31's mock-literal rule) sets its stat-row labels and its reason-word foot at 9 px mono; the refit band is DOM scaled as one block by the UI-scale variable, so at the 90 % tier those registers drew at 8.1 px, under `CLIENT_CONFIG.settings.monoFloorPx`. The HUD bar's `microScale` (a Pixi counter-scale, Story 8.6 ruling 2) now has a DOM twin: every DOM register configured at the 9 px floor divides its font-size by the UI scale when the scale is under 1, so the rendered glyph is never smaller than 9 px, and the container-fit law (epic-4 amendment 47) is re-checked at the 90 % tier with the counter-scaled size. Rejected: accepting 8.1 px at 90 % as the player's own choice. Standing rule for every later DOM surface (8.10 REDRAW, 8.19 LOADOUT, Epic 9): the 9 px floor is a floor on the RENDERED size, at every UI-scale tier. Why the card is DOM at all: the frozen CLAUDE.md invariant lists the refit window as DOM chrome ("Canvas is Pixi; DOM is only for chrome"); moving it to Pixi would be its own story.

44. **A FULL BELT WITH AN ALL-CONSUMABLE OFFER IS ACCEPTABLE AS DESIGNED.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). With four DIFFERENT consumable lines stocked and an offer of four other consumables, every card is greyed `SLOTS FULL` and the banked level waits: the player fires a stack to zero (which frees its slot — the belt is rebuilt from `cards` after every use) and then picks, or takes damage so the menu heal is spendable (until 8.8). The offer never rerolls (the FR19 pin stands). Nothing is lost; the level stays banked. Bots: Story 8.18 gives the bot spend policy the same shared `canStock` skip so a bot never re-picks a refused card (ledgered). Rejected: any reroll, skip or discard rule.

45. **NO GRACE ON THE REFIT WINDOW'S OPEN EDGE.** Eric ruling 2026-09-17. The window opens ONLY by the player's own Tab (verified in shipped code: the per-frame update re-renders an open window and hides it when the offer is gone; it never opens one), so a number key after a Tab press is a deliberate act. The one auto-open in the design — the level-zero offer during the countdown (UX-DR54, Story 8.10) — happens when the belt is empty and nothing can fire, so Eric's words: *"That will never be an issue in-game."* The 400 ms close grace (ruling 8 of spec-8-7) is the whole of the number-key guard. Corrects the Edge Case Hunter finding that assumed a level-up auto-opens the window mid-match. Note for Story 8.10: UX-DR54's "ratified as rendered 2026-09-11" auto-open + REDRAW stamp is the planning pass's own claim — Eric did not recall discussing an auto-open; 8.10 puts the whole opening flow to him explicitly before building it.

## Story 8.8 — Heal Is a Card (cycle 143, 2026-09-17)

46. **THE FREE PER-LEVEL AUTO-HEAL IS REPLACED BY OUT-OF-COMBAT REGEN: 1 % OF MISSING HULL PER SECOND, ONCE 30 S HAVE PASSED SINCE THE HULL LAST TOOK DAMAGE.** Eric ruling 2026-09-17 (AskUserQuestion, "Replace with out-of-combat regen", then the numbers confirmed: *"1%/s of missing, after 30 s"*). Resolves amendment 15 and `deferred-work.md`'s Story 8.3 hand-off: `development` carried the per-level version (10 % of missing over 5 s into its own pool, cycle 129) and NO out-of-combat regen existed anywhere; Eric's recollection was the intended design, so 8.8 builds it. Shape: every second a hull has been out of combat, `hp += (maxHp − hp) × 0.01` (asymptotic — fast when badly hurt, slow near full; the build snaps to full once under 1 hp is missing so the HULL REPAIR "full hull" refusal is reachable). It pays straight into `hp` — no pool, no pending band, no `heal` cue (a continuous trickle would loop the tone). Both numbers are `CONFIG.regen` tunables (`missingPctPerS: 0.01`, `outOfCombatMs: 30000`). DELETED: `CONFIG.damageControl.levelMissingPct` / `levelRegenMs`, `ShipRecord.levelRepairHp` / `levelRepairRate`, `grantLevelHeal`, the harness's level-heal arm. `CONFIG.damageControl` itself is gone: the paid heal's three numbers move to `CONFIG.hullRepair` (8.8 AC). **Supersedes** FR47's *"STAYS AS CURRENTLY BUILT"*, the Story 8.8 AC's *"the FREE per-level auto-heal's fields STAY exactly as built"*, and GDD open note 14 (closed: the per-level heal is retired, not parked). Rejected: leave as built; delete the free heal entirely; 1 % of MAX hull per second.

47. **ANY HULL DAMAGE RESETS THE 30 S CLOCK — STORM INCLUDED; DEALING DAMAGE NEVER DOES.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). One hook at the damage gate (`applyDamage`, step (d)): every hull-hp decrement that actually lands (`dealt > 0` — shells, torpedoes, mines, burn ticks, storm bites) stamps `ShipRecord.lastDamagedAt = now`. Consequence, stated to Eric and accepted: inside the storm and through the sudden-death collapse nobody regens, so the storm's listed rate is its net rate and NFR6's ceiling arithmetic is unchanged by regen. A hit fully eaten by a future SHIELD BLOCK (`dealt` 0) does not count as taking damage. Rejected: weapon damage only.

48. **CAPTAINS AND BOTS REGEN; FLEET DRONES NEVER DO.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). The regen reaches exactly the hulls the level heal reached (participants — humans and combat bots); a PvE fleet hull is environment (amendments 12, 24) and keeps the damage it takes, so a disengaged drone never comes back to full. Rejected: every afloat hull.

49. **A MINIMAL BOT HEAL TACTIC SHIPS NOW; STORY 8.18 STILL OWNS THE TACTIC TABLE.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). `HEAL_CHOICE` leaves the bot spend policy with the wire, so without this a bot could pick HULL REPAIR cards and never fire one until 8.18. One rule, on the existing ability channel: a bot holding a HULL REPAIR stack presses its belt slot when `hp / maxHp` is under its profile's existing `healHpFrac` (the same threshold that used to buy the `5` key). It is the first row of a consumable tactic registry beside `EQUIPMENT_TACTICS`; 8.18 may replace the row and retunes the spend scorer (a hurt bot still scores a HULL REPAIR card at the consumable kind base, not above it — unchanged, deliberately). Rejected: bots stop healing until 8.18.

50. **ONLY THE COPY LINES THAT BECOME FALSE ARE FIXED; HOW-TO-PLAY'S FULL RE-CUT STAYS WITH 8.20.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). The Settings keybinding row `1 – 5 … 5 IS DAMAGE CONTROL` becomes the true two-meaning line for `1 – 4` (pick a card while the refit window is open; fire a belt slot while it is closed), and the two UPGRADING sentences in How-to-Play that name DAMAGE CONTROL as a level spend are reworded to name HULL REPAIR as a card you stock and fire, with the free-heal sentence restated for the out-of-combat regen (amendment 46). Nothing else in How-to-Play or any in-game surface changes (standing rule: no in-game copy unasked). Narrows amendment 28's "untouched until 8.20" to lines that are still true. Rejected: Settings row only; leave both stale.

51. **HULL REPAIR'S POOL PAYS 50 HP OVER 5 S (10 HP/S) — "AS SHIPPED" GOVERNS; THE "5 HP/S" IN R13 / FR47 / THE 8.8 AC IS A STALE FIGURE.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). Measured: `regenHp 50 / regenMs 5000` = 0.01 hp/ms; the 5 hp/s wording dates from before balance cycle 1 (2026-08-20) doubled both amounts 25 → 50 while holding the pool time at 5 s, and the shipped pin file already recorded that supersession. No number moves: `CONFIG.hullRepair` is `{ instantHp 50, regenHp 50, regenMs 5000 }`, the card's OVER TIME row reads 50 hp over 5 s. Rejected: slowing the pool to 5 hp/s (a real balance change, 50 over 10 s). Correction of record against catalog-v3 R13's parenthetical, FR47 and the Story 8.8 AC wherever they say "5 hp/s".

52. **A DOWNWARD REFIT TOOLTIP TALLER THAN THE BAND SLIDES UP BY THE SHORTFALL — AMENDMENT 37 REFINED.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option). Deleting the DAMAGE CONTROL strip shortened the refit band by 46 px (it now ends at the card row's bottom, still seated 8 px above the bar per amendment 36), and amendment 37's downward budget IS the band's height: at the three shortest combos (1280×614 at 90 % and 100 %, 1600×768 at 125 %) the tallest catalog panel (261 px) fitted neither above the band nor inside it. A downward panel taller than the band now moves up by exactly the shortfall (17 px worst case, capped at the band's top so it can never leave the screen) into the water the band vacated; its bottom still sits 8 px above the bar and nothing clips; every panel that already fitted is byte-identical (offset 0). Both halves of amendment 37 ("never over the bar, never clipped") stay literally true. Rejected: letting tall panels clip on those combos.

53. **"FULL HULL" MEANS UNDER 1 HP MISSING — ONE DEFINITION FOR THE HULL REPAIR REFUSAL, THE REGEN SNAP AND THE CLIENT PRE-DENIAL.** Eric ruling 2026-09-17 (AskUserQuestion, recommended option), from the 8.8 review gate (both Fable hunters). Storm bites and burn ticks land fractional damage (a 0.2 hp bite), weapon damage is whole (amendment 39), so a hull can sit at 349.x of 350 for minutes (the regen closes MISSING geometrically and the readout floors to 349); the old `5` key refused only at exactly max, and carrying that over would let a whole scarce card go for under 1 hp. Now a shared predicate (`hullIsFull`: `maxHp − hp < 1`) is what the row refuses on (`blocked`, nothing spent), what the regen snaps on, and what the client's belt pre-denial mirrors. Rejected: only exactly full refuses.

## Story 8.9 — The Shift Boost, Universal (cycle 144, 2026-09-18)

54. **THE SHIFT BOOST IS +25 % OF MAX SPEED FOR 10 S ON A 25 S RELOAD — NOT R9'S 20 S.** Eric ruling 2026-09-18 (AskUserQuestion, verbatim: *"Build as-written, except 25s reload."*). `CONFIG.boost` is `{ factor: 0.25, durationMs: 10000, maxAmmo: 1, reloadMs: 25000 }`; the reload takes `cooldownScale` through the one multiply like every equipment row (R40), so a maxed RELOAD ladder (0.75) gives 18.75 s, not the 15 s catalog-v3 §4 quotes. The shipped `CONFIG.speedBoost` (+10 u/s flat, 6 s, 18 s) and the legacy `speedBoost` equipment id are DELETED; the v3 `boost` id in slot 1 is the boost. **Supersedes** the 20 s in catalog-v3 R9 / §4, FR49, AR49, EXPERIENCE.md's Shift row and the Story 8.9 AC; every other number there stands. The three numbers stay harness-tunable (`--tune boost.*`; `--set` never reaches it).

55. **THE +25 % SCALES THE LADDER-RAISED MAX.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option). `bonus = CONFIG.boost.factor × kin.maxSpeed` where `kin` is the POST-FOLD kinematics, so SPEED cards enlarge the boost: a SPEED-capped Torpedo Boat boosts to 68.75 u/s (55 × 1.25), Mine Layer 62.5, Battleship 56.25; base hulls 56.25 / 50 / 43.75. Forward cap only — `reverseSpeed`, accel, decel, turn rate and steerage are untouched, exactly as the shipped hook. The bonus is layered per tick by the one shared `boostedKinematics` at both call sites and is NEVER folded into `EffectiveStats.kinematics`, so the bots' `max(rated, actual)` deadband reading stands (FR49, AR49). Closes the "base or ladder-raised" half of GDD open note 21 and confirms AR49's attribution. Rejected: base max only (capped TB 65).

56. **SHIFT IS SUSPENDED WHILE THE REFIT WINDOW IS OPEN, LIKE Q/E/R.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option; verbatim on the same question: *"I can't use my equipment (like boost) while I have the upgrade window open."*). The shipped `boostAction` modal guard is the design, not an accident; resolves the UX validation report's `[Input 2.3]` and EXPERIENCE.md's `[ASSUMPTION: Shift is suspended with them]`. Rejected: the boost firing through the open window.

57. **SHIFT+TAB IS NOT A DESIGNED COMBINATION — THE AC'S "SHIFT+TAB STILL REACHES TAB" CLAUSE IS VOID.** Eric ruling 2026-09-18, verbatim: *"I think this came up because originally the upgrade menu opened with Shift instead of Tab. You can just straight up stop needing to think about this. Shift is boost, Tab is ability window, and I can't use my equipment (like boost) while I have the upgrade window open."* The client keeps its shipped handling (`Shift+Tab` is `preventDefault`ed for browser hygiene and does nothing further); a Shift press fires the boost on its own keydown edge whatever key follows. No test, no code and no copy is written for the chord. **Supersedes** the `Shift+Tab` clause of the Story 8.9 AC, FR49's reading of it and EXPERIENCE.md's `Shift+Tab reaches Tab [ASSUMPTION]` (TAB row and Input-capture paragraph).

58. **THE CLASS-SELECT CARDS DROP THE TORPEDO BOAT'S `E: SPEED BOOST` ROW AND THE MINE LAYER'S `E: RADAR BUOY` ROW.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option, both). The boost is a universal Shift ability on every hull (amendment 54) so it no longer differs between hulls and the card, which "sells what differs" (Eric 2026-07-24), does not list it; the buoy has been unreachable since Story 8.5 (amendment 22) and 8.15 deletes the module. No Shift row is added to any card; the Battleship's `Q BROADSIDE / E STAR SHELLS` and the two `Q` rows stand under the interim spawn seed (amendment 21). Rejected: leaving either row for 8.20; adding a Shift row to all three cards.

## Story 8.10 — The Opening (cycle 145, 2026-09-18)

59. **THE REFIT WINDOW AUTO-OPENS ON THE LEVEL-ZERO OFFER AT COUNTDOWN START.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option), resolving the note in amendment 45 and `deferred-work.md`'s "put the whole opening flow to Eric" thread. The window opens ITSELF once, on the first frame that carries a level-zero offer while the match is in countdown (an edge latched per match epoch, never re-opened per frame, never on a numeric rise so a reconnect mid-countdown still opens it), routed through the same guards as the Tab path (no stacking over settings/results; the bank chip re-arms). Tab still closes and reopens it. Confirms UX-DR54's auto-open as Eric's own decision rather than the planning pass's claim. Rejected: Tab-only with the strip cue as the only prompt.

60. **THE MULLIGAN IS BUILT AS DESIGNED: ONE `REDRAW` BUTTON UNDER THE ROW, COUNTDOWN ONLY, `SpendMsg.choice === MULLIGAN_CHOICE` (`-2`) ON THE WIRE.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option). One free redraw of the level-zero offer while `phase === 'countdown'`, the ship has not mulliganed and holds an offer; the redraw carries the same usable-card guarantee and re-queues `pt`; a second press, a live-phase press, a bot or any hull without an offer is a silent no-op leaving the offer byte-identical. The negative sentinel channel that Story 8.8 closed (PV 53, "any negative is malformed") is deliberately RE-OPENED for exactly this one value — `MULLIGAN_CHOICE` is exported from `shared/src/sim/offers.ts` and `PROTOCOL_VERSION` bumps 54→55. The pip beside the word is AMBER (the ratified mock draws it so; the queue pips above the row stay phosphor), hollow until spent, filled after. Rejected: dropping the mulligan from 8.10.

61. **THE COUNTDOWN GRANT IS SILENT — NO `LEVEL UP — TAB TO REFIT` TOAST AND NO `point` TONE AT THE START LINE; THE XP STRIP'S `TAB TO REFIT` CUE STAYS.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option). The window opening itself is the cue. Mechanism: the server still queues `pt` for the level-zero grant and for a mulligan (so the headless smoke and FR48's "re-queues `pt`" hold); the CLIENT suppresses the toast and tone for a `pt` that arrives while the hull is held at the start line (`waiting`/`countdown`). The strip cue is true copy (Tab does open the window) and is untouched. Rejected: suppressing the strip cue too; firing all three as for a normal level-up.

62. **THE CLASS-SELECT CARDS DROP THEIR WEAPON ROWS ENTIRELY.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option). With `SPAWN_SEED` deleted every hull spawns with the gun and Shift only, so `Q: TORPEDO TUBES`, `Q: BROADSIDE BARRAGE` / `E: STAR SHELLS` and `Q: PROXIMITY MINES` describe nothing the hull carries; the card sells what differs and at spawn only the deck differs. The three stat pips stay; the `LOADOUT` row table becomes empty for every hull and its DOM block renders nothing. Supersedes the "stand under the interim spawn seed" clause of amendment 58. Rejected: renaming the rows to the deck's weapon lines; leaving them for 8.20.

63. **MEASURED CORRECTIONS OF RECORD FOR 8.10 (orchestrator, 2026-09-18, verified against the code).** (a) The countdown row lift is a 44 px DELTA, not the 388→344 absolutes: the live row top at 1366×768 is **380** (bar top 614 − 8 px gap − 226 px card), so the countdown row top is **336**; UX-DR54 / DESIGN.md's 388/344 forgot the 8 px bar gap. The band grows by a 44 px footer (14 px gap + the 30 px button) and its bottom stays 8 px above the bar. (b) `Match.activate()` → `resetForMatchStart` → `redeployShip` today wipes `bankedLevels`, `offer`, `cards`, `loadout` and `deck` — 8.10 makes the activation redeploy PRESERVE the whole countdown economy (a card taken during the countdown is fitted at 0:00; a held offer stays held; `mulliganed` is per match, reset with it). (c) Bots receive the level-zero grant but the bot driver is frozen while the helm is locked, so a bot spends on its first live tick — unchanged behaviour, a few milliseconds after a human could; bots never send the sentinel. (d) The `pt` for a countdown-entry grant is delivered one tick after the phase edge (the pending→events swap is in `World.step`'s epilogue) — harmless, pinned. (e) `grantPoint` is private on `World`; 8.10 adds a public countdown-grant entry that guards `role !== 'fleet'` (a fleet hull's empty deck would otherwise latch `deckExhausted`).

64. **THE "OPENING NEVER LIES TO ME TWICE" WORDING IS THE EPIC AUTHOR'S, NOT ERIC'S; THE WEIGHTED FIRST DRAW AND THE `REDRAW` ARE ERIC'S OWN IDEAS AND STAND.** Eric 2026-09-18, verbatim on the Story 8.10 user-story line: *"That's not a lie. That's the nature of randomization in games. Its a short game, just play another if you die fast. NBD."* — and, asked whether that changed the safeguards: *"Those were my specific ideas… it doesn't change shit."* Both mechanisms are built as ruled in amendment 60. Nobody should later strengthen either (a second redraw, a pity, a pinned card) by citing the epic's "lie" wording; a bad first draw is accepted randomness.

65. **A DEV-ONLY `fitOverride` ROOM OPTION PRE-FITS LISTED CARDS AT SPAWN, SO THE TWO WEAPON SMOKES KEEP THEIR TORPEDO.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option). Deleting the spawn seed left `matchSmoke.mjs` and `weaponsSmoke.mjs` firing an empty Q slot (they are not in `npm run check`, so the gate stayed green; the failure is structural, not the ledgered flake). Fix: `fitOverride?: readonly string[]` beside `deckOverride` on the room options, honoured ONLY under `HC_DEV_OPTIONS=1` and stripped by `sanitizeRoomOptions` otherwise (logged like `deckOverride`); at spawn the listed line ids are applied through the ordinary card path (`applyCard`, consuming the deck copies as a pick would; unknown/stub ids dropped) for CAPTAINS only. The two smokes pass the old seed lines. No gameplay change; production never sees it. Rejected: rewriting the smokes to gamble on the countdown draw; ledgering them broken.

66. **THE COUNTDOWN ECONOMY SURVIVES ACTIVATION IN EVERY ROOM — NOT ONLY BOARDING ROOMS.** Orchestrator correction of record at the 8.10 review gate (2026-09-18), superseding the `holdStartLine` scoping in amendment 63(b). The batch-sim harness and the RL environment build a `Match` without `expectedCaptains`, so they took the old wipe path at activation and measured an opening production never plays (bots' countdown picks erased). Pre-active XP and damage are policy-disabled, so the only pre-active economy is the opening grant and there is nothing legitimate to wipe: `redeployEconomy` preserves `bankedLevels`, `offer`, `cards`, `deck`, `deckRng`, `deckExhausted`, `mulliganed` on every activation; the `hold` flag governs only the position re-roll, as before this story. With it: the opening grant latch is PER SHIP (a re-armed countdown grants to a newcomer, never twice to the same hull; a participant spawned while `countdownOpen` is granted at spawn), the client's auto-open and redraw latches reset only on the edge into `active`, and `World.mulligan` refuses a sinking hull.

## Story 8.11 — The Match Consumable Pool (cycle 146, 2026-09-18)

67. **THE POOL ROLLS FROM ALL FIVE CONSUMABLE LINES; STUB COPIES SIT IN THE DECK UNDEALT UNTIL THEIR STORIES.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option). Four of the five launch consumables (SHIELD BLOCK, SMOKE SCREEN, CHAFF, DECOY BUOY) are still `stub: true` until Stories 8.15/8.16; the pool is nonetheless rolled from all five as designed (hidden, count 10, each line ≤ its cap within the pool) and its stub copies join the deck but are never dealt — the same rule amendment 11 set for the authored decks' stub cards. Interim effect on staging: about two extra HULL REPAIR copies per hull per match on average (0–5), and the rest fills in with no code change when the stub flags flip. Rejected: rolling only from live lines (a 5-heal pool until 8.15); an empty pool until 8.15. **Supersedes** nothing; fills the gap between the 8.11 AC ("the five launch consumable lines") and amendment 41's empty consumable registry.

68. **MEASURED CORRECTIONS OF RECORD FOR 8.11 (orchestrator, 2026-09-18, verified against the code and the measurement test).** (a) THE 50-CARD MEASUREMENT (FR59's bar, set here for 8.18 to measure for real): on an unstubbed copy of the catalog (40 authored + 10 pool, all dealable), 2000 seeded economies per hull taking a uniformly random offered card per level — the one-copy line `deckGunTurret` has appeared in at least one offer by pick 8 / 12 / 15 / 20 in **56.1 / 73.4 / 81.0 / 90.5 %** of Torpedo Boat economies (Battleship 53.6 / 71.0 / 79.3 / 89.6; Mine Layer 52.3 / 69.5 / 80.3 / 90.0); an offer first holds fewer than four lines at mean level **≈ 46.0**, and the draw first comes back empty at mean level **≈ 49.7** (below 51 because pool copies past a line's cap are dead — R44). Recorded, not pinned tightly (`shared/src/__tests__/poolMeasure.test.ts` asserts monotonicity and loose bounds only). (b) THE AT-CAP GUARD IS LIVE: until this story every door-admitted deck satisfied "pool + held ≤ cap" for every line, so Story 8.3's guard could never bite; the pool is the first thing that pushes a line past its cap (HULL REPAIR 3 authored + up to 5 pool = 8 > 5). Consequence, deliberate: a ship holding a line at its cap is never offered it again, so an economy can now end on an EMPTY OFFER with dead copies still in the deck rather than on an empty deck; the harness's deck-only determinism pin was re-pinned accordingly and the pure economy sim now draws with `held` exactly as `World.materializeOffer` does (the `deferred-work.md` "draws unguarded" entry is closed). (c) A BARE `World` (no `poolSeed`, no `pool`) still rolls a pool from a TEST-ONLY map-seed derivation (constant `0x3c6ef372`, unused by any other stream) — parity with the `zoneSeeds` posture, so a forgotten seed in a harness arm can never silently measure a pool-less game; production rooms always pass adapter entropy, pinned by the room test. Tests that pin deck depth or offers inject `pool: []`. (d) `poolOverride` is a ROOM option (the pool belongs to the room, not the seat), sanitized through the same dev id-list admitter as `deckOverride`/`fitOverride` and filtered to consumable-kind ids by `sanitizePool`; the queue passes no pool option — the arena it creates rolls its own.

69. **CORRECTION OF RECORD AT THE 8.11 REVIEW GATE: POOL COPIES PAST A LINE'S CAP ARE NOT DEAD — THEY ARE GATED BEHIND USE.** Orchestrator 2026-09-18, from the Blind Hunter's confirmed finding, verified against `World.spendStock`. Amendment 68(b)'s "dead by design" wording (and the same sentence in the cycle's CHANGELOG, `CONFIG.pool` doc, `pool.ts` header and `world.ts` doc blocks, all corrected in-cycle) described a rule the code does not run for the only lines a pool can hold: a USED consumable copy leaves `ship.cards` (Story 8.7), `materializeOffer` passes `held = ship.cards`, so once a captain fires one of five stocked HULL REPAIR the at-cap guard reopens the line and the deck's remaining copies — authored or pool — are drawable again. Correct reading: the guard never OFFERS a line the ship holds at cap, and the copies past the cap wait in the deck until a use frees a stock slot; the pool is extra supply gated behind use, which IS Eric's "more heals and shields out there". Consequences: (a) "exhausted" (`reportExhaustion`, the once-per-ship `deck.exhausted` log/metric) means an EMPTY DRAW — the deck is empty OR every remaining copy is of a line currently held at cap; a later use can reopen the line and the next level's retry succeeds, while the latch still fires only once. (b) Amendment 68(a)'s measurement and the batch-sim deck-only economy are a NEVER-USE model — nothing ever leaves `held`/`fitted`, so a consumable line fitted to cap stays closed for the rest of the economy; their numbers (empty draw ≈ 49.7, first short offer ≈ 46.0) are a pessimistic FLOOR for consumable reachability, exact for equipment and ladder lines (whose copies never leave `cards` in production either). Story 8.18's real bar must come from the full harness, where bots fire consumables. (c) Equipment and ladder lines are unaffected: nothing in the pool is one, and their copies never leave `cards`. **Supersedes** the "dead by design" clause of amendment 68(b); everything else in 68 stands.

## Story 8.12 — Catalog v3: Ladders and the Deck Gun (cycle 147, 2026-09-18)

70. **THE HUD BAR'S GUN SQUARE AND ITS TOOLTIP SHOW THE DECK GUN'S TIER.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option). The refit card already reads the deck gun as a base-tier line (`I → II` on the first card, amendment-5-era 8.1 build) but the bar's gun square printed no numeral and its hover no ` · TIER n` ("honest rather than a fabricated tier I", written before the base-tier reading landed). Both now read the SAME number the server folds — `effectiveStats().equipment.gun.tier` = 1 + DECK GUN copies — so the square shows `I` at spawn and climbs to `V` at four cards, clamped to the five-rung ramp; no wire field is added (the client folds its own `cards`). Rejected: leaving the square and tooltip tier-less.

71. **`Turning` AND `Gun damage` ARE RATIFIED ROW LABELS.** Eric ruling 2026-09-18 (AskUserQuestion, recommended option). The refit card's stat-row words for TURNING and DECK GUN were agent-chosen minimal labels at Story 8.1 (ledgered under the no-in-game-copy-unasked rule, `deferred-work.md` 8.1 section); Eric keeps both. The DECK GUN face still prints ONE row (damage) though the line also moves its own reload — the v2 broadside-SPREAD precedent stands. Rejected: `Turn rate`; new words.

72. **THE `cooldownScale` FLOOR LITERAL STAYS 0.1 — 0.75 IS THE REACHABLE FLOOR BY CONSTRUCTION.** Eric ruling 2026-09-18 (AskUserQuestion, after asking what the floor is for and whether it touches the per-weapon tier step: it does not — `reload = base × (1 − 0.05 × (tier − 1)) × cooldownScale`, the two factors are independent and the product is pinned at 60 % at both caps). The `Math.max(0.1, …)` in `clampStats` guards plumbing against malformed data that never came from the catalog; the RELOAD ladder caps at five copies so no reachable stack passes 0.75 (pinned). Reading of record for the 8.12 AC's "floor moved 0.1 → 0.75 by construction": satisfied as written, no literal moves. Rejected: raising the literal to 0.75 (a safety stop would double as a balance ceiling that silently clamps any future ladder change).

73. **CORRECTION OF RECORD — THE 8.12 AC WAS PRE-SATISFIED BY STORY 8.1.** Orchestrator 2026-09-18, verified against the code. Amendment 5 recorded that 8.1 authored "the five ladders and the deck-gun family with catalog-v3 numbers" because they replace shipped v2 lines; every numeric clause of the 8.12 AC (caps, per-tier steps, whole-number gun damage per amendment 39, tier-I-equipped for ARMOR/SPEED/TURNING/DECK GUN, the equipment-before-global reload composition at 60 %, ARMOR's heal-on-grant, the cycle-42 proportional in-flight rescale surviving a tier grant, catalog-driven `STAT current → next` rows) was already live and pinned. 8.12 therefore lands as a verify-and-pin cycle: the client tier-step helper gains a cap clamp (the tier-step LABEL for an at-cap base-tier line read `V → VI` in a never-offered state; the drawn numerals were already clamped to five), amendment 70's HUD tier, and a NO-BUMP `PROTOCOL_VERSION` record (no number, stub flag, shape or event changed). The AC's `deferred-work.md:463` citation for the rescale rule now resolves to `:471`. Stories 8.13–8.16 must not re-author these eight lines.
