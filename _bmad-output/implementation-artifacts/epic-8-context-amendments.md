# Epic 8 Context — Ratified Amendments (durable)

This file is the DURABLE home for every ratified correction to Epic 8's context: Eric rulings, review patches, adjudicated contradictions. `epic-8-context.md` is regenerable and anything living only there is erased on recompile; on any conflict between an entry here and planning-artifact-derived content, the entry here WINS. Append-only / deliberate-edit only — no tooling step may regenerate, prune or rewrite it.

Format: numbered, dated, source-attributed. "Supersedes" names the exact planning-artifact clause it overrides.

---

## Story 8.0 — Colyseus 0.18 Upgrade (cycle 134, 2026-09-14)

1. **`@colyseus/core` 0.18.13 and `@colyseus/schema` 5.0.32, not the AC's 0.18.12 / 5.0.27.** Eric ruling 2026-09-14 (AskUserQuestion, "Latest patches"). The story's pinned target set was registry-verified on 2026-09-09; core 0.18.13 shipped 2026-09-12 as a security fix in exactly the area the staging gate lives (*a room's `onAuth()` and plugin `onAuth` hooks now run when a client joins with a valid `@colyseus/auth` token*), and schema had moved to 5.0.32. Eric took the latest patches under his standing *"keep everything CURRENT"* ruling. **Supersedes** the version literals in epics.md Story 8.0 AC, AR19, and game-architecture.md D10 for those two packages ONLY; every other package installs at the version the AC lists (`colyseus` 0.18.5, `@colyseus/tools` 0.18.3, `@colyseus/sdk` 0.18.2, `@colyseus/auth` 0.18.2, `@colyseus/database` 0.18.3, `@colyseus/admin` 0.18.5, `@colyseus/monitor` 0.18.3, `@colyseus/playground` 0.18.4). The actually-resolved set is recorded in `spec-8-0-colyseus-0-18-upgrade.md`.

2. **The three unmounted Epic 9 modules (`@colyseus/auth`, `@colyseus/database`, `@colyseus/admin`) live in `server/package.json` `dependencies`, not `devDependencies`.** Eric ruling 2026-09-14. They are production modules in Epic 9; installing them now proves the whole 0.18 set (including the `drizzle-orm` 1.0.0-rc.2 pre-release inside `@colyseus/database`) resolves together before any account code exists. Accepted cost: a few unused packages in the production image until Story 9.1 mounts them. Nothing is mounted, routed, or configured by Story 8.0 — no env var, no `render.yaml` change.

3. **`postgres` and `drizzle-kit` are DEFERRED to Story 9.1 — Story 8.0 does not install them.** Eric ruling 2026-09-14. **Supersedes** the *"`postgres` 3.4.9 and `drizzle-kit` 0.31.10 present"* clause of the Story 8.0 AC and the matching line in AR19 / D10. Reason: nothing in 8.0 runs a migration or opens a database socket, so "present" bought nothing; and the AC's `drizzle-kit` 0.31.10 is the 0.44-era kit while `@colyseus/database` 0.18.3 hard-pins `drizzle-orm` 1.0.0-rc.2, whose matching kit is the `1.0.0-rc` line — installing a mismatched kit now would have handed 9.1 a trap. Story 9.1 installs both at the versions verified against the ORM the module actually ships with; the mismatch is ledgered in `deferred-work.md`.

4. **Measured facts that correct the story text (no ruling needed — recorded so no implementer re-derives them).** (a) The AC's *"~26 Colyseus-importing test files"* is 11 test files (10 server, 1 client) plus 15 headless smokes in `server/scripts/`; none boots a real server, and there is no `@colyseus/testing` dependency. (b) `setSimulationInterval` still exists in 0.18 as a deprecated forwarder to `setTimestep`; the AC's conversion is nonetheless made for real at its one call site. (c) `setMetadata` moved from shallow-merge to full REPLACE; both rooms already write whole listing-meta objects, so the only change is comment honesty plus a pin. (d) `Client#id` has no use in this repo. (e) `@colyseus/tools` `config()`/`listen()`, `matchMaker.*`, `IRoomCache`, `ISeatReservation`, `static onAuth(token, options, context)`, `allowReconnection`, `ClientState`/`CloseCode`/`ErrorCode`, `createRouter` and the reconnection-token format are byte-identical 0.17.44 → 0.18. (f) `colyseus.__globalEndpoints` is REMOVED in 0.18 — `liveness.test.ts` read it. (g) The 0.18 JOIN_ROOM handshake is a wire break a 0.17 client fails BEFORE `protocolVersionError` runs, so server and client must deploy together; the PV bump (49 → 50) is still made because the schema encoder changed.

## Story 8.1 — The Card Model and Catalog Engine (cycle 135, 2026-09-15)

5. **Interim state between 8.1 and the content stories is "stay playable".** Eric ruling 2026-09-15 (AskUserQuestion, recommended option). Story 8.1 ships an interim deck of every catalog line whose mechanism already exists (the five ladders and the deck-gun family with catalog-v3 numbers; `heavyTorpedo` / `navalMines` / `broadside` / `starShells` slot fills; the add-ons whose doctrine verbs exist). Lines for weapons and consumables that later stories build are authored in full shape (copy 1 + empty tiers) but carry `stub: true` and are EXCLUDED from `buildDeck()` and therefore from every offer until their story flips the flag. Rejected: "engine only, degraded staging" and "author every v3 tier now". **Supersedes** nothing; fills a gap the epics leave open (the AC allows stubbing but says nothing about the draw pool).

6. **Legacy equipment ids: `torpedo` → `heavyTorpedo`, `mine` → `navalMines`.** Eric ruling 2026-09-15. The shipped single torpedo module becomes the HEAVY line and the shipped mine module becomes NAVAL MINES; Story 8.13 adds `lightTorpedo`, `supercavTorpedo` and `captiveMines` as new modules. Rejected: `torpedo` → `lightTorpedo`; keeping legacy ids until 8.13.

7. **The 29 `LINE_IDS` are locked** (Eric ruling 2026-09-15): `armor, speed, turning, radarSweep, reload, deckGun, deckGunTurret, deckGunBarrel, lightTorpedo, heavyTorpedo, supercavTorpedo, navalMines, captiveMines, missile, machineGun, flak, monitor, broadside, starShells, hullRepair, shieldBlock, smokeScreen, chaff, decoyBuoy, acousticHoming, foulingMines, heatSeeking, dazzleShells, phosphorShells`. The 18 equipment + consumable ids match AR56's server registry list byte-for-byte; display names live only in client copy (AR29). Changing an id is a wire change (PV bump).

8. **Interim refit card (until 8.6/8.7): kind word in neutral colour.** Eric ruling 2026-09-15. Rarity and category die in 8.1; the CURRENT refit card's meta row shows the v3 kind word (`WEAPON · UPGRADE · ADD-ON · CONSUMABLE`) plus the copy count, no tier-ramp colour, and the hotbar's per-slot `◆n` category count is removed. Rejected: applying DESIGN.md's tier ramp now; a blank meta row. Story 8.7 owns the real card face.

## Interstitial cycle 136 — Version scheme change (2026-09-15)

9. **The `0.17.X` version freeze is LIFTED: one-time move to `0.18.1`, then the patch number keeps counting landed build cycles across epics.** Eric ruling 2026-09-15 (AskUserQuestion, "0.18.1 once, then keep counting cycles"). Cycle 136 = 0.18.1; the next landed cycle is 0.18.2, and so on, with NO epic tie (rejected: minor-per-epic; real semver). `VERSION` + root `package.json` stay the single source, injected into the client by Vite. **Supersedes** the 2026-08-01 ruling "versioning is frozen at 0.17.X until all epics complete" (CLAUDE.md, project-context.md — CLAUDE.md is hook-frozen and now carries a stale line; it is NOT edited).

## Story 8.2 — Legal Decks, Default Decks, and the Door (cycle 137, 2026-09-15)

10. **DEFAULT DECK COUNTS.** Eric ruling 2026-09-15 (AskUserQuestion; delivered as a spreadsheet screenshot, transcribed here exactly). Every hull carries the same 30 universal cards: `armor 3, speed 3, turning 3, radarSweep 3, reload 3, hullRepair 3, shieldBlock 3, smokeScreen 2, chaff 2, deckGun 2, deckGunTurret 1, deckGunBarrel 2`. On top of those: **Torpedo Boat** `lightTorpedo 3, heavyTorpedo 3, machineGun 3, acousticHoming 1`; **Mine Layer** `navalMines 3, captiveMines 3, flak 3, foulingMines 1`; **Battleship** `missile 3, monitor 3, starShells 3, dazzleShells 1`. Each deck sums to 40 with exactly three equipment lines; every count is at or under its catalog cap. Five lines are UNHOMED (in no default deck): `supercavTorpedo, broadside, decoyBuoy, heatSeeking, phosphorShells`. Line composition follows FR56 except that the Battleship no longer carries HEAT SEEKING. **Supersedes** FR56's counts and catalog-v3 §1's starter counts. `DEFAULT_OWNED` (a fresh account's unlocks) is the union of the three decks' line ids — 24 of the 29 lines.

11. **INTERIM: the default decks ARE the frozen deck now; stub lines stay in the list but are never dealt.** Eric ruling 2026-09-15 (AskUserQuestion). From Story 8.2 on, a hull's default deck is the 40-card deck it sails; a stub line (amendment 5 — mechanism not built yet) stays in the frozen 40 so the legality check and the door see the real deck, but `buildDeckState` never deals it into `DeckState`, so it can never reach an offer. That leaves 23 drawable cards per hull (40 − 16 stub cards − the one carried copy) until Stories 8.7–8.15 flip the stub flags, at which point the same decks fill out with NO further code change. Rejected: keeping Story 8.1's interim "every buildable line at cap" deck. **Supersedes** nothing; fills the gap between 8.1's interim deck and the content stories.

12. **DRONES STAY GUN-ONLY — `FLEET_FIT` is STRUCK from the epic.** Eric ruling 2026-09-15 (AskUserQuestion). Eric: *"I don't know what that FLEET_FIT thing is, that should not have been added to the epic. It was never even discussed with me."* Epic-5 amendment 34 stands (*"each has a gun to defend itself"*): a PvE fleet hull carries its deck gun and nothing else, gets no deck, no cards, no `DeckState` (the frozen `EMPTY_DECK` and an empty `deckList`, pinned). Nothing named `FLEET_FIT` is built in 8.2 or any later story. Correction of record against epics.md Story 8.2 AC, Story 8.5 AC, FR45, AR38 and AR52 wherever they mention a `FLEET_FIT` — those clauses are void.

## Story 8.3 — The Draw (cycle 138, 2026-09-15)

13. **THE DRAW MODEL IS CONFIRMED AS "RESHUFFLE AFTER EVERY DRAW".** Eric ruling 2026-09-15 (AskUserQuestion). Eric's words: *"fairly from what is left in the deck"* means the deck reshuffles after every draw — it does NOT need to exhaust before unchosen cards re-enter the pool. The shipped model already does this: a draw READS the pool at full weight and only a FIT removes one card, so a passed-on card is back at full weight next level with no exhaustion step in between. Story 8.3 pins this reading and adds the missing pieces (the at-cap guard, the once-per-ship exhaustion callback, the `/metrics` counter); no new draw engine is built. **Supersedes** nothing; confirms `epic-8-context.md`'s "Draw economy" paragraph as already correct and closes the ambiguity a fresh reader could take from "fairly from what is left."

14. **AN EXHAUSTED DECK PRESENTS NO OPTIONS, BUT THE LEVEL STILL BANKS.** Eric ruling 2026-09-15 (AskUserQuestion). Eric's words: *"When you have expended all your cards, you are no longer presented with options."* An empty draw banks the level (the chip counts it), materializes no offer, queues no `pt` event, and shows no TAB cue. It stays spendable only via the menu heal — client-unreachable today (`deferred-work.md:982`) — until Story 8.8 makes heal a card. The "still banks" option was taken; rejected: "does not bank" and "open the band with heal only". **Supersedes** nothing; resolves the open re-derivation question left at `deferred-work.md:1995`.

15. **CORRECTION OF RECORD, DEFERRED TO STORY 8.8.** Eric ruling 2026-09-15 (AskUserQuestion). Eric's words: the free per-level heal "has been gone a while, it was replaced with a 1% missing HP per second while out of combat (over 30s since the last time you took damage)." Measured fact: `development` at `b18fba6` still carries `CONFIG.damageControl.levelMissingPct: 0.1` and `levelRegenMs: 5000` — the per-level, 10%-of-missing-hp-over-5-seconds heal — and no out-of-combat regen mechanism exists anywhere in shipped code or in any planning doc. Story 8.3 does NOT touch the level heal; Story 8.8's AC already carries a "check which version is live" clause, and that check must be resolved WITH Eric before either version is built. **Supersedes** nothing; ledgers the discrepancy for Story 8.8 (see `deferred-work.md`'s 2026-09-15 Story 8.3 section).

## Story 8.4 — The Damage Gate and the Ordnance Collector (cycle 139, 2026-09-15)

16. **SHELLS AND BURSTS DETONATE ANY ARMED MINE; CAPTIVE MINES STAY IMMUNE.** Eric ruling 2026-09-15 (AskUserQuestion). "Shooting ANY mine detonates it" (FR57, AR44) is read as: a shell or a burst sets off any mine on the water — the shooter's own or anyone else's — provided the mine has finished its arm delay; a mine still arming is not set off by a shell, and a CAPTIVE mine (the torpedo-launching kind, R2.18 / epic-7 ruling 2026-08-19) is never set off by a shell or a burst. Rejected: "any mine, armed or not, captive included"; "any armed mine, captive included". **Supersedes** the shipped owner-only gate in `detonateMinesInBurst` (own mines were the ONLY mines a burst could detonate); keeps the R2.18 captive carve-out.

17. **A MINE IS A POINT TARGET FOR A SHELL.** Eric ruling 2026-09-15 (AskUserQuestion, recommended option). A shell detonates a mine when its path passes within the SHELL's own radius (`CONFIG.<gun>.shellRadius`) of the mine's centre; no new mine-size number exists. A burst detonates a mine when the burst radius covers the mine's centre (unchanged rule). Rejected: the mine's trigger radius; a new CONFIG hit radius.

18. **A MINE BLAST CHAINS INTO ANY MINE IN RANGE, WHOEVER LAID IT.** Eric ruling 2026-09-15 (AskUserQuestion, recommended option). One detonation sets off every ARMED, non-captive mine within its blast radius regardless of owner, resolving the same tick through the existing visited set. Rejected: same-owner chains only (the shipped rule). Captive mines keep R2.18 in full: they neither receive nor propagate a chain (consistent with amendment 16). **Supersedes** the same-owner condition in `chainMines`.

19. **A BURST OVER A HULL THAT SANK THIS TICK MARKS `hc`, NOT `sp`.** Eric ruling 2026-09-15 (AskUserQuestion, recommended option) resolving `deferred-work.md` `:590`. When a multi-barrel click's first shell sinks a hull and a later shell of the same tick bursts over the wreck, the burst counts the wreck as a GEOMETRIC victim for the hit-call mark only: `hc` is emitted, no damage is dealt, nothing else changes. The zero-damage star shell keeps its exclusion (geometric victims never count for it — no unsanctioned detection channel). Rejected: keep `sp` and pin it as accepted. Refines the Story 4.3 `hc`/`sp` grammar (epic-4 amendments 16–18) for this one case.

20. **A MINE IS NEVER A COLLISION SUBJECT FOR A SHELL IN FLIGHT — ONLY THE BURST AT THE CLICKED POINT CAN SET ONE OFF.** Eric ruling 2026-09-16, verbatim: *"If I did not DIRECTLY click on the mine, then UNDER NO CIRCUMSTANCES WHATSOEVER SHOULD IT BLOCK A SHOT, REGISTER A HIT OR MISS, OR GIVE ANY INDICATION WHATSOEVER TO THE SHOOTER THAT ANYTHING MIGHT BE THERE. AT ALL."* A shell passing over a mine on its way to the aim point flies on untouched: the mine does not detonate, the shell is not consumed, no boom, no `hc`, no `sp`, nothing. A mine detonates by gunfire ONLY when the burst at the point the shooter clicked covers the mine's centre (the click IS the shooter's own information; the resulting mine boom stays sight-gated). Amendment 16's "shell or burst" therefore reads "burst"; amendment 18 (chains) and the captive/arming carve-outs stand. **Supersedes** amendment 17 (the in-flight point-target rule) in full and the Story 8.4 review ruling that a mine-consumed shell emits no mark (there is no mine-consumed shell any more).

## Story 8.5 — Nine Slots (cycle 140, 2026-09-16)

21. **INTERIM SPAWN SEED: A HULL SPAWNS WITH TODAY'S CARD-BACKED CLASS WEAPONS APPLIED AS CARDS.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). `loadoutFor()` is identical for every hull (gun + boost + seven empty), but until Story 8.10 hands out the level-zero card the spawn applies, through the ordinary card path, the card lines today's fit carries: Torpedo Boat `heavyTorpedo` (→ Q), Battleship `broadside` (→ Q) and `starShells` (→ E), Mine Layer `navalMines` (→ Q). The deck loses those copies exactly as today's carried seed does (`spawnDeck`/`carriedLines` read the seed from a per-hull list instead of the fitted loadout). Rejected: "truly empty until the first draw" (an interim gameplay hole); "one weapon per hull". Story 8.10 removes the seed. **Supersedes** nothing; fills the gap between the 8.5 AC's identical loadout and UX-DR54's "nothing from the deck is aboard yet", which 8.10 owns.

22. **THE RADAR BUOY GOES DARK FROM 8.5 UNTIL 8.15 DELETES IT.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). No card fits the legacy `radarBuoy` id and the loadout no longer fits it per hull, so no ship can reach it in play; its module, config row and tests stay (tests fit it directly). The Mine Layer loses the buoy now. Rejected: an interim Mine-Layer-only fit; deleting the module in 8.5.

23. **EVERY HULL GETS THE BOOST IN SLOT 1 NOW, AT THE SHIPPED `speedBoost` NUMBERS.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). Slot 1 holds the existing `speedBoost` module (+10 u/s, 6 s, 18 s reload, one charge) on every captain hull — the Battleship and the Mine Layer gain a boost they never had. Story 8.9 renames it to the v3 `boost` id and re-tunes it. Rejected: Torpedo Boat only until 8.9.

24. **PvE FLEET DRONES ARE ENVIRONMENT: THE DECK GUN AND NOTHING ELSE, BY THE LEAST-WORK PATH.** Eric ruling 2026-09-16, first verbatim: *"PVE FLEET DRONES DO NOT GET EXTRA SHIT. THEY ARE NOT PLAYER SHIPS. THEY DO NOT ACQURE LEVELS. THEY DO NOT GET WEAPONS BEYOND THE DECK GUN."* — then clarified in the same session: *"They don't need slots for PVE ships unless it is more work to create them without it. That's really what I was getting at. As I said, they are NOT combatants. They are part of the environment. They work the way that they do and that's it. They don't need special consideration."* Reading of record: drones get no design attention; the spawn's EXISTING fleet branch (the one that already hands a fleet hull the empty deck, amendment 12) also hands it a gun-only slot array (slot 0 the gun, the other eight empty) — one flag on the loadout builder, no new drone tests, no drone pins beyond the ones that exist. Extends epic-5 amendment 34 and epic-8 amendment 12. Rejected: an identical captain array with an unused boost (would need a "never presses Shift" pin — more consideration, not less).

25. **THE INTERIM HOTBAR KEEPS TODAY'S PITCH AND MAY CLIP ON SMALL VIEWPORTS.** Eric ruling 2026-09-16, verbatim: *"Ignore it entirely. The next story fixes the HUD."* Nine rows at the shipped 62 px slot / 14 px gap make a 670 px column that overruns the 614 px floor viewport and reaches the top edge at 768 px; no geometry work is done in 8.5 — Story 8.6's bar replaces it. Rejected: a tighter single column; two columns.

26. **A KEY ON AN EMPTY SLOT IS DENIED ON THE CLIENT ONLY — NO WIRE DENIAL REASON.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). The slot flashes its denied pulse and the denied tone plays; nothing is sent (a fair client cannot reach the server's `'empty-slot'` case, which stays server-internal as today). Under the held start line the lock wins and the key stays feedback-free. Rejected: a new `'empty'` `DenialReason` on the wire.

27. **DIGITS `1`–`4` WITH THE REFIT WINDOW CLOSED STAY INERT UNTIL STORY 8.7.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). The belt cannot be stocked before 8.7, so the digits keep their refit-only meaning in 8.5; 8.7 wires belt firing and the post-close grace. Rejected: wiring the digits to slots 5–8 now for an always-empty denial.

28. **HOW-TO-PLAY IS NOT TOUCHED IN 8.5.** Eric ruling 2026-09-16 (AskUserQuestion). Its EQUIPMENT lines (Q/E "class weapons and gear", R "pick up at sea", no Shift) stay as written, knowingly stale, until Story 8.20's re-cut. Rejected: a minimal update of the lines that become false.

29. **THE INTERIM SHIFT KEY CHIP SHOWS THE `⇧` ARROW (U+21E7).** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). The 22 px mono chip holds one glyph; the mock names the chip "Shift" without fixing one. Rejected: a wider SHIFT word chip; the letter S. Story 8.6 restyles the bar.

30. **A CLICK ON AN EMPTY BELT ROW IS SILENT UNTIL STORY 8.7.** Eric ruling 2026-09-16 (AskUserQuestion, recommended option). Key and click on one belt row behave the same way — nothing — until 8.7 wires the belt; the client-side empty denial (amendment 26) applies to the WEAPON row (Q/E/R) only. Rejected: belt clicks denying while the digits stay inert.
