---
idea: The deck model v3 as ONE unit (Eric: go big or go home): accounts + server-held per-hull authored 40-card decks (nothing required, copies = tier ceiling, Tier I = the unlock, equal per-card draw weight) + four consumable slots replacing the hand + heal as a scarce stockable consumable + gun-only spawns with a level-zero mulligan-able opening hand + deck editor + placement-scaled meta currency + match history as the live harness. Input: brainstorming-session-2026-09-01.md diffed against 2026-08-27.
goal: make it better — drive every open question in the 09-01 session to a decision or a named experiment; hardened forged-idea.md feeds the GDD update + new epic set
status: complete
updated: 2026-09-02T15:03
---

- (note) CARRIED from equipment-slot-rework (paused 08-28): its final 'PIVOT UNDER CONSIDERATION' (generic slots + slotless deck gun, one fixed arc per weapon, 4th weapon = replace, no hand/swap/sell-back) IS what the 09-01 brainstorm built. Three of its locks survive as standing constraints here: (1) ARC = SLOT BEARING x ITEM TRAVERSE, (2) SWAP CHEESE is a hard NEVER — moving a weapon must never yield more shots than leaving it alone, (3) THE SLOT KEEPS ITS CLOCK ACROSS REPLACEMENT (5s floor).
- (crack) CARRIED from boon-deck-rebalance: it LOCKED class-tilted OFFER WEIGHTING as the divergence engine ('Weighting sounds about right'). 09-01 #45 makes the draw equal per-card weight, which retires that lock as a DRAW rule; the tilt now lives in STARTER DECK COMPOSITION (a TB starter is torpedo-heavy) and in the author's copy counts. Unresolved: is composition-as-tilt sufficient, and what happens to the 07-30 soft-pity ruling (escalating rare weight) which #45 also retires.
- (note) Constraints of record for this forge: never invent a mechanic or number without Eric; his answers via AskUserQuestion/inline; the shipped Tab offer is untouched; the passive tick stays the anti-snowball floor; the master perception invariant keeps exactly six exceptions; accounts ship before the traffic push, which has NOT happened (soft release only, no ads yet).
- (lock) DRAW RULE: equal per-card weight, no pity, no class weighting in the draw. 'Deck size is the pity, composition is the tilt' — measured: a 1-copy line in a 14-line/40-card deck is offered at least once in 66% of 8-pick matches, 82% at 12, 90% at 15, 97% at 20 (vs 55-60% in the 60-card deck that bought the 07-30 pity). The class tilt the boon-deck-rebalance forge locked survives as STARTER DECK COMPOSITION only. Only smoothing anywhere is level zero (#81) if taken.
- (lock) CARD MODEL (A): a weapon is ONE ladder line (copy 1 = the weapon at base, copies 2-5 = authored tier bundles) plus separate 1-copy ADD-ONS (tube, doctrine verbs) drawable any time and held ahead of the weapon (#23). Universal lines (HULL, SPEED, COOLDOWN, SWEEP, gun BARREL/TURRET) are ladders on the same rule. Copies = tier ceiling (#40) only works under (A).
- (direction) ADD-ONS TARGET A WEAPON FAMILY, not one weapon: ACOUSTIC HOMING should apply to Light AND Heavy Torpedoes — the 08-27 tag alphabet (#22-28, 'equipment carries tags; upgrades target them') carries into v3 for add-ons. Multi-copy add-ons (raise draw odds and/or stack the effect) are a possible future, not needed now. A rework of which cards exist and which stats they move is expected later (Eric-authored, like 7-5-decks.md) — out of this forge's scope.
- (lock) LEGAL DECK (server-checked once at queue): (1) size EXACTLY 40 for now, but min/max are CONFIG dials and a floor band (e.g. at least 25, at most 40) MUST be tested — Eric undecided; (2) per line copies <= line max (5 ladder / 1 add-on); (3) every card unlocked on the account; (4) a deck belongs to one hull as a LABEL — catalog stays hull-agnostic, class-locked cards are NO; (5) no composition requirements (gunboat and zero-heal decks legal by design); (6) FROZEN AT QUEUE — the server snapshots the deck when you queue, later edits apply to the next match; (7) starter decks are ordinary decks that pass 1-5 against a fresh account's unlocks.
- (lock) CONSUMABLE SLOTS: four slots on the ship, keys 1-4 with Tab closed (Z-V the tested alternative). Full slots GREY the card in the offer and the SERVER REFUSES the pick — no instant-use (#82 retired); the immutable offer is the 'use one, reopen Tab, then take it' mechanism (Eric: 'use one and then pick it if it's what you really want'). A consumable LEAVES THE DECK ON PICK, not on use. Slot contents are server-owned ship state (sent on OwnShip), so a refresh/reconnect keeps them for free.
- (note) CONSUMABLE CONTENT IS OPEN — Eric: 'consumables should be able to do pretty much anything; it depends on what ideas I have.' The INSTANT/AIMED split is an ENGINE requirement, not a content taxonomy: the activation path must support key-fires (like boost) AND key-primes-then-click-fires (like Q/E/R through input.slot, one-round pool on the Equipment interface), and stay open to other shapes. The card face must tell the player which one it is.
- (lock) THE OPENING: level zero is granted at COUNTDOWN START; the first offer opens during the 10s countdown and can be picked before live or held. MULLIGAN = one free redraw of the level-zero offer only, only during the countdown — the single declared exception to 'reopening never rerolls', at a moment nobody is on the water. WEIGHTED FIRST DRAW is the default and built first: the level-zero offer is guaranteed to contain at least one ACTIVELY USABLE card (a consumable or a Tier I equipment) — Eric: 'my brain leans more towards weighted first pick'. PINNED CARD (#21, one deck card marked start-fitted) is a later CONFIG-gated experiment to test against it, not the default. Accepted consequence: the identity-fork 'promise' becomes 'you have something to DO at 0:00', not 'you hold your class weapon at 0:00'.
- (lock) HEAL CARD: effect is the shipped 100 hp (50 instant + 50 pooled at 5 hp/s) — what a heal DOES does not move here. Starter decks carry 4-5 heal copies [DRAFT starting value, derived from Eric's 08-27 sketch of 1-2 heals per 8 levels; ~c/10 of early offers carry a c-copy line], harness-tuned per hull. The 'heals during the sudden-death collapse' open question CLOSES BY CONSTRUCTION (at most 4 stocked, at most the copies in the deck). Per-level passive heal stays PARKED.
- (direction) PRIVACY POSTURE (Eric): store VERY LITTLE about a user; never store own emails or passwords; OAuth2 only (Google, Discord) with minimal scopes (openid / identify) so the account holds provider + opaque subject id and never an email, name or password. Age: 13+ by provider ToS proxy, not verified. Privacy-policy delta is one paragraph about signed-in accounts.
- (lock) ACCOUNT MODEL: NO GUEST TIER. Two states only. ANONYMOUS = today's game — open the URL, pick a hull, sail the hull's STARTER DECK; no currency, no history, nothing stored. SIGNED IN (OAuth) = decks, unlocks, currency, match history — the only reason to sign in is to KEEP things. Anonymous play uses the same starter decks and the same rules as signed-in play; signing in changes what you keep, never what you can do in a match. Killed: guest accounts (they existed only to persist what they cannot persist).
- (decision) UNLOCKS: an unlock is a whole LINE (all five tiers of a ladder, or the one copy of an add-on). FLAT price, any order [DRAFT — Eric: 'flat probably, idk'] — variety never power (08-27 #35). All three hulls and their starter decks are unlocked from day one; the starter decks' cards ARE the initial unlock list.
- (note) ARCHITECTURE FLAG, not ruled here: the account is the project's first persistent store + first non-ops HTTP API (OAuth callback, deck CRUD, history). Belongs to gds-game-architecture. Story 7-7 (frontend/backend split) stays DEFERRED and this must not pre-empt it.
- (decision) EARN SHAPE: an account LEVEL bar grants one UNLOCK TOKEN per level; a token unlocks any one line (flat). Unifies Eric's 'account levels or IGC'. Two dials only — XP per match (placement-scaled, Solo vs AI discounted) and XP per account level — both derived from ONE intent number: matches-to-unlock-the-launch-catalog. That number is OPEN (Eric: 'fuck if I know'); ship it as a CONFIG dial with a [DRAFT] placeholder in the 40-60 match band and tune from live match history (#79). Not asserted.
- (lock) BOTS: (1) each of the six profiles carries an AUTHORED 40-card deck in the player deck format, legality-checked at room creation — the bot is the first customer of the account rules; ghost decks (#63) are a later change of SOURCE, not mechanism. (2) A TOTAL ConsumableTactic record — one row per consumable card, no consumable ships without a bot use rule (instant ones keyed off profile state like the heal threshold; aimed ones through the weapon solve). (3) Two harness arms from day one: AUTHORED decks and RANDOM-LEGAL decks. (4) Pinned harness bars: starter-vs-veteran win band (#61), the two golden tests (torpedo-less TB #16, gunboat #67), heal-take rate, levels-wasted, one-copy appearance rate.
- (decision) 08-27 POSITIONAL SLOTS + PER-SLOT ARCS: PARKED, not dead — Eric: 'I don't think we'll need the mechanic.' Slots are generic, one fixed arc per weapon, a 4th weapon = replace-which. The arc-model lock (slot bearing x item traverse) stays on file. Tags survive only as add-on targeting.
- (lock) ENEMY DECKS: shown to no player (deck names and enemy reveal both rejected/parked). Every deck in every match IS recorded server-side in match history for Eric's internal tracking and metrics — the meta dashboard. Results/history show the player their OWN deck: brought, drawn, taken (#76).
- (lock) CATALOG BREADTH TARGET [DRAFT]: a hull can build from at least ~100 cards' worth of unlockables (~25-30 lines incl. add-ons and consumables; the shipped game has 28 lines total, already >40 cards available to every hull). Eric: '100ish is a solid target.' The catalog itself is an Eric-authored document (7-5-decks.md precedent), out of this forge.
