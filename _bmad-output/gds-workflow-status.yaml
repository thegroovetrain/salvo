# GDS Workflow Status
# Tracks progress through the Game Dev Studio workflow sequence.
# Each workflow entry is false (not started) or the path to its output artifact.
# Created manually (workflow-init not shipped in this GDS install) — 2026-07-15

generated: '2026-07-15'
last_updated: '2026-07-30'  # story 2-8 (Boon Catalog v1 — THE DECK MODEL) done via bmad-dev-auto (amendments protocol held: the brainstorm/party AC amendment recorded as amendment 38 + Eric's EIGHT AskUserQuestion rulings recorded as amendments 39-46 BEFORE implementation; orchestrated in 3 waves w/ /orchestrate model routing + Codex cross-model gate): THE DECK MODEL is live — per-player deck on ShipRecord (universal Intel/Ship/Gun lines + subdecks per carried equipment + rare 1-copy acquisition cards for absent equipment incl. EMERGENCY THROTTLE; per-ship decorrelated deckRng keyed by join ordinal), each level draws 4 DIFFERENT LINES weighted with escalating rare weight (CONFIG.deck dial — invisible pity, 2-10 tunes), chosen cards leave / unchosen return, copies ARE caps; the 42-line BOON_CATALOG v1 ships the ratified ladders verbatim (BoonDef +rarity/copies/exclusiveWith/healOnGrant + doctrine effect kind; validateCatalog authoring gate closes the 2.5 ledger entry), PV 15->16; the 14 legacy upgrades are STRIPPED WHOLESALE (upg field/event, applyUpgrade/applyGrantEffects, UPGRADE_*/CONFIG.upgrades, the effectiveStats counts param — signature is now effectiveStats(cls, boons)); damage/contactDamage/burstRadius/barrels/modes/litRadius/litDurationMs/triggerRadius promoted onto EffectiveStats with rangeU DERIVED from the folded radarRange (Intel is a stealth offense category — ratified); all four doctrine pairs have real on-water behavior (PLUNGING FIRE arcing/un-interceptable vs AP 3-hull pierce 100/50/25 w/ derived boom ids so the flying shell stays visible; ACOUSTIC HOMING steering + sight-gated torpU update events + 1300u travel budget vs COMMAND DETONATION radar-clamped point-det w/ point-blank clamp; SELF-PROPELLED creep w/ illegal-step rejection vs PROP-FOULING victim slow via self-private slowedUntil + the pinned boost->slow->hooks fold both sides; INCENDIARY DoT w/ 500ms-aggregated dmg events vs DAZZLE truesight cut + self-private dazzledUntil + honest fog shrink — dazzle deliberately does NOT cut radar, Eric FYI in ledger); doctrine swaps are free and ping-pong (rival card returns to the deck); acquisitions arrive LOADED (amendment 41 everything-loaded, incl. cap-raise top-ups), purge the equipment pool AND scrub banked offers (scrub-to-empty offers REMOVED — the FIFO deadlock died at review); shipHull cards heal their delta; baseline kit changes landed (amendment 39): star shells fully damageless (zone at stop point), mine is a click-aimed rear-arc WEAPON (placeRange 90, EQUIPMENT_IS_WEAPON flip, denial register on bad aims), mine chains are same-owner-only same-tick cascades (consume-first guard); the review HIGH became the same-click salvo single-hit rule (a TRIPLE MOUNT click would have one-clicked a 70hp TB — victims now take max one hit per salvo, guardrail extended over the aggregate); card UI: rarity tags, lineage pips II/V, name-by-stack-position ladders, live current->next contract text, doctrine REPLACES line, band overlap RATIFIED outright (amendment 40, pin re-taken at 236px cards). Review: 2 Fable hunters + Codex, 14 patches (1 high / 7 medium / 6 low), 1 deferred (pre-existing per-tick hull-snapshot staleness — Codex find), 5 rejected (incl. Codex's auto-revert 'exploit' — no-priming-state is the ratified architecture), 0 intent-gap, 0 bad-spec; every behavioral patch has a fail-without-fix regression test. Tests 1950->2081. PRIOR SAME DAY: boon-catalog brainstorm (the 2-8 design gate's dedicated design session WITH ERIC) complete same day — see brainstorm-game-boon-catalog below; PRIOR same day: story 2-7 (Offers — Roll, Bank, Spend) done via bmad-dev-auto (amendments protocol held: Eric's three AskUserQuestion rulings recorded as amendments 35-37 BEFORE implementation; invocation ruling enforced: TAB-not-SPACE against the stale epics/FR23/UX-DR14/EXPERIENCE wording): offers are now 4 BOON ids from 4 distinct categories rolled at earn-time from the INTERIM DUMMY BOON_CATALOG (amendment 35 — 5 categories x 2 stat-only boons on whitelisted BOON_STAT_PATHS, deep-frozen, insertion-order load-bearing/append-only, draft copy lives CLIENT-side in ui/boonCopy.ts, dies wholesale in 2.8; HOOK_REGISTRY stays empty) via catalog-parameterized rollBoonOffer(rng, catalog) (partial-Fisher-Yates category prefix, min(CONFIG.offer.size=4, categories), never throws) on the same decorrelated upgradeRng inside the UNCHANGED grantPoint seam; spendPoint bounds choice by the front offer's ACTUAL length (digit 4 live), shifts the FIFO, applies through World.applyBoon (the 2.5 seam now LIVE) and queues the NEW self-private 'bn' fitted event from spendPoint (applyBoon stays event-free, pin held); legacy applyUpgrade/applyGrantEffects/upg are production-unreachable (die in 2.8's strip; spend-driven heal/ammo top-up accepted gone per amendment 35). Wire: OwnShip.offer re-typed number[] upgrade indices -> string[] boon ids, PV 14->15. Client: refit window rebuilt as the ratified four-216px-card below-center DOM band (pure refitBandLayout pinned vs vitalsLayout/hotbar at 1366x768 + 1280x614; outer cards deliberately overlap the 38%-dimmed corner clusters at floor viewports — pinned as conscious, ledgered as a design tension for Eric), queue pips (filled current / hollow queued) + dashed ghost edge, STAY-OPEN-THROUGH-QUEUE (amendment 36 supersedes amendment 2's close-on-every-spend: pick latches+dims, ack advances to the next queued offer, timeout fires the 80ms denied pulse on the picked card, last-spend auto-closes via the offerView-null path), spendLatchReleased EXTENDED with the bn ack (latch.acked — the server's own receipt; with acked:false the predicate is byte-identical to the 2.6 rule) + new pure spendOutcome classifier + canLatchSpend gate; level-up toast+tone SUPPRESSED while dead/spectating (amendment 37 — closes the 2.6 deferred entry), bn drives the FITTED toast (deliberately not dead-gated: dead spend stays legal). Review: 2 Fable hunters + Codex (build-on-it x3), 7 patches (0 high, 1 medium, 6 low; the medium — both Fable hunters independently, Codex traced-and-accepted: the outcome classifier could fire a denied pulse on a spend that SUCCEEDED when a simultaneous passive bank cancels the pts drop and the next offer coincidentally rolls identical ids — killed by the bn-ack latch release; lows: vacated spectator spend-privacy pin restored + verifyFoggedEvent bn case, hidden-band pulse guard + hide() denied repaint, trySpend you-gone bail, empty-offer offerView null, CARD_SLOTS derives from CONFIG.offer.size, z-order comment fix), 1 deferred (band-corner overlap tension), 5 rejected (dev-only empty-catalog stuck level; 2 cosmetic hover/pulse corners; 67+ pip overflow unreachable under the zone timeline; sub-floor viewport clip is the deliberate never-wrap floor), 0 bad-spec; every behavioral patch has a fail-without-fix regression test. Tests 1894->1950. PRIOR: story 2-6 (XP Tick & Kill Bonuses) done via bmad-dev-auto (amendments protocol held: Eric's five AskUserQuestion rulings recorded as amendments 31-34 BEFORE implementation): level-ups are now the ONLY banking trigger — integer-ms XP accumulator on ShipRecord (xpMs/level), passive +dtMs per 50ms tick gated on the new xpEnabled policy flag (phase 'active', set by Match on the damageEnabled seam) AND alive AND human; kill credit via grantXp = 1 full level for a human victim, drone victims pay their SIZE TIER (droneSmall 1/4 / droneMedium 1/3 / droneLarge 1/2 — CONFIG.xp.droneTierLevels, the PvE fleet-tier hooks' first real consumer, amendment 31), fractions always carry (32), every threshold crossing banks through the unchanged grantPoint (pts === offers.length intact; offers stay legacy 3-card until 2.7); drones never accrue XP/offers (ratified bugfix — pre-2.6 they banked unbounded invisible offer arrays); redeploy wipes xpMs/level, respawn preserves; kill credit deliberately NOT alive-gated (dead-killer-still-banks preserved). Wire: self-private you.lvl + you.xp (0..1), PV 13->14, CONFIG.xp rides the welcome snapshot. Client: NEW render/xpRail.ts economy satellites in the gutter 2.2 reserved — 3px rail on the HP-rail idiom, LV tag, amber chip (breathing 2.4s -> static after 10s, re-arms on new bank or TAB refit open, hidden at zero, motion-scaled under the shared 1.1 Hz cap, ▲9+ clamp), 'LEVEL UP — TAB TO REFIT' cue line (amendment 33 vocabulary — 'banked' wording struck) — the bottom-right amber PTS readout DELETED (IN STORM reflows), toast now '▲ LEVEL UP — TAB TO REFIT', refit modal titled 'SPEND LEVEL — ×N'. Review: 2 Fable hunters + Codex, 6 patches (0 high, 1 medium, 5 low; the medium was Codex's find orchestrator-confirmed: the spend latch released on ANY pts/offer change, so a passive bank landing mid-flight re-enabled the modal and re-opened the double-spend-against-shifted-FIFO hazard — new pure spendLatchReleased predicate releases only on you-gone/pts-drop/front-offer-change/timeout, a bank now HOLDS the latch; both-hunters: non-finite grantXp guard, chip ▲9+ overflow clamp, state-preserving hideTransient so the P-toggle pose gap can't re-arm a decayed chip), 1 deferred (dead-killer toast advertises TAB while spectating where the reward is unspendable — 2.7's spend surface), 2 rejected (xp wire quantization: msgpack float64, no win; Codex's first-active-tick under-award: traced correct — activation wipes at the boundary, accrual over active time is exact), 0 bad-spec; every behavioral patch has a fail-without-fix regression test. Tests 1829->1894. PRIOR: story 2-5 (Boon Effect Engine — Two Homes + Hooks) done via bmad-dev-auto (amendments protocol held: Eric's three AskUserQuestion rulings recorded as amendments 28-30 BEFORE implementation): the ratified two-homes-plus-hooks engine lands as FULL DORMANT PLUMBING (amendment 28) — shared sim/boons.ts (BoonDef {id, category, effects[]}, four effect kinds: stat | slotFill | slotReplace | behavior, EMPTY deep-frozen BOON_CATALOG, fail-closed hasOwn resolve, ONE shared per-effect slot function used incrementally by the server and replayed by the client so slot ids can never fork) + sim/hooks.ts (kind-discriminated HookSpec, kinematics-ONLY attachment per amendment 30, EMPTY deep-frozen HOOK_REGISTRY with the signals-style literal-key parity lock armed, injectable at every call site so test hooks never enter production rows per amendment 29 — boost stays bespoke); effectiveStats(cls, counts, boons=[]) folds boon stat effects AFTER legacy stacking (zero-boon byte-identity pinned); OwnShip.boons self-private (rides `you` only, the upg precedent), PV 12->13; ShipRecord.boons + World.applyBoon (applyUpgrade mirror, no production caller until 2.7) + boost-then-hooks stepShips composition mirrored exactly by Predictor.tickKin (9-decimal replay parity vs a local server reference); redeploy wipes boons, respawn preserves them (loadout re-derived via slotsWithBoons). Review: 2 Fable hunters + Codex, 8 patches (0 high, 5 medium, 3 low; 1 CONFIRMED-reproduced: Object.prototype-key catalog lookups crashed the fail-closed path — hasOwn-gated everywhere; cross-model-corroborated: gun.maxAmmo removed from the stat whitelist so catalog data can never unpin the single-shot Deck Gun, fitted pools now clamp DOWN to a boon-lowered ammo cap; both-hunters: stat fold now skips non-finite/non-positive results and re-applies the ratified sweepRpm ceiling; Codex P1 encoded as convention: BOON_CATALOG/HOOK_REGISTRY content IS wire contract — any change REQUIRES a PV bump, else stale clients silently desync), 1 deferred (2.8 authoring-time validateBoonDef + top-up-on-raise + duplicate-fit design calls), 0 bad-spec; every behavioral patch has a fail-without-fix regression test. Tests 1754->1829. PRIOR: story 2-4 (Own-Vitals Cluster & Telegraph Restyle) done via bmad-dev-auto: the bottom-right cluster restyled in place to the ratified Afterimage register per the Eric-confirmed v2 composite (hud-composite-2.html) — horizontal HP bar + panel fill DELETED for a vertical ~6px HP rail on the body's right edge (dim phosphor track, bottom-up fill, bands phosphor >=50% / amber <50% / damageMarker <25%, HULL n/n floor-never-0-alive header, opacity-breathing pulse below 50% ramping 0.5->1.1 Hz on an INTEGRATED phase so the photosensitivity cap provably holds under storm drain, amplitude motion-gated w/ steady informational base at off); HDG/KTS 22px tabular phosphor suffix-form w/ dim-phosphor 0.7 labels (NO grey text in the cluster); 110px rudder track + clamped amber tick; telegraph shape-coded hollow-phosphor ordered rung vs solid amber needle; W/S·A/D key chips (drawer extracted to shared render/keyChip.ts, one family w/ hotbar) fade permanently after 3 successful LIVE-helm inputs per pair (labeled keys only; spectate/dead/arrows never count) persisted in a standalone hullcracker.* key surviving RESET SETTINGS, per-pair-max merged across tabs; 1.1 Hz ceiling promoted to ONE config token shared by rail pulse + storm vignette. Eric rulings recorded pre-implementation as amendments 24-27 (phosphor readouts over mock white; dim-phosphor labels; 3-inputs-forever fade; 6px rail). Review: 2 Fable hunters + Codex, 11 patches (2 high both cross-model-corroborated: pulse-phase strobe, spectate glyph-burn; 1 medium HULL-0-alive; 8 low), 0 deferred, 0 bad-spec; every behavioral patch has a fail-without-fix regression test. Client-only, no wire change, PV unchanged, no version bump. Tests 1688->1754. PRIOR: story 2-3 (Settings & Accessibility Options + Legibility Pass) done via bmad-dev-auto: the committed v1 settings land — DOM overlay (gear on home + in-match, non-pausing, port-chrome register, view-only binding reference from amendments 1-13) over a new hullcracker.settings localStorage store (legacy hullcracker-muted migrated read-once); motion full/reduced/off (one motionIntensity helper: ShakeDriver, hit flashes, storm-vignette pulse amp, juice effect peaks, hotbar pop; info never removed), UI scale 90/100/125% (125% disabled-with-note <1600px; Pixi hud-layer scale + layout divisor, DOM HUD tier via --hc-ui-scale transform; port chrome follows browser zoom; 9px post-scale mono floor), colorblind assist (implementer-drafted per Eric amendment 18: 20 hues -> 8 families swapped at the ships.ts PLAYER_HUES chokepoint, index%8 so wheel-adjacent never collide, pairwise deuteranopia-sim dE>=30 pinned by new cvd.test; baked blip outline ring + raised decayed-blip alpha floor), master/effects GainNode buses + mono merge + store-shared mute (M = overlay toggle). ABANDON MATCH (live-only, danger, 2-press confirm -> returnToPort chain) + RESET SETTINGS ship per Eric amendment 19. UNIFORM ESC LAW (amendments 21/23): ESC closes the topmost surface (results -> refit -> settings), else toggles settings; ESC NEVER returns to port; settings never stacks. ELIMINATION FLOW re-ruled (amendments 22/23): sinking immediately opens the results modal with the personal score (upgrades taken, roster-derived kill tally incl. drones, LOS-observed 'ships you sank' list excl. drones, placement othersAlive+1) or winner state; SPECTATE (live-only; ESC-equivalent) + RETURN TO PORT (Enter confirms at game end); silent auto-spectate deleted. LEGIBILITY PASS (amendments 14-17, Eric's standing complaint made mandatory): micro type lifted ~1.6x (hudMicro 9->14, label 11->17, per-module 10->16/12->18/13->20) across home/classSelect/hotbar/hud-ladder/results/killFeed/refit/toast/banner/nameplates with grown geometry (hotbar slot 54->62, tooltip 236->320); hotbar de-greyed (phosphor data + white names, cooling dims same colors 0.7 alpha); textMuted/textSecondary retired for load-bearing text everywhere. 'Standard Gun' renamed 'Deck Gun' (amendment 20). Server: sanitizeName() type-guard + 14-code-point cap closes deferred-work 127/130. Client + server-sanitizer only; NO wire change, PV unchanged, no version bump. Eric rulings recorded as amendments 14-23 BEFORE implementation (protocol). Tests 1545->1635. PRIOR: bugfix/tuning cycle (no version bump — Eric ruling): gun-family shell speed standardized to 500 u/s (retuned from 300 same day, Eric ruling) (gun/cannon/starShells, Eric ruling 2026-07-25) + Return to Port fix (extracted makeReturnToPort chain, leave() raced against 1s so a disposed room can never strand the player, results-phase Enter/ESC = RETURN TO PORT per UX-DR27); radar-sweep-rpm balance re-model landed (freeform bmad-dev-auto, NOT a gds phase/story — next_expected unchanged): sweep stat now tracked in RPM (CONFIG.vision.sweepRpm 15 = the same 4 s/rev the game always had; Eric ruled keep-15 after his 8 s premise was shown wrong), sweepSpeed upgrade re-modeled ×0.85-period/stack-uncapped → +3 RPM/stack HARD-CAPPED at 30 (the game's first capped stat; clamp lives in effectiveStats alone, offers untouched — 6th+ pick is a gunAmmo-precedent no-op), sweepPeriodMs stays on EffectiveStats derived 60000/rpm so all consumers/wire/PV unchanged; review: 2 Fable hunters, 7 patches (1 medium: fogSmoke.mjs read the deleted constant — smokes aren't type-checked), 0 deferred, 0 bad-spec; tests 1474→1476; spec-radar-sweep-rpm.md. ALSO (same day): story 2-2 (The Hotbar) done — the ratified Afterimage hotbar lands bottom-left: four 54px slots top-to-bottom Gun/Q/E/R with the KEYLESS GUN on top (Eric ruling, amendment 10 — the gun keeps a slot for stats/reload display, ghost key chip keeps row alignment), full 7-state grammar (idle silver .28 / ready-weapon phosphor .4 +10px glow / ready-ability .65 +14px glow + 9px chamfer shape-mark / selected amber + inset wash + FILLED amber key chip dual-coded / cooling card-scrim + 2px conic perimeter track + live seconds / activated <=80ms phosphor pop on the optimistic press edge / empty dashed slate + '— awaiting refit —' / denied red edge pulse — never silence), procedural Pixi icon linework per equipment id, ammo badge only on pools >1, hover tooltip (236px panel, mono caps name + amber interaction line, BOONS block rendered as ABSENCE), quick-info DMG/CD from effectiveStats+CONFIG via one equipmentInfo seam (amendment 13); slots are CLICKABLE and key-equivalent through the single keyboard.slotAction path, and a press anywhere on a row is SWALLOWED so it never fires the gun (amendment 11); own-vitals cluster (telegraph ladder, rudder, HDG/KTS, IN STORM) moved bottom-LEFT -> bottom-RIGHT beside the HP bar in current style (amendment 12, restyle is 2.4) and the interim chip row is DELETED; client-only, no wire change, PV unchanged. Eric rulings 2026-07-25 recorded durably in epic-2-context-amendments.md (amendments 10-13). Review: 2 Fable hunters + Codex, 14 patches applied (full-row hit test, amendment-13 literal DMG/CD split, ammo-aware cooling, hidden-hotbar click routing, single click decision path, tooltip presence/modal gating, poly helpers extracted to util/poly.ts). Tests 1474->1528; story 2-1 (The New Input Scheme) done — Q/E/R slot keys (weapon switch-to w/ auto-revert, ability activate-immediately via EQUIPMENT_IS_WEAPON), F+Space bound-inert (F = Foghorn-reserved per Eric ruling), TAB-toggled refit modal (digits 1-4 / click pick, FULL combat lockout, REPAIR spend DELETED end-to-end, PV 11→12), single keydown chokepoint (preventDefault all bound keys, text-entry guard, sim never pauses), canvas-only mouse fire + contextmenu suppression, alive camera zoom X-in/Z-out/wheel clamped 0.5-1.5×, both Epic-1 press-swallow ledger debts CLOSED (client FIFO denied feedback; server per-tick accepted-intent queue, bound = rate cap after tri-reviewer burst fix). Eric rulings 2026-07-24 recorded durably in epic-2-context-amendments.md (TAB supersedes SPACE-hold; digits refit-or-nothing; no repair; zoom values; debt scope). Review: 2 Fable hunters + Codex, 7 patches (1 high regression-proven), 2 deferred, 0 bad-spec. Tests 1444→1474; story 1-14 (Home & Class-Select Chrome) done — EPIC 1 COMPLETE: ratified home chrome lands over a NEW live ambient CIC Pixi scene (range rings, rotating sweep, decaying blips, island masses, radial scrim — never a blank canvas); wordmark/.IO, 14-cap callsign, Class Chip, Color Hoist (20 round swatches — the missing colorPref WRITER; connect() plumbing untouched), amber OUTLINE+GLOW primary (spine over mock's filled slab), inert gear/How-to-Play (quiet 'LATER REFIT' note pattern), server-status probe (probeServer(), CHECKING/READY/UNREACHABLE + connect states); class-select layer: 3×356px cards + dashed ghost on a rail, verbatim fantasy lines, long-form loadout rows, REAL-value pips on Eric's absolute anchors (speed 60 / hp 200 / turn 1.0 → TB 4/2/4, BS 3/4/2, ML 3/3/3), keys 1-3/arrows/Enter/ESC, footer SET SAIL picks+deploys; first-run forced choice (NO default, TB pre-focused); menu.ts deleted. ALSO Eric rulings 2026-07-23: truesight base 220→330 and starShells litRadius = SIGHT/2 (165, structurally derived; burst circle stays coupled; lit radius never scales with sight upgrades — ratio pinned in barrel.test); golden frames regenerated + audited. Client+shared-tunables only, PV unchanged; story 1-13 (Class Silhouettes on the Water) done: truesight nameplates land — screen-space callsign labels (mono 9px hud-micro, uppercase, 0.18em) float above every truesight combatant hull (own + contacts), colored in the pilot's WCAG text-safe personal hue (drones tag literal "DRONE" in droneOutline verbatim); plates live in a NEW screen-space plateRoot (constant size at any zoom incl. 0.5× spectate), ride the SAME snapshot sample + Fader alpha as the hull (fade in/out with truesight), and latch text/color once the roster name+hue resolve (no plate before resolve, never a session id, persists after a roster leave); ellipsizeName + NAME_MAX hoisted to new util/text.ts, entry cap tightened 16→14 (Eric ruling — menu maxLength/sanitize + stored-name re-slice); the silhouette/hitbox ACs already held by construction (Story 1.3's shared silhouette.ts is the sole geometry source ShipView.draw traces) — no shared/server/wire change, PV unchanged; story 1-12 (The Regatta Hoist) prior: personal-combatant colors land end-to-end — the server assigns each human a unique Regatta hue INDEX (0..19) FCFS at join (new pure game/regatta.ts: pref-if-free / nearest-free-clockwise-on-tie / seeded-random-free / defensive fallback) off one decorrelated mulberry32; the index rides the roster (PlayerMeta.color uint8, 255 = drone/no-hue sentinel) + a plumbed colorPref join option (sanitized 0..19, no UI writer yet); wheel ORDER promoted to shared REGATTA_HUES (names only — hex stays client tokens); client renders hull outline in the bright hue + SOLID darker fill (config.ts playerFills: 12 DESIGN-verbatim + 8 HSV-value×0.45 rule-derived), own wake in the hue, ordnance markers (mine/decoy/lit-zone) in the FIRER's hue for ALL observers via new MineView.by/DecoyView.by wire attribution (a deliberate intel grant), kill-feed names in 600-weight WCAG-text-safe variants (new util/color.ts textSafe); drones stay greyscale; PV10→11; perception invariants extended for `by`; sprint-status.yaml is the per-story source of truth
project: Hullcracker.io
project_type: 'game'
project_context: brownfield # existing playable RT prototype (v0.16.0); new ideation cycle

# --- Phase 1: Preproduction ---
brainstorm-game: _bmad-output/brainstorming-session-2026-07-15.md # complete 2026-07-15
brainstorm-game-supplemental: _bmad-output/brainstorming-session-2026-07-19.md # complete 2026-07-19 (focused design loop-back: gunboat replacement → 6-class roster + ability formula + Hades-hammer upgrade model; feeds gds-gdd update + gds-correct-course before story 1-1)
brainstorm-game-boon-catalog: _bmad-output/brainstorming-session-2026-07-30.md # complete 2026-07-30 (the 2-8 design gate's dedicated design session: THE DECK MODEL supersedes weighted rolls — per-player deck, 4-different-card draws, chosen cards leave, caps ARE copy-counts, escalating rare weight; 4 exclusive pairs + full 9-category catalog + ~60 dry-technical draft card ladders + Exclusive Law; party-review rulings incl. the mandatory 2.8 AC amendment: commons-are-stats settlement + smoke-generator deferral)
domain-research: false # optional
create-game-brief: _bmad-output/planning-artifacts/briefs/brief-Hullcracker.io-2026-07-15/brief.md # complete 2026-07-15 (addendum + decision log alongside)

# --- Phase 2: Design ---
gdd: _bmad-output/planning-artifacts/gdds/gdd-Hullcracker.io-2026-07-16/gdd.md # complete 2026-07-16 (epics.md + decision-log.md alongside; merged in PR #23); updated 2026-07-19 (three-class beta re-scope: gunboat cut, TB = torps + speed boost, ML ability OPEN, roster formula + Hades-hammer model + session design laws folded in; sub/carrier/decoy recorded as deferred expansion blueprint)
create-narrative: false # optional
ux: _bmad-output/planning-artifacts/ux-designs/ux-Hullcracker.io-2026-07-16/DESIGN.md # complete 2026-07-16 (EXPERIENCE.md peer contract + decision log + validation reports + mockups/ alongside; 4-lens reviewer gate passed; GDD corrections flagged: 4-choice offers, heal question, "Solo vs AI" naming)
prd: false # optional

# --- Phase 3: Technical ---
generate-project-context: _bmad-output/project-context.md # complete 2026-07-17 (AI-agent implementation rules; root TODOS.md/DESIGN.md marked deprecated, CLAUDE.md reconciled in same PR)
game-architecture: _bmad-output/game-architecture.md # complete 2026-07-17 (custom TS engine ratified; Colyseus 0.16→0.17 as work-item-#0; 8 decisions, 5 novel patterns; validated PASS)
create-epics-and-stories: _bmad-output/planning-artifacts/epics.md # complete 2026-07-17 (8 epics / 59 stories from GDD + architecture + UX; Epic 0 = Colyseus 0.17 work-item-#0; hosting move decoupled to trigger-based; 10 explicit design-decision gates; two party-mode review rounds applied; validated PASS)
check-implementation-readiness: _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-17.md # complete 2026-07-17 (READY — 0 critical; ~99% FR coverage; 14 non-blocking findings incl. medals trace, Story 6.4 pre-split, listening-ring ruling, 7.5 doc-hygiene batch)

# --- Phase 4: Production ---
sprint-planning: _bmad-output/implementation-artifacts/sprint-status.yaml # complete 2026-07-17 (8 epics / 59 stories / 8 retrospectives; all backlog — no story files exist yet; sequence E0→E1→E2→E3→E4→E5→E6→E7)
# story cycle (create-story -> dev-story -> code-review) tracked in sprint-status.yaml
retrospective-epic-0: _bmad-output/implementation-artifacts/epic-0-retro-2026-07-18.md # complete 2026-07-18 (epic-0 done 4/4; no epic-1 plan changes; critical path: matchSmoke hardening chore + Eric's human browser pass; rulings list awaiting Eric's confirm)
correct-course: _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-19.md # complete 2026-07-19, Eric-approved (three-class re-scope propagated: 1.9 tombstoned; 1.3/1.6/1.8/1.10/1.13/1.14/2.8/4.2 amended; FR1/FR4/FR10 + UX-DR9/10/26 recast; Epic 1 = 13 stories / 58 total; two new design gates carried in 1.6 (boost × torpedo) and 1.8 (ML ability + mine mechanics); DESIGN.md/EXPERIENCE.md reconciliation deferred to story 7.5 per Decision Point 1)
retrospective-epic-1: _bmad-output/implementation-artifacts/epic-1-retro-2026-07-24.md # complete 2026-07-24 (unattended, Epic 0 precedent; 13/13 reviewed — 122 patches/15 high/1 intent_gap/0 bad_spec, tests 770→1444, PV 3→11; no Epic 2 plan changes; critical path: fix epic-context recompile hazard BEFORE create-story 2-1, then Eric's browser pass + doc-sync batch + follow-up-review policy call; consolidated Decision Queue for Eric incl. unconfirmed Epic 0 rulings)

next_expected: create-story 2-9 (The Build Must Be Felt — FR22: every boon lands with an audible cue, a visible hull/slot change, and behavior on the water; a fit-check pass over the FULL 42-line catalog confirms no boon is presentation-silent; the hotbar tooltip finally renders the accrued-boon list as ◆Name + effect line). What 2-9 inherits from the 2-8 ledger (2026-07-30): AP pierce later-hit visuals, prop-fouling slow HUD tell, incendiary burn identity (dmg events pre-aggregated to 500ms windows), card-text overflow register on the longest doctrine cards, EXCLUSIVE rarity color currently in the storm purple family (needs Eric sign-off), and the boonTone seam beside fireTone in audio/tones.ts; every cue must respect the a11y caps (1.1 Hz pulse ceiling, <=3 flashes/s, dual-coding). After 2-9, story 2-10 (Economy Batch-Sim Harness) owes the evidence pass over every 2-8 draft dial (CONFIG.deck rare weight, catalog step values, doctrine factors; time-to-first-exclusive distributions per amendment 38) — note the abandonment-classification telemetry fix must land before 2-10's tuning pass per epic context. The heal question REMAINS open (healOnGrant is deliberately hull-card-only; engine stays heal-compatible). PRIOR: 2-8 was the design gate — its brainstorm (brainstorming-session-2026-07-30.md) and amendments 38-46 are the durable record; Option D swap-refund and the smoke screen stay ledgered past v1.
