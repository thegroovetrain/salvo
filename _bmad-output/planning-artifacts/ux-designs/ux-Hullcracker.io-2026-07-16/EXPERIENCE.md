---
title: EXPERIENCE.md — Hullcracker.io experience spine
status: final
project: Hullcracker.io
created: 2026-07-16
updated: 2026-09-11
design_reference: ./DESIGN.md
sources:
  - _bmad-output/planning-artifacts/gdds/gdd-Hullcracker.io-2026-07-16/ (GDD + epics)
  - _bmad-output/planning-artifacts/briefs/brief-Hullcracker.io-2026-07-15/ (brief + addendum)
  - _bmad-output/brainstorming-session-2026-07-15.md
  - imports/DESIGN-v0.16-root.md (via ./reconcile-design-v016.md)
  - .decision-log.md (canonical decisions, this run)
  - validation-report.md (reviewer gate, applied 2026-07-16)
  - epic-1..7 context amendments (reconciliation pass, Story 7-6, 2026-08-21)
  - _bmad-output/planning-artifacts/gdds/gdd-Hullcracker.io-2026-07-16/catalog-v3.md (v3 update, 2026-09-10/11)
  - game-architecture.md amendments 2026-09-09 (the account store) + 2026-09-10 (the deck)
  - mockups/hud-composite-3.html · mockups/countdown-results-1.html · mockups/shipdeck-1.html (v3 key screens, 2026-09-11)
  - .working/shipdeck-directions-v3-1.html (the merged Ship & Deck direction board, 2026-09-11)
---

# EXPERIENCE.md — Hullcracker.io

How the game works, surface by surface. Peer contract: [DESIGN.md](./DESIGN.md) (visual identity — every `{curly.path}` reference below resolves there). Distilled from `.decision-log.md`; **spines win on conflict with any mock, wireframe, or import.** Reviewer-gate findings ([validation-report.md](./validation-report.md)) applied 2026-07-16.

**Reconciliation stamp (Story 7-6, 2026-08-21).** Every statement below about shipped behaviour was re-checked against `client/`, `server/` and `shared/`, or against a ratified amendment. Where a 2026-07-16 ratification was superseded by a later Eric ruling, the later ruling governs and is cited inline. The aesthetic and interaction spine — the accessibility floor, the helm rows, the HUD zone anatomy, the photosensitivity caps, the anti-patterns — carries forward unchanged. Anything still unbuilt is marked as deferred rather than described as if it shipped.

**v3 stamp (E8 The Deck + E9 The Account, 2026-09-11).** Every passage marked `(v3, unbuilt)` below was designed in the 2026-09-10/11 update (`.decision-log.md` § Update 2026-09-10, every ruling Eric's) and ships with E8/E9; where it reverses a shipped or previously ratified rule the reversal is stamped inline in one clause, and same-day interim rulings live only in the log. Journeys B and C carry facilitator drafts tagged `[ASSUMPTION]` for Eric's redline; the home's anonymous/signed-in fork is `[OPEN — Eric]`.

## Foundation

- **Platform:** desktop browser (current Chrome, Edge, Firefox, Safari), keyboard + mouse only. Mobile/touch out of scope for beta.
- **Render split:** PixiJS 8 canvas for everything tactical; DOM only for chrome (home, class-select layer — SUPERSEDED 2026-09-11 (v3): the merged Ship & Deck screen, a DOM layer over home at the same 1318 px —, queue modal, results, settings, How-to-Play, privacy policy, kill feed, toasts; v3, unbuilt: the sign-in row, the Ship & Deck screen, match history). The bottom-centre bar is Pixi (v3, unbuilt).
- **Performance floor (UX constraints):** 60 FPS sustained in a full 20-ship match on the ratified reference device — MacBook Pro 16,1 (2019), i7-9750H, Radeon Pro 5300M, `devicePixelRatio` 2 (epic-7 amendment 1, read off the machine at run time) — where *"60 FPS sustains"* is a dropped-frame test, not a p95 threshold: median at the refresh period (≤18 ms) and dropped frames ≤0.5% of presents (epic-7 amendment 13). Playable from a cold load of the direct URL in under 10 s; feel intact up to ~150 ms latency. **The old "low-end school Chromebook" framing is struck** (Eric ruling 2026-08-21: *"Idgaf about the chromebook"*); the **1366×768 floor viewport survives independently** (NFR7, UX-DR39 — see Responsive & Platform) and every open review finding grounded on that viewport keeps its severity.
- **Input fantasy:** "hands describe the fantasy" — left hand helms the ship, right hand fights it.
- **Visual identity:** DESIGN.md is the reference for every token, component visual, and hard rule cited here.

## Information Architecture

Journey spine: **home → class select (v3, unbuilt: the merged Ship & Deck screen — hull, deck and confirm on ONE surface) → queue (Solo) *or* instant create (Solo vs AI) → boarding at a frozen start line → countdown → live → death reveal + results modal → return to port → re-queue from home.** Nothing is deeper than one layer from the water. Launch modes: **Solo** and **Solo vs AI**, presented as a MODE ROW; mode chrome grows only when more modes exist (the row is built to take DUO/TRIO as in-line siblings, which are not scoped and carry no reservation here — Eric ruling 2026-08-21, *"those come after beta launch. K.I.S.S."*). The GDD's "Solo vs Bots" naming was corrected to "Solo vs AI" GDD-side; that flag is closed. **Signed-in port loop (v3, unbuilt):** sign in → the deploy buttons / the Class Chip open the **Ship & Deck screen** (pick the hull, build and name the deck, buy copies, CONFIRM SELECTION) → sail → read history. The deckbuilder is neither a page nor a route — it lives in the layer the class pick already opens; only history is a port page, one route from home like How-to-Play `[ASSUMPTION: reached from the account chip]`; the one-layer rule is about the water and still holds.

| Surface | Medium | Delivers | Notes |
|---|---|---|---|
| Home (at rest) | DOM over live ambient CIC canvas | First 5 seconds must feel **"cool"** — the acquisition hook | Wordmark, callsign field (14-char cap), Class Chip (one glance = what you'll sail, opens the class bay — v3, unbuilt: the merged Ship & Deck screen, the home's ONLY door to decks), **MODE ROW** — the dominant amber outline+glow `SOLO` button with the unlit phosphor `SOLO VS AI` door centered on the row below (Story 6.5) — How-to-Play link, server-status line, settings gear, bottom-left liveness register, version register. **The mode buttons carry NO sub-line at all** (epic-6 amendment 50, Eric 2026-08-19 — the deletion is also a bugfix: an inline `visibility:visible` on a descendant defeated the home's yield to the settings overlay). `SET SAIL` no longer exists anywhere in the client. The Color Hoist lives ONLY in the class bay footer now — the duplicate home hoist is retired — but the player's hue still tints the chip and the callsign field. **v3, unbuilt — two home states; the anonymous/signed-in fork itself is `[OPEN — Eric]`** (2026-09-11: the 2026-09-09 two-state ruling stands until he rules; the dev/staging host is account-only, FIRM). A `SIGN IN` row renders only while `/liveness` reports accounts live — ABSENT otherwise, never disabled; sign-in is an OAuth popup (Google / Discord, no email scope), 30-day token; a popup closed or blocked, or a provider error, leaves the home anonymous and reports on the home status line in the `denied` register `[ASSUMPTION: copy unruled under THE COPY LAW]`. Signed in: `{components.account-chip}` `LV n · n ⬢` top-right by the gear; home is otherwise today's home — no `DECKS` link, decks are reached only through the deploy buttons / the chip; the callsign field is unchanged (the stored callsign; local seeds the account on first sign-in, the account wins after). The mode buttons still carry no sub-line |
| Ship & Deck screen (v3, unbuilt) | DOM layer over the dimmed home at the class layer's 1318 px | "The Hades weapon pick" — a complete playstyle promise, WITH the deck it sails | **Replaces the shipped class-select layer (SUPERSEDED 2026-09-11: the scroll rail, the ghost card and the class frame of [home-class-picker-1.html](./mockups/home-class-picker-1.html) are retired; its keys carry over — 1–3 / arrows highlight a tile, Enter confirms, ESC returns to home) and is the deckbuilder's only home — no route, no `/decks`, no `DECKS` link.** Opened by the deploy buttons / the Class Chip exactly as the class layer is today. **LEFT** pane: three tall class tiles (silhouette, name, the SPEED / ARMOR / TURNING pips, the player's hue on the selected tile), the Color Hoist at the foot. **MIDDLE**: the collection grid — all 29 lines as `{components.deck-card}` compact tiles (glyph · name · `{components.copies-rail}`; the full five-row card on hover), 5 wide, the only thing on the screen that scrolls; unowned lines wear the locked grey face with `{components.lock-price}` on the rail. **RIGHT**: `{components.deck-column}` — the named-deck dropdown + ✎ rename + × delete in its head, the deck's rows, the `n / 40 · n / 3 EQUIPMENT` legality line, SAVE (unlit while illegal). CONFIRM SELECTION bottom-right returns to home with the chip updated. Legality = exactly 40 · ≤3 equipment lines · caps · owned copies, evaluated client-side by the ONE shared function the server runs at the door. Named decks per hull: the hull's starter is deck 1 (default name STARTER); rename/delete; a new-deck control (copy DRAFT under THE COPY LAW); the dropdown defaults to the account's last-used deck for that hull; names are visible to their author only — the feed stays name-free. **Edge cases (Eric 2026-09-11):** × delete asks to confirm and cannot delete a hull's last deck; CONFIRM SELECTION with unsaved edits saves if legal, otherwise blocks on the legality line; a pending amber rung cancels on ESC or any click elsewhere; an unaffordable rung is inert with its price dimmed; deck names are 1–16 characters under the callsign's own filter, and a focused rename field suppresses the layer's keys as any text input does. Edits after queuing apply to the next match; the client sends a deck id, never contents. **Door refusal:** a deck the server refuses at the door (a copy revoked, a cap changed) shows the reason and offers `SAIL STARTER DECK` (architecture's wording) — never a dead screen; the surface it occupies (queue modal or home status line) is `[OPEN — Eric]`. **ANONYMOUS (ratified):** the IDENTICAL layout — anonymous differs by capability, never by layout; absent exactly: the dropdown, ✎ ×, SAVE, the − / + steppers, lock + ⬢ price, the account chip; the column reads STARTER DECK static with `40 / 40`, and the whole grid is visible with the starter's copies lit. Mock: [shipdeck-1.html](./mockups/shipdeck-1.html) — frame 1 signed in, frame 2 anonymous |
| Queue modal (Solo) | DOM modal over home | The wait, honestly | Opens exactly while being pooled is meaningful; closes on the seat, on an error, or on CANCEL. It says **three things and invents nothing** (epic-6 amendments 41/42): `N/20 QUEUED` (both numbers off the payload, never a literal cap), a `STARTS IN m:ss` countdown that exists ONLY while the pool is genuinely armed — an unarmed slot holds its line but stays EMPTY rather than showing a clock that cannot fire — and `CANCEL`. ESC cancels (amendment 45). No title sentence, no reassurance prose, no lobby-collapse register. The status line beside HOW TO PLAY speaks for the SERVER and nothing else |
| Liveness register | DOM, bottom-left of home | The port's only knowledge of the world outside it | `PLAYERS ONLINE: n` over `LIVE GAMES: n`, global totals rather than per-mode (epic-6 amendments 33-35, 40, 43). A genuine zero RENDERS — see Surface cold states |
| Solo vs AI door | DOM button on the mode row | One human + 19 AI captains, no wait | A `create`, never a queue (epic-6 amendment 29): the door mints a fresh locked room, so it starts instantly and can never pool. Game logic never forks — roster composition is a parameter, and an AI captain may legitimately win |
| How-to-Play page | DOM, standard page chrome, its own entry at `/how-to-play` | Onboarding surface (coach marks were pared to this) | Static page, ESC/back returns to the port (never `history.back()`). Sections as shipped: THE OBJECTIVE · STEERING · SHOOTING · EQUIPMENT · UPGRADING, with **keys rendered as keycaps** in the in-game refit-card chip so the scheme reads as one system (Eric ruling 2026-08-19). Closes with a link onward to the privacy policy. No Pixi, no socket, no analytics — a reader never downloads the renderer to read the manual. Solo vs AI is the live tutorial. **SUPERSEDED IN PART 2026-09-11 (v3, unbuilt):** EQUIPMENT and UPGRADING are re-cut for decks, the nine slots, consumables, Shift and the countdown REDRAW; copy is written at dev time — new-feature explanation goes here, never in-game |
| Privacy policy page | DOM, the same standard page chrome, its own entry at `/privacy` | The disclosure surface | Every sentence is a claim about shipped behaviour, and it speaks as **Hullcracker.io** in the third person — no operator name, no entity type, no country of residence (epic-7 amendment 19, Eric 2026-08-19). **v3, unbuilt:** gains one paragraph on signed-in accounts — what is held (provider + opaque subject id; callsign, colour preference, last class; the settings store; decks and deck names as stored text; unlocks and progress; own per-match rows), the 30-day localStorage token, self-serve deletion leaving anonymised match rows, nothing stored for anonymous play, the admin console can read the tables |
| Match history (v3, unbuilt) | DOM, standard page chrome `[ASSUMPTION: reached from the account chip; no route or entry was ruled]` | Your own record | Own rows per match — brought / drawn / taken with `T+mm:ss` stamps, placement, kills — cursor-paged; enemy decks never; the hidden match pool appears only as the pool cards you drew (unruled reading). States `[ASSUMPTION]`: loading draws nothing; an empty history and a failed fetch both render as absence (Surface cold states); rows in the kill-feed line grammar |
| Settings overlay | DOM | Accessibility + audio + bindings reference | Gear entry on home **and** non-pausing ESC overlay in match — opening mid-fight is the player's own risk (same philosophy as the refit window). Doubles as the in-match binding reference (view-only — bindings are fixed for v1; remapping deferred post-beta). Not reachable while spectating: ESC there reopens the score screen instead (epic-7 amendment 17, a consequence taken deliberately). **v3, unbuilt:** the reference lists the v3 scheme (Q/E/R generic, Shift, 1–4 belt, no `5`); signed in it gains SIGN OUT and DELETE ACCOUNT (with a confirm) and the settings are account-backed; remapping stays deferred — its recorded "no accounts" reason no longer holds and the deferral needs a fresh reason `[OPEN — Eric]` |
| Boarding — the held start line | Pixi HUD text | Lobby honesty, and the gate is real | `CAPTAINS BOARDING — n ABOARD` + `ALL STATIONS LOCKED`. **The sailable weapons-safe ready room is GONE in production** (epic-6 amendments 1 and 8, Eric 2026-08-14): everyone drops onto their real start location with **movement locked, weapons locked and radar off**, and the room holds until the last captain has loaded. Your spawn position is therefore disclosed to you before the gun — a ruled change, not a leak. No denominator: `expectedCaptains` is the queue's number and never reaches the client. The sailable grammar survives ONLY behind the dev/sandbox door, where the tag still reads `WEAPONS SAFE`. **v3, unbuilt:** the locked weapons are the deck gun, Shift and seven EMPTY slots — nothing from the deck is aboard yet |
| Countdown | Pixi | Match start | `MATCH STARTING` + big center count, 0:10. The tag stays `ALL STATIONS LOCKED` — the countdown is held exactly as boarding is, so softening the tag would read as the moment the helm came back. **v3, unbuilt (Eric 2026-09-11, ratified as rendered):** level ZERO is granted at countdown start and the refit window AUTO-OPENS on its offer (a weighted first draw guarantees ≥1 usable card); one `REDRAW` button (`{components.redraw-button}`, Primary Button register + one hollow pip) sits under the row, countdown only, and vanishes when the water goes live — the ONE exception to never-reroll; the row sits 44 px higher during the countdown so the button seats (accepted: the cards cover the held own hull); Tab still closes and reopens; bots never redraw. Mock: [countdown-results-1.html](./mockups/countdown-results-1.html) frame 1 |
| Live HUD | Pixi + DOM feed/toasts | The hunt | Full anatomy in HUD & Diegetic UI below |
| Refit window | DOM overlay, **TAB** toggles | Spend banked levels; (v3, unbuilt) take or REDRAW the countdown offer | Non-blocking, game runs behind; see Component Patterns. (v3, unbuilt) A card the belt cannot stock is greyed before the press and the server silently refuses it |
| Death: sinking → reveal | Pixi | "Losing into learning" | ~5 s ritardando (guns live) → omniscient reveal: fog drops, camera pulls back to the whole ocean, nameplates on every hull. **The reveal is the BACKDROP, not a beat of its own** (epic-5 amendment 24) — the results modal opens over it at founder, and the fullscreen dim is thin enough (`fogBase` at `results.dimAlpha`) to see the ocean through |
| Results modal | DOM Modal | Close the loop fast | The `SUNK` death-register banner with `9TH OF 14` under it, the `<CALLSIGN> · <CLASS>` identity line in your own hue, three stat tiles (**KILLS · PLACEMENT · TIME AFLOAT**), a **MATCH LOG** stamping every kill and your own death at `T+mm:ss`, `SHIPS YOU SANK`, and the `BOONS ACCRUED` / `LAST OFFER` blocks — **SUPERSEDED 2026-09-11 (v3, unbuilt, Eric, ratified as rendered):** those two blocks are replaced by ONE `LOADOUT` block (`{components.loadout-block}`) — the bar's own slot row at ended state with tier numerals on the absolute ramp, plus one line of the five ship-ladder tiers; brought / drawn / taken lives ONLY in match history (E9); the match pool is hidden (history shows the pool cards you drew — unruled reading); modal height at the floor viewport accepted. Mock: [countdown-results-1.html](./mockups/countdown-results-1.html) frame 2. **Two actions: SPECTATE + RETURN TO PORT** (epic-2 amendments 22/23) — SPECTATE is offered only while the match is still live. **ESC means SPECTATE**, never return to port. **No re-queue from the modal, ever** (epic-5 amendment 30, Eric: *"You MUST return to the home screen to requeue"*) — pinned by a test on the rendered action set. A responsive display ad sits in the right gutter while the score screen is up, and only once Google reports the slot filled; the panel and the ad centre as one group (epic-7 amendment 18) |
| Return to port | — | "Frantic to Play, Light to Hold" | Ends in a real page load back at the port; the next match is seconds, not menus; no account, no grind — **SUPERSEDED IN PART 2026-09-11 (v3, unbuilt):** an OPTIONAL account holds decks, unlocks and history (variety, never power); no account is required, and whether anonymous play stays on production is `[OPEN — Eric]` |

## Voice and Tone

Terse naval-command with a playful wink — "Silly Is Sanctioned": the tension is real, the wrapper never is.

- **Command register** for actions, as shipped: `SOLO`, `SOLO VS AI`, `CANCEL`, `RETURN TO PORT`, `SPECTATE`, `REFIT`, `ALL STATIONS LOCKED`, `WEAPONS SAFE` (dev door), `DEPLOY AS…` on the class cards. **v3 (unbuilt), ratified 2026-09-11:** `REDRAW`, `LOADOUT`. Working words, unruled under THE COPY LAW: `CONFIRM SELECTION` (the merged screen's confirm, ratified as rendered), `SAVE`, `STARTER`, `SIGN IN`, `SIGN OUT`, `DELETE ACCOUNT`, `SAIL STARTER DECK` (architecture's wording), the reason word `SLOTS FULL` (the ratified card's example).
- **Death register** is dry-naval: `SUNK` over `9TH OF 14`. Grim facts, no mockery, no exclamation points.
- **Microcopy rules:** uppercase mono for labels and system lines ({typography.label}); sentences only in descriptions, How-to-Play and the privacy policy; numbers always tabular; no banned/required word list exists — hold the register the locked mocks speak.
- Kill feed is naval theater: named vessels, personal colors, `X SUNK BY Y` / `X LOST WITH ALL HANDS`.

**THE COPY LAW (epic-6 amendment 41, Eric 2026-08-18 — quoted verbatim, and it binds every implementer):**

> **That was scope Eric never granted, and the process rule is the durable half of this amendment: a ruling to put information somewhere is NOT a licence to author the copy that goes there.** When a state seems to need words that were not ruled, the answer is to leave it empty and ask.

Two consequences already recorded against it. Copy authored in-cycle without a ruling is marked DRAFT and carried until Eric rules: `COULD NOT REJOIN YOUR MATCH — BACK IN PORT` (home status line, tone `info`, after a failed resume) was authored that way and is now **RATIFIED as-is** (Eric, 2026-08-21). And a deletion ruling deletes the machinery, not just the text — the mode-button sub-lines took their whole reserved-slot apparatus with them rather than being hidden.

## Component Patterns

Behavior only; visuals in DESIGN.md · Components (same names).

- **Hotbar Slot** — SUPERSEDED 2026-09-11 (v3, unbuilt, Eric: the 2026-07-16 bottom-left stack, the Gun–Q–E–R column and the per-class fit — torpedo + speed boost / broadside + star shells / mine + radar buoy — are retired): nine slots on ONE bottom-centre bar (`{components.hud-bar}`), left→right **Gun · Shift · Q E R · belt 1–4**. **The deck gun is keyless and SHOWN** at the head so its 5 s reload reads like every other cooldown; it is the permanently-selected default (Eric rulings 2026-07-21 / 2026-07-24 stand). **Shift** is the universal speed boost — every hull, no card: +25 % of the ladder-raised max for 10 s on a 20 s reload [DRAFT], scaled by the RELOAD ladder. **Q/E/R are generic weapon slots, EMPTY at 0:00**, filled by drawing a weapon's tier I; a deck holds ≤3 equipment lines so they never overflow and there is no replace flow; a slot keeps its clock across replacement. The **belt** (`{components.belt-slot}`, framed) holds four consumable stacks with a count: HULL REPAIR / SHIELD BLOCK / SMOKE SCREEN / CHAFF fire on their key (one physical press = one use; a second copy of a stocked line increments `×n`), DECOY BUOY primes on its key and fires on click into the mine's rear arc — the prime is replaced by any other prime and cleared by a click outside the arc `[ASSUMPTION]`; the card face carries no activation wording, and where the player learns which is which is `[OPEN — Eric: the activation channel]`. A depleted stack is plain empty. Every cooling slot wears `{components.cooldown-wipe}` (radial clock-wipe + seconds numeral). The activated-ability chamfer is RETIRED — group framing and key chips carry the weapon/consumable distinction. A weapon key primes its slot; the same key reverts to the gun; firing auto-reverts to the gun **on RELEASE, never on press** (Eric 2026-09-11), so the MACHINE GUN's stream runs until pointer-up, arc exit, an empty pool, Tab opening, or the window losing focus, and never resumes on close without a fresh press. A click on any slot is that slot's key. Labels: NAME; tier numeral in the slot corner on the absolute ramp (9 px, the floor); the tooltip carries the full list. Reloads tick on every slot regardless of selection; switching is tempo, not penalty. **No draw-pile counter anywhere** (Eric 2026-09-10 — inferrable from level). Mock: [hud-composite-3.html](./mockups/hud-composite-3.html).
- **HP Globe / Helm Globe** (v3, unbuilt) — the bar's two ends, replacing the HP Rail and the Telegraph Cluster. `{components.hp-globe}` left: `HULL n/n`, the threshold colours and pulse of Own HP unchanged; while shielded the register reads hull + shield (`HULL 312/250`, past max) in a changed colour `[ASSUMPTION: {colors.info} — no reserved hue]`, reverting on deplete/expire (Eric 2026-09-11); `[OPEN — Eric: whether `{components.shield-ring}` and the hull ring survive alongside the number, and whether an absorbed hit plays a distinct cue]`. `{components.helm-globe}` right: HDG/KTS inside, the 9-detent telegraph as a tick arc over its crown (hollow ordered rung + solid amber actual needle), the rudder track along its floor, W/S and A/D chips — every register at the 9 px floor (raised 2026-09-11; the globe grows a few px to seat them).
- **Ammo Badge** — appears only on slots whose system stores >1 round; counts down on fire, up on reload. (v3, unbuilt) On a belt slot it is the stack count — down on use, up when a card stocks it, never on reload.
- **Slot Tooltip** — on hover: name, interaction class, description, full accrued-boon list with effects (qualitative, Hades-style — this is where the player checks their build). (v3, unbuilt) For a weapon slot that list is the fitted line and its tier; the deck itself is reviewed on the Ship & Deck screen and in match history.
- **Banked-Level Chip** — appears at the head of the hotbar stack when ≥1 level is banked (v3, unbuilt: at the TAIL of the XP strip); count inside; breathing, never flashing; breathing decays to a static glow after ~10s unspent, re-arming on a new bank. Hidden at zero banked levels. The cue line beside it reads `LEVEL UP — TAB TO REFIT` (v3, unbuilt: `TAB TO REFIT` — the ratified frame drops `LEVEL UP —`, the chip's digit says it). (v3, unbuilt) Its first appearance is the level-zero offer at countdown start.
- **XP Rail** — passive XP + kill bonuses fill the rail; on level-up it wraps, the Banked-Level Chip increments, and a Toast fires. (v3, unbuilt) Now `{components.xp-strip}` — the full-width strip beneath the slots, `LV` at its head (LV 0 at 0:00), the bank chip at its tail.
- **Refit Card** — the spend window: **TAB toggles it open and closed** (Eric ruling ratified 2026-08-21 — see Interaction Primitives; the 2026-07-16 "SPACE is HOLD, never toggle (absolute)" ratification is RETIRED). Four cards side by side (offers present **4 upgrade choices**, `CONFIG.offer.size`); pick with **1/2/3/4**, and **5** spends on the always-available DAMAGE CONTROL rail one seam below the row — SUPERSEDED 2026-09-11 (v3, unbuilt): `5` and the rail are RETIRED, the heal is the HULL REPAIR consumable on the belt. Offers are **drawn from the player's own deck — up to 4 DIFFERENT card LINES, weighted by rarity** (epic-2 amendment 38 replaced the old "3 upgrades from 3 distinct categories" roll wholesale; v3, unbuilt: equal weight per card, from your 40 authored cards plus the match's hidden pool of 10 — a consumable leaves the deck on pick, a ladder copy when fitted); they are materialized once at earn-time and **never reroll — and banked offers never expire** (explicit guarantee: peek, close, fight, re-open; the time pressure is self-managed). (v3, unbuilt) Never-reroll has ONE exception: the countdown `REDRAW`. A card the belt cannot stock (all four slots hold OTHER consumables) is GREYED before the press — dim + the reason word `SLOTS FULL` + a hollow key chip — and its digit or click SENDS NOTHING: no spend, no latch, no denied pulse; the card stays in the offer (use one, then pick it). Queue pips + ghost edge show offers waiting behind the current one. A pick LATCHES: on success the next offer renders IN PLACE and the window stays open; on a server rejection or timeout the picked card fires the denied pulse and the level stays banked (epic-2 amendment 36 — spending no longer closes the window). When the window closes — Tab, ESC, or the last level spent — the digits are inert for a short grace (implementer dial) so a mashed pick cannot spend a belt slot (Eric 2026-09-11). Non-blocking: the battle stays visible in every gap, and only the slot groups (Gun · Shift · Q E R · belt) dim to 38 % while open — the globes, the helm and the XP strip stay at full (Eric 2026-09-11) — but while it is open the game is under full combat lockout (mouse fire suppressed, a held stream ends, **Q/E/R/F suspended**; v3, unbuilt: `1`–`4` are the card keys while it is open, so the belt cannot fire — `[ASSUMPTION: Shift is suspended with them; the lockout was ruled before Shift existed]`); the helm (W/S/A/D) stays live. Refit trades attention for build progress — deliberate. The row keeps its 388 px seat live and never wraps; the own-hull keep-out is WAIVED — cards may cover a hull, live and countdown (Eric 2026-09-11; DESIGN · Refit Card).
- **The card face is MINIMAL, and the explanation is hover-only** (epic-7 amendment 26, Eric 2026-08-19): name, lineage, rarity tag, `current → next`. The prose moved to a hover tooltip precisely because the Tab/1-4/5 shortcut exists to SKIP the reading — wiring the explanation into the shortcut would put it in front of the player who does not want it. The rarity ladder rides the loot-tier colour ramp (green → blue → purple → red → gold), absolute rather than normalised, and still dual-coded by the Roman numeral and the ladder name. **SUPERSEDED IN PART 2026-09-11 (v3, unbuilt, ratified) — the face:** ICON · NAME · TIER on the absolute ramp · KIND word · FIVE fixed stat rows (`STAT current → next`, unused rows blank) · a fixed reason-word foot; NO activation wording, NO prose. Ship ladders ARMOR / SPEED / TURNING / DECK GUN start at Tier I equipped (the first card reads I→II); RADAR SWEEP and RELOAD are cards I–V; tierless cards (consumables, add-ons) leave the tier slot blank. Hover-only explanation stands; the shortcut is Tab/1–4. The ramp now rides the TIER numeral.
- **Class Card / Class Chip / Color Hoist** — chip on home shows the current pick and opens the layer; cards carry silhouette, pip scales (SPEED / **ARMOR** / TURNING — real values on absolute anchors: TB 4/2/4 · BS 2/4/2 · ML 3/3/3) and two special-slot rows keyed Q/E — SUPERSEDED 2026-09-11 (v3, unbuilt): the card becomes a TILE on the Ship & Deck screen (silhouette, name, pips, hue; the shipped keys carry over) with no Q/E rows and no deck row — the deck picker is the `{components.deck-dropdown}` in the head of the `{components.deck-column}`, so the column shows the selected deck's rows and a captain sees what they are about to sail; a deck is never a forced choice; CONFIRM SELECTION returns to home with the chip updated `[ASSUMPTION: the chip carries the deck name beside the class, as the mock's backdrop home shows]`. Hoist sets the color *preference* (granted if free; if taken, nearest free hue clockwise — FCFS at join, Eric ruling 2026-07-23) — UI must never imply claiming or locking; it reads as a WHEEL (epic-7 amendment 36). First-run: forced choice — no default class is ever pushed, the chip reads `SELECT CLASS`, and either deploy button opens the layer instead of connecting; Torpedo Boat pre-focused (Eric 2026-07-19 / 2026-07-24).
- **Deck Card** (v3, unbuilt) — the collection grid's compact tile: glyph · name · copies rail; the full five-row refit face appears on hover; a locked line wears the grey face with its rail live — the rail, not the face, is the control.
- **Copies Rail** (v3, unbuilt) — one rung per copy up to the line's cap; press a hollow rung to light its price, press the same rung again to buy; a pending rung cancels on ESC or any click elsewhere; rung states and the − / + steppers in State Patterns › Copies-rail rung states.
- **Lock Price** (v3, unbuilt) — the lock glyph + `⬢ n` on an unowned line's rail; pressing rung 1 IS the unlock; unaffordable = inert with the price dimmed; absent for anonymous.
- **Deck Column** (v3, unbuilt) — the selected deck's rows, the legality line (failing count in `denied`) and SAVE, unlit while illegal and lit on a legal edit; a successful save has no confirmation beyond the line settling (copy unruled under THE COPY LAW); CONFIRM SELECTION saves a legal edit or blocks on the line.
- **Deck Dropdown** (v3, unbuilt) — that hull's named decks with `✓` on the current one, defaulting to last-used (STARTER on a fresh account), `+ NEW DECK` (copy DRAFT), ✎ rename (1–16 chars) and × delete (confirm; never the last deck) beside it in the column head; ESC closes an open menu before it closes the screen; anonymous reads STARTER DECK static.
- **Account Chip** (v3, unbuilt) — `LV n · n ⬢` top-right by the gear, signed in only; updates on sign-in, sign-out and every token spent; absent anonymous `[ASSUMPTION: opens match history]`.
- **Mode Row** — `SOLO` queues in the shown class/color preference; `SOLO VS AI` creates a fresh 1+19 room instantly. **Enter is bound to the callsign field and runs SOLO only** — the SOLO VS AI button carries no `⏎` chip, because a key chip is a truthfulness claim and may appear only where Enter really does that thing. Neither button carries a sub-line. (v3, unbuilt) Signed in, the callsign field is unchanged and shows the stored callsign. While a join is in flight both doors disable and the status line reports; on failure it reports plainly (`CONNECTION FAILED — IS THE SERVER RUNNING ON :2567?`), never a dead screen.
- **Kill Feed** — max **6 lines, 8 s TTL**, newest on top; names in personal colors (text-safe variants), PvE fleet hulls greyscale; long names mid-ellipsize. **The feed is GLOBAL** (the Public Register): every captain's sinking reaches every client at any range through any fog, by any cause including the storm, and presentation is identical for a witnessed and a reported kill. The payload is IDENTITY ONLY — no position, class, hue, damage or weapon — so *location* stays exactly as protected as it always was, and an unwitnessed kill draws no sink plume. Drones are not combatants: a fleet sinking reaches only its witness and its killer. A `☠︎` mark rides the kill leader's name wherever it appears in a line, as killer or victim.
- **Toast** — transient self-events only (level banked, boon fitted, `YOU ARE THE KILL LEADER`); max 3, 3 s TTL; never carries enemy information.
- **Modal** — results, settings overlay, queue modal. Modals never stack; the ESC settings overlay does not pause the game. Results keys: **Enter = RETURN TO PORT, ESC = SPECTATE**; while spectating, ESC reopens the score screen (epic-7 amendment 17). No re-queue key exists anywhere in the modal. (v3, unbuilt) Settings gains a DELETE ACCOUNT confirm; the Ship & Deck screen is a DOM LAYER over home like the class layer it replaces and match history is a page — neither is a modal, so never-stack is untouched.
- ~~**Bounty Bloom**~~ — **RETIRED 2026-08-10 (Story 4.6, Eric ruling)**: no location disclosure of the kill leader ships, ever. The bounty survives as a held throne over captain-only kills, presented as KILL LEADER (player-facing copy ruling; internal naming stays `bounty`) — a skull mark (`☠︎`) rides the leader's name wherever it appears in the feed, as killer or victim, with a static faint glow on that name; the claim register reads "☠︎ <NAME> IS THE NEW KILL LEADER"; the BR Chrome Bar carries a persistent "☠︎ <NAME>" register; and a self-only "YOU ARE THE KILL LEADER" toast + `bounty` tone fires when the throne lands on you. Visual: DESIGN · BR Chrome Bar / Kill Feed (the Bounty Bloom row there is likewise retired).
- **BR Chrome Bar / Foghorn Chevron / HP Rail / Telegraph Cluster** — see State Patterns and HUD & Diegetic UI. (HP Rail and Telegraph Cluster SUPERSEDED 2026-09-11 (v3, unbuilt) by `{components.hp-globe}` / `{components.helm-globe}` — see HP Globe / Helm Globe above.)

## State Patterns

**Hotbar Slot states** (visual grammar in DESIGN.md):

| State | Trigger | Signal |
|---|---|---|
| Ready (weapon) | Loaded, unselected | Soft phosphor outline |
| Ready (ability) | Off cooldown | Brighter phosphor + chamfer — SUPERSEDED 2026-09-11 (v3, unbuilt): Shift wears the weapon's ready outline; the chamfer is retired |
| Selected | Q/E/R press on a weapon (the gun is selected by default and has no key) — (v3, unbuilt) a FITTED weapon, or a belt digit on the DECOY BUOY | Amber outline + glow; inset wash + filled key chip (hue secondary) |
| Cooling | Fired / reloading | Icon dims, conic perimeter ring fills, seconds in quick-info — SUPERSEDED 2026-09-11 (v3, unbuilt): `{components.cooldown-wipe}` — icon at 40 % under a dark overlay uncovering clockwise from twelve, the seconds numeral centred (tenths under 2 s); the deck gun and Shift wear it too |
| Activated flash | Ability triggered | ≤80 ms phosphor pop, decays to idle |
| Empty | Extra slot unfitted — SUPERSEDED 2026-09-11 (v3, unbuilt): any Q/E/R or belt slot unfilled — the opening state of all seven at 0:00 — and a depleted stack (*"empty is empty"*, no ghost glyph) | Dashed outline, "— awaiting refit —" (v3, unbuilt: dashed outline + em dash, no label) |
| Denied | Fire **or activation** attempt while invalid (cooling, no ammo, empty slot) | 80 ms red edge pulse + icon flash — **never silence**. (The held start line is not a denied state: the helm, trigger and scope are held by the match itself, and the HUD says so with `ALL STATIONS LOCKED` rather than blaming the player. The dev/sandbox ready room is likewise not denied — weapons genuinely fire there, damage suppressed). (v3, unbuilt) A key on an EMPTY slot is denied — the normal opening state, suppressed under the held start line, where the lock wins; a decoy PRIME is not a denied press; a GREYED refit card is NOT a denied state: its digit sends nothing and nothing pulses |
| Stocked (v3, unbuilt) | A belt slot holding a consumable | The belt square with its count on the ammo badge |
| Held fire (v3, unbuilt) | MACHINE GUN click-and-HOLD inside its arc (drawn only while a stream kind is primed and draining — never from the pointer level alone) | Amber outline + the ammo pool draining along the slot floor; leaving the arc gives ONE out-of-arc denial, then silence; the stream ends on pointer-up, arc exit, an empty pool, Tab opening, or the window losing focus, and the prime reverts to the gun on release |

**Copies-rail rung states** (v3, unbuilt; `{components.copies-rail}` on the Ship & Deck screen, ratified 2026-09-11) — one rung per copy up to the line's cap: **hollow** = unowned · **silver-filled** = owned · **phosphor-lit** = in deck · **amber + `⬢ n`** = a hollow rung pressed, awaiting the confirming press on the SAME rung (press-then-confirm everywhere); a pending rung cancels on ESC or any click elsewhere; an unaffordable rung is inert with its price dimmed. Unlocking a line IS buying copy 1. `−` / `+` beside the rungs are the in-deck steppers (`−` dim at in-deck 0; `+` dim at in-deck = owned, or for an equipment line outside the deck at 3 / 3). Anonymous: rungs render but nothing is pressable — no steppers, no lock, no price.

**Attention priority** — arbitration across the HUD's animated channels (one hierarchy; photosensitivity caps still bind every tier). Pips obey the intensity ruling: urgency = intensity, never source type.

| Tier | Channels | Rule |
|---|---|---|
| 1 — Threat | Denied pulses (ALL of them — the on-water arc/marker pulse and the per-slot hotbar pulses), HP Rail pulse in its crimson band (<25%) (v3, unbuilt: the `{components.hp-globe}` pulse — the rail is retired) | Always animate (rate-capped by the photosensitivity floor); own the player's eye |
| 2 — Match state | Ring-countdown final-10s amber pulse (1 Hz), in-storm vignette (1.1 Hz) | Animate unless a Tier 1 channel is active, then hold steady at the lit keyframe. A Tier 2 channel does NOT hold under another Tier 2 |
| 3 — Economy | Bank-chip breathing, toasts, XP-rail wrap | Freeze at the dim keyframe while any higher tier is active — including Tier 2 alone (the asymmetry with the Tier-2 rule is deliberate and implemented literally). Chip breathing decays to static after ~10s regardless. Of the three channels named, only the bank chip actually breathes: toasts fade on a TTL and the XP rail does not animate. (v3, unbuilt) `[ASSUMPTION: the cooldown wipe, the held-fire drain and the shield ring are progress readouts, not attention channels — they take no tier, as the conic ring took none]` |

**The Listening Ring is DEFERRED and is deliberately NOT a Tier-1 channel** (epic-4 amendment 1; `render/attention.ts` states it in code). Nothing may list it as an input until it exists. The shipped bearing surface is the **Foghorn Chevron** — see HUD & Diegetic UI.

Corollary (amber overload): only the highest-tier active amber channel pulses; every other amber element holds steady — amber keeps meaning "look here" at the climax.

**Own HP** — threshold colors + accelerating pulse, blended channels: {colors.phosphor} at ≥50%, {colors.amber} below 50%, {colors.damage-marker} below 25%; pulse rate rises with damage **and hard-caps at 1.1 Hz** (the storm vignette's ratified exception ceiling; opacity-breathing, never strobing — the cap resolves the acceleration-vs-floor contradiction). [ASSUMPTION-lite, log-flagged] The blend (both channels rather than either/or) is a facilitator resolution, unobjected-pending. (v3, unbuilt) **Shielded:** the register reads hull + shield (`HULL 312/250`, allowed past max) in a changed colour `[ASSUMPTION: {colors.info} — amber and the red family may not carry it]` and reverts — number and threshold colour — when the shield depletes or expires (Eric 2026-09-11). `[OPEN — Eric: whether the globe ring + hull ring survive alongside the number; whether an absorbed hit plays a distinct cue]`.

**Banked levels** — 0: no chip; ≥1: breathing chip + count + the `LEVEL UP — TAB TO REFIT` cue; while refit open: queue pips show current vs waiting offers; spend latch dims cards while a spend is in flight. **Spend failure:** if the server rejects or times out a spend, the latch releases with the denied pulse register on that card, the level stays banked, and the window stays open. (v3, unbuilt) The level-zero bank exists at countdown start with the window auto-open and `REDRAW` available; a GREYED card is a different dim from the spend latch — it carries the reason word and a hollow key chip, and it never pulses.

**DAMAGE CONTROL** — **SUPERSEDED 2026-09-11 (v3, unbuilt, Eric): the heal IS a card.** HULL REPAIR is a stockable consumable in the belt (100 hp: 50 instant + 50 pooled at 5 hp/s — the shipped effect), fired on its digit with the refit window closed; `5`, the rail and `HEAL_CHOICE` are retired. Heals are bounded by stock (four belt slots) and by the deck (≤10: 5 authored + up to 5 from the hidden match pool), so the sudden-death ceiling is finite by construction; the shipped free per-level auto-heal stays until the balance pass (GDD note 14). *Retired rule (2026-08-21):* an always-available rail one seam below the refit row, digit **5** or a click, on the reserved `HEAL_CHOICE` sentinel — never drawn, never exhausted, never in the offer payload (epic-2 amendments 58-64, 67); the same 50 + 50 hp, uncapped by banked levels.

**Color grant feedback** — REJECTED (Eric ruling 2026-07-23, Story 1.13): no contested-hoist toast. Color is FCFS at join; the granted hue simply flies on the nameplate. Do not build absent a new ruling. There is likewise no `--player-color` CSS variable: personal hues reach DOM chrome as inline hex through `textSafe()` at each site, and no per-hue variant table exists — the lightening is algorithmic against the ≥4.5:1 rule.

**Ring phases** (legible phases are the pillar's word): **four** ring groups × an internal minute rhythm — clear seas → a reserved supply-drop beat that is a structural no-op with zero HUD trace → next ring revealed ("where you must be is now known") → ring closes. The first three groups run to **12:00**, where the **endgame ring** stands at two truesight diameters (`terminalSightFactor × sight` = 660u — the Endgame Guarantee, and the same number radar range reaches by an independent derivation). **A fourth group then runs 12:00 → 16:00 (SUDDEN DEATH, cycle 82, Eric ruling 2026-08-14):** clear 12:00–13:00, supply 13:00–14:00, **reveal at 14:00 — the collapse point is marked with an X** — and from **15:00 to 16:00 the endgame ring collapses CONCENTRICALLY onto its own centre to radius 0.** The map is 100% storm at 16:00, which is what structurally ends a match the geometric bar could not. **No new player-facing copy ships with it:** the readout grammar is generic over group count and simply reaches `RING CLOSED` at 16:00 instead of 12:00. The BR Chrome ring readout counts down each closure and pulses amber at 1 Hz in the final 10 s. Storm never blinds sensors; it only damages (4 hp/s, flat, no ramp — Eric ruled the geometry, not the damage curve). In-storm: purple vignette pulse + "IN STORM" line. **The storm is NOT a radar return** — cycle 72 deleted the radar storm wall by preference; the ring renders on the water exactly as before.

**Match length.** ~15 minutes is the estimated game time; the theoretical ceiling with nothing banked is a little over 17 — closure at 16:00, and the game's toughest hull sinking roughly a minute and a half later at 4 hp/s (Eric, 2026-08-21). Hoarded heals push the outer edge further (v3, unbuilt: bounded at ten HULL REPAIR per deck), but a match always terminates.

**Match lifecycle** — queue (Solo) or instant create (Solo vs AI) → **boarding at the frozen start line** (movement, weapons and radar all locked; spawn location disclosed) → countdown (still held; v3, unbuilt: level zero granted, the refit window auto-open on its offer, `REDRAW` available) → live → own death (sinking window ~5 s, guns live) → omniscient reveal as the BACKDROP with the results modal opening over it → SPECTATE or RETURN TO PORT. **Reveal HUD survivor set (ratified):** BR Chrome Bar + Kill Feed persist through the reveal; hotbar, XP Rail, Banked-Level Chip and own-vitals die with the hull (v3, unbuilt: the whole bottom bar — slots, belt, XP strip, both globes — dies with the hull). Nameplates appear on ALL revealed ships, and **your own wreck stays on screen with its nameplate** (epic-5 amendment 24 — hiding it was only defensible while the reveal was a curtain nobody saw through). **Sinking window inputs:** combat inputs stay live; helm inputs are accepted but decay (the hull slows to a stop regardless). **Omniscient reveal inputs:** the modal owns the screen; ESC = SPECTATE, Enter = RETURN TO PORT. Disconnect mid-match: a bare `RECONNECTING…` banner while the client auto-retries — no countdown, no attempt count (epic-6 amendment 49) — and a successful resume returns seamlessly to the live HUD; a page REFRESH also resumes, inside the 60 s grace. Leaving is always deliberate — RETURN TO PORT or settings' ABANDON MATCH, never ESC and never a refresh — and a captain who never returns is scuttled at grace expiry, reading as an ordinary sinking. A failed reconnect routes home with `COULD NOT REJOIN YOUR MATCH — BACK IN PORT` on the home status line, never a dead screen. A resuming player simply MISSED the kill-feed lines that passed while they were away: the gap is accepted rather than back-filled, because a roster-delta digest could name who sank but never who sank them.

**Surface cold states** — home renders over a live ambient CIC canvas (never a blank page); the boarding line shows the full HUD so the first match teaches itself; an empty kill feed and zero kills render as **absence, not placeholders**. **That rule is scoped to DECORATIVE empties** (epic-6 amendment 39, Eric 2026-08-17): *"absence is right when the empty state carries no information; the honest number is right when the emptiness IS the information."* So `PLAYERS ONLINE: 0` and `LIVE GAMES: 0` are DRAWN, not suppressed — at beta population the truthful line is zero, and hiding it is what makes an empty server read as a broken one. Genuine UNAVAILABILITY — a failed or malformed fetch — still renders as absence under the original rule: the whole block simply does not appear rather than showing a misleading `0`. (v3, unbuilt) An empty weapon or belt slot is a dashed square — the empty IS the state; the `SIGN IN` row is ABSENT, not disabled, while `/liveness` says accounts are not live; the anonymous home shows no account chip — the only thing that distinguishes the two homes `[ASSUMPTION: the fork is OPEN]`; the anonymous Ship & Deck screen renders the FULL layout with its account-only affordances (dropdown, ✎ ×, SAVE, steppers, lock + price, chip) ABSENT, not disabled — absence by capability, never by layout; a match history that is empty or fails to load renders as absence `[ASSUMPTION]`; a door refusal is never silence — its reason and `SAIL STARTER DECK` are drawn.

## Interaction Primitives

| Input | Action |
|---|---|
| **Q / E / R** | The three keyed hotbar slots. Q/E are the class specials, R the pickup/extra slot (inert while empty) — SUPERSEDED 2026-09-11 (v3, unbuilt): three GENERIC weapon slots, all EMPTY at 0:00, filled by drawing a weapon's tier I; a key on an empty slot is denied. **The gun is slot 0 and has NO key** — it is the always-selected default; a weapon key primes its slot, the same key reverts to the gun, and firing auto-reverts on RELEASE, never on press (a weapon key pressed mid-stream switches the prime and ends the stream without a denial). **Suspended while the refit window is open** |
| **F** | **FOGHORN** (Story 4.5, epic-4 amendment 56 — the reservation is closed). One honk per physical press, edge-gated so OS auto-repeat cannot machine-gun it; suspended with Q/E/R while the refit window is open (the belt digits are not suspended there — they change meaning and pick). A honk is a bearing, not a message — see HUD & Diegetic UI. Deliberately live on the frozen start line |
| **TAB** | Toggles the refit window open and closed. It also closes on ESC and on spending the last banked level; on any close the digits are inert for a short grace (implementer dial). Opening ends a held stream. Shift+Tab reaches Tab — the boost tap must not swallow the refit toggle `[ASSUMPTION]`. (v3, unbuilt) During the countdown the window opens ITSELF on the level-zero offer (an edge on the grant, never re-opened per frame); Tab still closes and reopens it, taking `REDRAW` with it |
| **1 / 2 / 3 / 4** | In match: refit card pick, **only while the refit window is open** — refit-or-nothing otherwise, evaluated at the key's own keydown, so a stray digit can never misfire a future consumable — SUPERSEDED 2026-09-11 (v3, unbuilt): the future consumable is here, so the digit has TWO meanings by the window's state, still read at keydown — window OPEN: pick that card (a greyed card's digit sends nothing); window CLOSED: fire that belt slot (HULL REPAIR / SHIELD BLOCK / SMOKE SCREEN / CHAFF) or prime it (DECOY BUOY, then click into the mine's rear arc); inert for a short grace after the window closes (Eric 2026-09-11); one physical press = one use — key repeat on a held digit fires once `[ASSUMPTION]`. Ship & Deck screen: 1–3 highlight a class tile (Enter confirms) |
| **5** | Spend a banked level on the **DAMAGE CONTROL** rail (the reserved `HEAL_CHOICE` sentinel), under the exact same window-only rule as 1–4 — RETIRED 2026-09-11 (v3, unbuilt): unbound; the heal is the HULL REPAIR consumable on the belt |
| **SPACE** | **Bound-inert.** Prevented at the chokepoint so it can never scroll the page, and it performs no action |
| **Shift** (tap) | (v3, unbuilt) The universal speed boost — every hull, no card: +25 % of the ladder-raised max for 10 s on a 20 s reload [DRAFT], scaled by the RELOAD ladder; reads as the second slot on the bar. Windows Sticky Keys hazard ACCEPTED (Eric 2026-09-11) — see Input capture |
| **W / S** (tap) | Telegraph engine order ±1 of 9 detents (set-and-forget; hold does not repeat) |
| **A / D** (hold) | Rudder −1…+1; rudder authority reduces below steerage speed |
| **Z / X** | Camera zoom out / in |
| **Mouse wheel** | Camera zoom. Range is **0.5×–1.5×** of the base radar-fit framing while alive and **0.5×–1.0×** while spectating (epic-2 amendment 8). Fog is server-authoritative — zoom can never reveal what sight or sweep hasn't legitimately painted |
| **Mouse move** | Aim, constrained to the selected weapon's real firing arc. (v3, unbuilt) Six new arcs — LIGHT TORPEDO twin sector; HEAVY / SUPERCAVITATING / MONITOR / MISSILE / MACHINE GUN bow sectors — the decoy takes the mine's rear sector; the deck gun's 360° stands, and an empty slot has no arc |
| **Left click** | Fire one shot; denied fire always gives explicit feedback. (v3, unbuilt) The MACHINE GUN alone is click-and-HOLD — a stream inside its arc that ends on pointer-up (listened for on the window, not the canvas), arc exit (ONE out-of-arc denial, then silence), an empty pool, Tab opening, or the window losing focus; the prime reverts to the gun on release. A primed DECOY fires on a click inside the mine's rear arc; a click outside the arc clears the prime `[ASSUMPTION]` |
| **ESC** | Closes the topmost open surface; if none is open (home or live), opens the settings overlay (non-pausing). On the results modal ESC means **SPECTATE**; while spectating it reopens the score screen. It never leaves the match |
| **Enter** | Home: runs `SOLO` (bound to the callsign field; `SOLO VS AI` has no Enter binding and therefore no `⏎` chip). Results modal: RETURN TO PORT. Ship & Deck screen: CONFIRM SELECTION (the class layer's carry-over) unless a text field has focus |
| **M** | Master mute (carry-over binding) |
| **P** | Prediction debug — dev build only, stripped from production |

**Fixed bindings (v1).** Bindings are fixed for v1 — key remapping is **deferred post-beta**; its recorded reason (no accounts, so remaps meant heavy localStorage) no longer holds under v3, and the deferral stands on a fresh reason still to be given `[OPEN — Eric]`. The settings overlay lists all bindings as a view-only reference — the scheme's only in-match self-documentation, alongside the How-to-Play page's keycap tables. **The Tab-toggled refit window supersedes the 2026-07-16 SPACE-hold ratification** (Eric ruling 2026-08-21, closing the repo's longest-standing doc-vs-code conflict): `Tab` opens and closes, `1`–`4` pick, `5` heals, and Space is bound-inert — SUPERSEDED IN PART 2026-09-11 (v3, unbuilt): `1`–`4` pick with the window open and fire/prime the belt with it closed, `5` is unbound, Shift boosts. UX-DR14's "SPACE is hold-not-toggle (absolute)" clause and UX-DR12's "HOLD SPACE TO REFIT" cue copy are RETIRED with it. The scheme also supersedes the old 1/2/3 weapon keys, the earlier CTRL modifier, and all placeholder keys in the mocks. All key glyphs share one visual family (DESIGN · Components).

**Input capture & browser hygiene.** With CTRL out of the scheme, browser-shortcut interception reduces to standard hygiene: every bound key is `preventDefault`ed at a single keydown chokepoint — **including Space (page scroll)** and Tab (focus cycle) — and `contextmenu` is suppressed on the canvas (right-click is one misclick from left-click-to-fire). DOM overlay or text input with focus = keyboard suppressed from the sim; the sim never pauses (typing "wasd" in the callsign field must not steer the ship). While the settings overlay or the results modal is up, every bound key but ESC/Enter/M is swallowed — helm included, unlike the refit window's partial lockout. `P` (prediction debug): dev-build only, stripped from production. **Shift (v3, unbuilt):** the boost is a TAP — both Shift codes, fired on the keydown edge with OS auto-repeat dropped, so a held Shift is one press; the Windows Sticky Keys prompt on repeated Shift taps is a known OS hazard, accepted with no mitigation (Eric 2026-09-11); Shift+Tab reaches Tab `[ASSUMPTION]`. The digits' `preventDefault` stands in both of their meanings. A lost window focus (blur, tab hidden) clears every held key AND a held stream — the neutral input carries `held: false`, so no captain streams into another window.

## Accessibility Floor

Non-negotiable floor (post-triage scope, 2026-07-16):

- **Dual-coding for class / threat / state meaning** — shape (chamfer, class silhouettes, drone chevron, pip geometry), position, and text co-carry every such signal. (v3, unbuilt) The chamfer is retired; the new states are dual-coded — greyed card = dim face + an UNDIMMED reason word + hollow chip; cooldown = wipe + numeral; held fire = outline + the fill's waterline; shield = the register's changed colour + the `HULL 312/250` numeral (rings `[OPEN — Eric]`); stocked = count; empty = dashed + em dash; locked line = grey face + the lock glyph on a rail outside the fade. **Informed waiver:** individual combatant *identity* is color-first — Eric accepts that trade against the floor; drone-vs-combatant, class, threat, and state still read without color (the drone chevron is a fifth silhouette no player class wears).
- **Audio-visual redundancy** — every audio cue has a visual twin (the foghorn chevron IS the visual of the honk) and vice versa for combat-critical events; all audio respects mute. **Binding rule, and it is now stronger than it was written:** the two-column audio-event ↔ visual-twin table shipped as `TONE_TWINS` in `client/src/audio/twinMap.ts`, a `Record<AudioCueId, string>` — **exhaustive at the TYPE level over all 34 audio cues** (the 33 `ToneId` tones plus the foghorn), so a new cue cannot be added without a visual twin at all. That is a compiler guarantee rather than the review-time discipline UX-DR36 asked for. (v3, unbuilt) The count MOVES with v3's new cues; every one still lands with a twin, and E8 re-points every row whose surface it retires (`heal` → the HP globe + belt count, `point` → `TAB TO REFIT`, `placeBuoy` → the decoy). CHAFF's only owner-side twin is the `×n` decrement, by ruling — the fakes are never drawn for the owner. A shield absorb / expire cue and its twin are `[OPEN — Eric]`.
- **Scope (Eric 2026-09-11):** the mouse is mandatory — you target and fire with it — so keyboard-only paths are NOT a floor requirement; no surface (the Ship & Deck screen, the card tooltip, the OAuth popup) specifies one, and the hover-only tooltip stands.
- **Photosensitivity restraint** — breathing glows (≥2 s cycles), one-shot 80 ms pulses rate-limited (≥300 ms apart), no full-screen strobes; storm vignette capped at its 1.1 Hz pulse; HP pulse capped at 1.1 Hz; final-10s ring pulse at 1 Hz. **Aggregate budget:** no element or screen region flashes more than 3×/s regardless of how many compliant events stack; repeated same-source flashes (e.g. hull hit flashes) share the 300 ms floor; the motion setting's "reduced" tier also halves flash intensity.

Committed v1 options (settings overlay). Key remapping is **not** among them — deferred post-beta; nor is a hold/toggle refit option — bindings are fixed, and the refit window is Tab-toggled:

1. **Motion/shake** — tiers full / reduced / off, covering directional screen shake, camera motion effects, and pulse/flash intensity (reduced halves it); overrides every juice rule below. **The death-reveal zoom is NOT exempt** (epic-5 amendment 26 — the setting's own law is *"off removes motion, never information"*, and exempting it buys nothing because at `off` the camera SNAPS to the identical framing on frame one). Motion scales the DURATION, never the destination. Default full.
2. **UI scale** — 90 / 100 / 125%; **125% is gated to viewports ≥1600 px wide** (settings note; prevents chrome/feed collision at the floor). Applies to the Pixi HUD **and** DOM HUD elements (kill feed, toasts, refit cards; v3, unbuilt: the bottom bar); port chrome — the Ship & Deck screen and the history page included — follows browser zoom instead. No mono type below 9 px post-scale. A 150% tier is deferred post-beta. Default 100%.
3. **Colorblind assist** — family-distinct palette adjustment: the 20 Regatta hues regroup into ~8 clearly separated color families so "boat A vs boat B" is distinguishable (identity degrades to family). Also boosts blip outlines and raises the minimum decayed blip opacity. Default off. Acceptance: families distinguishable under simulated deuteranopia at blip scale.
4. **Audio** — master + effects volume sliders and a **mono-audio** toggle (unilateral hearing loss loses the stereo bearing field; the foghorn chevron is the visual backstop). Defaults 100 / stereo.

| Setting | Values | Default | Persistence (anonymous · signed in — v3, unbuilt) |
|---|---|---|---|
| Motion/shake | full / reduced / off | full | localStorage · account |
| UI scale | 90 / 100 / 125% (125% gated ≥1600 px) | 100% | localStorage · account |
| Colorblind assist | off / on | off | localStorage · account |
| Master / effects volume | 0–100 each | 100 | localStorage · account |
| Mono audio | off / on | off | localStorage · account |
| Mute | off / on (key M) | off | localStorage · account |

(v3, unbuilt) Persistence is TWO-STATE: localStorage anonymous — byte-identical to today — and account-backed when signed in (local seeds the account on first sign-in, the account wins after, edits write both, sign-out leaves local as it was). One store, two backends.

Surfaces: gear on home + non-pausing ESC overlay in match; never make in-match the only path to any accessibility setting. **One accepted gap of record:** ESC while spectating reopens the score screen rather than settings (epic-7 amendment 17), so mid-spectate access to volume and motion is lost; nobody is trapped — the score screen's own RETURN TO PORT is a better-signposted exit than settings' ABANDON MATCH — and if it matters it wants its own key, not this one back. Cognitive floor: **banked offers never expire** (guarantee, see Refit Card); the per-card explanation lives in the refit card's hover tooltip and How-to-Play's UPGRADING section explains the mechanism (the 2026-07-16 "boon glossary on How-to-Play" is superseded — that page carries the refit's rules and keycaps, not a catalog listing); accrued boons + last offer are reviewable from the results modal (v3, unbuilt: the final `LOADOUT` from the results modal; brought / drawn / taken from match history).

## HUD & Diegetic UI

Full composite ratified — [hud-composite-2.html](./mockups/hud-composite-2.html) (squint-test steady-state load approved on v1; v2 corrects v1's superseded HP bar, keys, supply ghost, CTRL copy, and blip scale — v1 is retained in `.working/` as audit trail only). Note that the mock predates the Tab binding, the heatmap return colours and the foghorn chevron. **SUPERSEDED 2026-09-11 (v3, unbuilt, ratified):** [hud-composite-3.html](./mockups/hud-composite-3.html) frame 1 is the in-match HUD of record — the bottom-centre bar; hud-composite-2 stays as the 2026-07-16 record.

| Zone | Element | Content |
|---|---|---|
| Bottom-left | XP Rail + Banked-Level Chip + Hotbar (4 slots, Composition 2 vertical stack) | Build, economy, weapons — SUPERSEDED 2026-09-11 (v3, unbuilt): the bottom-left stack is gone |
| Bottom-right | Own-vitals cluster: HP Rail + HDG/KTS readouts + rudder gauge + Telegraph Cluster | HP as a vertical rail on the cluster's **right side**, mirroring the XP rail — CONFIRMED by Eric on the v2 composite (2026-07-16). SUPERSEDED 2026-09-11 (v3, unbuilt): the right-edge rails are gone |
| Bottom-centre (v3, unbuilt) | The bar (`{components.hud-bar}`): HP globe · Gun · Shift · Q E R · belt 1–4 · helm globe, the XP strip beneath | Vitals, build, economy, weapons, helm in ONE cluster — direction B TRISTRAM (Diablo II register) with direction A's radial cooldown on every cooling slot; LV at the strip's head, bank chip + `TAB TO REFIT` at its tail; no draw-pile counter; no mono register below 9 px (tier numerals, HDG/KTS, the W/S/A/D chips all sit at the floor — Eric 2026-09-11). The bar covers the water directly astern only at maximum zoom-in, and zoom is the remedy (Eric 2026-09-11, accepted — no rule): the click gate on HUD elements stands, and the tier numeral and ammo badge render above the cooldown wipe |
| Top-center | BR Chrome Bar | `n AFLOAT · n KILLS · T+mm:ss · <ring readout> · ☠︎ <NAME>`. AFLOAT counts **participants** (captains and AI captains, not PvE fleet hulls — epic-6 amendment 30, superseding the epic-4 wording); the ring readout runs the continuous-countdown grammar `RING CLOSES IN m:ss` → `RING CLOSING m:ss` → `RING CLOSED`; the trailing **KILL LEADER register** is the only optional segment and the only per-player HUE the bar has ever carried. **No supply-drop reservation** — that beat is a structural no-op with zero HUD footprint |
| Top-right | Kill Feed | Personal-color naval theater (text-safe variants), global |
| Screen edge | Foghorn Chevron | The bearing surface for a honk heard through fog: a chevron pinned near the viewport edge pointing down the bearing, weighted by an eight-value volume band of the LISTENER's intel range, fading over ~1.2 s. It carries bearing and volume and nothing else — no position, no identity, no correlation handle. The honker gets an own-hull bloom instead, because a bearing to yourself is meaningless |
| World | Firing arcs | Drawn on aim only (carry-over behavior); deliberately absent from steady-state |

**Camera zoom (Z/X + wheel)** makes edge-of-screen radar content player-manageable: zoom out to read the ring and the far radar annulus (radar paints beyond a 16:9 half-height are a real thing at the floor viewport), zoom in for knife fights. Range is 0.5×–1.5× alive, 0.5×–1.0× spectating; the death-reveal pull-back to the whole ocean is exempt from the alive clamp because it is derived from the live map radius and radar range rather than a literal. Fog stays server-authoritative, so zoom is a viewport choice, never an information exploit.

**INTEL RANGE is the one ruler, and every sensor boundary is a named eighth of it** (Story 4.9, the Eighths Ladder): 3/8 `detect` 247.5u, 4/8 `sight` 330u, 5/8 muzzle-flash / wounded-smoke 412.5u, 7/8 `farRadar` 577.5u, 8/8 `radar` 660u. **Since cycle 119 the ladder is FROZEN at those numbers for every player, every match** — the `intelRange` card that scaled it was deleted from the catalog (epic-7 amendment 31), so there is one set of sensor radii for everyone. `radar = sight × 2` and the endgame ring radius are the same number by two independent derivations.

**Sensor presentation (three tiers):**

- **Truesight bubble** — live hulls at `dist ≤ sight`, LOS-clear. **Islands block every sensor** (Eric ruling 2026-08-02) — binary, at any range, for truesight, the detect rung, muzzle flash, wounded smoke and the foghorn's muffle. **Radar is the ONE sensor that is height-aware** rather than binary (Story 4.11) — see below; nothing else moved with it.
- **Radar** — the rotating sweep paints decaying returns onto a phosphor lattice. **Returns take HEATMAP BAND COLOURS by return strength, never a personal hue** (Eric ruling, cycle 64: *"any particular point is either red, blue, green, or none of the above"*): exactly three bands, taken verbatim with no interpolation — red *"this is definitely a thing"*, blue *"probably a thing, but fuzzy"*, green *"honestly not sure, could be something tiny"* — drawn at ONE opacity, with age the only continuous quantity that survives to the screen. **This retires the 2026-07-16 "blips are personal-coloured (Variant C, preferred default)" ratification and the Variant P build flag with it.** Four things ride the same lattice: hull returns; **radar wakes** (server-owned, identity-free, one clock for the water and the ribbon — the fastest playable hull's full-ahead track is exactly the 3/8 detect rung, and older water lays FEWER cells rather than dimmer ones); **height-aware terrain shadows**, where a ray accumulates against the height raster so a low island no longer hides a distant ship, the slope paints up to the peak on the side you are looking at, and a shadow is simply UNPAINTED rather than marked; and **jamming fakes**, server-generated false returns from a jamming-doctrine radar buoy that are wire-indistinguishable from real ones by construction — SUPERSEDED 2026-09-11 (v3, unbuilt): the radar buoy and its jamming doctrine are DELETED; the fakes machinery survives as **CHAFF**, a consumable bursting 10 server-generated fakes in r120u around YOU for 15 s, wire-indistinguishable by construction, the owner never seeing their own, and no "chaff active" HUD readout — nothing about it rides the wire to the owner. A DECOY BUOY paints through the buoy's footprint path (unruled default). Returns are DISPLAYED at 20% opacity inside 1/8 of intel range, ramping to 100% at 5/8 — you aim by sight up close, not by ghosts. The storm is not a radar return.
- **Hearing** — the foghorn chevron (above). The **Listening Ring never shipped and is deferred**; `render/attention.ts` says so in code. Every 2026-07-16 passage specifying it as live, as a Tier-1 channel, or as "the primary torpedo warning channel" is superseded by this line.

**PvE fleet hulls** render the legacy chevron silhouette (+ its sizes) at hull and blip scale — a fifth silhouette no player class wears, so drone-vs-combatant reads without color. ~~**The Bounty** (Component Patterns) is the one sanctioned exception to sweep-only radar paints.~~ **RETIRED 2026-08-10 (Story 4.6, Eric ruling)** — the bounty carries no radar presence of any kind; there is no sanctioned exception to sweep-only radar paints.

**Torpedoes:** the fish itself is detected at the 3/8 `detect` rung (247.5u — a real combat buff Eric asked for by name, a sibling of `sight` rather than a narrowing of it) and confirmed visually at truesight. **Radar never paints the torpedo — but it does paint its WAKE** (cycle 70, Eric: *"I have decided that I do, in fact, want it to track torpedo wake"*, reversing the earlier ban). `CONFIG.torpedo`'s "never painted by radar" stays true of the fish and only of the fish; the torpedo's wake factor is held at half a ship's so a fish's tell never runs longer than any hull's. Camouflage is by STRUCTURE, not strength: a wake is the same green pixel at the same opacity as the chop around it, and the eye must pick a coherent LINE out of random dots. The materialization treatment (boundary rings + wake, DESIGN · Torpedo) makes detection → sight read as one continuous event. Mines share the detect rung. (v3, unbuilt) Three torpedo lines — LIGHT (twin sector), HEAVY (bow), SUPERCAVITATING (bow, a straight-runner that never homes) — share these rules as one wire kind; `[ASSUMPTION: the supercavitating fish's wake and speed reading — at 195 u/s a half-factor track would outrun every hull's tell; unruled]`. NAVAL and CAPTIVE MINES share the detect rung; any spotted mine can be shot to detonate it, and there is no live-mine cap.

**Enemy damage is diegetic only** — wounded ships trail smoke ({colors.wounded-smoke}) above the fog; **no enemy HP bars, ever.** Own damage is HUD-private (HP Rail — v3: the {components.hp-globe} — + shake + vignette). (v3, unbuilt) SHIELD BLOCK is owner-private: the HP register reads hull + shield (`HULL 312/250`) in a changed colour while shielded and reverts on deplete/expire (Eric 2026-09-11); `[OPEN — Eric: whether `{components.shield-ring}` on the globe and the faint hull ring survive alongside the number, and whether an absorbed hit plays a distinct cue — today an absorbed hit fires the shooter's Hit Call and the victim-private damage reads 0, so no shake, thud or vignette]`; enemies see nothing. SMOKE SCREEN puffs are grey and differ from wounded smoke by SHAPE — a trail of discrete expanding puffs vs a plume off a hull — never by colour.

**Fog/world reads:** fall-of-shot splashes ({colors.splash}) are self-private and gun-family only, so your own misses render at the true impact point even in fog and bracket-and-walk works; the **Hit Call** covers ALL ordnance including mines and carries NO severity channel of any kind — no amount, no hp, no victim id, no kill flag; muzzle flash ({colors.muzzle}) carries to the 5/8 rung and identifies nobody — no shooter id, no hue, no weapon type — so shooting is being seen without being named; fog banks shrink your truesight while hiding you from others' — SUPERSEDED 2026-09-11: no fog bank ever shipped; the player-laid occluder is **SMOKE SCREEN** (v3, unbuilt) — a trail astern for 5 s, each puff r40→60u for 30 s, occluding SIGHT only (a puff hides its occupant and blinds an observer inside it; radar is untouched); own puffs are always visible to their owner. **DECOY BUOY** (v3, unbuilt): dropped into the mine's rear arc in the OWNER'S hue (the Regatta propagation row extends to it; the deleted radar buoy leaves it), 50 hp, undying until destroyed, owner-only hp readout, homing bait and a physical block for any torpedo or missile — the owner's own not exempt.

## Game Feel & Juice

Carried inventory (current client, kept): directional screen shake on own damage (4→16 px lerped by hit size, exponential decay); 130 ms white hull flash on struck contacts (same-source flashes share the 300 ms floor — aggregate budget applies); amber hit spark vs miss splash ({colors.splash}); crimson expanding sink ring ({colors.damage-marker}); denied-fire 80 ms red pulse on arc + slot, rate-limited; a **33-tone** WebAudio set — tones only, no sound files, mute-aware — grown from the original thirteen as equipment, economy, gunnery and status cues landed (`client/src/audio/tones.ts`; `ToneId` is the register). With the foghorn — a cue with a visual twin but not a synthesized tone — that is **34 audio cues** in total (at 2026-08-21; the count moves with v3's new cues, unbuilt, each landing with a twin).

Added by decision:

- **Hit Call** — muffled boom + {colors.hit-bloom} bloom through fog: you know you connected, not how badly. It deliberately reaches the SHOOTER beyond sight — a declared exception to the master perception rule, taken so that fog gunnery is a conversation rather than a void. Victims and bystanders keep their existing rules unchanged: there is no "an unseen shooter hit you" cue for anyone else. (The 2026-08-04 rationale of record cited decoy disambiguation; the decoy role was deleted at Story 7-5, and the exception stands on its own terms. v3, unbuilt: the DECOY BUOY returns as a consumable — `[ASSUMPTION: a hit on a decoy fires no Hit Call, the old oracle; unruled]`.)
- **Fall-of-shot** and **muzzle-flash-carries** — see HUD section; feel features that are also information.
- **Wounded smoke** — hurt ships trail it; hurt = trackable. Its reach shares the 5/8 muzzle rung by ruling.
- **Low-HP escalation** — HP Rail threshold colors + accelerating pulse capped at 1.1 Hz (see State Patterns), with its own audio sting (v3, unbuilt: on `{components.hp-globe}` — the rail is retired, the colours and cap are not).
- **Denied input is never silence** — every refused fire/activation gets its pulse and its tone.
- **Death ritardando** — ~5 s sinking window, hull slows to a stop, guns stay live (go down shooting) → **omniscient reveal**: fog drops, the camera pulls back to the whole ocean, nameplates on every ship including your own wreck → the results modal opens over it. The reveal is the BACKDROP the modal is read against, never a screen of its own held until Enter. It converts losing into learning. Sequence mock: [death-reveal-results-1.html](./mockups/death-reveal-results-1.html) — its middle stage was deleted by ruling; the final frame is the shipped composition.
- **The build must be felt** — every boon lands with audio + hull visual + on-water behavior, or promise + growth is a spreadsheet. (v3, unbuilt) A weapon's tier I landing in an EMPTY slot is the biggest such moment: the square fills, its arc exists, the key means something.
- **Wake drafting** (v3, unbuilt) — riding inside another hull's wake gives a small speed lift (magnitude Eric's, [DRAFT]); it is FELT and has NO HUD readout.
- **No kill-streak spectacle** (Eric ruling 2026-08-21): sinking several ships quickly produces nothing beyond the kill feed and the KILL LEADER throne. The throne IS the whole recognition surface — no toast, no sting, no centre-screen celebration beat. Do not build one absent a new ruling.

**Motion/shake settings override everything in this section**, the death-reveal zoom included.

## Key Flows

Personas are constructed from the brief's audience sketches (no upstream personas exist). Journey A accepted with Beat 6 corrected; Journey B accepted (no objections through Finalize). [ASSUMPTION] Beat prose below reconstructs the accepted skeletons from the log's anchors; wording is not Eric-authored. Both were re-grounded on the shipped build in the 2026-08-21 reconciliation — the beats' *lessons* are unchanged, the mechanisms carrying them are the ones that exist. (v3, unbuilt) Journey A's beats 2/4/5/8 carry the v3 opening and results; Journey B's beats 1–3 and failure path and the whole of Journey C are facilitator DRAFTS from the v3 kit for Eric's redline (2026-09-11), each tagged `[ASSUMPTION]`.

### Journey A — "Marco" (13, brand-new, floor-viewport laptop)

1. Direct link → home in under 10 s over the live CIC canvas. It looks *cool*. He types a callsign; the chip reads `SELECT CLASS`, so pressing `SOLO` opens the class bay instead of connecting — no default is ever pushed at him. He takes the pre-focused Torpedo Boat and presses `SOLO`. The queue modal opens: `N/20 QUEUED`, `CANCEL`.
2. The cohort forms and he **boards**: he is already at his real start location on the ring, the whole HUD is live around him, and the tag reads `ALL STATIONS LOCKED`. Nothing answers yet — helm, trigger and scope are all held — so the first thing he learns is where he is starting from, with the telegraph ladder's W/S glyphs and the rudder's A/D sitting there waiting. (v3, unbuilt) One thing does answer: the refit window opens itself over the held hull — four cards, `LV 0` on the strip, one level banked, a `REDRAW` button under the row; his bar reads Gun · Shift and seven dashed squares. He takes a torpedo's tier I and the Q square stops being dashed. `MATCH STARTING`, ten seconds, and the ocean comes to life all at once.
3. First minutes: the sweep leaves a faint green speck out at the rim; he turns toward it and it hardens into a blue core as the range closes. Truesight resolves a greyscale chevron drone.
4. He clicks, the Hit Call booms, the drone sinks; the XP Rail wraps and the Banked-Level Chip starts breathing: `LEVEL UP — TAB TO REFIT` (v3, unbuilt: the strip wraps to `LV 1` and the chip at its tail breathes again — `TAB TO REFIT`).
5. He presses TAB — four cards over the still-running battle. He does not read the tooltips; he presses 2, the boon lands on its slot with a visible change, and the next offer renders in place (v3, unbuilt: card 2 is HULL REPAIR — it stocks belt slot 1 with a `×1`; his next level fills E with the machine gun). TAB again, back to the hunt.
6. **(corrected beat)** A thin green line has been crawling across his scope for a while, coming from nowhere he can see. He reads it as clutter and keeps hunting. It is a wake. The torpedo itself only registers at the detect rung — close, wake astern, inbound. Too late to helm out.
7. Sinking ritardando: five seconds, listing, guns live — he fires his last shells at his killer's smoke.
8. **Climax:** the omniscient reveal opens behind the score screen — fog gone, the whole ocean, every hull named, his own wreck among them. `SUNK`, `9TH OF 14`, two kills, and a MATCH LOG telling him exactly when each of them happened and when he died (v3, unbuilt: and his `LOADOUT` — the slots as they ended, tier numerals on the ramp). He sees the torpedo boat that stalked him and the line he ignored suddenly makes sense. He returns to port and presses `SOLO` again — seconds, not menus. **The lesson is "read the water."**

Failure path *is* the flow: death is the teaching surface, and it costs two presses, zero menus.

### Journey B — "Dee" (WoWS refugee, Mine Layer)

1. `[ASSUMPTION]` (v3, unbuilt) `SOLO` opens the Ship & Deck screen. She presses the Mine Layer tile — three hulls, three starter decks, no grind wall (unlocks are variety, never power); anonymous, the column reads STARTER DECK `40 / 40` with the starter's rows, and the whole collection is there to read — her starter's copies lit, everything else hollow, nothing pressable. She hoists Rose at the foot of the tiles and presses CONFIRM SELECTION.
2. `[ASSUMPTION]` (v3, unbuilt) Countdown: the window opens on her level-zero offer with NAVAL MINES I in it; she takes it and Q fills. Early game: radar discipline — she hunts by return strength, keeps islands between herself and a Battleship's sweep, and seeds a mine seam across a channel mouth from the rear arc.
3. `[ASSUMPTION]` (v3, unbuilt) A Torpedo Boat finds her. Her second level was CAPTIVE MINES I: she drops one behind the headland and runs shallow — its 144u trip ring is the watcher she cannot be, its fish the shot she cannot take. A FLAK GUN draw later lets her swat his torpedoes out of the water when he pushes.
4. Ring reveal beat: the next ring is known — the hunt must funnel through her channel. She re-seeds the seam on the funnel line and waits, engines at STOP.
5. He takes the bait: his return crosses her seam line.
6. **Climax:** proximity fuse — Hit Call boom, kill feed in their two colors: `SALT SHAKER SUNK BY DEE'S KETTLE`. The trap she authored paid off — the trapper fantasy delivered. The feed is global, so the whole ocean read it.
7. Endgame: the ring settles at two truesight diameters with everything in view; then the collapse begins at 15:00 and there is nowhere left to stand. She places 3rd, reads her results and her match log (v3, unbuilt: and her final `LOADOUT`), and returns to port — about fifteen minutes, zero grind.

Failure path: `[ASSUMPTION]` (v3, unbuilt) if the Torpedo Boat spots the seam (mines are confirmed at the detect rung — and any spotted mine can be shot), the trap converts to a chase — islands, naval mines with FOULING laid behind her to slow him, a SMOKE SCREEN or CHAFF if the match pool dealt her one `[ASSUMPTION: the pool dealing a consumable she has not unlocked — GDD open note 20(b)]`, a DECOY BUOY dropped astern to eat his homing fish are the escape line; being killed routes her through the same reveal-and-requeue as Marco.

*(The party/friend-group protagonist reserved in 2026-07-16 is DELETED — Eric ruling 2026-08-21: those modes come after beta launch, and the mode row is built to take them when they are actually ruled in. Dee's beats double as the read for the brief's primary 16–35 "design compass" audience; the third journey is Journey C, the signed-in loop, added 2026-09-11.)*

### Journey C — "Okafor" (28, signed-in regular, Battleship) (v3, unbuilt — every beat a facilitator draft for Eric's redline)

`[ASSUMPTION: the name and the protagonist]`

1. `[ASSUMPTION]` Home: accounts are live, so a `SIGN IN` row is there; the Google popup closes and the home comes back with `LV 7 · 2 ⬢` by the gear — nothing else on the home has changed. His callsign was already in the field — local seeded the account.
2. `[ASSUMPTION]` `CHANGE` on the chip opens the Ship & Deck screen on his Battleship tile, STARTER in the column: the collection — all 29 lines as compact tiles, the refit card he already knows appearing under the pointer — in the middle. BROADSIDE GUN's rail is all hollow behind the lock; he presses rung 1, it turns amber `⬢ 1`, he presses again and the rung fills silver — copy 1 bought, the line unlocked (price `[DRAFT]`). He makes a second deck, names it (a name only he sees), swaps MONITOR GUN for BROADSIDE GUN with the steppers, watches `39 / 40` sit in denied red until a fifth RELOAD goes in; SAVE lights.
3. `[ASSUMPTION]` The column's dropdown: he picks his named deck, presses CONFIRM SELECTION, and the chip on home reads his Battleship and the deck's name; then `SOLO`.
4. `[ASSUMPTION]` Countdown: `LV 0`, the window open on its own — BROADSIDE GUN I is in the offer, so `REDRAW` goes unpressed. The water goes live.
5. `[ASSUMPTION]` **Climax:** mid-match the pool deals him SHIELD BLOCK — belt 2, `×1` `[ASSUMPTION: SHIELD BLOCK is locked on his account; the pool dealing it is GDD open note 20(b)]`. A Torpedo Boat rushes at the ring's edge; he presses 2 and his HULL register jumps to `312/250` in its shielded colour; the fish lands and the number falls, back to `250` in phosphor as the shield goes; his broadside answers and the feed reads it in their two colours.
6. `[ASSUMPTION]` Results: `LOADOUT` — Gun IV · Shift · Q BROADSIDE GUN III · E HORIZONTAL MISSILE II · R — · belt SHIELD BLOCK ×0 reads empty; RETURN TO PORT.
7. `[ASSUMPTION]` History: his rows — brought / drawn / taken with `T+` stamps, placement, kills; the pool card he drew is on the list; the account level ticks. He tweaks the deck on the Ship & Deck screen; the edit applies to his next match.

Failure path: `[ASSUMPTION]` (v3, unbuilt) the door refuses his named deck (a copy revoked, a cap changed since he saved) — the reason and `SAIL STARTER DECK`; he sails the starter and fixes the deck after the match. Or the Google popup is closed or blocked: the home stays anonymous, the status line says so, and `SOLO` still runs on the starter.

## Inspiration & Anti-patterns

| Reference | Take | Leave |
|---|---|---|
| League of Legends ability hotbar | Icon-in-square slot grammar, cooldown-in-icon | Panel chrome |
| Hades | Boon-on-slot display; the lobby pick as a complete weapon/playstyle promise; "the boon you picked never lies to you" | — |
| colonist.io | Color-preference precedent (pick your hue) | Its paid-color model "feels kinda bad" — premium colors parked |
| Diablo II (v3, unbuilt) | The bottom-bar register: HP globe left, helm globe right, the consumable belt, the XP strip beneath | Its dimmed-refill cooldown (League's wipe was taken instead); potion-chugging as economy |
| League of Legends HUD (v3, unbuilt) | Radial clock-wipe + seconds numeral on every cooling slot; ONE bottom-centre cluster | Its stacked HULL/XP bars (direction A) — the globes won |
| Hearthstone / MTG Arena (v3, unbuilt) | The collection grid showing the WHOLE catalog, locked cards greyed and unlocked IN PLACE; copies BOUGHT one rung at a time on the rail, press-then-confirm | Packs, crafting currencies, a separate armory page, copy packs and tiered pricing (offered and declined); MTG Arena's full-screen overlay (direction B); a standalone collection page at all — the grid lives on the Ship & Deck screen |
| World of Warships | Class fantasy, gunnery feel, naval tension | Grind (v3's account level and unlock tokens are variety, never power — a starter is viable at the top); damage minutiae; carriers/subs (being attacked by something you can't fight back against violates the escape fantasy); class-invalidating spotting |
| Battleship (board game) | Hidden information, calling shots into the dark | Turns, grids |
| Risk of Rain | Stacking upgrades with named, felt thresholds — power you can SEE | — |
| surviv.io / OpenFront.io | Top-down BR structure, browser distribution, clip-able reveal moments | Loot-scavenging spine |
| Maelstrom (2018) | — | **Pacing anti-pattern:** validated the fantasy and died anyway; slow oceans kill retention |

Rejected upstream, do not reintroduce: "The helm is the star" guardrail; hex-era "not playful" mood; per-client random colors (screens would disagree). The v0.16 carry-over reference set (buddyboardgames / papergames / openfront) is retired — this table replaced it.

## Responsive & Platform

- **Desktop-only scope.** No touch, no gamepad, no Steam intent.
- **Floor viewport:** 1366×768. It stands on its own terms — NFR7 and UX-DR39 name that viewport directly — and no longer derives from the retired Chromebook premise (Eric ruling 2026-08-21). HUD authored at 1920×1080 reference with the ~1.6× post-playtest register; corner anchors hold at every size.
- **UI scale** 90/100/125% multiplies the HUD ramp (125% gated to viewports ≥1600 px wide); corner anatomy never rearranges (muscle memory is the contract); refit cards never wrap — the 1–4 keys map spatially (v3, unbuilt: onto the row when the window is open, onto the belt when it is closed; the Ship & Deck screen's collection is a GRID, 5 wide and scrolling — the never-wrap law is scoped to the in-match row); no mono type below 9 px post-scale.
- **DOM chrome** (home, results, settings, How-to-Play, privacy policy; v3, unbuilt: match history) centers at {spacing.chrome-max-width} (1100px). The Ship & Deck screen (v3, unbuilt; it replaces the class layer) is a layer over home at the class layer's **1318 px**, not the 1100 chrome width; its grid scrolls at the floor viewport (accepted).
- **The results-screen ad unit** is responsive and sits in the right gutter, with the panel and the unit centred as one group; below the group's breakpoint, or when the slot is blocked, unfilled or unconfigured, the panel stays exactly where it has always been and nothing is drawn (epic-7 amendment 18).
- Canvas fills the window; fog composite rebakes on resize; camera zoom (Z/X + wheel) is the player's own framing lever.

## Monetization & Disclosure Surfaces

Stated here because they are player-facing surfaces this spine previously did not describe.

- **The game is fully playable with ads blocked**, functionally and visually. Nothing waits on an ad callback, and nothing draws an empty box, a bed or a reserved hole when a slot does not fill.
- **One interstitial**, at return-to-port only — an AdSense H5 ad break named for the moment rather than the screen, because "the end of a match" is what makes it a permitted full-screen placement. There is no ad surface of any kind inside a live match.
- **One display unit**, in the right gutter of the score screen, revealed only when Google reports the slot filled. It is created and pushed **exactly once per match** and merely shown/hidden thereafter — ESC toggling the score screen must never mint a fresh impression.
- **Consent** is Google's certified CMP, delivered by the AdSense loader in the document head; the game's own consent card was deleted at Story 7.4. Our own settings keep only a local analytics override, which persists because every return-to-port is a full page load. (v3, unbuilt) A signed-in session is a 30-day token in localStorage that survives that page load; Google and Discord are sign-in providers only (`openid` / `identify`, no email) and are named on the privacy page.
- **Premium cosmetic colours are parked** — no hue is gated; monetization ships as advertising only (the colonist.io model *"feels kinda bad"*). (v3, unbuilt) Unlock tokens gate COPIES for variety — one `⬢` price per copy on the rail (unlocking a line = buying copy 1; price `[DRAFT]`; starter deck quantities being re-cut `[OPEN — Eric]`) — earned by play, never bought with money; this ruling is about hues and money and is untouched.

## Resolved question register

The 2026-07-16 spine carried 25 numbered Open Questions. All 25 are closed. Each decision is stated in the body above; this table is the audit trail from the old number to its answer. Rows 26+ are the 2026-09-10/11 v3 rulings (Eric; all unbuilt — E8/E9); #1, #14 and #19 carry v3 stamps. Rows whose ruling was superseded the same day are withdrawn and left as gaps (the interims live in `.decision-log.md`); #55–#62 are the post-review rulings.

| # | Topic | Decision | Source |
|---|---|---|---|
| 1 | Heal-as-upgrade | Heal exists and is **not a card** — the always-available DAMAGE CONTROL rail, digit 5. **REVERSED 2026-09-11 (v3, unbuilt):** the heal IS a card — HULL REPAIR, a belt consumable; `5` and the rail retired | epic-2 amendments 58-64, 67; Eric 2026-09-11 |
| 2 | GDD corrections | Applied GDD-side (4-card offers; "Solo vs AI") | Story 7-6 |
| 3 | Supply drops | Wholly parked; the beat is a structural no-op with zero HUD trace | `shared/src/sim/zone.ts` |
| 4 | Premium cosmetic colours | No hue is gated; monetization ships as advertising only | Story 7-4 |
| 5 | Reference sites | Overtaken — the Inspiration table replaced the v0.16 carry-over set | this document |
| 6 | Island colours | The four-band hypsometric ramp is ratified; the provisional `island-fill`/`island-stroke` tokens are retired | Eric 2026-08-06 / 2026-08-21; DESIGN.md |
| 7 | Storm edge treatment | Solid live ring, dashed next-ring telegraph, low-alpha storm-side fill | epic-3 amendment 14 |
| 8 | Sound-event map | Shipped as `client/src/audio/twinMap.ts`, exhaustive at type level; the two-column contract holds. Residual: a timbre listening pass | Story 4.x |
| 9 | Kill-streak spectacle | **Nothing.** The KILL LEADER throne is the whole recognition surface | Eric 2026-08-21 |
| 10 | `--player-color` CSS var | Not adopted — inline hex via `textSafe()` at each site | `client/src/util/color.ts` |
| 11 | Readiness-pressure indicator | Overtaken by the queue; boarding reads `CAPTAINS BOARDING — n ABOARD`, no denominator | epic-6 amendment 8 |
| 12 | Third journey | **Deleted.** Party modes come after beta launch | Eric 2026-08-21 |
| 13 | Class-card pip values | Real values on absolute anchors: TB 4/2/4 · BS 2/4/2 · ML 3/3/3; the middle row is **ARMOR** | Eric 2026-07-24; epic-7 amendment 35 |
| 14 | Boon copy | Eric's v2 catalog is canon; minimal card face + hover tooltip. **Stamped 2026-09-11 (v3, unbuilt):** catalog v3 (29 lines / 114 cards) is canon; the face is ICON · NAME · TIER · KIND · five fixed stat rows, hover tooltip stands | epic-7 amendments 20 / 26; catalog-v3.md; Eric 2026-09-11 |
| 15 | First-run default class | No default is ever pushed; the chip reads `SELECT CLASS`, Torpedo Boat pre-focused | Eric 2026-07-19 / 2026-07-24 |
| 16 | Camera-zoom range | 0.5×–1.5× alive, 0.5×–1.0× spectating; the reveal pull-back is exempt while live | epic-2 amendment 8; epic-5 amendment 25 |
| 17 | Torpedo bridge signal | Closed — the 3/8 detect rung plus the wake's inner bound cover the old `(detect, sight]` dead band | Story 4.9; cycle 70 |
| 18 | 150% UI scale tier | Deferred post-beta; three tiers ship (90/100/125) | `client/src/settings/store.ts` |
| 19 | Key remapping | Deferred post-beta; v1 bindings are fixed. **Stamped 2026-09-11:** the recorded reason (no accounts) is stale under v3; the deferral stands and needs a fresh reason `[OPEN — Eric]` | `client/src/settings` |
| 20 | Foghorn key binding | **F** | epic-4 amendment 56 |
| 21 | Per-hue text-safe variant table | No table — `textSafe()` is algorithmic | epic-7 amendment 37 |
| 22 | Nameplate scope | Nameplates on ALL — every truesight combatant hull and every revealed ship; PvE fleet hulls tagged in drone grey | Eric 2026-07-16; UX-DR22 |
| 23 | Whirlpools | Overtaken — deferred by ruling; the systems layer is declared complete | epic-5 amendment 47 |
| 24 | PvE fleet tier legibility | **Size alone is not enough** — a shape mark (rank ticks on the silhouette) carries the tier, so it survives at blip scale where no nameplate exists. Colour is structurally unavailable (fleet hulls are locked greyscale). *Decided 2026-08-21; the render change is in flight, not described here as shipped.* The nameplate keeps its ratified `DRONE`-in-grey grammar | Eric 2026-08-21 |
| 25 | Reveal-zoom motion exemption | **Not exempt.** Motion scales the duration, never the destination | epic-5 amendment 26 |
| 26 | Draw-pile counter | **DELETED** — no `23 LEFT` register on any surface; inferrable from level | Eric 2026-09-10 |
| 27 | Shift boost readout | A slot in the hotbar, with a `Shift` key chip | Eric 2026-09-10 |
| 28 | In-match HUD base | Direction B TRISTRAM — one bottom-centre bar, HP globe left, helm globe right, belt, XP strip; the 2026-07-16 Composition-2 stack and right-edge rails superseded; chrome bar + kill feed unchanged; `hud-composite-3.html` frame 1 ratified | Eric 2026-09-11 |
| 29 | Cooldown channel | Direction A's radial clock-wipe + seconds numeral on B's slots | Eric 2026-09-11 |
| 30 | Deck-gun slot | SHOWN, keyless, at the head of the row | Eric 2026-09-11 |
| 31 | Refit card face | ICON · NAME · TIER · KIND · FIVE fixed stat rows · reason-word foot; no activation wording, no prose; greyed = dim + reason word + hollow chip; tierless cards blank the tier; ratified | Eric 2026-09-11 |
| 32 | Tier-I base | ARMOR / SPEED / TURNING / DECK GUN start at Tier I equipped (first card I→II); RADAR SWEEP and RELOAD are cards I–V; every ladder tops at V | Eric 2026-09-11 |
| 33 | Mulligan affordance | AUTO-OPEN + one `REDRAW` button under the row, countdown only; row lifted 44 px during the countdown (cards covering the held hull accepted); ratified as rendered | Eric 2026-09-11 |
| 34 | Results own-deck block | `LOADOUT` — the slot row as ended + the five ship-ladder tiers; BOONS ACCRUED / LAST OFFER retired; brought / drawn / taken only in history (E9); ratified | Eric 2026-09-11 |
| 35 | On-water hue for the new objects | DECOY BUOY in the owner's hue; SMOKE SCREEN and CHAFF neutral; smoke differs from wounded smoke by SHAPE | Eric 2026-09-11 |
| 36 | — | *withdrawn same day — see `.decision-log.md`; the standing ruling is #61* | — |
| 37 | Activated-ability chamfer | RETIRED — framing and key chips carry the distinction | Eric 2026-09-11 |
| 38 | Depleted consumable slot | Empty is empty — reads as never-stocked, no ghost glyph | Eric 2026-09-11 |
| 39 | Journey B repair | Facilitator drafts beats 1–3 + failure path from the v3 kit, `[ASSUMPTION]`; Eric redlines | Eric 2026-09-11 |
| 40 | Journey C | ADDED — the signed-in loop, facilitator draft, `[ASSUMPTION]`; the party protagonist stays deleted | Eric 2026-09-11 |
| 41 | Shift / Windows Sticky Keys | ACCEPT AND DOCUMENT — no mitigation; the 2026-07-16 Shift objections closed (tap, not hold) | Eric 2026-09-11 |
| 42–43 | — | *withdrawn same day — see `.decision-log.md`; the standing ruling is #52* | — |
| 44 | Unlock flow | The Hearthstone / MTG Arena shape — the whole catalog shown in the collection grid, locked lines greyed, unlocked IN PLACE from the rail (per copy — #49); no armory page | Eric 2026-09-11 |
| 45 | Deck names | REVERSES the forge's rejection — named deck slots chosen from a dropdown; the starter is deck 1, renameable, deletable (never the last — #60); names shown to their author only, never in the feed | Eric 2026-09-11 |
| 46 | — | *withdrawn same day — see `.decision-log.md`; the standing ruling is #51* | — |
| 47 | Anonymous play on production | `[OPEN — Eric]` — the 2026-09-09 two-state fork stands until he rules; the dev/staging host is account-only, FIRM | Eric 2026-09-11 |
| 48 | — | *withdrawn same day — see `.decision-log.md`; the standing ruling is #52* | — |
| 49 | Per-copy acquisition | REVERSES the GDD's "a token unlocks a whole line": copies are BOUGHT one at a time at a `⬢` price on the rail; unlocking a line = buying copy 1; price `[DRAFT]`; starter deck quantities being re-cut `[OPEN — Eric]`; copy packs, tiered pricing and buy-or-earn declined | Eric 2026-09-11 |
| 50 | Unlock / buy press | PRESS, THEN CONFIRM — first press lights the price, second spends; press-then-confirm everywhere | Eric 2026-09-11 |
| 51 | `DECKS` link on home | DROPPED — home = today's home + the account chip; the deploy buttons / the chip are the only door to decks | Eric 2026-09-11 |
| 52 | Merged Ship & Deck screen | RATIFIED — replaces the class-select layer and the interim standalone page: class tiles left (hoist at the foot) · collection grid middle · deck column right (dropdown + ✎ × in its head) · CONFIRM SELECTION bottom-right, at the class layer's 1318 px; no route, no `DECKS` link; `shipdeck-1.html` frame 1 | Eric 2026-09-11 |
| 53 | Anonymous Ship & Deck | RATIFIED — the SAME layout, differing by capability only; absent exactly: dropdown, ✎ ×, SAVE, − / + steppers, lock + ⬢ price, account chip; STARTER DECK static, `40 / 40`; frame 2 | Eric 2026-09-11 |
| 54 | Copies-rail grammar | RATIFIED as rendered — hollow = unowned · silver-filled = owned · phosphor-lit = in deck · amber + `⬢ n` = pressed awaiting confirm; − / + stay the in-deck steppers | Eric 2026-09-11 |
| 55 | 9 px floor | RAISED — HDG/KTS, the W/S/A/D chips, slot tier numerals and tile pip labels go to 9 px; the helm globe grows a few px; the floor stands as written | Eric 2026-09-11 |
| 56 | Collection grid tile | COMPACT TILE — glyph · name · copies rail; the full five-row card on hover; five wide stays; the 0.6-scale refit card in the grid is retired | Eric 2026-09-11 |
| 57 | Refit dim + card row | Only the slot groups dim to 38 %; globes, helm and XP strip stay at full; the row keeps its 388 px live seat; the own-hull keep-out is WAIVED (live and countdown) | Eric 2026-09-11 |
| 58 | Bar occlusion astern | ACCEPTED, not a defect — zoom is the remedy; the click gate on HUD elements stands; no new rule | Eric 2026-09-11 |
| 59 | Interaction lifetimes | Digits inert for a short grace after the refit window closes (implementer dial); the machine-gun stream ends on pointer-up, arc exit, an empty pool, Tab opening, or the window losing focus; auto-revert to the gun on RELEASE, never on press | Eric 2026-09-11 |
| 60 | Ship & Deck edge cases | × delete confirms and cannot delete a hull's last deck; CONFIRM SELECTION with unsaved edits saves if legal, otherwise blocks on the legality line; a pending amber rung cancels on ESC or any click elsewhere; an unaffordable rung is inert with its price dimmed; deck names 1–16 chars under the callsign's filter | Eric 2026-09-11 |
| 61 | SHIELD BLOCK readout | The HP register reads hull + shield (`HULL 312/250`) in a changed colour while shielded, reverting on deplete/expire; the changed colour `[ASSUMPTION: {colors.info}]`; the globe ring + hull ring alongside the number, and a distinct cue on an absorbed hit, `[OPEN — Eric]` | Eric 2026-09-11 |
| 62 | Keyboard-only access | OUT OF SCOPE — the mouse is mandatory to target and fire; every keyboard-only finding is waived by scope; the hover-only card tooltip stands | Eric 2026-09-11 |
