# Decision Log — UX run ux-Hullcracker.io-2026-07-16

**FINALIZED 2026-07-16.** Both spines `status: final`. Sequence: Discovery (4 source extracts + import, 8 creative boards/mocks, all topics closed) → distillation → reviewer gate (rubric walker, HUD legibility, accessibility, input-scheme; 78 deduped findings; 11 developer-call verdicts by Eric) → edit pass (~55 editorial fixes + all verdicts) → key-screen re-renders (composite v2, refit v2, death sequence) → final-render verdicts (HP rail confirmed; results modal = leave-only) → mock coverage confirmed (4 surfaces spine-only by choice) → promotion to `mockups/` (6 keepers) → editorial polish (11 light edits + 2 consistency fixes: wordmark weight 700, callsign cap [PROPOSAL]-aligned). GDD corrections flagged for a future gds-gdd update: 4-choice offers, heal-as-upgrade question, "Solo vs AI" naming.

Canonical record of UX decisions made during Discovery and Finalize. Spines (DESIGN.md / EXPERIENCE.md) are distilled from this log; spines win on conflict with any mock, wireframe, or import.

---

- 2026-07-16 — Run created (Create intent). Eric's framing: "I know this is optional but I think the UX needs an overhaul to make sure its right." Existing root `DESIGN.md` (v0.16.0 design system) noted as candidate import; upstream sources on disk: brainstorm (2026-07-15), brief (2026-07-15), GDD (2026-07-16). Awaiting Eric's confirmation of sources + brain dump.
- 2026-07-16 — **Sources confirmed (Eric):** all four — GDD (+epics), brief (+addendum), brainstorm session, and root `DESIGN.md`.
- 2026-07-16 — **Root DESIGN.md disposition (Eric):** import as foundation — "keep what works" — but nothing is sacred; every choice is open to re-examination. "Just make sure the design is right."
- 2026-07-16 — **Problem statement (Eric, brain dump):** likes a lot of the current design scheme, but a LOT changed since the game went real-time. There is no coherent plan for the user interface — "just kinda slapped together." Ship classes don't even have unique graphics yet (Destroyer/Cruiser/Battleship visually indistinct). Goal: "I want the best game."
- 2026-07-16 — Import logged: `imports/DESIGN-v0.16-root.md` (copy of repo-root DESIGN.md as of this run).
- 2026-07-16 — Extracts complete in `.working/`: `extract-gdd.md`, `extract-brief-brainstorm.md`, `extract-design-current.md`, `extract-current-ui.md`. Load-bearing upstream facts: aesthetic locked as "CIC Tactical Display, Evolved"; guardrail "information noise must never bury the hunt"; tone "Silly Is Sanctioned" (supersedes hex-era "not playful"); desktop browser KB+M only; 13-year-old-on-a-Chromebook legibility at 60 FPS; no personas/journeys/accessibility spec exist upstream (this run creates them).
- 2026-07-16 — **Stakes (Eric):** indie — public-beta-bound, real accessibility floor, coherent brand, onboarding that survives strangers.
- 2026-07-16 — **Working mode (Eric):** Coaching path — walk decisions together, creative tools woven in.
- 2026-07-16 — **Pain point (Eric):** current upgrade menu disliked; note the upgrade system itself is changing (GDD E2: XP levels + pre-rolled 3-boon offers, no heal), so the spend UI is designed for the NEW system, not restyled from the old.
- 2026-07-16 — **Pain point (Eric):** home-page ship selection "not right in its current form" — to be rethought, not merely restyled.
- 2026-07-16 — **Direction (Eric):** ship graphics are "the best they have ever been" since the project began — keep the current rendering quality as the baseline; BUT each class (Destroyer/Cruiser/Battleship) must gain a unique visual identity (today: one chevron silhouette, scale-only differences).
- 2026-07-16 — **Pain point (Eric):** cooldown bars + "current weapon" selected-indicator area rejected outright ("don't like … literally at all") — candidate for full redesign, not adjustment.
- 2026-07-16 — **Direction (Eric):** ships get COLOR. PvE ships (drones) render greyscale; combatant (human) ships each get a nice, unique color. (Open: interplay with the locked restrained-functional-color rule green=yours/amber=action; palette size for 20-ship matches; colorblind handling; where the color propagates — hull, blips, kill feed, results.)
- 2026-07-16 — **Weapon area — root cause (Eric):** the core offense is that it "looks cheap." Full redesign, not adjustment.
- 2026-07-16 — **Weapon/ability HUD — target design (Eric):** each equipment/ability is an ICON IN A SQUARE appropriate to what it is; cooldown may be indicated inside the icon in the current style (the standalone bars are dead, the fill/sweep indication itself is fine within a square); hover tooltip explains the slot and records the stat upgrades picked up (or some other way to check stats — open). References: League of Legends ability hotbar; Hades slot display showing the boon attached to each main slot (basic/special/cast/dash).
- 2026-07-16 — **Weapon/ability HUD — two interaction classes (Eric):** not every slot is a weapon you switch to; some are ACTIVATED ABILITIES. The design must visually and behaviorally account for both (selected-weapon state vs press-to-trigger). Aligns with GDD slot grammar (universal gun / two specials / offer-filled extra slot).
- 2026-07-16 — **Color system — palette rule superseded (Eric):** "We can forget the locked palette rule, you get your personal color." The hex-era green=yours rule retires; every combatant (including you) gets a unique personal color; drones greyscale.
- 2026-07-16 — **Color system — tactical-layer propagation (Eric):** blips/tactical layer "could use these colors, too" — undecided by design: build it BOTH ways (phosphor-functional vs personal-color tactical layer) as an easy swap and playtest. UX spec must define both variants.
- 2026-07-16 — Creative artifacts rendered: `.working/hotbar-directions-1.html` (4 directions A–D), `.working/ship-color-system-1.html` (3 palettes + P-vs-C tactical comparison). Both reviewed by Eric in browser.
- 2026-07-16 — **Hotbar pick (Eric):** blend of **D "Afterimage"** (floating 1px-outline squares, no panel, phosphor glow, cooldown = icon dim + perimeter progress ring) with **B "Ship's Manifest"** text descriptions (slot-role + equipped-item labels). Composition (bottom-center horizontal vs left vertical stack) still open — blend mock to show both.
- 2026-07-16 — **Color palette pick (Eric):** **Regatta Hoist** (saturated regatta-flag brights). Treatment: bright personal color on the ship OUTLINE; interior/fill a slightly darker shade of the same hue. Drones greyscale.
- 2026-07-16 — **Tactical-layer color pick (Eric):** **Variant C** (personal-color blips + kill feed) is the preferred default; Variant P (phosphor-anonymous) still built as the easy swap for playtesting.
- 2026-07-16 — **Ship picker — grounding:** Eric: no longer 3 classes — 4 at launch with plans to add more; GDD confirms roster (Torpedo Boat / Battleship / Mine Layer / Gunboat, "the Hades weapon pick"). Presentation AND information are both wrong today: picker must scale past 4 classes and must SHOW how ships differ (hull envelope feel + loadout/playstyle). Concept direction (Eric): a menu that opens on the home page to pick class before queueing.
- 2026-07-16 — **Home page emotional target (Eric):** a brand-new player's first five seconds should feel "cool." (Facilitator gloss: coolness is the acquisition hook for the 13-year-old-Chromebook audience; pairs with GDD "frantic to play, light to hold".)
- 2026-07-16 — **Class visual identity (Eric):** genuinely distinct hull silhouettes preferred "if possible"; distinguishing dressing acceptable as supplement. Facilitator note (shared with Eric): GDD makes silhouettes gameplay-load-bearing — radar blips carry the hull OUTLINE ("class readable at blip range"), so silhouette-first is effectively mandated; dressing alone can't survive blip scale. Procedural vector linework keeps distinct silhouettes cheap (code-drawn, no asset pipeline).
- 2026-07-16 — Creative artifact rendered: `.working/hotbar-blend-DB-1.html` (D+B blend, two compositions + six-state grammar legend). Awaiting Eric's composition pick.
- 2026-07-16 — **Color assignment — RESOLVED (Eric, via structured unpack):** (A) **Match-consistent** — server/shared-seed assigns at match start; color index rides the already-synced roster; every screen agrees (voice callouts, spectate, kill feed, results). (B) **Preference pick** — player picks a favorite Regatta hue on the home page; gets it unless claimed, else nearest free hue; others randomized. (C) **HUD tint: accents only** — personal color on own hull, nameplate, small ownership accents (e.g. own blip ring); HUD chrome stays phosphor. (D) **Blip identity: YES — full Variant C colored blips**; contacts are trackable across fog gaps (grudges/bounty-hunting embraced as a deduction-game enrichment; Eric accepts reduced contact anonymity). P/C swap flag still planned for playtest.
- 2026-07-16 — Superseded within the unpack: original "randomly assigned by the client" idea (per-client colors would disagree across screens).
- 2026-07-16 — **Hotbar composition — RESOLVED (Eric):** Composition 2 — bottom-left vertical stack, "hands down." Label spec amended: NO slot-role line; line 1 = weapon/ability NAME; quick info must show DAMAGE and COOLDOWN at minimum. Weapons that store >1 ammo get a NUMBER BADGE over the icon square itself.
- 2026-07-16 — **Accrued boons placement (Eric):** boons attach to their hotbar slot via tooltip AND "show up in the quick information" (the visible label text). Spine to define how multiple boons compress in the visible line (tooltip carries the full list).
- 2026-07-16 — **Spend window — interaction (Eric):** CTRL is HOLD, never toggle: holding opens the menu; releasing closes it; spending all banked levels (keys pressed while CTRL held) also closes it. Keys stated as "1, 2, 3, or 4" — [OPEN] GDD offers are 3 boons from 3 categories with no heal; what key 4 does (or whether it exists) needs Eric's clarification.
- 2026-07-16 — **Spend window — layout (Eric):** rejected: vertical list. Wanted: CARDS, SIDE BY SIDE. "Other than that, its fine" — hold-to-open mechanic itself is liked.
- 2026-07-16 — **Torpedo Boat silhouette (Eric):** reads "really long and skinny — I guess it works"; accepted FOR NOW with an explicit balance worry (very thin hull = hard-to-hit profile). Flag for balance/architecture: visual silhouette vs collision envelope may need decoupling or geometry tuning; do not treat 9:1 as final.
- 2026-07-16 — Other three silhouettes (Battleship fortress / Mine Layer stern-bite / Gunboat wedge): no objections raised.
- 2026-07-16 — Creative artifacts rendered and reviewed: `.working/spend-window-1.html` (hold-CTRL refit window + closed-state banked indicator), `.working/home-class-picker-1.html` (home at rest + class-select layer, scroll-rail scale-past-4). **Eric: "I *love* the spend window *AND* the homepage!"** — both locked as rendered (placeholder pips/boon copy still placeholder; first-run default class = Gunboat as proposed, unobjected).
- 2026-07-16 — **Offer size — RESOLVED, supersedes GDD (Eric):** offers present **4 upgrade choices at a time** (keys 1–4 while CTRL held). ⚠ GDD-conflict flag: GDD says "3 upgrades from 3 distinct categories" — needs a GDD-side correction note; UX spec designs for 4 cards.
- 2026-07-16 — **Heal-as-upgrade — [OPEN, game-design owned] (Eric):** unsure whether to keep self-heal as an upgrade choice; leaning that *some* healing must exist in the game ("I don't want *no* healing to be in the game at all"). ⚠ Conflicts with GDD "There is no heal option in the economy." UX impact if kept: heal card appears within the 4-choice offer. Not a UX decision — parked for GDD update/correct-course.
- 2026-07-16 — **Torpedo Boat hitbox (Eric):** silhouette IS the hitbox for now; accepted knowingly ("keep an eye on it") — if the class turns dominant, revisit hitbox-vs-visual decoupling or hull-shape change. Supersedes the earlier decouple-flag as the default; the flag remains as the named fallback.
- 2026-07-16 — **Player journeys (Eric):** "I'm not sure honestly" — Eric cannot narrate cold. Facilitator fallback: draft [ASSUMPTION]-tagged journey skeletons from the brief's audience sketches (Chromebook newcomer / WoWS-refugee veteran) for Eric to redline; his corrections become the canonical journeys.
- 2026-07-16 — **Color preference contention — refined (Eric):** NOT first-come locking. Color choice is a *preference*; when multiple players prefer the same hue, the match assigns it to one of them by fair random draw (others fall to nearest free hue). Picker UI must not imply claiming/locking.
- 2026-07-16 — **Torpedo sensing rule (Eric, gameplay fact for HUD):** near-surface torpedoes are HEARD on the listening ring from long distance; SEEN only when they enter truesight range — or possibly a slightly tighter radius than truesight, tuned by how easy dodging proves once spotted ("balanced depending on how easy they are to dodge"). Listening-ring pips are therefore the primary torpedo warning channel.
- 2026-07-16 — **Journey A ("Marco", Chromebook newcomer) — ACCEPTED with Beat 6 corrected:** the torpedo that kills Marco was audible on the ring pips for a long approach, and became visible at truesight — he ignored the pips and saw it too late; the lesson is "listen." All other beats approved ("Otherwise looks good"). Climax: the omniscient reveal converting death into understanding.
- 2026-07-16 — **HUD IA — own vitals (Eric):** own HP as a BAR, co-located with the speed/rotation (telegraph) indicator cluster. Enemy damage stays diegetic only (wounded smoke "or whatever else we do") — no enemy HP bars.
- 2026-07-16 — **HUD IA — timers (Eric):** ring-closing countdown: yes. Up-counting game timer: yes. Supply-drop spawn indicator: yes IN PRINCIPLE, activates "once we revisit that" — supply drops are a future/parked mechanic; HUD spec reserves the slot, feature-flagged.
- 2026-07-16 — **HUD IA — listening ring (Eric):** compass-rose AROUND THE SHIP; segments "light up" in the direction of noise with intensity proportional to loudness/closeness.
- 2026-07-16 — **HUD IA — BR chrome (Eric):** yes — ships-afloat + kills, TOP CENTER. XP/level progress bar: "a good idea" — adopted; placement to be proposed in composite (candidate: with banked-level indicator at hotbar head).
- 2026-07-16 — **Accessibility floor — RESOLVED (Eric):** non-negotiable floor accepted (dual-coding / audio-visual redundancy / photosensitivity restraint) AND all four optional items committed to v1: motion-shake controls, key remapping, UI scale (90/100/125% class), colorblind assist mode (CVD-optimized palette swap and/or boosted blip outlines). Structural consequence: a SETTINGS surface must exist (none does today) — placement to be resolved before IA closes.
- 2026-07-16 — **Settings surface — RESOLVED (Eric):** gear entry on the home page + non-pausing ESC overlay in-match (opening mid-fight is the player's own risk — same philosophy as the CTRL refit window).
- 2026-07-16 — **Microcopy voice — RESOLVED (Eric):** confirmed the register the mocks speak: terse naval-command with a playful wink ("DEPLOY AS…", "SET SAIL", "FIT TO HULL", "REFIT", "WEAPONS SAFE"); death messages dry-naval ("SUNK — 9TH OF 14" register). No banned or required words specified.
- 2026-07-16 — **Own low-HP escalation (Eric):** "hp bar pulsing faster as your health gets lower could be cool. that or it turns yellow at 50% and red at 25%." Facilitator resolution (flagged to Eric, unobjected-pending): spec BOTH — threshold colors (≥50% phosphor, <50% amber, <25% red) + pulse rate accelerating with damage; channels are complementary. [ASSUMPTION-lite: blend rather than either/or.]
- 2026-07-16 — **Reviewer Gate (Eric):** ALL four lenses — rubric walker, HUD legibility, accessibility, input-scheme.
- 2026-07-16 — **Input scheme (Eric):** Q, E, R, F = the ability/slot keys, F being the empty slot that gets filled in-game (the offer slot). 1, 2, 3, 4 = consumables ("when/if we get there" — future flag) AND upgrade-card selection while CTRL is held. Supersedes the current 1/2/3 weapon-select keys and the placeholder keys in the hotbar mocks. CONFIRMED by Eric 2026-07-16: Q/E/R/F map one-to-one onto the four hotbar squares top-to-bottom (Q = universal gun, E/R = class specials, F = offer slot); weapons switch-to, abilities activate.
- 2026-07-16 — **HUD composite — reviewed (Eric): "pretty good."** Verdicts: (1) own-vitals cluster bottom-right APPROVED; (2) XP phosphor rail APPROVED + NEW: HP bar becomes a matching vertical rail climbing the RIGHT side [facilitator interpretation: right edge of the own-vitals cluster, mirroring the XP rail — flagged for confirm at key-screen re-render]; (3) supply-drop reserved ghost REMOVED — mechanic wholly undecided, park with no HUD reservation; (4) listening-ring register APPROVED ("looks great!"). Squint test: steady-state load approved. Mock-scale caveat logged: enemy blips drawn gigantic / own ship tiny — artifact, not a decision. Firing arcs deliberately absent from the steady-state frame; arc-on-aim behavior carries over from current game.
- 2026-07-16 — **Ledger triage (Eric):** A) light mode DROPPED — dark-only is the declared identity. B) home page stays as locked; mode chrome gets tweaked when more modes exist; launch modes = **Solo and Solo vs AI** (home must offer both minimally, no full mode-selector). C) info blue #38BDF8 + danger red #8B2020 KEPT as semantic tokens. D) **Death flow decided:** on death → reveal the map and zoom out so you see everything (the omniscient reveal IS the death screen) + a results MODAL: your kills, your placement, option to leave; spectate becomes a FUTURE planned option (current follow-killer spectate superseded in the spec).
- 2026-07-16 — **Premium cosmetic colors (Eric):** parked, ambivalent (colonist.io precedent "feels kinda bad"); monetization scope for now = AdSense only, cross other bridges later.
- 2026-07-16 — Damage-feedback principle from old DESIGN.md ("desaturated crimson, avoid visual vibration on black") adopted into the spine unobjected.
- 2026-07-16 — **Refit modifier — CHANGED CTRL → SPACE (Eric + input-scheme lens):** the input review found holding CTRL while tapping W fires browser close-tab (uninterceptible on Chromium/ChromeOS — the core audience platform). Eric approved switching to Space or Shift ("either would be fine"); facilitator selected SPACE per delegation — thumb-hold preserves all four helm fingers (Shift pinky-hold competes with A) and avoids the Windows Sticky-Keys 5-tap prompt hazard on Shift. Refit = HOLD SPACE, pick with 1–4, release dismisses; hold-not-toggle behavior unchanged. CTRL leaves the scheme entirely; browser-interception contract reduces to standard hygiene (context menu, scroll-on-Space preventDefault).
- 2026-07-16 — **Reviewer-gate triage — Eric's verdicts on the 11 developer-call findings:**
  1. Blip identity (critical): Variant-P assist REJECTED ("making everything green" doesn't help red-green CVD). Resolution: SHAPE codes what a thing is; **NEW: drones reuse the existing chevron boat model + its sizes** — a fifth silhouette no player class wears, so drone-vs-combatant reads colorlessly. Individual combatant identity stays color-first — **informed waiver**: Eric accepts that trade against the dual-coding floor (floor still holds for class/threat/state meaning).
  2. 125% UI scale gated to ≥1600px viewports: ACCEPTED.
  3. Radar-vs-screen: resolved by **NEW FEATURE — player camera zoom** (Eric had wanted this anyway): zoom out to read map features (ring etc.), zoom in for close combat. Bindings provisional: Z/X + mouse scroll wheel. Zoom range/limits = open tuning; fog is server-authoritative so zoom reveals nothing extra.
  4. Green-band Regatta hues: shift approved ("if it makes you feel better") — keep 20, move ~3 off the phosphor band.
  5. CVD assist = color-FAMILY distinctness, not 20 unique: ACCEPTED ("just different enough that I can tell boat A from boat B").
  6. Pip type-coding REJECTED — **informed design intent**: pips are pure noise intensity (more/closer = brighter), source ambiguity is deliberate; torpedoes AND mines become visible in truesight when close — sight is the confirmation channel.
  7. Hold/toggle refit option REJECTED ("one-handed players would probably have a hard time playing, anyway"). Hold-Space stays absolute.
  8. Slots during refit: while Space is held, Q/E/R/F are SUSPENDED — "release space to get back to the battle." Helm (W/S/A/D) stays live per Journey B.
  9. **Key remapping DEFERRED POST-BETA** (Eric, refined from initial "No. Learn to play."): accessibility is a fine consideration, but with no accounts, remaps mean lots of localStorage — defer until after beta. v1 ships fixed bindings; remapping goes to Open Questions as a post-beta item. (Refines the earlier v1 remapping commitment.)
  10. Torpedo tighter-than-truesight bridge signal: DEFERRED (conditional on that balance tuning ever happening).
  11. 150% UI scale: DEFERRED post-beta.
- 2026-07-16 — Accessibility floor, post-triage scope: dual-coding for class/threat/state meaning (individual identity waived, see above) · audio-visual redundancy · photosensitivity caps · motion/shake controls · UI scale 90/100 (+125% gated ≥1600px) · CVD family-distinct assist palette. Remapping and hold/toggle: out.
- 2026-07-16 — **Mock coverage confirmed (Eric):** waiting room/weapons-safe, countdown, settings overlay, How-to-Play = SPINE-ONLY by choice ("we're good on those") — none carries novel layout. All other IA surfaces have rendered mocks (home+class picker, live HUD composite v2, refit window v2, death reveal + results modal, plus the silhouette and color-system boards anchoring visual language).
- 2026-07-16 — **Final render verdicts (Eric):** (1) HP vertical rail on vitals cluster's right edge CONFIRMED — assumption resolved. (2) Reveal HUD survivor set ratified: BR chrome + kill feed persist; hotbar/XP/vitals/listening ring die with the hull. (3) Nameplates on ALL revealed ships (OQ 22 resolved for the reveal context). (4) Results modal: TIME AFLOAT kept; **NO RE-QUEUE from the modal** — "I explicitly said I don't want to requeue from here"; actions = SPECTATE (deferred/future) or LEAVE (return to home page). "SET SAIL AGAIN" removed; re-queue happens from home. Corrects a distillation drift (IA row had claimed one-click re-queue from death). (5) Refit v2 + composite v2: approved, no vetoes — remaining [PROPOSAL] tags in those mocks ratify as rendered.
- 2026-07-16 — **Journey B ("Dee", WoWS-refugee Mine Layer) — tentatively accepted:** no objections raised alongside Journey A's redline; treating as accepted unless Eric flags a beat. Third-protagonist question (party/friend-group session) still open — non-blocking. color "can be randomly assigned by the client, as it doesn't matter at all"; optionally let the player choose their color and randomize the rest at match start; optionally tint the player's HUD in their chosen color ("Or not. I don't care."). Decision surface identified by facilitator: (A) per-client vs match-consistent assignment, (B) player choice + collision policy, (C) HUD tint scope. Interacts with Variant C tactical color and with fog-anonymity/deduction (colored blips = persistent contact identity).

## Update 2026-09-10 — v3 surfaces (E8 The Deck + E9 The Account)

- 2026-09-10 — **Update opened (Eric, via gds-create-epics-and-stories step 1):** the epics run for E8/E9 halted at step 1 on Eric's ruling *"Run gds-ux first"* — DESIGN.md/EXPERIENCE.md were last reconciled 2026-08-21 and carry no deck, consumable-slot, draw-pile-counter, deck-editor or sign-in surface. Sources for this update: `gdd.md` (v3 sections, 2026-09-03 + 2026-09-09), `gdds/.../epics.md` E8/E9, `catalog-v3.md`, `game-architecture.md` amendments 2026-09-09 (D9–D18) + 2026-09-10 (D2, D19–D31), `forge/deck-model-v3` + `forge/identity-fork`. Extractions → `.working/extract-gdd-v3.md`, `.working/extract-arch-v3.md`, `.working/extract-spines-v3-collisions.md`. Rule for this update: capture Eric's picks on every v3 surface; never author a layout, colour or binding he has not ruled.
- 2026-09-10 — **Working mode (Eric): FULL COACHING.** Every v3 surface walked live; HTML mocks rendered for the hotbar, refit window, deck editor and home before distillation. (Fast path and the coach-HUD-only hybrid were offered and declined.)
- 2026-09-10 — **Draw-pile counter — DELETED (Eric), reversing the GDD's "Eric: 'Yes.'" (gdd.md § Deck model v3 L167):** *"Nowhere. This is inferrable from level. You have a tendency to add info no one needs. This is one of them."* No `23 LEFT` register on any surface — not the HUD, not the chrome bar, not the refit window. `OwnShip.deckLeft` (architecture D22) may still ride the wire for the harness/metrics, but nothing in the client renders it. Facilitator note: the GDD and E8 story 3 ("draw-pile counter") need a correction stamp — flagged for the epics run, not edited here.
- 2026-09-10 — **Shift boost readout (Eric): A SLOT IN THE HOTBAR.** Rendered as one more slot with a `Shift` key chip, inheriting the slot's ready/cooling grammar (and the orphaned activated-ability chamfer, pending the hotbar rethink below). Helm-corner chip, no-readout, and KTS-flip were offered and declined.
- 2026-09-10 — **Hotbar composition — REOPENED, and wider than the hotbar (Eric):** asked to choose among column layouts for the nine-slot loadout, Eric declined all of them: *"I don't think I like the columns, or the current UX in-game at all, either."* The 2026-07-16 Composition-2 ruling (bottom-left vertical stack, "hands down") is no longer safe to build on, and the dissatisfaction is with the in-match UX as a whole, not one element. Brain dump requested before any mock is rendered.
- 2026-09-11 — **In-match HUD brain dump (Eric, verbatim):** *"Ultimately, I have had some players get back to me that its just not very good or intuitive. The XP and HP bars shoved off to the side are a bit weird to read. I just think it needs to be cleaner, and the current implmenetation just isn't doing it. The chrome bar and kill feed are fine. Some players are also just picking refits randomly rather than try to figure out which one is which, so it needs better visual indication of what you're getting."* Attention: *"I find myself looking all over during a fight. There's very little I *never* look at. I STILL want to be able to zoom out all the way to see the whole map if I want, and then zoom back in to me."* References: *"Diablo I/II, league of legends."* Hotbar: *"I don't think columns is really working out. A hotbar at the bottom might work better. I need to visually see my abilities and cooldowns."* Facilitator reading (to be confirmed by his picks, not assumed): (a) the 2026-07-16 corner anatomy's bottom-left stack + right-edge HP/XP rails are OUT — both references put vitals and abilities in ONE bottom-center cluster; (b) BR chrome (top-center) and kill feed (top-right) are RETAINED as-is; (c) cooldown must be a visible per-slot channel (LoL radial sweep / Diablo dimmed icon), not only a ready/cooling outline; (d) the refit card's minimal face (Story 7-5 R2.17, hover-only explanation) is implicated by "picking refits randomly" — the face must show what you get; (e) player camera zoom (whole map ↔ own hull) is a standing requirement carried unchanged.
- 2026-09-11 — **Deck selection at join — RESOLVED (Eric): ON THE CLASS CARD.** Each class card gains a deck row (the account's last-used deck for that hull, with a switcher); anonymous cards show the starter deck with no switcher. Home-chip picker and editor-only "active deck" were offered and declined.
- 2026-09-11 — **Signed-in home display — OPEN, and Eric raised the prior question:** *"Wow, I don't know. Yeah I would say that you wouldn't have any access to account-only features at all, that should be pretty obvious. But do I even want to continue allowing anonymous players with no accounts? That's a better question."* The 2026-09-09 two-state ruling (D17: anonymous = starter deck, localStorage; signed in = the account) is therefore UNDER QUESTION by its owner. Put to him next as its own decision.
- 2026-09-11 — **Deckbuilder surface — OPEN; complexity concern recorded verbatim (Eric):** *"Wow. Fuck. I don't know. This UI is rapidly getting more complicated and it worries me that simply adding in this feature might force change elsewhere that will be really hard for *you* to keep track of, let alone me."* Facilitator response of record: the ripple IS the tracked artifact (`.working/extract-spines-v3-collisions.md`, 164 rows, each with severity); the anonymous-path question above is the single largest lever on that ripple (one home state instead of two, one settings backend, one door posture); and the simplest surface is the one that touches nothing in-match — a page, like How-to-Play — pending his call.
- 2026-09-11 — **Unlock flow — reference given, not a layout (Eric):** *"I don't know dude. Look to Hearthstone or MTG Arena as good examples, maybe."* Facilitator reading to confirm: both references show the whole catalog in the collection view with unowned cards greyed/locked and craft/unlock IN PLACE from that grid (MTG Arena wildcards, Hearthstone dust) — i.e. unlock inside the deckbuilder on the locked card; no separate armory page.
- 2026-09-11 — **Anonymous play on production — OPEN, Eric's to think through (no further framing questions):** *"I don't know honestly. I know that the dev/testing server should be 100% account only so I can control access. Whether or not it stays potentially anonymous on production isn't something that immediately asking me another framing question is going to resolve. I need to really think this through."* FIRM: **the dev/staging host is account-only** (an access-control ruling — interacts with the `HC_STAGING_KEY` gate and D9's "module inert without `DATABASE_URL`" posture; flagged to `gds-game-architecture` and the epics run, not designed here). The production two-state fork stays as ruled 2026-09-09 UNTIL he rules otherwise; this update designs every surface that does not depend on the answer first, and marks the home's anonymous/signed-in fork `[OPEN — Eric]` rather than resolving it.
- 2026-09-11 — **Unlock flow — RESOLVED (Eric): the Hearthstone / MTG Arena shape.** The deckbuilder's collection grid shows the WHOLE catalog; unowned lines are greyed with a lock and the token price; unlocking happens in place from that card; no separate armory page. Token count and account level therefore surface inside the deckbuilder at minimum.
- 2026-09-11 — **Rendered `.working/hud-directions-v3-1.html`** — four in-match HUD compositions against the brain dump, same identity/palette/type, differing on composition and cooldown channel: **A SUMMONER'S RIFT** (LoL: one bottom-centre bar, HULL + XP stacked bars over Gun · Shift · Q E R · 1–4, radial clock-wipe + seconds numeral, helm compact bottom-right); **B TRISTRAM** (Diablo II: HP globe left, helm globe right with telegraph tick-arc, consumable belt, XP strip full width, dimmed icon refilling bottom-up); **C BRIDGE CONSOLE** (one continuous instrument strip, helm hand far left, vertical HULL/XP gauges inside the bar, thin drain bar + numeral, machine-gun held-fire state shown); **D CLEAN DECK** (minimal combined bar, smaller consumable squares, icon-area conic wipe, numeral only under 3 s). Each shows empty / cooling / stocked-with-count / banked-chip states, the retained chrome bar + kill feed, and two refit cards in its density (LIGHT TORPEDO II→III on the absolute loot ramp; HULL REPAIR greyed with the reason word SLOTS FULL). Renderer's flagged inventions (not decisions): tier-II torpedo rendered at 1 tube per the floor rule; a 4-square stock row on the heal card; activation wording KEY FIRES / KEY PRIMES · CLICK FIRES; effect-line phrasing; scenery names/hues.
- 2026-09-11 — **In-match HUD base — RESOLVED (Eric): DIRECTION B "TRISTRAM"** (Diablo II register): HP globe at the LEFT end of a bottom-centre bar, helm globe at the RIGHT end (HDG/KTS inside, 9-detent telegraph as a tick arc over its crown with hollow ordered rung + solid amber actual needle, rudder track along its floor, W/S/A/D chips), the four consumables as a framed BELT, XP as the full-width strip beneath the slots with LV at its head and the banked-level chip at its tail. This SUPERSEDES the 2026-07-16 Composition-2 ruling (bottom-left vertical stack) and the right-edge HP/XP rails; BR chrome bar (top-centre) and kill feed (top-right) are unchanged.
- 2026-09-11 — **Cooldown channel — RESOLVED (Eric): the RADIAL CLOCK-WIPE + SECONDS NUMERAL from direction A**, applied to B's slots (a dark overlay uncovering clockwise, large centred seconds count). B's own dimmed-refill treatment is dropped.
- 2026-09-11 — **Deck-gun slot — RESOLVED (Eric): SHOWN.** A keyless slot at the head of the row so the gun's 5 s reload is visible like every other cooldown (A/C's treatment; B's omission reversed).
- 2026-09-11 — **Refit card face — RESOLVED with a correction (Eric):** *"'Refit card B density' is the best, but get rid of the 'key primes - click fires' and all other wording like that, its WAY TOO MUCH INFO and you have a tendency to do this. Also I'd prefer when it affects multiple stats that each stat gets its own line. So it needs to be 'tall' enough to account for this."* So the face = ICON + NAME + TIER on the absolute loot ramp + KIND word + ONE LINE PER AFFECTED STAT (current → next), at B's density and a taller card; NO activation wording, NO explanatory clauses of any kind on the face (that is the "tendency" — second instance this update, after the draw-pile counter). The GDD's "the card face says which" (key-fires vs key-primes) is therefore NOT satisfied by words; if it is satisfied at all it must be by a non-verbal channel or dropped — flag for Eric at the mock, not decided here. Hover-only explanation (Story 7-5 R2.17) stands. This also retires the 2026-07-16 "one text row, empty for a verb card" face rule.
- 2026-09-11 — **Refit card height — CORRECTED (Eric):** *"Nono, not expanding vertically as needed. We can actually *know* how many stats are affected by the card that affects the most. That's how many 'lines' we need."* The card is a FIXED height sized to catalog v3's maximum. Counted from `catalog-v3.md` §4: 4 stat rows (LIGHT TORPEDO, HEAVY TORPEDO, NAVAL MINES, CAPTIVE MINES, STAR SHELLS each change four stats per tier); 3 (HORIZONTAL MISSILE, FLAK GUN, BROADSIDE GUN); 2 (SUPERCAVITATING, MACHINE GUN, MONITOR GUN, DECK GUN); 1 (five universal ladders, TURRET, BARREL). **Card = 4 stat rows, fixed**, cards with fewer rows leave the rest blank. OPEN for Eric: whether NAVAL MINES' DERIVED trigger ring (2/3 of blast) gets its own row — if yes, the maximum is 5 and the card is 5 rows. The previous entry's "taller card" wording is superseded by this one.
- 2026-09-11 — **Mine trigger row — RESOLVED (Eric): one line.** *"It can be one line. Blast/Trigger Radius."* NAVAL MINES' blast radius and its derived trigger ring share a single stat row (BLAST/TRIGGER RADIUS, e.g. `48/32 U → 52.8/35.2 U`); the maximum stays 4 and **the refit card is 4 stat rows, fixed, final.**
- 2026-09-11 — **Refit card rows — REVERSED (Eric): FIVE rows.** *"NVM make it 5 rows, that reads too cluttered."* Blast radius and trigger radius get SEPARATE rows on the NAVAL MINES card; the maximum affected-stat count is therefore 5 and **the card is 5 stat rows, fixed, final** — cards with fewer rows leave the rest blank. Supersedes the two entries above.
- 2026-09-11 — **Rendered `.working/hud-composite-v3-1.html`** — the blended key screen: (1) LIVE HUD, B TRISTRAM bar + A's radial clock-wipe on every cooling slot + the keyless deck gun at the head of the row (Gun · Shift · Q E R · belt 1–4), HP globe left, helm globe right, XP strip with LV at head and bank chip + TAB TO REFIT at tail; chrome bar and kill feed unchanged. (2) REFIT WINDOW over the same HUD at 38%: four fixed-height cards (216×226) at B density — ICON · NAME · TIER on the absolute ramp · KIND · FIVE fixed stat rows · a fixed reason-word foot; card 1 armed, card 2 greyed + SLOTS FULL + hollow key chip; callout 4 / 2 / 1 / 3 of 5. (3) SLOT STATE STRIP at 1:1. Renderer's flagged inventions, awaiting Eric: two card-only glyphs (ARMOR shield plate, ACOUSTIC HOMING torpedo + arcs); the tier slot left BLANK on tierless cards; the fixed reason-word foot row; the in-slot HELD FIRE fill (amber, draining along the slot floor); cooldown numeral in tenths under 2 s; `TAB TO REFIT` alone on the bank cue; a HULL 212/250 vs ARMOR-card 300 inconsistency in the scenery numbers.
- 2026-09-11 — **Live HUD — RATIFIED (Eric): "YES THIS IS PERFECT!"** `hud-composite-v3-1.html` frame 1 is the in-match HUD of record: TRISTRAM bar (HP globe left, helm globe right with telegraph tick-arc + rudder track, Gun · Shift · Q E R · belt 1–4, XP strip with LV at head and bank chip + TAB TO REFIT at tail), radial clock-wipe + numeral on every cooling slot, deck gun shown keyless at the head. Chrome bar + kill feed unchanged. Promote to `mockups/hud-composite-3.html` at Finalize.
- 2026-09-11 — **Refit card — RATIFIED (Eric): "Yes, that's the card."** ICON · NAME · TIER on the absolute loot ramp · KIND word · FIVE fixed stat rows (`STAT current → next`, unused blank) · a fixed reason-word foot (blank except e.g. SLOTS FULL); greyed card = dim + reason word + hollow key chip; no prose on the face; hover tooltip carries explanation. Both flagged inventions KEPT: tierless cards (consumables, add-ons) leave the tier slot BLANK; the machine gun's held-fire state is an amber fill draining along the slot floor. New card glyphs (ARMOR shield plate, ACOUSTIC HOMING torpedo + arcs) stand until an icon pass says otherwise.
- 2026-09-11 — **NEW FACT (Eric): ship-upgrade ladders start at Tier I EQUIPPED.** *"Ship upgrades (like Armor) actually start with Tier I 'equipped.' As in you are considered to start the game with Tier I Armor, Tier I speed, Tier I turning, etc."* So the first ARMOR/SPEED/TURNING card is I→II and a 4-copy ladder tops out at V — the ramp's five rungs fit exactly. OPEN: whether "etc." covers RADAR SWEEP and RELOAD (cap 5 — a Tier-I-equipped base would reach VI) and the DECK GUN (cap 4; catalog R14 says "tier I is a real step"). Asked next. Flag for the epics run: the GDD/catalog wording ("copies = tier ceiling") needs a stamp saying base = Tier I for the ship ladders.
- 2026-09-11 — **Tier-I base scope — RESOLVED (Eric):** *"Armor / Speed / Turning / Deck Gun start at Tier I. Radar and Reload should not get a Tier VI, so its fine if the cards stay Tier I to Tier V."* So ARMOR, SPEED, TURNING and DECK GUN are Tier I at spawn (four copies → II–V; the deck gun's base 15 dmg IS Tier I, matching the catalog's five-value ladder); RADAR SWEEP and RELOAD have no equipped base tier — their five copies are cards I–V. Every ladder tops at V; the five-rung ramp is exact. Flag for the epics run: catalog R14's "tier I is a real step" wording for the deck gun needs a stamp.
- 2026-09-11 — **Mulligan affordance — RESOLVED (Eric): AUTO-OPEN + ONE BUTTON.** The refit window opens ITSELF at countdown start on the level-zero offer; a single REDRAW button sits under the card row, countdown only, and vanishes when the water goes live; Tab still closes/reopens the window. The verb `REDRAW` is the working copy (Eric picked the option carrying it; confirm at the mock under THE COPY LAW).
- 2026-09-11 — **Results own-deck block — RESOLVED (Eric): JUST THE FINAL LOADOUT.** The results modal shows the nine slots as they ended plus the ship-ladder tiers; BOONS ACCRUED / LAST OFFER are retired; the full brought / drawn / taken record lives ONLY in match history (E9). Three-column icon view and pick timeline were offered and declined. Flag for the epics run: E8 story 9 ("Own-deck results view brought/drawn/taken") is re-scoped — results = final loadout; brought/drawn/taken = E9 history.
- 2026-09-11 — **On-water hue rule for the new objects — RESOLVED (Eric): DECOY BUOY in the owner's hue; SMOKE SCREEN and CHAFF neutral.** The decoy is a placed object like a mine (Regatta Hoist propagation row extends to it; the deleted radar buoy leaves that row). Smoke puffs are grey and must differ from wounded smoke by SHAPE (trail of discrete expanding puffs vs. a plume off a hull), never by colour; chaff fakes are ordinary phosphor blips by construction (wire-indistinguishable — no tint possible or wanted).
- 2026-09-11 — **SHIELD BLOCK owner view — RESOLVED (Eric): BOTH.** The HP globe gains a phosphor ring while shielded that thins as the 100 hp is consumed (the number), AND a faint ring around the own hull on the water for the duration (the glance). Enemies see nothing (architecture D28's quiet default stands).
- 2026-09-11 — **Activated-ability chamfer — RETIRED (Eric).** No slot wears the cut corner; group framing (the belt) and key chips carry the weapon/consumable distinction. DESIGN.md Shapes › "Activated-ability mark" and the Do's-and-Don'ts mention are struck; the 9px-cut token goes.
- 2026-09-11 — **Rendered `.working/countdown-results-v3-1.html`** — frame 1 COUNTDOWN: the shipped phase overlay, the ratified bar in its 0:00 state (LV 0, empty strip, bank chip 1, Q E R + belt dashed), the refit window AUTO-OPENED on HEAVY TORPEDO I · HULL REPAIR · SPEED I→II · RADAR SWEEP I, one centred `REDRAW` button with a single hollow pip; a live-inset at T+00:12 with the button gone. Frame 2 RESULTS: the shipped cycle-79 modal with BOONS ACCRUED / LAST OFFER replaced by one `LOADOUT` block — the HUD's own slot row at .72 with tier numerals on the ramp, plus one line of the five ship-ladder tiers in ramp colours. Renderer's flagged departures: the card row LIFTED 44 px in the countdown frame only (the ratified row leaves 8 px above the bar, no room for a button — the live inset is back at 388 px), so during the countdown the cards cover the held own hull; three new glyphs (HEAVY TORPEDO, SPEED, RADAR SWEEP); a bare `I` tier line with no arrow for a first card of a no-base line; a depleted consumable (CHAFF ×0) drawn as the plain EMPTY state; modal ≈735 px tall at 1366×768 leaving ~16 px of reveal top and bottom; the mockup file `death-reveal-results-1.html` F3 predates the shipped SPECTATE + MATCH LOG modal, so the shipped `results.ts` was the reference.
- 2026-09-11 — **Countdown + level-zero offer — RATIFIED (Eric): as rendered.** The refit window auto-opens at countdown start; one `REDRAW` button (Primary Button register, single hollow pip) under the row, countdown only; the row sits 44 px higher during the countdown so the button seats, and drops to its live position when the water goes live — the cards covering the held own hull during the countdown is ACCEPTED. `REDRAW` is ratified copy.
- 2026-09-11 — **Results modal LOADOUT block — RATIFIED (Eric): as rendered.** The HUD's own slot row at ended state with ramp-coloured tier numerals, plus one line of the five ship-ladder tiers; replaces BOONS ACCRUED / LAST OFFER; modal height at the floor viewport accepted. `LOADOUT` is ratified copy.
- 2026-09-11 — **Depleted consumable slot — RESOLVED (Eric): "empty is empty."** A belt slot whose stack ran out reads exactly like a never-stocked slot; no ghost glyph.
- 2026-09-11 — **Journey B repair — RESOLVED (Eric): facilitator DRAFTS, Eric redlines.** Beats 1–3 and the failure path are rewritten from the v3 kit (naval + captive mines, flak, fouling; decoy/smoke/chaff as pool draws), tagged [ASSUMPTION] in EXPERIENCE.md for redline at Finalize.
- 2026-09-11 — **Journey C (the signed-in loop) — RESOLVED (Eric): ADD IT, facilitator drafts** a named protagonist and the loop (sign in → build a deck → pick it on the class card → sail → read history), tagged [ASSUMPTION]. This supersedes the 2026-07-16 "no third journey" note ONLY in that a third journey now exists — the deleted party/friend-group protagonist stays deleted.
- 2026-09-11 — **Shift / Windows Sticky Keys — RESOLVED (Eric): ACCEPT AND DOCUMENT.** No mitigation; noted in the input-capture rule as a known OS hazard. This closes the collision with the 2026-07-16 CTRL→SPACE entry's Shift objections: the pinky-vs-A objection dissolves for a TAP, and the Sticky Keys hazard is accepted.
- 2026-09-11 — **Rendered `.working/port-directions-v3-1.html`** — three port compositions, each with the deckbuilder, the class card's deck row, and the signed-in home (anonymous fork tagged `[OPEN — ERIC]`, not designed): **A THE COLLECTION** (Hearthstone register, its OWN PAGE at 1100 width: collection grid of all 29 lines as the ratified card with a copies rail, locked cards carry lock + token price and unlock in place; narrow deck column with `39 / 40 · 3 / 3 EQUIPMENT` and SAVE; hull tabs + slots 1·2·3; class card `DECK 1 ▸` with hover switcher; home gains `LV 7 · 2 ⬢` beside the callsign and `DECKS` in the underplay links); **B THE ARMORY** (MTG Arena register, FULL-SCREEN OVERLAY over the dimmed home: dense icon-tile grid with kind filters, the deck as a 40-slot strip along the bottom, two-press unlock on a locked tile; class card `1 · 2 · 3` segment; account chip top-right by the gear); **C THE MANIFEST** (list-first MODAL: one grouped list of 29 rows with steppers, no card face; class card `STARTER · 1 · 2 · +` chips; home shows callsign + `LV 7` only, DECKS reached from the class card). Renderer's flagged inventions: token price `⬢ 1` (flat price is [DRAFT]); deck-gun lines wear the kind word UPGRADE; a 28 px copies rail under the untouched card face; 22 placeholder glyphs; `DECKS` as the header word; the shipped `home.ts`/`classSelect.ts` geometry used because `mockups/home-class-picker-1.html` is stamped SUPERSEDED.
- 2026-09-11 — **Deckbuilder — RESOLVED (Eric): DIRECTION A "THE COLLECTION".** Its own PAGE (a route, like How-to-Play) at the 1100 px chrome width: the collection grid of all 29 lines as the ratified refit card with a copies rail beneath; locked lines greyed with a lock + token price, unlocked IN PLACE; a narrow deck column listing the deck's lines (glyph · name · ×n) with the `39 / 40 · 3 / 3 EQUIPMENT` legality line and SAVE (unlit while illegal); hull tabs above. Page scrolls at the floor viewport (accepted by choosing A). `DECKS` is the working header word.
- 2026-09-11 — **Deck slots are NAMED and chosen from a DROPDOWN — Eric, REVERSING the forge's "deck names (in the feed or anywhere)" rejection:** *"I want the player to be able to name their deck slots. Selecting it from a dropdown makes the most sense. The 'starter deck' is automatically the first deck, a logged in user can delete or change it if they want."* So: the class card's deck row is a DROPDOWN of that hull's decks by name (replacing A's `DECK 1 ▸` hover switcher); the deckbuilder's slot control is the same dropdown; every account starts with the hull's starter as deck 1 (default name STARTER), editable and deletable. Scope of the reversal, as read: names are the player's OWN labels, shown to the player on their class card, deckbuilder and history; nothing here puts a name in front of another player (the feed stays name-free). FLAGS: GDD/forge "Rejected" list and E9 story 3 need a stamp; architecture D12's `decks` table gains a `name` column and a seeded starter row on first sign-in, and D14/D15/the privacy paragraph gain "deck names" as stored user-entered text (`gds-game-architecture`); a user-entered string visible only to its author has no moderation surface.
- 2026-09-11 — **Signed-in account chip — RESOLVED (Eric): TOP-RIGHT BY THE SETTINGS GEAR** (`LV 7 · 2 ⬢`); the callsign field is unchanged; `DECKS` joins the underplay links. The anonymous/signed-in fork itself stays `[OPEN — Eric]`.
- 2026-09-11 — **Discovery closed.** Every IA surface v3 adds now has a ruling or a tagged draft owner: HUD (ratified mock), refit card (ratified), countdown + REDRAW (ratified), results LOADOUT (ratified), deckbuilder (A + named dropdown), class-card deck row (dropdown), signed-in home chip (top-right), history (spine-only: own rows brought/drawn/taken with T+ stamps — E9), settings (DELETE ACCOUNT + binding reference — spine-only), How-to-Play (sections re-cut — spine-only, copy at dev time), on-water objects (hue + shield rulings), journeys (B repaired + C drafted, [ASSUMPTION]), anonymous fork ([OPEN — Eric]). Finalize begins: deckbuilder key screen, spine distillation, reviewer-gate offer, promotion, polish.
- 2026-09-11 — **Reviewer Gate (Eric): ALL FOUR LENSES** — rubric walker, HUD legibility, accessibility, input scheme — as in the 2026-07-16 run; to run after distillation.
- 2026-09-11 — **Rendered `.working/deckbuilder-v3-1.html`** — frame 1 the DECKS page (Direction A + the named-deck dropdown: header `DECKS · TB BS ML · NIGHT RUNNER ▾ · ✎ · × · LV 7 · 2 ⬢`, 29 cards with copies rails, 7 locked lines with lock + price, BROADSIDE GUN mid-unlock, deck column `39 / 40 · 3 / 3 EQUIPMENT`, SAVE unlit; insets: the open dropdown `STARTER · NIGHT RUNNER ✓ · GUNBOAT · + NEW DECK` and the unlock sequence); frame 2 the class layer (TB card: pips → `DECK · NIGHT RUNNER ▾` → SELECTED, no Q/E rows; anonymous inset reads static STARTER DECK, tagged `[OPEN — ERIC]`); frame 3 the signed-in home (`LV 7 · 2 ⬢` by the gear, DECKS in the links, no SIGN IN row, anonymous fork tagged); a state strip (owned ×0 · ×3 of 5 · at cap · locked · equipment outside the deck at 3/3). Renderer's flagged inventions: `✎` rename and `×` delete as unlit affordances; pressed-lock as an amber-lit rail (one-press vs confirm unruled); dropdown as a hairline box + `▾`; class chip `CLASS · DECK NAME`; `+ NEW DECK` wording; the `[draft]` `⬢ 1` price carried.
- 2026-09-11 — **DECKS page — RATIFIED (Eric): "overall very yes, this looks excellent."** Direction A + the named-deck dropdown with rename/delete stand as rendered. **Unlock press — RESOLVED: PRESS, THEN CONFIRM** (first press lights the price, second spends the token). **Class card deck row — RATIFIED as rendered.** **Signed-in home — REDLINE requested, content not yet stated** (asked next).
- 2026-09-11 — **COPIES ARE ACQUIRED, not granted with the line — Eric, REVERSING the GDD's "a token unlocks a whole line (every copy up to its cap)":** *"I do think now that players should need to buy/acquire additional copies. And I am going to rework the starter deck quantities around this. But this means that the collection should indicate how many copies I can have at max, and a means to acquire more copies. I don't know how exactly so give me some ideas."* So the collection must show owned / cap per line and carry an acquire-more-copies affordance; the mechanism is OPEN (ideas requested — options put to him next). FLAGS: GDD § Account progression + E9 stories 3/5 (unlock model), architecture D12 `unlocks (user_id, line_id)` → per-copy counts, `deckRules` ownership check becomes per-copy, the `matchesToCatalog` intent dial re-derives over 114 cards rather than 29 lines; starter deck quantities to be re-cut by Eric (catalog v3 sheet).
- 2026-09-11 — **Deckbuilder entry — Eric asks:** *"how do I get to the deckbuilder, and can I access it from Ship Select?"* Current mock: `DECKS` in the home underplay links only. Ruling to take: an entry from the class-select layer beside the deck dropdown (put to him next).
- 2026-09-11 — **Copy acquisition — RESOLVED (Eric): PER-COPY TOKEN PRICE ON THE RAIL.** The copies rail shows owned / cap as filled vs hollow rungs; pressing a hollow rung lights its price (`⬢ n`), a second press buys that copy (press-then-confirm everywhere); unlocking a line = buying copy 1. One currency. Price per copy is `[DRAFT]` (flat, Eric's dial). Copy packs, tiered pricing and buy-or-earn were offered and declined.
- 2026-09-11 — **Home redline — RESOLVED (Eric): DROP `DECKS` FROM THE UNDERPLAY LINKS.** Home = today's home + the account chip by the gear (signed in). The deckbuilder's only door is Ship Select (next entry).
- 2026-09-11 — **SHIP SELECT AND DECK SELECT/EDIT MERGE INTO ONE INTERFACE (Eric):** *"I think we should merge ship select and deck select/edit into one interface tbh. That way a player can see what is in their current selected deck from this screen, and a no-account player can see what is in their starter deck. Somehow."* This SUPERSEDES the just-ratified standalone `/decks` page as the deckbuilder's HOME (the page's contents — collection grid, copies rails, lock-price, deck column, named-deck dropdown — carry into the merged screen; what changes is WHERE they live and that the class pick and the deck are one surface). Anonymous captains see the starter deck's contents read-only. Composition undesigned — direction board next.
- 2026-09-11 — **Rendered `.working/shipdeck-directions-v3-1.html`** — three merged Ship-Select + deck compositions, each in signed-in and anonymous (`[OPEN — ERIC]`) states, plus a 1:1 per-copy rail legend (hollow = unowned, silver-filled = owned, phosphor-lit = in deck; a pressed hollow rung turns amber with `⬢ 1` awaiting confirm; unlocking a line IS pressing rung 1): **A THE DOCK** (compact hull rail across the top with the Color Hoist folded in; the ratified deck column left, the collection grid right; CONFIRM SELECTION bottom-right; anonymous = STARTER DECK rows read-only, grid area blank); **B THE MANIFEST CARD** (the shipped class layer; the selected card expands to twice its width with the deck inside — dropdown, rows, legality, SAVE, EDIT; EDIT slides the grid over the other two cards, which collapse to chips; anonymous = STARTER DECK rows, no EDIT); **C TWO PANES** (three tall class tiles left; the right pane is the builder — dropdown, grid above, the deck as a 40-slot glyph strip along the bottom; anonymous = the strip only). Renderer's flagged inventions: rung-state colours; `−`/`+` kept as in-deck steppers beside the rungs; A's blank anonymous grid; B kept at the shipped 1318 px width and drops the ghost MORE CLASSES card; C's hoist wrapped to two rows.
- 2026-09-11 — **Merged Ship + Deck screen — Eric: C's LEFT PANE, but with THE COLLECTION's right side, and NO stripped anonymous view.** *"I like C the best, but I still also like the full interface with the decklist on the side and the collection in the middle, right? You removed the entire reason I picked the design I did earlier. I do NOT like that the anonymous starter deck user gets some minimal fucking bullshit in it. This is the closest but its still not quite right."* So: LEFT pane = C's three tall class tiles (silhouette, name, pips, hue) with the Color Hoist; RIGHT pane = the ratified deckbuilder layout — the deck column (dropdown, rows, legality, SAVE) beside the collection grid — NOT C's 40-slot strip; CONFIRM SELECTION pinned bottom-right. **Anonymous captains get the SAME full interface** (deck column showing the starter's rows + the whole collection grid), with only the account-dependent affordances absent — `[ASSUMPTION: no dropdown (reads STARTER DECK), no SAVE, rails inert (no − / +), no lock-price]` — for Eric's redline at the key screen. Facilitator error of record: the board's anonymous frames minimised the surface; the ruling is that anonymous differs by capability, never by layout.
- 2026-09-11 — **Per-copy rail grammar — RATIFIED (Eric): as rendered.** Hollow = unowned · silver-filled = owned · phosphor-lit = in deck; pressing a hollow rung turns it amber with `⬢ n` awaiting the confirming press; unlocking a line = pressing rung 1; `−`/`+` remain the in-deck steppers.
- 2026-09-11 — **Rendered `.working/shipdeck-v3-1.html`** — the merged SHIP & DECK screen at the class layer's 1318 px: C's three tall class tiles left (hoist at the foot) · the 29-card collection grid in the middle (5 wide, scrolls) with per-copy rails · the deck column right (`NIGHT RUNNER ▾ ✎ ×`, rows, `39 / 40 · 3 / 3 EQUIPMENT`, SAVE); account chip in the header; CONFIRM SELECTION bottom-right. Frame 2 ANONYMOUS is byte-identical in layout: `STARTER DECK` static, `40 / 40`, no SAVE, full grid visible, rails inert; corner tag `OPEN — ERIC: ANONYMOUS PLAY ON PRODUCTION`; the six absent affordances listed as `[ASSUMPTION]`. Renderer's flagged inventions: pane order tiles · grid · column (Eric: "decklist on the side and the collection in the middle"); 5-wide grid (six will not seat beside a tile and a column at 1318); dropdown + ✎ × moved into the column head; a tile hover state; anonymous legality line shown.
- 2026-09-11 — **SHIP & DECK screen — RATIFIED (Eric): "Yes, build from this."** `shipdeck-v3-1.html` frame 1 is the surface of record: class tiles left (hoist at the foot) · collection grid middle · deck column right (dropdown + ✎ × in the column head) · CONFIRM SELECTION bottom-right, at the class layer's 1318 px. It REPLACES both the shipped class-select layer and the standalone `/decks` page; the home's only door to it is the deploy buttons (SELECT CLASS / CHANGE on the chip), and the `DECKS` link is gone. **Anonymous frame — RATIFIED: "Yes, exactly that."** Same layout; absent for anonymous: the deck dropdown, ✎ rename / × delete, SAVE, the − / + steppers, lock + ⬢ price, the account chip. Anonymous play on production itself stays `[OPEN — Eric]`. Promote to `mockups/shipdeck-1.html`; `deckbuilder-v3-1.html` stays in `.working/` as the superseded standalone page.
- 2026-09-11 — **Spines distilled (two passes each) and Reviewer Gate dispatched.** DESIGN.md 294 → 333 lines (32 component keys: 15 new, 10 extended, 4 retired; 31 dated supersession stamps); EXPERIENCE.md 324 → 385 (IA rows merged/added, nine-slot bar, Shift primitive, `5` retired, Journey A re-beat, Journey B repaired + Journey C "Okafor" drafted as [ASSUMPTION], register #26–#54). Promoted: `mockups/hud-composite-3.html`, `mockups/countdown-results-1.html`, `mockups/shipdeck-1.html`; `hud-composite-2`, `spend-window-2`, `home-class-picker-1` stamped SUPERSEDED (kept). Four lenses running — rubric walker, HUD legibility, accessibility, input scheme — each overwriting its 2026-07-16 `review-*.md`.
- 2026-09-11 — **Reviewer Gate returned** (rubric walker: 0 critical / 4 high / 10 medium / 21 low; HUD legibility 0/7/8/7; accessibility 1/10/15/7; input scheme 0/3/11/12 — pre-merge). Shared roots: five mono registers under the spine's 9 px floor in the ratified mocks; the collection grid's 0.6-scale card (the one critical); interactions specified at their start, silent on their end; the whole bar dimming under refit; and supersession bloat from same-day interim rulings recorded inline (a facilitator method defect — to be collapsed at polish; the log keeps the interims).
- 2026-09-11 — **9 px floor — RESOLVED (Eric): RAISE THEM.** HDG/KTS in the helm globe, the W/S/A/D chips, slot tier numerals and tile pip labels all go to 9 px; the helm globe grows a few px; the floor stands as written.
- 2026-09-11 — **Collection grid — RESOLVED (Eric): COMPACT GRID TILE.** In the Ship & Deck grid each line is a smaller tile — glyph, name, copies rail only; the full five-row card appears on hover/focus; five wide stays. The 0.6-scale refit card in the grid is RETIRED (the critical closes).
- 2026-09-11 — **Refit dim + card row — RESOLVED (Eric): DIM SLOTS ONLY; CARDS MAY COVER A HULL.** While the refit window is open only the slot groups dim to 38 %; globes, helm and XP strip stay at full. The card row keeps its 388 px live seat and the own-hull keep-out is WAIVED (extending the countdown acceptance to live).
- 2026-09-11 — **Bar occlusion astern — ACCEPTED, not a defect (Eric):** *"I think you're mistaken about this, what bar are you talking about? I can zoom in and out to see more of my ranges, when would this occlude anything?"* Facilitator's answer of record: the bottom-centre bar covers ~240 px of water directly astern only at maximum zoom-in; the mine leash lands above it at that zoom; zooming out clears it. Zoom is the remedy; the shipped click gate on HUD elements stands; no change.
- 2026-09-11 — **Interaction lifetimes — RESOLVED (Eric): all three.** (a) When the refit window closes, the digits are inert for a short grace (implementer dial) so a mashed pick cannot spend a belt slot. (b) The machine-gun stream ends on pointer-up, arc exit, an empty pool, Tab opening, or the window losing focus. (c) Auto-revert to the gun happens on RELEASE, never on press.
- 2026-09-11 — **Ship & Deck edge cases — RESOLVED (Eric): the set.** × delete asks to confirm and cannot delete a hull's last deck; CONFIRM SELECTION with unsaved edits saves if legal, otherwise blocks on the legality line; a pending amber rung cancels on ESC or any click elsewhere; an unaffordable rung is inert with its price dimmed; deck names are 1–16 characters under the callsign's own filter.
- 2026-09-11 — **SHIELD BLOCK readout — RESOLVED (Eric, his own mechanism):** *"The temporary shield is added to the current HP number and the color is changed, even if this exceeds the 'max' HP. It reverts when the shield is depleted or expires."* So the HP register reads hull + shield (e.g. `HULL 312/250`) in a changed colour while shielded, reverting on deplete/expire; `[ASSUMPTION: the changed colour is {colors.info}]` — no reserved hue (amber/red) may carry it. The earlier "both rings" ruling is superseded by this readout for the NUMBER; `[OPEN — Eric: whether the globe ring + hull ring survive alongside the number, and whether an absorbed hit plays a distinct cue]` — the accessibility finding on silent absorbed hits is not yet ruled.
- 2026-09-11 — **Keyboard-only access — OUT OF SCOPE (Eric):** *"You need the mouse to target/fire, NO ONE is keyboard-only."* The hover-only card tooltip stands; every reviewer finding premised on a keyboard-only player (tooltip focus path, Tab order on the Ship & Deck screen, DOM keyboard paths) is WAIVED by scope: the game requires a mouse. Recorded against the accessibility floor as a scope statement, not a floor change.
- 2026-09-11 — **Promoted mocks re-rendered to the gate rulings.** `mockups/hud-composite-3.html`: HDG/KTS/W-S-A-D/tier numerals all 9 px, helm globe 96 → 104 px, refit dim on the slot groups only (globes + strip at full), card row at 388 px, a ninth state-strip item SHIELDED (`HULL 312 /250` in `#38BDF8`), `SLOTS FULL` in silver. `mockups/shipdeck-1.html`: the grid is 138×84 px compact tiles (glyph · 9 px name · 16×12 px rungs), five wide, all 29 lines in six rows with no scroll; hover shows the full 216×226 card; locked tiles in text-muted with the lock at full; pip labels 9 px; anonymous rails rungs-only. The `.working/` originals are untouched as the pre-gate record.
- 2026-09-11 — **Final pass + polish landed.** DESIGN.md 339 lines / 35 component tokens (18 new incl. sign-in row, match-history row, door refusal; 4 retired), stamps 23 → 15, strikethroughs 44 → 15; EXPERIENCE.md 401 lines, stamps 38 → 22; same-day interim rulings collapsed out of both spines (this log is their record); zero Eric quotes or code citations in v3 passages; all token references resolve. Round-one and round-two gate rulings carried; the accessibility critical (0.6-scale grid card) is closed by the compact tile.
- 2026-09-11 — **Open items triaged (Finalize).** No phase-blocker: the one decision Eric has deferred — anonymous play on production — does not block the E8/E9 epics run, since both home states are specified and the fork is a capability difference, not a layout one. Non-blockers carried in the spines as `[OPEN — Eric]`: whether the shield's globe ring + hull ring survive alongside the `HULL n/n` readout, and whether an absorbed hit plays a distinct cue; the door-refusal surface (queue modal assumed); the remapping deferral's fresh reason; the DOM z register not re-cut; the ghost MORE CLASSES card's seat on the tile column; `+ NEW DECK` copy; the machine-gun activation-channel detail. `[DRAFT]`: the per-copy token price; the Shift boost numbers. Facilitator `[ASSUMPTION]` tags (34 DESIGN / 41 EXPERIENCE) are redline targets, not decisions — chiefly Journeys B and C, the compact tile's dimensions, the shield colour (`{colors.info}`), tile hover, and the sign-in / history / door-refusal rows.
- 2026-09-11 — **Corrections owed upstream (for the epics run, `gds-gdd` and `gds-game-architecture`):** draw-pile counter deleted (GDD § Deck model v3, E8 story 3); results = final LOADOUT, brought/drawn/taken history-only (E8 story 9); ship ladders ARMOR/SPEED/TURNING/DECK GUN start at Tier I equipped (catalog R14 wording); deck NAMES allowed (forge/GDD "Rejected" list; D12 `decks.name`; privacy paragraph); COPIES acquired per copy (GDD § Account progression, E9 stories 3/5, D12 `unlocks` per-copy, `deckRules` ownership, `matchesToCatalog` over 114 cards; starter quantities to be re-cut by Eric); the standalone `/decks` route is gone — Ship Select and the deckbuilder are ONE screen (E9 story 3; `ui/deckEditor.ts` + `classSelect.ts` merge); the dev/staging host is account-only (deploy config); keyboard-only access is out of scope (NFR accessibility wording); Windows Sticky Keys hazard accepted (input-capture rule).
- 2026-09-11 — **Finalized.** DESIGN.md + EXPERIENCE.md `status: final`, `updated: 2026-09-11`; validation-report.html/.md written with same-day dispositions; mocks promoted (`hud-composite-3`, `countdown-results-1`, `shipdeck-1`), direction boards and the superseded standalone deckbuilder kept in `.working/`; trackers stamped (gds-workflow-status.yaml `ux:` + sprint-status.yaml index). Next: `gds-create-epics-and-stories` for E8/E9 (halted at step 1 on 2026-09-10 pending this pass) — carry the corrections-owed entry above into it.
