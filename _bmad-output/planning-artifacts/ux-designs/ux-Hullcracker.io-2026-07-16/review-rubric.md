# Spine Pair Review — Hullcracker.io (v3 update, 2026-09-11)

Rubric walk of `DESIGN.md` + `EXPERIENCE.md` as the contract for architecture and story-dev (E8 The Deck, E9 The Account). Inputs: `.decision-log.md` § Update 2026-09-10 onward, both spines in full, all nine `mockups/*.html` (the three v3 ones: `hud-composite-3`, `countdown-results-1`, `shipdeck-1`), `imports/`, the gds-ux example shapes and `design-md-spec.md`, and the source documents the frontmatter names (`gdd.md`, `epics.md`, `catalog-v3.md`, `game-architecture.md`). Findings are tagged `[v3]` (introduced by this update) or `[pre-existing]` (carried from the 2026-07-16 / 2026-08-21 spine). Severity is downstream impact. This file replaces the 2026-07-16 review; that review's findings were all closed or superseded by the 2026-08-21 reconciliation and are not restated.

## Overall verdict

Mechanically the pair is still extraction-clean: every `{path.to.token}` in both files resolves (58 distinct in DESIGN, 24 in EXPERIENCE), all 40 colour tokens carry hex, every mock is linked with what it illustrates and every superseded one is stamped, and each v3 surface has a frontmatter token and a DESIGN row. The update's real defect is method, not coverage: the spine now keeps every same-day interim ruling inline as strikethrough plus "SUPERSEDED … later the same day" chains, three and four layers deep inside single table cells and inside frontmatter token VALUES — the decision log is the record, the spine is the contract, and a consumer now parses history to find the live rule. Two contract-level gaps need an Eric call before E8/E9 stories are cut: the ratified bar specs 7/8 px mono type against the spine's own 9 px floor, and the Ship & Deck / account surfaces have no failure or edge states (unsaved edits at CONFIRM, cancelling a pressed rung, insufficient tokens, deleting the last deck, sign-in failure, the door-refusal surface).

## 1. Flow coverage — adequate

Sources carry no named journeys (GDD silent; brief audience sketches: 16–35 compass · younger floor-viewport player · WoWS refugee). EXPERIENCE › Key Flows: Journey A "Marco" (8 beats, climax, failure-is-the-flow), Journey B "Dee" (7 beats, climax, failure path), Journey C "Okafor" (7 beats, climax) — the compass audience now has a protagonist. E8/E9 stories mapped against the beats: E8 1/2/4/5/6/7/10–14 land in A2/A5/B2/B3/C4/C5; E9 1/3/4/6/7 land in C1/C2/C3/C7 and the IA privacy row; E8 story 3 (draw-pile counter) and E9 story 5 (whole-line unlocks) are recorded as reversed, E8 story 9 as re-scoped.

### Findings
- **medium** `[v3]` Journey C has no failure path — the only journey without one, on the surface with the most new failure modes (EXPERIENCE › Key Flows › Journey C). *Fix:* one beat: the door refuses his deck (unlock revoked, cap changed) → reason + `SAIL STARTER DECK`; or the sign-in popup is closed and the home status line reports it.
- **low** `[v3]` Journey B beat 1 carries three texts at once — the pre-v3 beat, the "Re-cut 2026-09-11" parenthetical, and a 2026-08-21 note — so the flow itself is ambiguous (Journey B, beat 1). *Fix:* keep the re-cut sentence; the rest is log material.
- **low** `[v3]` `SIGN OUT`, `DELETE ACCOUNT` (with confirm) and the door refusal `SAIL STARTER DECK` are each named once in an IA cell and appear in no flow or state (IA › Settings overlay; IA › Ship & Deck). *Fix:* one line each in State Patterns naming the surface and what the player sees, or fold into C's failure path.

## 2. Token completeness — adequate

Extracted every frontmatter token (40 colours with hex, 5 typography, 5 rounded, 8 spacing + `chrome-max-width`, 40 components) and every `{…}` reference in both bodies and inside component values. All resolve; `{curly.path}` in the EXPERIENCE header is the syntax example. Contrast stated for `text-primary`/`void`, `phosphor` and `amber` on `void`, `text-muted` on `void`, the ≥3:1 graphic and ≥4.5:1 text floors for all twenty Regatta hues (with the algorithmic `textSafe()` rule), and `storm` → `storm-readout` for the ring edge.

### Findings
- **high** `[v3]` The ratified bar specs mono type below the spine's own floor. Frontmatter `hotbar-slot.tier` = 8px/600 mono, `helm-globe.keys` = 7px mono muted, the Typography table's HUD readout row = HDG label 8px muted · KTS suffix 7px muted; the mock is the source (`hud-composite-3.html` `.tier{font:600 8px …}`, 3 × 7px / 15 × 8px). DESIGN › Typography: *"no mono type renders below 9px post-scale"*; DESIGN › Colors › Text: muted *"never below 11px for load-bearing info"*; EXPERIENCE › Accessibility Floor and Responsive repeat the 9 px floor. At the 90 % UI-scale tier 8 → 7.2 px and 7 → 6.3 px. Spines-win means the mock is wrong as written, but the values were RATIFIED with the frame. *Fix:* Eric rules — either the floor gains a named carve-out (corner numeral + helm key chips, with the 90 % tier exempting them like the micro tier) or the four values rise to 9 px. Until then tag all four `[OPEN — below the 9 px floor]`.
- **low** `[v3]` Frontmatter component values carry prose and history rather than values: `deck-dropdown.row` embeds *"(SUPERSEDED 2026-09-11 v3: the class-card DECK row)"*, `class-card.role` embeds a supersession clause, `bank-chip.size` embeds *"(was 30px at the hotbar head)"*, `hotbar-slot.empty` embeds *"= the 0:00 state of every weapon slot"*, `copies-rail.acquire` is a 60-word paragraph. The spec's resolver flattens these at consumption. *Fix:* values only in the frontmatter; the sentence goes to the row or the log.
- **low** `[v3]` No contrast statement for the loot-tier ramp on the `panel` bed, where it now carries tier on every card and slot (`info`, `storm-readout`, `denied` numerals on `#0A0F0D`). By my arithmetic all three clear ≈5:1 at the sizes used. *Fix:* one clause in Contrast, so a consumer does not have to compute it.

## 3. Component coverage — adequate

Walked 42 DESIGN Components rows (40 tokens + Phase/Status Text, Torpedo, Combat Effects, Privacy & Consent, which are colour-driven and carry no token — pre-existing, acceptable) against EXPERIENCE › Component Patterns (14 bullets) and the state tables. Every v3 component has a real DESIGN row (anatomy, sizes, opacities, states). The four retired rows (XP Rail, DAMAGE CONTROL Rail, HP Rail, Telegraph Cluster) are correctly kept with their replacements named — a story-dev needs to find what to remove.

### Findings
- **medium** `[v3]` Six v3 components have a DESIGN row and token but NO Component Patterns entry: Deck Card, Copies Rail, Deck Column, Deck Dropdown, Account Chip, Lock Price. Their behaviour exists — spread across the IA "Ship & Deck screen" cell (~450 words), State Patterns "Copies-rail rung states", and the Class Card bullet — but the section's own contract is *"Behavior only; visuals in DESIGN.md · Components (same names)"*. Redraw Button and Loadout Block likewise live only in IA cells. *Fix:* one bullet each in Component Patterns, pointing at the state table where one exists.
- **medium** `[v3]` Three v3 surfaces have behaviour in EXPERIENCE and no visual spec anywhere in DESIGN: the anonymous home's `SIGN IN` row (OAuth popup, Google/Discord — ruled 2026-09-09 D17; the fork being `[OPEN — Eric]` does not un-rule the row), the match-history page (`[ASSUMPTION: kill-feed line grammar]` in EXPERIENCE, nothing in DESIGN), and the door-refusal register (reason word + `SAIL STARTER DECK` — no surface named: queue modal? home status line?). *Fix:* one row each, or an explicit `[OPEN — Eric]` that names the surface it will occupy.
- **low** `[v3]` The Loadout Block renders "the hud-bar's own slot row" — a Pixi grammar — inside a DOM modal; nothing says whether that is a DOM re-implementation of the slot or a Pixi render-to-texture (DESIGN › Loadout Block; EXPERIENCE › Foundation says the bar is Pixi). The Deck Card avoids this by reusing the DOM refit card. *Fix:* one clause; otherwise architecture decides by default.
- **low** `[v3]` Deck Dropdown keyboard behaviour is unspecified (arrows/Enter in the menu; what ESC does with a menu open, given ESC's "closes the topmost open surface" rule). `+ NEW DECK` is correctly tagged `[OPEN — not mocked]`.

## 4. State coverage — thin

Walked every IA surface. Pre-existing coverage stands (cold load, connection failure, reconnect, boarding, countdown, every slot state incl. denied, HP thresholds, banked 0/≥1/open/in-flight/rejected, ring phases, death → reveal → results, empty-feed absence, resize). v3 covered: signed-in vs anonymous Ship & Deck, illegal deck (SAVE unlit, failing count in `denied`), `+` dimmed at 3/3, locked line, pressed rung awaiting confirm, greyed card + `SLOTS FULL`, held fire, empty = depleted, REDRAW present/absent, `LV 0`, seven empty slots at boarding.

### Findings
- **high** `[v3]` The Ship & Deck screen has no edge or failure states, and every one of them is a decision story-dev would otherwise make alone (EXPERIENCE › IA › Ship & Deck; State Patterns has only the rung table): (a) CONFIRM SELECTION or ESC with unsaved column edits — confirm the last-saved deck, discard, or block? (the client sends a deck id and "edits after queuing apply to the next match", so this decides what sails); (b) a hollow rung pressed and not confirmed — cancelled by pressing elsewhere, ESC, or a timeout?; (c) a hollow rung pressed with insufficient `⬢` — denied pulse, inert, or the price in `denied`?; (d) deleting the selected deck, or the only deck; (e) rename bounds — length, empty, characters (the callsign has a 14-char cap, the deck name has none, and architecture D12 gains a `name` column). *Fix:* a Ship & Deck state table beside the copies-rail one; (a)–(c) are Eric calls, (d)–(e) can be facilitator `[ASSUMPTION]` for redline.
- **medium** `[v3]` Account errors are unspecified: sign-in popup closed or blocked, provider error, token expired mid-session (a 401 on SAVE), a failed save (network). The home status line already owns this register (`info` waiting, `denied` failure). *Fix:* one line each naming the surface and tone; no new copy (THE COPY LAW).
- **low** `[v3]` REDRAW post-press: the pip is "hollow = unspent"; after the press nothing states whether the pip fills, the button goes, and the new offer renders in place — it is one-shot by the pip count but not by a sentence (IA › Countdown; DESIGN › Redraw Button). *Fix:* one clause.
- **low** `[v3]` Belt stock: a second copy of a stocked consumable presumably increments `×n`; the per-slot ceiling (catalog cap 5, or up to 10 with the pool) is unstated (Component Patterns › Hotbar Slot). *Fix:* one clause.

## 5. Visual reference coverage — strong

`mockups/` (9 files) — all linked inline from DESIGN with what each illustrates, 7 of them from EXPERIENCE as well: `class-silhouettes-1` (ratified board), `home-class-picker-1` (home frame stands; class layer stamped SUPERSEDED by shipdeck-1), `hud-composite-2` (2026-07-16 record, SUPERSEDED by -3), `hud-composite-3` (HUD + refit window + slot states, ratified), `spend-window-2` (SUPERSEDED by -3), `countdown-results-1` (countdown offer + REDRAW, results LOADOUT, ratified), `shipdeck-1` (merged screen, signed-in + anonymous, ratified), `death-reveal-results-1` (sequence), `ship-color-system-1` ("not chosen"). `imports/DESIGN-v0.16-root.md` linked as foundation with the reconciliation ledger. No `wireframes/` directory. Spines-win stated once in each spine's header. The `.working/` directions boards are cited by name as superseded and not linked — correct. No orphans.

### Findings
- **low** `[v3]` DESIGN `sources` lists the `.working/` precursors `hud-composite-v3-1.html` and `countdown-results-v3-1.html` (byte-identical to the promoted mocks) and `mockups/shipdeck-1.html`, but not `mockups/hud-composite-3.html` or `mockups/countdown-results-1.html`. *Fix:* list the three promoted mocks; drop the identical precursors.
- **low** `[pre-existing, now stale]` Game Feel cites `death-reveal-results-1.html` as *"the final frame is the shipped composition"*; its F3 predates SPECTATE + MATCH LOG and now LOADOUT (the log records that `results.ts` was the reference instead). *Fix:* stamp F3 SUPERSEDED by `countdown-results-1` frame 2.
- **low** `[v3]` `.working/hotbar-blend-DB-1.html` is still the linked source for "slots, tooltip" in the Components header; the slot grammar moved to `hud-composite-3` and only the tooltip is unchanged. *Fix:* cite it for the tooltip alone.

## 6. Bloat & overspecification — broken

### Findings
- **high** `[v3]` The supersession-record method. 52 `SUPERSEDED` markers and 88 strikethrough runs across the pair, most for rulings that lived HOURS on 2026-09-11 and were never built: the class-card deck row, the standalone `/decks` page (Direction A), the `DECKS` home link, the four-row card. Instances: IA › Ship & Deck cell = the shipped class layer + two interim rulings + the merged screen + anonymous in one cell; DESIGN › Class Card row is four layers deep (356 px card → dropdown row → tile → no deck row); Deck Card, Deck Dropdown, Lock Price and Account Chip rows each strike an interim state; register rows #42/#43/#46 are superseded by #48–#54 in the same table; the IA "Signed-in port loop" paragraph keeps the `/decks` sentence it supersedes. The pair is 28,353 words. The decision log already holds every one of these. *Fix:* for same-day interims keep ONE clause — *"an interim ruling the same day was superseded; see log"* — and delete the struck text. Keep strikethrough only for shipped-then-retired surfaces (XP Rail, HP Rail, DAMAGE CONTROL rail, Telegraph Cluster, chamfer), where the struck text tells a story-dev what to remove.
- **medium** `[v3]` Ratification ceremony in spec cells: *"YES THIS IS PERFECT!"*, *"Yes, build from this"*, *"Yes, exactly that"*, *"overall very yes"*, *"the decklist on the side and the collection in the middle"* — nine Eric quotes inside DESIGN Components rows and IA cells. Provenance is the log's job; the contract needs the date and the word RATIFIED. *Fix:* strip the quotes from rows; keep them in the register's Source column if at all.
- **medium** `[pre-existing]` 24 code line-number citations (`client/src/render/xpRail.ts:178-186`, `client/src/ui/upgradeToast.ts:22`, …) in a UX contract. They drift with every commit, and several now point at surfaces v3 retires. *Fix:* cite the module, never the line.
- **low** `[v3]` The same numbers restated in three places — frontmatter token, DESIGN row, EXPERIENCE cell (216×226, 388/344 px, 38 %, 5-wide, 236/718/280, 44 px lift). One edit and the pair disagrees. *Fix:* the number lives in the token; prose cites the token.
- **low** `[pre-existing]` Frontmatter comment blocks (terrain ramp 11 lines, wheel 7 lines) restate the body's Colors rows.

## 7. Inheritance discipline — adequate

`sources` resolve (see Mechanical notes for the two path-less entries). Pillar and guardrail names verbatim. Catalog v3 line names match bar three shorthands (below). The v3 reversals of upstream (draw-pile counter, per-copy unlocks, deck names, results block) are recorded as reversals in the register with the source ruling — the correct pattern.

### Findings
- **medium** `[v3]` A GDD contradiction stated as settled: `gdd.md` § Consumables and `epics.md` E8 say *"the card face says which"* (key-fires vs key-primes); EXPERIENCE › Hotbar Slot says *"the card face never says which."* The log records the ratified face has no activation wording and flags the non-verbal channel *"for Eric at the mock, not decided here"* — no ruling followed. The spine asserts a conclusion the log did not reach, and nothing says where the player learns that DECOY BUOY primes (hover tooltip? How-to-Play?). *Fix:* tag `[OPEN — Eric: the activation channel]` and add the GDD line to the corrections list below.
- **medium** `[v3]` No consolidated list of the corrections v3 owes upstream. The log names them one by one — GDD/E8 story 3 (draw-pile counter), E8 story 9 (results re-scope), catalog R14 wording, GDD "a token unlocks a whole line" → per-copy (E9 story 5, D12 per-copy counts, `deckRules`, `matchesToCatalog`), deck names (D12 `name` column, D14/D15/privacy, the forge's "Rejected" list), starter quantities re-cut, the dev host account-only — but the register carries only four of them (#26, #34, #45, #49), and a downstream epics run reassembles the rest from the log. *Fix:* a "Corrections owed upstream" sub-list under the register (the 2026-07-16 spine used this pattern for the 4-card offer; register #2).
- **low** `[v3]` Name drift for one thing: the component is **Class Card** in both spines' frontmatter and headings, but the same object is "class tile(s)" / "TALL CLASS TILE" in seven places; "hotbar" survives in 26 places for what the token calls `hud-bar`; the screen is "Ship & Deck screen" / "SHIP & DECK" / the mock's "Ship + Deck v3-1". Catalog shorthands: SUPERCAVITATING (catalog: SUPERCAVITATING TORPEDO), TURRET / BARREL (DECK GUN TURRET / DECK GUN BARREL). *Fix:* one name per thing; the frontmatter key is canonical.
- **low** `[v3]` Journey B failure path has *"FOULING MINES laid behind her"* — FOULING MINES is a one-copy add-on to NAVAL MINES, not a mine she lays. Facilitator draft, for redline. *Fix:* "naval mines with FOULING".

## 8. Shape fit — adequate

DESIGN body order is canonical (Brand & Style → Colors → Typography → Layout & Spacing → Elevation & Depth → Shapes → Components → Do's and Don'ts); frontmatter keys per spec plus provenance extras. EXPERIENCE carries every default (Foundation, IA, Voice and Tone, Component Patterns, State Patterns, Interaction Primitives, HUD & Diegetic UI, Game Feel & Juice, Accessibility Floor, Responsive & Platform, Key Flows); Input Schemes is collapsed into Interaction Primitives — correct for a KB+M-only title. Invented sections: Inspiration & Anti-patterns (earns its place; v3 rows are take/leave with the declined options named), Monetization & Disclosure (earns it), Resolved question register (see below).

### Findings
- **medium** `[v3]` `components` frontmatter values are sentences, not values (finding 2, second bullet). The spec defines the object as *"component tokens mapped to values or `{path.to.token}` references"*; a value with a parenthetical supersession note is neither, and a resolver that flattens it emits the history into the consumer's output.
- **low** `[v3]` Both spines say `status: final` while the v3 body carries 39 `[ASSUMPTION]`, 13 `[OPEN — Eric]`, 11 `[DRAFT]` and two facilitator-drafted journeys awaiting redline. *Fix:* either state that `final` covers the 2026-08-21 body and the v3 sections are `draft`, or hold `final` until the redline.
- **low** `[v3]` The Resolved question register (54 rows) now contains rows superseded by later rows of the same table (#42, #43, #44, #46 → #48–#54) and a same-day-reversed row inside #31. A "resolved" register carries the resolution; the reversals are log material. *Fix:* collapse each pair to its terminal row with a *"via #n"* pointer.

## Mechanical notes

- **Cross-refs:** 58 distinct `{path}` references in DESIGN (body + component values), 24 in EXPERIENCE; every one resolves to a defined frontmatter token. Six frontmatter components (`br-chrome`, `kill-feed`, `class-chip`, `color-hoist`, `modal`, `toast`) are never referenced by `{components.…}` from their own DESIGN rows — harmless.
- **Links:** every `.md` link (peer spine, `imports/`, `reconcile-design-v016.md`, `validation-report.md`) and every `.html` link resolves. The three `.working/` precursors named in the frontmatter are byte-identical to their promoted `mockups/` copies (`cmp` clean).
- **Sources:** all five path-bearing DESIGN entries resolve on disk. EXPERIENCE's `epic-1..7 context amendments` — no epic-1 amendments file exists (`epic-1-context.md` only; amendments run 2–7); `game-architecture.md amendments …` carries no path (it is `_bmad-output/game-architecture.md`).
- **Names:** 40 frontmatter components ↔ 42 DESIGN rows ↔ 14 EXPERIENCE Component Patterns bullets (gap in §3). Kebab key ↔ Title Case prose is consistent; the drifts are the ones in §7.
- **Type floor:** four frontmatter/typography values at 7–8 px mono against the stated 9 px floor (§2) — the one internal contradiction found.
- **Numbers cross-checked:** pool of 10 / 50 at queue, 40 authored, ≤3 equipment lines, 29 lines / 114 cards, 216×226, 5 rows, 388/344 px, 236·718·280 = 1234 + gutters at 1318, deck-card 130×152 = 0.6 × (226 + 28) — all consistent across the pair and with `gdd.md` / `catalog-v3.md` / the mocks.
- **Tag inventory:** DESIGN 8 `[ASSUMPTION]` · 13 `[OPEN]` (4 Eric, 6 not-mocked, 2 icon/effects, 1 z-register) · 5 `[DRAFT]` · 4 `[PROPOSAL]`; EXPERIENCE 32 `[ASSUMPTION]` · 9 `[OPEN — Eric]` · 6 `[DRAFT]`. Each carries an owner or a condition — the correct pattern; none is smuggled as fact.
- **Size:** DESIGN 12,310 words / 334 lines; EXPERIENCE 16,043 words / 386 lines; 52 `SUPERSEDED` markers; 88 strikethrough runs (§6).
